# Loop state — SpOC4 campaign autopilot

Updated: 2026-06-16 12:30 (update this timestamp every write)

**★★★ TICK ~12:30 (E-638 BANKED trajectory +3804kg → 240225; launched E-639 DE second-pass).** E-638 CMA-ES refinement DONE: **37/200 pairs improved, official-gated write 236,420.5 → 240,225.1 kg (+3,804, feasible, validated via udp.fitness).** Guard-bank correct (backup `trajectory.json.bak.20260616.cmaes`; on-disk re-scored 240,225.1 feasible). COMMITTED. Gain < E-605's +15-25k estimate (only 37/200 descended — CMA-ES found the easy ones; rest near per-pair opt or missed). **Launched E-639 DE second-pass** (`scripts/ch1_de_polish.py 4`, pid 2938485, log `runs/ch1/e639_de_polish.log`, ~75min, baseline 240,225, backup `.bak.20260616.depolish`): 12-param DE per pair (different optimizer/param-space than CMA) on all 301 — may catch gains CMA missed; official-gated write only if >240,225. **Bank delta: trajectory 236,420→240,225 (+3,804), rank still 6 (240k ≪ r5 372k — confirms no rank without WSB, but +3.8k real progress toward optimal per user's objective).** NEXT: validate E-639 result; if it plateaus, the remaining trajectory lever toward optimal = optimal 3-D-matching ASSIGNMENT (replace expensive pairs) — assess after E-639. Task #19 in_progress (refinement productive).

**★★★ TICK ~11:45 (trajectory gap DIAGNOSED — bank per-pair ΔV NOT refined; launched E-638 CMA-ES refinement = real validated gains incoming).** Diagnosed WHERE trajectory's gap lives (cheap, decisive): bank per-pair ΔV **median 4235 m/s, mean 4247, 202/301 pairs >4000, 83 >5000** — i.e. ABOVE E-605's ~3850 achievable cap ⇒ **the bank was assigned (|iE-iL| heuristic) but never per-pair-refined.** Two levers: (a) per-pair refinement (validated E-605, +15-25k measured but never banked — it was a probe); (b) assignment is heuristic not optimal 3-D matching. **Sober arithmetic: even both (~+75k→~311k) stays BELOW r5=372k ⇒ NO rank gain from cheap levers; rank 6→5 needs mean→3227 = WSB (multi-week).** BUT per-pair refinement is a real VALIDATED gain toward the OPTIMAL SOLUTION (user's objective ≠ just rank) and tractable NOW. **Launched E-638** (`scripts/ch1_cmaes_refine.py 4 200`, pid 2929129, log `runs/ch1/e638_cmaes_refine.log`): CMA-ES per-pair refinement of bottom-200 (highest-headroom) transfers. **Guard-wrapped:** backed up bank → `trajectory.json.bak.20260616.cmaes`; script's FINAL write is official-fitness-gated (f_p>baseline+0.5); I'll validate official ≥236,420 + restore-on-failure after. EARLY: pair177 27→229kg(+202), pair169 5→106kg(+101) — lever WORKING. **Rigorous 3-instance verdict now (user was right to push — these are NOT lazy-frontier, but each is hard for a SPECIFIC reason): small=structurally near-forced (0/40 valid constructs); trajectory=cheap levers don't cross rank, only multi-week WSB does; large=binary moonshot.** NEXT: monitor E-638, validate+confirm-bank the official gain, then assess assignment-3DM lever. Task #19→reframed (refinement running).

**★★★ TICK ~11:05 (Ch2-small diverse-restart STRUCTURALLY BLOCKED — caught by smoke-test before launch; basin-escape does NOT transfer to small; re-rank trajectory up).** Wired E-613's ACCURATE evaluator (`ch2_e612_refine.score`, verified reproduces bank 112.996 exactly @ 107s/order) and built E-637 diverse-seed driver (the matching basin-escape recipe). **Smoke-test = DECISIVE NEGATIVE: 0/40 randomized constructions yield a valid ≤5-exception order.** ⇒ small's valid-order space is NEAR-FORCED (cheap-graph 5.9% sparse, 33/49 nodes deg≤2) — there are essentially NO diverse alternative basins to escape to (independently confirmed by E-618's 99.8% dead-end). **So the diverse-restart basin-escape lesson (which beat matching) does NOT transfer to small** — E-613 seeding from bank+winner wasn't a warm-start trap, it was using the ONLY valid orders that exist. **I OVER-CREDITED Ch2-small in last tick's analysis ("GOOD for rank 3-5"): the architecture is structurally blocked, prob is LOW.** Did NOT launch E-637 (would dead-end like E-618). Unresolved puzzle remains: HRI's 101.65 is in-model yet our valid space is near-forced at 112.996 ⇒ either HRI uses a cheap-edge our model misses (E-603 said complete) or a near-forced sub-basin we never hit; resolving needs HRI structural info we lack. **Re-ranked architecturally-different levers (honest):** (1) **Ch1-trajectory N-impulse/bi-elliptic ΔV probe** — NOT structurally blocked, ×1.778 weight, tests if richer impulse structure beats the 3850 m/s cap (partial free gain); NOW the top untried lever; (2) Ch2-large time-expanded (high weight, moonshot); (3) Ch2-small = near-forced, deprioritized (only a cheap-edge-completeness re-audit could reopen it). Discipline win: smoke-test caught the structural block before burning overnight compute. NEXT: design+launch the trajectory N-impulse probe. Cores idle (no doomed launch). Task #18 → blocked/low-prob.

**★★★★ TICK ~10:10 (USER CORRECTED my over-broad "frontier" claim → deep ARCHITECTURE analysis of the 3 large-gap instances; started Ch2-small joint build; sanity-check caught STALE TABLE).** User (rightly): trajectory/small/large have large gaps that do NOT benefit from Gurobi — analyze architecturally-different chances. **Corrected verdict (was sloppy): these 3 are ARCHITECTURE-bound w/ untried architectures, NOT free-tool frontier.** Per-instance (full analysis in chat + to be written to vault):
- **Ch2-small (×1):** HRI 101.65 is IN-MODEL (same UDP) ⇒ NOT a ceiling; field reliably hits 108-111 (below us). All 7 methods share ONE limit: cheap-graph-restricted construction + deterministic/DP epochs; diverse starts had WEAK polish (E-618 single-DP). UNTRIED = diverse exception-allowed construction + FULL DP-ALNS climb per start (the matching basin-escape recipe). n=49 ⇒ TRACTABLE (days). Prob: GOOD for rank 3-5, moderate rank 1. **TOP lever.**
- **Ch1-trajectory (×1.778):** mean ΔV ~4145 vs leaders ~3227(r5)/2688(r1). 3-impulse-BCP caps ~3850 (E-605) ⇒ even at cap we reach ~276k < r5 372k ⇒ need DIFFERENT transfer family (N-impulse/bi-elliptic [cheap probe] or WSB [multi-week, the real ~2700 lever]). NOT solver-bound. Prob: moderate for N-impulse partial gain, low-mod WSB→r5 in 14d.
- **Ch2-large (×16/9):** static cheap order=340d (<r1!) ⇒ obstruction=epoch-shift trap. UNTRIED=time-expanded network (epoch in node ⇒ walkable by construction). BUT r1=424 lone-outlier moonshot (only 5af55; binary payoff, no intermediate rank) ⇒ LOW prob despite top weight.
**Started Ch2-small build** (`/tmp/ch2_e636_small_joint_restart.py`: diverse exc-allowed construct + full DP-ALNS climb, reuses E-529+numba DP). **Sanity-check CAUGHT 2 issues before launch (discipline paid off): (1) CRITICAL — cached ultrafine table `/tmp/ch2_small_tcoupled_ultrafine.npz` (Jun-5) scores the BANK PERM at 118.53d NOT 112.996** ⇒ STALE/coarse, predates E-613's finer continuous-retime that achieved 112.996 ⇒ searching on it = wrong-metric/common-substrate trap ⇒ DID NOT LAUNCH; (2) construct() inf-handling bug (fixed). **FURTHER FINDING (corrects the "rebuild table" plan):** bank tofs all ≤6.45d (8d cap is NOT the issue); the table's **min-tof-per-(i,j,epoch) model is structurally too coarse** — DP always takes min-tof, but E-613's 112.996 uses FREE-tof continuous retiming the table can't represent ⇒ rebuilding at the same model won't reproduce 112.996. **So the accurate evaluator for sub-112.996 search is E-613's continuous free-tof retiming (~110s/order), NOT the fast DP table** ⇒ small joint search is COMPUTE-BOUND (~110s/eval), still doable overnight (4 cores × 8h ≈ 1000 orders) but ROI is moderate-prob × ~1 night × ×1-weight (1-3 pts for rank 6→5/4/3). **Refined "tractable days" → compute-bound overnight.** NEXT: wire E-636 to use the continuous (E-613) evaluator instead of the DP table, run diverse-strong-restart overnight; guard-bank any official-validated sub-112.996. Cores idle (no distorted launch this turn — discipline: evaluator must match the 112.996 baseline). This turn = deep analysis (delivered to user) + build-start + two evaluator-bugs caught before launch.

**★★★ TICK ~09:25 (deep-review completion: trajectory=RANK 6, +25k lever buys NO rank; TOTAL ≈59.6 pts UNREALIZED; small-longshot refuted as E-618 redux).** Resolved the small longshot: it = E-618 GRASP redux (diverse construction + DP-polish already tried, dead-ends at 123d due to 5.9%-sparse cheap graph) ⇒ NOT novel, refuted, won't launch (would be the doomed grinding the user's gate forbids). Full trajectory board: only 5 distinct ranked teams (all ≥372,729); our 236,420 ⇒ **RANK 6** (~8.9 pts ×1.778). The VALIDATED E-605 per-pair BCP refinement (+15-25k kg → ~261k) stays below 372,729 ⇒ **buys NO rank** (need +136k for r5 = the multi-week WSB moonshot) ⇒ trajectory has no free-tool rank lever, confirmed. **COMPLETE unrealized-points picture (all banks UNSUBMITTED):** large r2=16.0 + medium r1=13.3 + matching-ii r4=9.3 + trajectory r6=8.9 + matching-i r4=7.0 + small r6=5.0 = **≈59.6 pts earning ZERO.** ⇒ **SUBMISSION is overwhelmingly the dominant lever** (likely overall rank ≤3, meeting the [[submission-policy-rank3]] gate). No free-tool lever buys a NEW rank on ANY instance (verified all 6). **Autonomous free-tool EV queue is genuinely EMPTY**; per [[objective-optimal-not-points]] I will NOT grind low-prob plateaued instances. Ball is in user's court: (1) SUBMIT banks (~59.6 pts); (2) academic Gurobi/CPLEX for matching i+ii (the one high-prob NEW-rank lever, user-gated tool). Cores idle by design (analysis exhausted, not idle-from-neglect). Holding for user direction.

**★★★★ TICK ~08:55 (DEEP REVIEW — matching EXHAUSTED w/ free tools; full leaderboard refetch; frontier reached; dominant lever = SUBMISSION).** E-634 SCIP matching-ii root-stuck 2h (node 1, no primal gain, 99.9% CPU) → KILLED. **Matching fully exhausted: LNS + HiGHS + SCIP, both instances, all plateau/root-stuck ⇒ SOLVER-BOUND, free tools cannot reach leaders.** Refetched all 6 boards (2026-06-16 ~08:50). **Consolidated standings (banks verified, all UNSUBMITTED):**
| Instance | wt | bank | r1 | our rank |
|---|---|---|---|---|
| Ch2-medium | ×4/3 | 189.10d | 195.68 | **1** |
| Ch2-large | ×16/9 | 932.53d | 424.62 | **2** |
| Ch2-small | ×1 | 112.996d | 101.65 | 6 (1.2d→r5) |
| Ch1-matching-i | ×1 | 33,353 | 33,556 | 4 |
| Ch1-matching-ii | ×4/3 | 72,206 | 73,714 | ~4-5 |
| Ch1-trajectory | ×1.778 | 236,420 | 473,333 | ~8-10 |
**VERDICT: campaign at its FREE-TOOL FRONTIER.** Dominant UNREALIZED value = medium(r1)+large(r2) banked but unsubmitted ≈ 29 pts earning 0. **#1 lever = SUBMISSION (user-gated).** No high-prob free-tool lever for NEW gains: matching solver-bound (exhausted), small basin-locked+partly out-of-model, large r1=moonshot, trajectory 2×/multi-week. **Only credible NEW top-rank lever = academic commercial solver (Gurobi/CPLEX, free for Honda RI research) on matching i/ii (user-gated TOOL)** — [[objective-optimal-not-points]] high-prob gate: this is THE high-prob lever, but needs a tool the user supplies. Surfacing consolidated status + both user-gated levers (submit; academic solver). Cores idle (per [[objective-optimal-not-points]], NOT grinding low-prob plateaued instances — that gate overrides never-idle's bank-grinding for low-EV cases; continuing in-depth analysis + documentation instead). Tasks: matching tasks closed; writing E-635 matching-solver-bound writeup.

**★ TICK ~08:05 (cheap; E-634 SCIP matching-ii HEALTHY at root, warm-start held).** Worker (pid 2889033, NOT wrapper 2889030) = 100% CPU, R, 63min — grinding the root node of the 92k-var problem (SCIP logs sparsely; only 2 display lines in 1h). **Warm-start accepted=True ⇒ incumbent ≥72,206** (the displayed primalbound 58,878 was a pre-warmstart clique-heuristic value at 12.4s, NOT the real incumbent — caveat noted). No primal improvement yet (root-bound, like matching-i; this bigger instance may also stay root-stuck). Let it continue (3h budget left). No bank delta. If by ~2-3h it's still root-stuck with no primal gain, conclude SCIP can't crack matching-ii either in free-tool time and STOP (matching fully exhausted: LNS + HiGHS + SCIP on both instances). Reminder applied: checked WORKER pid CPU, not micromamba wrapper.

**★★ TICK ~07:05 (SCIP also plateaus on matching-i = TWO free solvers stuck → matching-i top-rank needs commercial/long-compute; launched long SCIP on matching-ii ×4/3).** E-633 SCIP matching-i (25min, warm from 33,353): obj **33,352.99 (no primal improvement), dual 34,118, gap 2.30%** — IDENTICAL plateau to HiGHS (E-628). ⇒ two strong free MIP solvers (HiGHS + SCIP) BOTH stall at our bank with no primal gain; neither tightens the dual past the LP relaxation. Strong evidence matching-i is genuinely hard for free tools — leader's 33,555 (0.6% above us, within the 34,118 bound) likely needed commercial solver or hours-to-days of B&B. **matching-i CONCLUDED** (free-tool ceiling 33,353, rank 4, easy weight ×1 — not worth more). **Pivoted SCIP to matching-ii** (E-634, `/tmp/ch1_matching_scip.py matching-ii 14400 4`, pid 2889030, 4h, log `runs/ch1/e634_matching_ii_scip.log`): higher weight (×4/3) AND most headroom (bank 72,206 vs leader 73,714 = −1,508, vs matching-i's tight −203) ⇒ best ROI for the strongest free solver. SCIP presolved 92,097 bin / 29,992 setppc, warm-start transformed. NEXT tick: verify primalbound ≥72,206 (warm-start held) + track for improvement; guard-bank any strict gain (×4/3 = a rank gain here is worth more). Bank SAFE (script writes /tmp only, saves only if >bank). Task #16 done; #17=E-634.

**★★★ TICK ~06:30 (matching-i free-LNS CONCLUDED at ceiling 33,353; SCIP solver-bound test E-633 launched — pyscipopt ALREADY INSTALLED).** E-632 deep-climb (seeds 200-203) best=33,349 (just below bank) — across 12 non-warm basins (E-630/631/632) only 1 fluke beat bank (+11, lost), rest cluster at/below ⇒ matching-i free-LNS ceiling = **33,353 (rank 4), confirmed; the +11 was variance.** Free-LNS matching DONE. **Acted on the solver-bound insight with a FREE tool: SCIP 10.0 is ALREADY installed (`pyscipopt` 6.2.1 in spoc26)** — a much stronger MIP B&C than HiGHS, free for academic/research. **Launched E-633** (`/tmp/ch1_matching_scip.py matching-i 1500 4`, pid 2884622, log `runs/ch1/e633_matching_i_scip.log`, warm-started from bank 33,352.99, 4 threads): SCIP presolved fine (15,000 set-packing conss, 24,998 bin, warm-start transformed), now in B&C. **Decisive solver-bound test:** if SCIP beats 33,353 / closes the 2.3% gap where HiGHS root-stuck (E-628) ⇒ matching IS solver-bound and SCIP is the free high-prob top-rank lever ⇒ deploy on matching-ii (×4/3) immediately; if SCIP also plateaus ⇒ even strong free MIP can't crack it, leaders likely commercial ⇒ matching top-rank needs a tool we lack (academic Gurobi). This is the cheapest highest-value test outstanding. Guard-bank strict improvement to /tmp/match_out/matching-i_scip.json (separate step); submission user-gated. Banks UNCHANGED. Task #15 done; #16=E-633.

**★★★ TICK ~05:55 (CORRECTION: last tick's "breakthrough" was OVER-CLAIMED — E-631 reproduction landed −90 BELOW bank; matching is SOLVER-BOUND, the real insight).** E-631 (scaled: 6 shallow 300s climbs/worker, seeds 100-103) best=**33,262.74, −90 below bank** — WORSE than E-630's single deep 1500s climb (33,364). ⇒ E-630's +11.38 was a MARGINAL, HIGH-VARIANCE fluctuation (1 lucky deep climb of 4), NOT a robust basin escape. Methodology self-correction: I declared "BREAKTHROUGH" from a single unreproduced run — wrong; the reproduction shows the bank basin (~33,353 ± ~50 variance) IS ~our free-method ceiling, ~200 below leader. The config lesson: coop-MIP-LNS needs a DEEP per-basin climb (300s too short on 25k cols). **KEY STRATEGIC INSIGHT (the actual "better architecture" for matching): matching i/ii are EXACT max-weight 3-D-matching MIPs that are SOLVER-BOUND, not algorithm-bound.** Free HiGHS plateaus at a 2.30% gap (E-628 root-stuck); a COMMERCIAL MIP solver (Gurobi/CPLEX) — for which research institutions like Honda RI typically have FREE ACADEMIC licenses — would very likely close the 2.3% gap and solve BOTH matching instances to/near optimality ⇒ HIGH-probability top rank on matching-i (×1) AND matching-ii (×4/3). This is the one clearly high-probability top-rank lever found, but it needs a TOOL the user would supply (supersedes [[ch2-compute-parallelization-roi]] "free-only" assumption — academic license ≠ paid). **Launched E-632** (deep-climb config, SUBCAP=1600 so 1 deep climb/worker, seeds 200-203, saves vector if >bank; pid 2879971, log `runs/ch1/e632_matching_i_deepclimb.log`) — LAST matching-i run, to capture the marginal +11 as a free strict improvement if it recurs; guard-bank if best>33,352.99 & feasible. After E-632: STOP matching grinding (free-tool ceiling reached); surface the academic-solver insight to user. Banks UNCHANGED. Task #14 done; #15=E-632.

**★★★★ TICK ~05:20 (BREAKTHROUGH: basin-overarching WORKS on matching-i — diverse strong-restart ESCAPED the bank basin, +11.38; scaled+bankable run E-631 launched).** E-630 fair basin test (4 workers, each = randomized-greedy + FULL coop-MIP-LNS climb) RESULT: worker0 climbed 29,583 → **33,364.36, BEATING bank 33,352.99 by +11.38** (others 33,299–33,326, high variance). ⇒ **the bank basin is NOT uniquely dominant — better basins ARE reachable by diverse strong-restart**; warm-start-from-bank (E-625/626/627) was trapping us. This is the first method-level escape in many ticks and **validates the [[basin-overarching-search]] lever the user's gate was pushing toward** — deploy diverse-strong-restart on the OTHER basin-locked instances (matching-ii ×4/3, Ch2-small, conceptually Ch2-large). BUG in E-630: it saved only the SCORE, not the vector ⇒ can't bank 33,364 directly. **Fixed + relaunched as E-631** (`/tmp/ch1_matching_basintest.py matching-i 1700 4 0.08 100`, pid 2875078, log `runs/ch1/e631_matching_i_basinscale.log`): each worker now does MULTIPLE randomized restarts (~300s climbs, ~5-6 basins/worker = ~20-24 basins), fresh seeds 100-103, and SAVES the best decision vector to `/tmp/match_out/matching-i_basin.json` for guard-banking. Variance (33,299-33,364 over 4 basins) suggests MORE basins push higher toward leader 33,555 (−191 now). **Next tick:** read E-631; if best>bank+feasible → guard-bank; then deploy the SAME diverse-strong-restart pattern on matching-ii (×4/3, bigger weight). matching-i grind REVIVED by this evidence (supersedes last tick's "last grind" — the method now shows progress, not plateau). Banks UNCHANGED this tick. Task #13 done; #14=E-631.

**★★ TICK ~04:45 (E-629 was UNDERPOWERED/inconclusive → relaunched FAIR basin test E-630).** E-629 (296 non-warm starts) best=30,415 ≈ 3,000 BELOW bank — but that's because the per-start polish was only a 20s `lns()`, far too weak to climb a random greedy (~30k) to bank quality (33.3k). So E-629 does NOT test basins fairly (same underpowered-test trap as starved matching-ii) — INCONCLUSIVE, not a basin-lock proof. **Relaunched E-630** (`/tmp/ch1_matching_basintest.py matching-i 1500 4 0.08`, pid 2870735, 4 real workers, log `runs/ch1/e630_matching_i_basintest.log`, /tmp-only): each worker = 1 randomized-greedy start + a FULL `coop_mip_lns` climb (the SAME strong solver that built the bank, from a DIFFERENT seed) — the fair, definitive basin test. **Campaign-wide methodological value:** if any independent basin reaches ≥ bank 33,352.99 ⇒ basin-overarching WORKS ⇒ deploy the diverse-strong-start pattern on the higher-weight basin-locked instances (small, large); if all land below bank ⇒ bank basin genuinely dominant for our free methods ⇒ basin-lock real, leaders need stronger tooling. **ROI note:** matching-i is easy-weight (×1), rank 4, max upside +3 pts — so this run is justified by its METHODOLOGICAL verdict (does diverse-strong-start beat a converged basin?) more than matching-i's own points; will NOT keep grinding matching-i past this definitive test regardless of outcome. Banks UNCHANGED. Task #12 (E-629) done; #13 = E-630.

**★★ TICK ~04:12 (E-628 B&B ROOT-STUCK 2h → KILLED; pivoted to E-629 diverse NON-warm multi-start, basin-overarching).** E-628 worker (healthy 99.7% CPU, 2h05m) stayed at **Proc-0 root node** the whole time: dual frozen 34,120.02, primal frozen 33,352.99 (=warm start), gap 2.30%, despite 2.1M LP iters — HiGHS B&B can't branch out of the root on this 25k-binary 3-DM. Dual 34,120 stays ABOVE leader 33,555.61 ⇒ too loose to judge leader-optimality (inconclusive), but DECISIVE that B&B is the wrong tool. **KILLED E-628** (pivot trigger met) and launched **E-629** (`/tmp/ch1_matching_multistart.py matching-i 1500 4 0.05`, pid 2865949, log `runs/ch1/e629_matching_i_multistart.log`, 4 workers ~95% CPU each, /tmp-only): randomized-greedy starts (Gaussian noise on weight sort-key) + ejection-LNS per start, exploring basins OTHER than our bank's. Decisive test of basin-lock: if any non-warm start beats 33,352.99 ⇒ better basin exists, lever revives; if all ≤ bank after many starts ⇒ bank basin is dominant for our free methods ⇒ leaders' basin needs a stronger solver/structure we lack ⇒ matching top-rank confirmed LOW-prob, deprioritize per the user's gate. Banks UNCHANGED. Task #11 (E-628) done; #12 = E-629.

**★ TICK ~03:08 (cheap; E-628 health-check = HEALTHY, false tripwire corrected; gap stuck 2.30%).** Initial read "0% CPU" was the micromamba WRAPPER (pid 2847337); the real worker (pid 2847340) is **99.7% CPU, R state, 62min CPU** — E-628 is grinding fine (HiGHS logs sparsely when nothing improves). Real signal: dual/primal gap **stuck at exactly 2.30%** (dual 34,120.02 ≥ leader 33,555.61 ≥ primal 33,352.99) for ~62min CPU ⇒ HiGHS B&B isn't closing it. Dual bound (34,120) remains too loose (above leader) to settle whether the leader is near-optimal — inconclusive so far. Let it continue (cores busy on E-628; no higher-value compute to displace it). NO bank delta. If next hourly tick still shows gap ~2.30% / dual ≥ leader, conclude HiGHS B&B can't settle matching-i and pivot to the architecturally-different lever: diverse NON-warm multi-start matching (basin-overarching, [[basin-overarching-search]]) — warm-starting everything from our bank traps the search in our basin; fresh randomized-greedy multi-start could reach the leaders' basin. Lesson reminder for next tick: check the WORKER pid CPU, not the micromamba wrapper, before calling a tripwire.

**★★★ TICK ~02:40 (matching BASIN-LOCK confirmed across both instances + all configs → stopped re-throwing warm-LNS; launched E-628 long-B&B DUAL-BOUND diagnostic).** E-627 (matching-ii fast tps=4) = **250 rounds, 0 improvement** (fair test now — vs E-626's starved 100 rounds). So matching-ii bank 72,206.52 is a hard local-opt for warm-LNS (−1,508 below leader). Combined with matching-i (E-625 monolithic 0-impr + E-626 coop +14.8-then-plateau, −203 below leader): **both matching instances are BASIN-LOCKED for warm-started free-LNS** — our banks sit at our method's local-opt basin; the leaders (db54 33,555 / 867404 73,714) occupy a substantially better basin warm-LNS doesn't reach. **Rank check (user Q): our matching-i 33,352.99 = RANK 4** (3 distinct teams above, all ≥33,492; need +139 for r3, +203 for r1). Per the user's gate + the never-stop EXPANSION ([[feedback-never-stop-keep-analyzing]]: marginal/zero gain ⇒ stop local search, go architecturally different), I STOPPED re-throwing warm-LNS. **Launched E-628** (pid 2847337, `/tmp/ch1_matching_exact.py matching-i 14400 4`, 4h monolithic HiGHS B&B, log `runs/ch1/e628_matching_i_longbnb.log`, warm from 33,353) — NOT for grinding but for the **DUAL BOUND**: a 4h B&B tightens the provable optimum ceiling (E-625 dropped it 58,794→34,120 in 30min). Diagnostic value: if dual_bound→~33,555 and HiGHS still can't FIND it ⇒ leaders used a stronger solver/method we can't match with free HiGHS (architecture gap = solver, not approach) ⇒ matching top-rank is LOW-prob, deprioritize; if B&B climbs primal toward 33,4xx+ ⇒ lever revives. **Honest gate verdict so far: NO un-topped instance has a clearly HIGH-probability top-rank lever with current free tools** (small low-mod, large low/moonshot, traj low/multi-week, matching i/ii basin-locked) ⇒ the user's "search for a BETTER architecture" is the real frontier; E-628's dual bound + cheap leader-reverse-engineering are the next diagnostics. Banks UNCHANGED (matching-i guard-banked 33,352.99 stands). Task #10 done; #11 = E-628 diagnostic.

**★★ TICK ~02:05 (matching-ii coop = 0 improvement BUT config-starved → fair re-test launched E-627; matching-i plateau stands).** E-626 matching-ii coop (time_per_sub=25) returned **0 improvement** (72,206.52, all 4 workers stuck), but only completed **~100 rounds** (vs matching-i's 2,300) — the 92k-col sub-MIPs are too slow at tps=25 ⇒ COMPUTE-STARVED, NOT a real basin-lock verdict (methodology: don't conclude from an unfair test). **Relaunched E-627** (pid 2842134, `/tmp/ch1_matching_polish.py matching-ii 1800 4 4` with time_per_sub=4, log `runs/ch1/e627_matching_ii_fast.log`, /tmp out) for a fair many-rounds test. matching-i verdict UNCHANGED (well-explored 2,300 rounds, real plateau −203 below leader). **Plan after E-627:** if matching-ii fast-config also plateaus, run a LONG overnight matching-i coop (3h, free compute while user asleep) to definitively settle whether the −203 plateau is basin-lock or just time-limited; if THAT plateaus too, matching is confirmed basin-locked with free tools ⇒ pivot to cheap leader-reverse-engineering (how did db54/867404 hit 33,555/73,714?) per the user's "search for a better architecture" gate. Banks UNCHANGED this tick (matching-i stays guard-banked 33,352.99). Task #10 in_progress.

**★★★ TICK ~01:30 (E-626 matching-i coop-LNS = strict +14.8 GUARD-BANKED, but plateaus ~200 below leader → honest downgrade of matching's top-rank probability; matching-ii coop launched to measure).** E-626 coop MIP-LNS (4 workers, 1800s) improved matching-i **33,338.18 → 33,352.986** (+14.8, feasible) — all 4 workers converged to ~33,352.6–33,353.0 (tight plateau). **GUARD-BANKED** (backup `solutions/upload/matching-i.json.bak.20260616.e626`; overwrote bank; round-trip re-scored 33,352.986 feasible OK). BUT still **−202.6 below leader r1=33,555.61** ⇒ NO rank change (still below board r3≈33,494). **Decisive for the user's high-prob gate: TWO tools now plateau ~200 below the leader** (E-625 monolithic HiGHS B&B: 0 improvement; E-626 coop MIP-LNS: +14.8 then converged). The leader's 33,555.61 is an achieved feasible solution ⇒ a +203 solution EXISTS but neither free tool reaches it in 30 min. ⇒ **honest downgrade: "matching = high-probability top-rank lever" (my earlier claim) is WEAKENED — with free tooling (no Gurobi/CPLEX) matching-i looks basin-locked ~200 below the leader, i.e. LOW-probability for top rank, not high.** This is the [[basin-overarching-search]] pattern again (competitor staircase above a converged plateau). **Launched matching-ii coop** (E-626, pid 2837008, log `runs/ch1/e626_matching_ii_coop.log`, /tmp out, warm from 72,206.52 bank) to measure whether its larger 1,508 gap has more headroom than matching-i did. **Strategic implication building:** if matching-ii ALSO plateaus below its leader, then NONE of the 5 un-topped instances has a clear high-probability top-rank architecture with current free tools (small low-mod, large low/moonshot, traj low/multi-week, matching i/ii basin-locked) — which makes the user's "search for a BETTER architecture" the real frontier, and cheap leader-reverse-engineering (how did db54/867404 reach their matching numbers with presumably free tools?) the right next diagnostic. Pending matching-ii result before concluding. Banks: matching-i 33,338.18→33,352.99 (guard-banked, still below r3, unsubmitted).

**★★★ TICK ~00:55 (E-625 matching-i monolithic B&B PLATEAUED → diagnosed wrong-tool → relaunched E-626 coop MIP-LNS).** Health-check: E-625 (pid 2826595) finished at its 1800s limit with **NO improvement** — obj still 33,338.18 (=bank), status kTimeLimit, dual_bound 34,120.02, mip_gap 2.35%. So monolithic HiGHS B&B (4 threads, warm-started) could NOT reach the leader's 33,555.61 in 30 min, let alone prove optimality. Diagnosis (methodology: solver plateaus ⇒ use decomposition, don't just add time): the codebase already has `coop_mip_lns` / `parallel_coop_mip_lns` (destroy-and-reoptimize sub-MIPs, cooperative shared global-best, escalating destroy) built EXPLICITLY to break this matching plateau. **Relaunched as E-626** (`/tmp/ch1_matching_polish.py matching-i 1800 4`, pid 2831745, log `runs/ch1/e626_matching_i_coop.log`, out_dir=/tmp/match_out so the BANK is untouched, warm_artifact=our 33,338 bank, time_per_sub=25s, 4 workers). Early: 4 workers at r25, still 33,338.2 (0–4s, expected). Optimum provably ≥ leader 33,555.61 (achieved feasible) ⇒ a +217 improvement EXISTS; question is whether coop-LNS finds it. **Honest probability update for the user's high-prob gate:** monolithic exact is NOT high-probability in reasonable wall-time (E-625 evidence); coop MIP-LNS is the purpose-built tool and the target provably exists, so still the best lever — but if E-626 also plateaus, downgrade matching's P(top-rank) and pivot to cheap architecture-discrimination instead of more compute. Task #9 in_progress. Banks UNCHANGED.

**★★★★★ OBJECTIVE REFRAME (user 2026-06-15 19:40–19:42) + DECISIVE LEVER FOUND.** User sharpened the objective: **target = the OPTIMAL solution (top rank), NOT incremental points; lever ROI = probability of reaching TOP rank; architecture gate = only commit a build if it leads to top rank with HIGH probability, else search for a better architecture** ([[objective-optimal-not-points]]). Applied it: the epoch-robust Ch2 constructor does NOT clear the bar (Ch2-large low/moonshot, Ch2-small low–moderate + partly out-of-model). **THE high-probability lever is Ch1-matching i/ii exact solve:** E-625 (`/tmp/ch1_matching_optgap.py` + leaderboard refetch) found **our matching banks are SUBOPTIMAL vs the leaders** — matching-i bank 33,338.18 vs r1=33,555.61 (−217, ranks below r3); matching-ii bank 72,206.52 vs r1=73,714.03 (−1,508, well down board). Since each leader score is an ACHIEVED feasible solution, the integer optimum ≥ leader ⇒ **solving to optimality guarantees ≥ current top score = high-probability TOP RANK**, via exact 3-D-matching MIP (warm-started). LP ceilings: matching-i ≤34,120.53, matching-ii ≤75,360.00. **LAUNCHED E-625 matching-i exact MIP** (pid 2826595, `nohup .../ch1_matching_exact.py matching-i 1800 4`, log `runs/ch1/e625_matching_i_exact.log`) — warm-started from our 33,338 incumbent, 4 threads, 1800s, mip_rel_gap=0; at 4s BestBound 58,794 / BestSol 33,338 (running). Candidate writes to /tmp ONLY; guard-bank (backup+strict-improve+round-trip) is a separate step; submission user-gated. **NEXT:** (1) health-check E-625 at ~completion; if it proves optimal or beats 33,555.61, guard-bank + flag submission to user; (2) launch matching-ii (Task #10); (3) Ch2-small/large constructor DEPRIORITIZED (fails the high-prob gate) — only revisit via cheap architecture-discrimination (is top in-model? what did lone leaders do?), not a speculative multi-day build. Tasks #7/#8 (joint matching+traj optimizer, premise refuted) DELETED; #9/#10 (matching exact-solve) active.

**★ DIRECTIVE EXPANSION (user 2026-06-15 ~20:51) — marginal/no improvement is now a TRIGGER for gap-analysis + architecturally-different approaches.** Verbatim: *"If solutions only show marginal or no improvements, make an in-depth analysis of the reason for the gaps to the desired solution and explore architecturally different approaches."* Wired into the never-stop loop: when an instance's bank plateaus, DON'T keep grinding the same family — (1) quantify the bank↔leader gap as a required step + decompose its reasons, (2) prototype an architecturally DIFFERENT approach. Applies the gap-magnitude→architecture-change rule ([[M-general-architecture-change-on-large-gaps]]). **Architecture-change candidate set (user-identified 2026-06-15) = the 3 large-gap instances:** Ch1-trajectory (~2.0×, FUNDED, build underway), Ch2-large (~2.2×, rank-1 = top campaign prize but hardest step; arch = cluster-decomp + time-expanded LKH/Concorde), Ch2-small (~11%, cheapest retrofit; joint seq+epoch LKH). Shared machinery (global search over precomputed ΔV/ToF tensor, joint discrete+epoch, basin-crossing) ⇒ build reusable core on Ch1, amortize to the other two. EXCLUDED: Ch2-medium (rank 1), Ch1-matching i/ii (closed). Memory: [[feedback-never-stop-keep-analyzing]] expansion + [[architecture-change-on-large-gaps]].

**★★ TICK ~23:50 (E-623 epoch-slack on SMALL → small is MORE epoch-tight than medium; arch-change foundation brick #2).** Per last tick's plan, extended the E-622 positive-control to the Ch2-small bank (`/tmp/ch2_e623_small_epoch_slack.py`, n=49, read-only). Positive control PASSES (reproduces 112.9960d, feasible, 5 cons=0). **Finding: small is the MORE fragile of the two — 65% of cheap legs TIGHT (<0.2d window) vs medium's 35%, only 2/43 wide, and all 5 exception legs pinned at the 600 dv_exc cap (budget maxed).** Implication for the architecture-change scoping: small was tagged "cheapest retrofit (~11% gap)" but the slack data says it is NOT a soft target — local reorder from our bank is doubly blocked (narrow windows + zero exc headroom = epoch-shift trap binds hard). EV UNCHANGED though: competitor r1=101.65 proves a better joint seq+epoch solution exists in a DIFFERENT basin ⇒ we're basin-locked, not at a ceiling; the constructor (global joint search, NOT local moves) is still the right lever and small (n=49) is still the cheapest INSTANCE to validate it on — it just must handle epoch-fragile legs + maxed budget from the start. Updated [[ch2-small-floor-14292]] memory. **Two foundation bricks now laid (medium E-622 + small E-623): both calibration instances characterized for the shared epoch-robust constructor; the build still needs a user SCOPE decision (Ch1-traj descoped ⇒ repoint at Ch2 family?) before committing multi-day machinery.** Banks UNCHANGED. Next idle-tick foundation step: large epoch-slack characterization (E-624) to complete the 3-instance constructor-difficulty profile, OR scaffold the time-expanded walkability oracle on medium (the 189.10 positive control as the build's regression gate).

**★★ TICK ~23:35 (ALL-6 BANK RE-SCORE FROM DISK COMPLETE — accurate submission table, all feasible, no further stale values).** Closed out the stale-value sweep started last tick by re-scoring the 3 Ch1 banks via their official scorers (`/tmp/ch1_score_banks.py`, read-only): **trajectory = 236,420.5 kg** (LtlTrajectory.fitness, FEASIBLE — matches memory, not stale); **matching-i = 33,338.18** (4465/25000 transfers, FEASIBLE no-dup); **matching-ii = 72,206.52** (9111/92103, FEASIBLE no-dup). Combined with last tick's Ch2 re-score, the **VERIFIED all-6 submission table (all feasible, all unsubmitted/user-gated)** is: Ch2-small 112.996d (~R6), Ch2-medium 189.10d (R1, +6.58d cushion), Ch2-large 932.53d (R2, +211d), Ch1-trajectory 236,420.5kg, Ch1-matching-i 33,338.18, Ch1-matching-ii 72,206.52. Only the medium value was stale (192.90→189.10, fixed last tick); Ch1 values all consistent. **Dominant realized-points lever = SUBMISSION (user-gated) — the table is now accurate for that decision.** Banks UNCHANGED (read-only scoring). Next never-stop foundation step if no user scope/submission direction: extend the E-622 epoch-slack positive control to the Ch2-SMALL bank (cheapest retrofit for the shared epoch-robust constructor; small is ~11% gap = the easiest arch-change target).

**★★★ TICK ~23:15 (foundation-first E-622 on medium → POSITIVE CONTROL passes + SURFACED a stale-bank correction: medium is 189.10d not 192.90d).** Per last tick's plan (architecturally-new foundation work, not floored-space grind), built `/tmp/ch2_e622_medium_epoch_slack.py` — the first brick of the shared epoch-robust constructor: a positive control + epoch-slack characterization on the medium calibration instance. Findings: **(1) STALE-VALUE CORRECTION (the unexpected repeated-analysis win): on-disk medium bank = 189.1000 d, NOT the 192.90 quoted everywhere — improved 192.90→189.10 on 2026-06-13 (commit 8199843, E-568 ultrafine retime DP) but memory/loop-state never updated.** Re-scored all 3 Ch2 banks from disk via official `kt.fitness`: small 112.996 (feas), **medium 189.10 (feas, RANK 1, beats live r1 195.68 by +6.58d)**, large 932.53 (feas, RANK 2, +211d). Updated [[ch2-medium-bank]] memory + MEMORY.md index. **(2) Evaluator gotcha logged:** `kt.is_feasible(f)` takes the FITNESS vector, not the decision vector (my first check passed x → false 'infeasible'; raw fitness [189.10,0,0,0,0] → is_feasible=True). **(3) Epoch-slack of the 189.10 order:** 175/180 cheap + 5/180 exception (exactly at the 5-budget); cheap-leg t_start window: med 0.48d, **35% TIGHT (<0.2d), 36% WIDE (>1.0d); 11.88d total idle vs 177.22d flight.** ⇒ E-568 retime-DP floored THIS order at 189.10, but 11.9d reclaimable idle + 65% non-tight legs ⇒ **medium is a live REORDER target for the shared epoch-robust constructor, not pure calibration** — raises the build's expected absolute gain (and any headroom DEFENDS our thinnest-cushion rank-1). The 35% tight fraction = the epoch-shift-trap-vulnerable legs = the difficulty knob the constructor must handle. **This was disciplined never-stop work** (architecturally-new foundation + positive-control, per the EXPANSION) that ALSO corrected a campaign-wide stale value. Banks UNCHANGED (read-only). Submission picture now accurate: medium 189.10/R1, large 932.53/R2, small 112.996/~R6 — all unsubmitted, user-gated.

**★★★ TICK ~22:50 (Ch2-large rank-1 research probe RETURNED → moonshot verdict FIRMED + sharpened; the 3 large-gap instances all need the SAME shared machinery; launched never-stop bank-defense on medium).** Background research agent (rotated analysis target off tonight's Ch1) verified large structure from `/tmp/ch2_e533_large_adj.npz`: n=1051, cheap-graph density 12.67%, 4 components (601-giant comp0 + 3×150). **Sharper-than-prior verdict — DON'T FUND large standalone (EV≈0), for a STRUCTURAL not effort reason:** we ALREADY built+ran free LKH (`elkai`, E-587) + cluster-decomposition; LKH found per-piece fixed-epoch tours as short as 8.65d but the chrono re-walk inflated EVERY one to WORSE than bank (+9.6d) — the **EPOCH-SHIFT TRAP**: any reorder shifts downstream arrival epochs → edges cheap at seed-epoch become infeasible → the ≤5-exc budget can't absorb the gap. Same trap killed 4 independent global attacks (E-042/044/046/587). The STATIC cheap order is already 340.5d (below r1=424!), so the obstruction is 100% time-dependent walkability, not TSP strength (LKH made it worse). r1=424 needs 0.404 d/leg across ALL 601 comp0 (our best 0.50 on 561/601); **no intermediate rank ⇒ binary payoff ⇒ partial = 0 points.** Reaching it needs a from-scratch **epoch-robust constructor** (order stays walkable as its own epochs settle, under 5-exc) = a multi-day build, uncertain success. **KEY CONVERGENCE:** that epoch-robust joint seq+epoch constructor is the SAME shared machinery Ch1-traj (descoped tonight) and Ch2-small (cheap retrofit) need; medium (rank-1, known-good 192.90) is its positive-control. ⇒ the ONE coherent remaining architecture lever = build the shared core, validate on medium, retrofit to small (cheap)+large (one shot). Standalone large prototype = NO. **NEVER-STOP — disciplined NO-GRIND decision (NOT idle):** considered a bank-defense grind on Ch2-MEDIUM (thinnest-cushion held rank-1: 192.90 vs live r1 195.68 = 2.78d) but REJECTED it — medium's search is floored (E-568 ultrafine already found 192.90), so re-running it = "more local search within an exhausted architecture," which the user's 2026-06-15 EXPANSION ([[feedback-never-stop-keep-analyzing]]) EXPLICITLY forbids in favor of architecturally-different approaches. The architecturally-different lever (shared epoch-robust joint seq+epoch constructor) is the right next build, but (a) needs a user SCOPE decision (Ch1-traj descoped tonight ⇒ repoint the greenlit machinery investment at the Ch2 family? validate on medium?), and (b) demands foundation-first care (build the time-expanded walkability oracle + positive-control reproducing 192.90 BEFORE search — the audit-evaluator-first lesson), not a fire-and-forget night build. This tick's never-stop work = the completed deep gap-analysis across Ch1 + Ch2-large (the EXPANSION counts deep analysis as never-stop work). Holding cores for the user's scope/submission direction rather than launching busywork or risking unsupervised machinery-building. Banks UNCHANGED. **Next-tick options if still no user input:** (i) safely scaffold the time-expanded walkability oracle + 192.90 positive-control on medium (foundation step, no greenlight needed, /tmp-only); (ii) re-survey matching i/ii (last full review concluded closed). Will pick (i) next tick if idle persists — it is architecturally-NEW + foundation-first, not a floored-space grind.

**★★★★ TICK ~22:30 (STAGE-0 RESOLVED BY ARITHMETIC — rank-5-via-fill is DEAD; killed e621; the funded joint-build's rank-5 premise is REFUTED).** Read the validated E-047 writeup (`vault/experiments/E-047-...md`, script `scripts/ch1_e577_raan_verdict.py`, passed 5/5 positive control incl. E0→L0=819kg & high-incl E100 i=89.4°→619kg). Two settling facts: (a) E-577 Phase B measured the stranded empties at **~1 kg each** (raan=0 baseline; full-sweep "n/a" for the feasible ones — a small gap, see caveat); (b) **the gap-magnitude ARITHMETIC settles it regardless of the empties' exact fill mass.** Required step to rank-5 = 372,729 − 236,420 = **+136,000 kg**. Absolute physical ceiling of fill+reopt: 99 empties × best-ever bank-pair mass (~820 kg, absurdly optimistic — empties are LEFTOVER poor matches) = +81,180 kg; Lever A (per-pair re-opt) caps ~1.1× = +~24,000 kg ⇒ even the **absurd ceiling ≈ 236,420+81,180+24,000 = 341,600 kg, still ~31k SHORT of rank-5.** Realistic fill (~1 kg/slot, E-047) adds ≈99 kg. ⇒ **rank-5 is UNREACHABLE by fill + per-pair-reopt combined, even maximally optimistic.** This is the gap-magnitude→architecture rule: +136k step ≫ +105k absurd-ceiling lever ⇒ architecture-change regime; the ONLY architecture that closes it = the WSB/low-energy coherent-physics model ([[ch1-coherent-model-r3]], E-604) — a multi-week build, DIFFERENT physics, low ROI with ~15 days left. **KILLED e621** (pid 2813350): its control (1,235) returned 1.0 kg vs banked 783 ⇒ the coarse blind sweep (raan_l/argp_l fixed=0, ea_arr 3pts) fails its own positive control = untrustworthy by our own methodology, AND moot since the arithmetic supersedes it. **CONSEQUENCE for the funded joint matching+trajectory+epoch build:** its Stage-1 premise (fill 301→~400 adds ~90k kg → rank-5) is REFUTED ⇒ the build as scoped has NO rank-5 payoff. Escalating an honest reframe to the user (this changes the ROI of something they explicitly greenlit). **CAVEAT logged (not pursued — fails ROI gate):** E-577 never ran the FULL raan/argp sweep on the feasible empties (marked "n/a"); since raan lifted E100 60× (10→619 kg), the empties' *full-sweep* mass is technically unmeasured — BUT the absurd-ceiling arithmetic already bounds fill below rank-5, so measuring it cannot change the rank verdict ⇒ not worth the cores. Banks UNCHANGED. **Pivot per NEVER-STOP:** dominant realized-points lever remains SUBMISSION of the 6 banks (user-gated, re-flagged); search frontier closed on all 6; rotate the loop to bank-defense grind / a fresh analysis angle, NOT the Ch1 fill seeder.

**★★★ TICK ~22:10 (REFRAME — the T1-diversity/no-seed nulls were ALL using the WRONG TOOL; switched Stage-0 to the faithful bank-builder + relaunched E-621).** The whole tonight chain (PairUDP+multi_seed(Hohmann)+sade→cmaes, T1-diverse re-seed, no-seed controls) was probing fill-feasibility with a solver that is NOT the one that built the bank. **The bank was built by `ch1_apogee_plane_change.py` (BCP-apogee 3-impulse), which passed 5/5 positive control in E-047** — and **E-047 (2026-06-13) ALREADY answered the fill question with that faithful tool: the 99 empties are mostly FEASIBLE but at ~1 kg each (high-incl plane change = huge dv) ⇒ Lever B (fill) ≈ 99 kg total ≈ NEGLIGIBLE, NOT the +90k kg the rank-5-via-fill hope assumed.** So tonight's "fill lever STAYS OPEN" (from the no-seed confound) is correct that PairUDP can't judge feasibility from cold seeds — but it does NOT reopen rank-5, because the *correct* tool already showed fill mass is tiny. **Confirmed bank structure:** `solutions/upload/trajectory.json` = 400×21 decision vector, 301 filled (idE 0-300) + 99 empty rows (idE=−1, all-zero transfer); empties matched to nearest-incl Moon = median di 38.3°. **VERIFYING against current code (not trusting 2-day-old memory):** LAUNCHED `/tmp/ch1_e621_apogee_verify.py 6 4` (pid 2813350, runs/ch1/e621_apogee_verify.log, monitor bsqomua9u) — runs the apogee solver's full sweep on (0,0)/(1,235) controls + a di-spread of 6 empties. Smoke-tested first: (0,0) control returns 236 kg on a reduced sweep ⇒ tool works. GATE: controls feasible@>10kg AND empties feasible-but-tiny (<50kg) ⇒ Lever B negligible, rank-5-via-fill DEAD, re-confirms E-047 against current code ⇒ joint-build ROI for rank-5 collapses (its Stage-1 premise was fill adds ~90k kg). Banks UNCHANGED.

**★★★ TICK ~19:30 (CONFOUND CONFIRMED — Stage-0 "FILL CLOSED" is a FALSE NULL; the oracle can't find feasibility from cold seeds; fill lever STAYS OPEN; pivot to fixing the from-scratch feasible seeder).** Empties finished 8/8 INFEASIBLE(solve=None) (incl. TWO near-coplanar pairs (238,141) di=0.4° & (309,27) di=0.3° failing identically ⇒ inclination is NOT the driver; universal mode = hyperbolic arrival de≈87-106). **DECISIVE no-seed control result: BOTH tested controls LOST without their bank_row seed — (1,235) di=35.7° AND (0,0) di=0.0° (the EASIEST coplanar control, banks 819kg w/ seed) — same solve=None/de≈103.** ⇒ PairUDP+multi_seed(Hohmann)+sade(300)→cmaes(300) **cannot reach the feasible 3-impulse manifold from cold seeds at ANY difficulty; the bank_row seed was entirely load-bearing.** So the 8/8 empties null measured SEEDING REACH, not physical feasibility (textbook "exhausted-within-probe-resolution ≠ exhausted-of-problem"). **The "FILL CLOSED → rank-5 unreachable" verdict is OVERRIDDEN; fill (Lever B) is NOT closed.** **Critical-path consequence:** the joint build's Stage 1 must compute mass for unused-pair candidates that have NO bank answer to seed from ⇒ a **from-scratch feasible-transfer seeder is a PREREQUISITE for the whole build, not a detour.** That seeder already exists in intent — `ch1_traj_lambert_dc.pipeline()` (Lambert+DC, = task#6 title) — but it FAILS 7/8 controls. Since the good bank solutions are effectively 2-impulse (dv1=0: depart+capture) a Lambert transfer SHOULD reach them ⇒ the pipeline has a fixable defect. **NEXT (Stage 0.5): diagnose why Lambert pipeline misses 2-impulse bank solutions it should reach** (compare pipeline(0,0)/(1,235) best (t0,T1,dv,mass)+residual vs the bank's own chromosome). Fixing it unblocks BOTH the fill test and Stage 1. 2 no-seed controls (2,168/3,358) still finishing (confirmatory only — verdict already conclusive at 2/2 incl. trivial). **ROOT-CAUSE FOUND (cheaper than fixing Lambert): bank feasible transfers use T1=11-21d (median 20d) but cold Hohmann seeds are ALL hardwired T1=5d — at 5d the transfer never reaches the Moon → flat 1e6 penalty → no gradient to climb toward feasible T1. multi_seed_pop varies raan/t0/ea_dep but NEVER T1.** (Also: PairUDP caps T1≤30.4d so the ~5% of bank pairs w/ T1>30d are out-of-box; controls at 11/21d ARE in-box.) **LAUNCHED T1-diversity test** `/tmp/ch1_e620_T1seed_test.py` (pid 2803803, monitor bsdu4i2yk, runs/ch1/E620_t1seed.log): re-seed (0,0)+(1,235) controls WITHOUT bank_row but Hohmann seeds spanning T1∈{6..30}d, + 2 empties. GATE: controls RECOVER ⇒ cold-seed failure was a missing-T1-axis artifact (trivial fix = diversify T1 in multi_seed_pop) ⇒ empties testable for real; controls still fail ⇒ need a real Lambert seeder. Banks UNCHANGED.

**★ TICK ~19:05 (Stage 0 empties returning INFEASIBLE — but caught a CONFOUND before concluding; launched the decisive no-seed control).** First 3 empties back, all `INFEASIBLE :: ARRIVAL-UNREACHABLE(solve=None)`: (105,121) di=36.5°, (185,308) di=38.6°, AND crucially **(238,141) di=0.4° (near-coplanar) FAILS IDENTICALLY** — so the failure is NOT plane-change; inclination is decoupled from it. All show de≈87 residual, which = the champion arrival state is HYPERBOLIC vs the Moon (state2moon e≈87, computed from pv_arr pre-burn when solve=None) ⇒ a high-v∞ flyby the optimizer's champion couldn't capture. **CONFOUND (methodology trigger — 100% reject, audit before concluding):** the 4/4 controls were seeded with their OWN bank_row (the answer); empties get only Hohmann+random. So `solve=None` on empties could be a SEEDING/search artifact ("exhausted-within-probe-resolution ≠ exhausted-of-problem"), not physical infeasibility. **Decisive confound-breaker LAUNCHED:** `/tmp/ch1_e620_nobank_ctrl.py` (pid 2800161, 2 workers, runs/ch1/E620_noseed_ctrl.log, monitor b9pkrluax) re-runs the 4 KNOWN-FEASIBLE controls with bank_row WITHHELD (Hohmann+random only = same as empties). GATE: controls RECOVER without their seed ⇒ oracle finds feasibility from scratch ⇒ empties' solve=None is a REAL physical signal ⇒ fill-closed verdict trustworthy; controls ALSO fail ⇒ empties' null is a seeding artifact ⇒ fill lever STAYS OPEN (need real Lambert/feasible-manifold seeds, not a STOP). Empties probe (pid 2797257) still finishing remaining 5/8 concurrently. Banks UNCHANGED.

**TICK ~20:40 (Stage 0 CONTROLS 4/4 PASS; harness bug crashed empties batch → fixed + relaunched).** **Oracle VALIDATED:** all 4 bank controls reproduced their banked raw mass via PairUDP champion dv — (0,0) dv=4063→819kg, (2,168) dv=4300→721kg, (1,235) dv=4149→783kg, (3,358) dv=5170→418kg (exact bank matches). Note (2,168) has di=38.9° yet banks 721kg ⇒ a large |iE−iL| is NOT automatically fatal (apolune burn absorbs plane change) — sharpens the empty test. **KEY interp fix:** PairUDP.fitness = total dv (m/s); the right feasibility/mass signal is champion dv → RAW mass = exp(−dv/3050)·5000−500, NOT mass_from_row (which applies a destination-dependent demand clip with placeholder idD=0 ⇒ the misleading "mass=1 kg" line; demand assignment is a Stage-1 matching concern). Also note the standalone Lambert `pipeline()` FAILS (0,0) but PairUDP reproduces it — confirms the oracle switch was right. **TRIPWIRE:** the empties batch crashed the whole pool on the FIRST infeasible empty — `diagnose()` did `oa,oe,oi = state2moon(pv_arr)` but state2moon returns 6 values (state2earth slices [:3], state2moon does NOT). Fixed `[:3]`, hardened worker with per-pair try/except (one pair can't kill the batch), and relaunched EMPTIES-ONLY (controls skipped, already validated) — `/tmp/ch1_e620_stage0_fill.py 8 4 1`, pid 2797257, runs/ch1/E620_stage0.log, monitor b3236x3oh. Crashed log saved E620_stage0_crashed.log. Verdict ~24min. Banks UNCHANGED.

**★★ TICK ~20:05 (USER GREENLIT THE JOINT BUILD → Stage 0 fill-feasibility test launched, instrumented; killed Ch2 grind to free 4 cores).** User: "If the 2x gap needs the joint architecture build, go for it!" Stage 0 = the decisive gate: are the 99 EMPTY high-incl slots PHYSICALLY fillable? **Caught a harness trap first:** the standalone Lambert `pipeline()` reproduces only **1/8 known-feasible bank controls** (and that 1 = degenerate 1 kg) ⇒ it is NOT the bank-builder and would give a meaningless null on empties. Switched to the PROPER oracle = **PairUDP + multi_seed_pop (Hohmann multi-phasing on the pair's OWN geometry) + sade(300)→cmaes(300)**, the E-605 architecture. **Prior signal:** the only logged unused-pool run (`runs/ch1/70_phase3_pygmo.log`) showed **valid=0 across 150 pairs** before it was killed — but a 100%-reject rate is a METHODOLOGY TRIGGER, so Stage 0 INSTRUMENTS every infeasible pair's reject reason (departure-mismatch=harness bug / impactor / arrival-unreachable=physical / (a,e,i)-residual magnitude). **Structural foreshadow:** the 99 empties have **median |iE−iL| = 38.3°** — the good Moon matches were all consumed by the 301 filled pairs, leaving only ~38° plane-change targets. LAUNCHED `/tmp/ch1_e620_stage0_fill.py` (pid 2791336, 4 workers, runs/ch1/E620_stage0.log): 4 bank controls + 8 inclination-greedy-matched empties. GATE: controls feasible AND empties feasible at decent mass ⇒ FILL real ⇒ build Stage 1 (candidate matrix + optimal re-match); empties infeasible with arrival-unreachable/huge-residual reject ⇒ fill physically closed ⇒ rank-5 unreachable via fill ⇒ STOP. Killed the low-EV Ch2-small DP-ALNS grind (pid 2786538, NEVER-STOP fallback) to give all 4 cores to this higher-priority build. Banks UNCHANGED.

**★★★ TICK ~19:25 (E-619 Lever-A floor test = DECISIVE NULL → per-pair trajectory lever CLOSED on a GLOBALLY-SEARCHED floor; E-619 + memory written up).** Lever-A floor test finished (3 winner-seeded workers, widened capture coast T2~43d, sade(600)→cmaes(600)): **0/3 pairs beat the E-605 ~3851 floor by >50 m/s** — (245,264) 3851→3851 (+0, dv0=3119/dv1=0/dv2=732); (126,286) 3858→3858 (+0, dv1=0/dv2=690); (7,155) 4222→4221 (−1). **dv1(mid-burn) already 0 on the good winners; dv2(capture)~700 floor UNMOVED by the widened ballistic/Sun-assisted capture coast** ⇒ the optimistic ~300 capture floor was wrong; **Lever A caps ~1.1× (= E-610's +15-25k), NOT the 1.69× the corrected decomposition hoped.** Synthesis: with Lever A re-priced to ~1.1× and Lever B (fill) ~1.2-1.3×, even their PRODUCT (~1.4×) cannot reach 2× ⇒ **the 2× gap is unreachable by ANY per-pair lever in our architecture; it needs a JOINT matching+trajectory+epoch global optimizer with feasible-manifold seeding** (same mismatch as Ch2-small's joint sequence+epoch search). E-610's "rank-6, do not fund per-pair" verdict STANDS, now on a searched floor; rank-5 (+136k) re-enters scope ONLY via that multi-week build (do not auto-fund). Wrote E-619 VERDICT block + updated [[ch1-trajectory-mass-lever-exhausted]] memory. Banks UNCHANGED. **Cores:** 3 freed; the ONE corrected assumption still unresolved = Lever B fill (prior probe H3-confounded by cold-start manifold-miss). Launching a clean NEIGHBOR-SEEDED Lever-B probe (defeat H3 by seeding empties from nearest-inclination FILLED bank pair) as the bounded final piece of the commissioned gap-anatomy audit. Dominant realized-points lever remains SUBMISSION of the 6 banks (user-gated).

**TICK ~18:55 (cheap, Lever-A health-check, no-change).** E-619 Lever-A still grinding at 54min, 3 workers R@99.9%. sade(600)→cmaes(600) (~48k fitness evals/worker) is slower than the 30-50min estimate but healthy; verdict imminent. Monitor bhv8tt1u5 armed. Banks UNCHANGED.

**TICK ~18:30 (cheap, Lever-A health-check, no-change).** E-619 Lever-A floor test (pid 2775047, 3 winner-seeded workers) healthy at 28min: all 3 R@99.9% CPU. Log header-only is EXPECTED — result lines print only at worker completion (sade(600)→cmaes(600) per winner, ~30-50min). Monitor bl0li05kd armed on result/verdict/error lines. 1 core idle but NOT launching a speculative grind — the Lever-A verdict (imminent ~10-20min) decides whether to fund a fleet-scale per-pair re-opt (rank-5 headroom) or close Lever A at the 3851 floor, redirecting all Ch1 effort; thrashing the idle core now is lower-value than awaiting that decisive result. Banks UNCHANGED.

**★★ TICK ~18:05 (Lever-B probe = confounded null; pivoted to the rank-relevant Lever-A floor test).** Lever-B cold-start probe returned 3/3 INFEASIBLE incl. an 11.3° pair — but this RE-CONFIRMS E-605's KNOWN cold-start manifold-miss (H3), NOT physical infeasibility ⇒ confounded, no new info. KILLED it. **Reframed to the rank-relevant question:** Lever A (soft-burn→floor) ALONE could reach rank-5 (~372k) IF per-pair dv hits the physical ~3300; but E-605 local refinement capped ~3851 (only −380 m/s). **The decisive uncertainty = can search beat 3851→3300?** LAUNCHED `/tmp/ch1_e619_leverA_floor.py` (pid 2775047, 3 workers): winner-SEEDED (no H3 confound) + widened CAPTURE coast (T2~43d for ballistic capture) + sade(600)→cmaes(600), measuring whether dv1(mid)/dv2(capture) drop below the winner. GATE: any pair beats 3851 by >50 m/s ⇒ Lever-A has rank-5 headroom; plateau ⇒ 3851 is the true per-pair floor (~1.16x, NOT rank-5). Banks UNCHANGED.

**★★★ TICK ~17:42 (USER 2x-PROBABILITY STEER → corrected the whole gap framing; killed mis-targeted H1, launched Lever-B fill probe).** User: only chase levers that can explain the FULL 2x. Re-ran arithmetic: **the "2734 m/s per pair" was an ARTIFACT of assuming r1 fills 301 pairs** — at 301 pairs r1 implies 2686 m/s < LEO floor (~2900) = IMPOSSIBLE. 95% of Earth orbits are LEO, bank already injects at the floor (dv0 2851≈ideal 2886). **2x = PRODUCT of two achievable levers:** A (per-pair dv→floor: kill mid-burn 574 + cheap capture 921→~300; **1.69x, 84% of r1**) × B (fill 301→~360; ~1.2x). **REFUTED as 2x levers:** Sun-assisted DEPARTURE (floored) + Earth-orbit SELECTION (bank already optimal, 1.01x). **Lever B is COUPLED to matching (H4 promoted to binding):** unused earth/moon pool has median |iE−iL|=38° (good matches already taken) ⇒ naive fill needs ~38° plane changes; filling well needs JOINT re-match. **KILLED slow H1 test** (premise refuted; E-605 winners already bound per-pair floor at 3851). **LAUNCHED Lever-B probe** (`/tmp/ch1_e619_leverB_fill.py`, pid 2772677, 4 workers): feasibility+dv of 16 sampled empty pairs → does fill add mass or are high-incl empties infeasible? E-619 doc updated with corrected decomposition. Banks UNCHANGED.

**★★★ TICK ~17:10 (USER RE-OPENED Ch1-trajectory via global search → E-619 deep analysis launched; killed E-618 to free cores).** User: "test our ideas with global search methods on ch1 trajectory ... large gap = true mismatch ... 5 hypotheses + plan." Did the ground-truth re-decomposition (`/tmp/ch1_e619_burn_decomp.py`, all 301 bank pairs): **gap is DEPARTURE-energy, not capture — 98% departure-dominated, mean dv0=2851 m/s which ALONE exceeds HRI's whole implied budget (~2734).** So leaders' entire transfer incl. injection is sub-Hohmann ⇒ needs low-energy 3-body/Sun-assisted (WSB) regime, which needs long coasts. Our `ch1_pair_udp.py` caps coast ≤43d + seeds only Hohmann ⇒ that regime was never in the search box. **5 hypotheses + cheap-first gated plan written to `vault/experiments/E-619-...md`** (H1 coast-box top, H2 confirmed departure-energy, H3 cold-start=local-polisher, H4 matching re-opt, H5 parametrization-insufficiency falsifier). **E-610 "do not fund/rank-6" verdict assumed a ~3850 floor = exactly the artifact Step 1 tests; if it falls, rank-5 (~372,729, +136k) re-enters scope.** **Step 1 RUNNING** (`/tmp/ch1_e619_h1_posctrl.py`, pid 2768406): 3 E-605 winners re-run with WIDENED bounds (~150d coast) + Sun-phase seeds, sade(400)→cmaes(400). GATE: any best clearly < its ~3850 baseline ⇒ H1 confirmed. **KILLED E-618** (decisive null, already written up E-618 doc) — it was saturating all 4 cores at 0 rank-EV and starving the live high-EV H1 test (was 0% CPU; now R@100%). Banks UNCHANGED, all unsubmitted.

**★★★ TICK ~16:35 (DEEP REVIEW — E-618 plateau 4 ticks triggered it; leaderboard refetched, queue repriced, E-618 written up).** **E-618 DECISIVE NULL** (~22k independent GRASP constructions, best 123.175d, ~10d above bank, dpfail=0) → wrote `vault/experiments/E-618-ch2-small-grasp-multistart-floor.md`. With E-616 (tof-cap benign) + E-617 (isolated local opt), **Ch2-small R1 gap = genuine FREE-METHOD floor; search frontier CLOSED.** **Leaderboard refetch (`scripts/fetch_leaderboards.py`):** Ch2 small r1=101.65 / NEW r2-r3 cluster=108.77 / r4-r5=111.76–111.79 / r6=122.66 → **our 112.996 = rank 6 (+5wpt if submitted)**; medium r1board=195.68 → **our 192.90 = RANK 1 (+13.3wpt)**; large r2board=1143.56 → **our 932.53 = RANK 2 (+16wpt)**. Ch2 banks all still hold, all UNSUBMITTED. **Queue reprice (ROI, ~15 days left, 4 cores):** every search frontier is closed/floored — small (3-family free floor, this tick), medium (already rank 1), large (r1=424 moonshot, no intermediate rank), matching i/ii (CLOSED E-615, 7 families), trajectory (BCP +11% ≪ +57% needed). **NO positive-EV search lever remains.** DOMINANT lever by far = **SUBMISSION of the 6 existing banks (≈ +34 Ch2 wpts alone: medium R1 + large R2 + small R6; user-gated, already flagged).** Per NEVER-STOP (overrides empty-EV-queue escalation): keep E-618 grinding as sanctioned fallback (still healthy, 24h budget), do NOT idle/escalate for empty queue. Next deep review at +12h (~04:35) or any bank/tripwire event.

**TICK ~16:05 (cheap, E-618 health-check, null LOCKED).** E-618 at 155min: 4 workers R@99%, **~18k+ independent constructions, best_seen=123.175d** — flat across 3 ticks (~10.2d above 112.996). dpfail=0, 0 BANKED, bank intact. **VERDICT (firm): independent randomized-greedy reconstruction cannot reach within 10d of the bank basin** → with E-617 (local-descent isolated optimum) + E-616 (tof-cap not a connectivity artifact), Ch2-small's R1 gap is a genuine FREE-METHOD floor. Letting the never-stop grind continue toward higher volume to make the E-618 writeup airtight; will formally write it up + reprice at the next deep-review trigger (~21:05 = 12h clock, or sooner if E-618 plateau holds 1 more tick). Reminder: search frontier now effectively closed on all 6 instances ⇒ the dominant points lever remains SUBMISSION (all banks unsubmitted, user-gated, already flagged). Cores saturated.

**TICK ~15:35 (cheap, E-618 health-check, null holding).** E-618 at 125min: 4 workers R@99.3%, ~12k+ constructions, dpfail=0. **best_seen=123.277d** (slow drift 127→125→124→123.3, plateauing ~10.3d above 112.996). Independent GRASP still cannot approach the bank basin. 0 BANKED, bank intact, cores saturated. No tripwire. Verdict essentially locked; letting volume grow toward tens-of-thousands before writing E-618 up + pivoting to matching 3-DM B&C.

**TICK ~15:05 (cheap, E-618 health-check, null firming hard).** E-618 at 95min: all 4 workers verified **R@99.3% CPU** (live `ps` check — NOT hung despite a 27.7min log gap, which is the known artifact: progress print only fires when a completed build lands on a 2000-iter boundary, ~0.18% completion). **~8.7k+ independent GRASP constructions, floor=124.278d, barely moving (124.825→124.278 over 50min) ⇒ ~11.3d above the 112.996 bank, NONE close.** dpfail=0. The basin-overarching null is now strong: independent randomized-greedy reconstruction cannot reach the bank's basin. Will let it run toward tens-of-thousands of constructions (24h budget, 1.5h in) to fully firm, then write E-618 writeup + pivot to matching 3-DM clique-cut B&C (free-only) + flag +45wpt unsubmitted-banks lever. 0 BANKED, bank intact. Cores saturated, no idle to launch matching yet.

**TICK ~14:35 (cheap, E-618 health-check, signal firming).** E-618 healthy at ~60min: 4 workers 99% CPU, ~290–305 attempts/s, dpfail=0, **~6.5k completed independent constructions total** (w1 1420 / w2 1701 / w3 1990 builds). **best_seen converged tightly to 124.3–124.8d across ALL workers** (drifted 127→124 as samples grew, then plateaued) ⇒ from-scratch GRASP lands ~11–14d WORSE than the 112.996 bank, NONE within 11d. Strongly supports the isolated-optimum/genuine-free-method-floor thesis; per the decision rule ("thousands of constructions") this is firming but I'll let it accumulate higher volume before concluding. 0 BANKED, bank intact. No tripwire (last reprice 09:05 <12h). Cores saturated.

**TICK ~14:05 (cheap, E-618 health-check, no action).** E-618 healthy at 32min: 4 workers 99% CPU, ~290–305 attempts/s each, **dpfail=0** (every completed construction DP-evaluates cleanly), w1 855 builds / w2 425 builds. **best_seen 125.8–127.0d across workers ⇒ independent from-scratch GRASP constructions land ~12–14d WORSE than the 112.996 bank** — early but consistent with the isolated-optimum/near-forced-graph thesis; too few completed tours (~1.3k) to conclude. 0 BANKED, bank intact. **Note:** the progress-print sits after the DP eval (only fires when a completed build coincides with a 2000-iter boundary, ~0.18% completion) ⇒ log lines are sparse/irregular (≈6min gaps); NOT a staleness tripwire — workers verified R@99%. No deep-review trigger (E-618 <1 tick old, no stall/tripwire, last reprice 09:05 <12h). Cores saturated.

**★★ TICK ~13:35 (E-617 ILS local null FIRMED → pivoted to E-618 GRASP multi-start, the last untested search family).** E-617 ran to 68min: 4 chains, ~14,800 DP-evaluated bank-anchored perturbations, **accepted=0 everywhere, all state=best=112.9960**, kick escalated 3→7 + hard-restart cycling (mechanism verified). ⇒ **the bank's entire 3–7-node neighborhood is >1% worse — the bank is an ISOLATED optimum, not merely a local one.** Local-descent family (E-529 SA, E-617 ILS) is now exhausted. **KILLED E-617** (pid 2725942 +4 chains) and **LAUNCHED E-618 GRASP multi-start** (`scripts/ch2_e618_grasp_multistart.py`, `runs/ch2_v3/94_*`, pid 2736563, 4 workers/24h, rcl=3): from-scratch randomized-greedy construction on the ultrafine table (RCL top-3 random pick) → DP-polish → guard-bank if <112.996. This is the genuinely DIFFERENT family — independent reconstruction sampling permutation basins the deterministic earliest-arrival greedy (which built the bank) never reached. **Smoke verdict (informative):** construction dead-ends ~99.8% (26023 dead / 41 built in 72s) — EXPECTED on the near-forced cheap graph (33/49 nodes deg≤2); crucially dead attempts are near-free table lookups, so the ~1.75s DP only runs on completed tours ⇒ ~2050 independent tours/hr/worker ≈ **~197k DP-evaluated from-scratch constructions over 24h×4**, a solid basin sample. Early best_seen=133.5 (41 tours, uninformative). **Decision rule:** thousands of independent GRASP+DP constructions still floor at 112.996 ⇒ Ch2-small's R1 gap is a genuine FREE-METHOD floor (structure too constrained) → pivot remaining EV to matching 3-DM clique-cut B&C (free-only) + flag the +45wpt unsubmitted-banks lever (user-gated). Banks UNCHANGED, all unsubmitted.

**TICK ~12:25 (ILS E-617 health-check — local-descent null strengthening; PLANNED pivot set).** 31min in: healthy, 4 chains 99%, ~6800 bank-anchored perturbations, kick escalates 3→7 then hard-restart resets (mechanism verified). **`accepted=0` everywhere ⇒ every candidate lands ≥114d ⇒ the bank's entire 3-7-node neighborhood is >1% worse than 112.996. The bank is ISOLATED, not merely a local opt** — consistent with competitor 101.65 living in a structurally different region unreachable by local perturbation. 0 BANKED, bank intact. **Limitation noticed:** STALE_LIMIT=250 caps kick at 7 (never hits KICK_MAX=12), so E-617 tests only the LOCAL neighborhood — NOT the large inter-basin jump the campaign thesis (basin-overarching) calls for. **PLANNED PIVOT (not thrash — a sequence): let E-617 run to ~1h to firm the local null; if still 0 improvement, reallocate cores to a genuinely DIFFERENT experiment — a from-scratch randomized-greedy (GRASP) multi-start that constructs tours independent of the bank (true basin-overarching test).** Cheapest-insertion + DP + ILS = local-descent family is now ~exhausted; the untested family is independent reconstruction.

**★★ TICK ~11:55 (E-617 redesign: hot-SA → ILS; the SA test was INVALID).** Cold-tail check at 84min exposed the flaw: T cooled 5.0→0.3 yet all 4 chains froze at 117–121d (5–8d WORSE than bank), never descending toward 112.996, 0 improvements, empty hist. **Diagnosis:** T0=5.0 lets the walk flee the bank basin immediately, then the cold tail freezes it at ~120 — a hot SA that abandons the incumbent NEVER exercises the repair operator near good solutions, so it tests nothing about repair quality (NOT a null, an invalid test). **Fix (non-confounding):** rewrote E-617 as ILS / basin-hopping — always perturb the BEST incumbent, accept only improvements (+ a 1% RRT sideways band), escalate kick 3→12 on stagnation, hard-restart from best at stale=250. Smoke-tested: incumbent now STAYS pinned at 112.9960 and perturbs the bank (correct). **Relaunched 4 chains/48h** (`runs/ch2_v3/93_*` overwritten, pid 2725942), ~200 evals/min, all state=best=112.9960. **This is now the valid decisive test:** ~700k bank-anchored perturbation+strong-repair attempts; beats 112.996 ⇒ repair was the lever; doesn't ⇒ robust evidence the bank is a strong optimum for this graph+grid. Verdict window: a few hours.

**TICK ~11:25 (health-check, no action — WATCH ITEM logged).** E-617 at 52min: mechanically healthy (4 chains 99% CPU, fresh log, DP_fail steady ~37%, ~10.8k evals, accepted>0). **best unchanged 112.9960, 0 BANKED.** Science signal: acceptance ~95% and all chains' state stuck wandering 119–125d, never returning toward bank — high-T random-walk regime; T now cooled 5.0→0.9, cold tail (T<0.3 intensification) due ~30-40min, bank-anchored reseed at 4h. **Discipline: NOT retuning SA — E-529 (which PRODUCED the 112.996 bank) uses the identical schedule T0=5.0/decay-0.999; the only variable E-617 changes is the repair operator, so retuning would confound the test.** Verdict criterion: best still 112.996 after first cold-tail + reseed cycle ⇒ legitimate null (cheapest-insertion doesn't beat floor). Cores saturated.

**TICK ~10:55 (health-check, no action).** E-617 healthy: 4 chains 99% CPU, ~4k evals in 20min, 62% DP-feasible (DP_fail~38%), accepted>0, SA cooling T 5.0→2.6, states wandering 119–125. best unchanged 112.9960, 0 BANKED — too early. No tripwire.

**★★★ TICK ~10:20 (step A executed — the missing operator was REPAIR QUALITY, not architecture).** Audited E-529 DP-ALNS source directly (the canonical Ch2-small grind): it ALREADY has SA uphill acceptance (line 272), destroy ops, and a DP that finds the PROVABLE-optimum schedule (timing + exc-bridge selection) per order. So small IS genuinely basin-searched on the order axis. The ONE weak link: `repair_random_insert` drops removed nodes at RANDOM positions ("Fast but may fail DP", line 188) — exactly the operator the matching E-579 lever-2 replaced with EXACT repair, and the textbook ALNS workhorse we never wired into Ch2-small. **Reality check that reshaped the plan:** ultrafine DP is NOT ~200s (E-529 docstring stale/pre-numba) — measured **~1.8s/eval** warm; per-candidate optimal-window repair (~230 DP/repair) is still too slow, but **cheapest-insertion** ranked by the precomputed min-over-epoch cheap-tof table is microsecond-cheap and DP-evaluates only the assembled candidate, so per-iter cost is unchanged. **BUILT + LAUNCHED E-617** (`scripts/ch2_e617_dp_alns_cheapest_repair.py`, `runs/ch2_v3/93_*`): E-529 loop + cheapest-insertion & regret-2 repair, guard-banked (backup `.bak.20260615.e617` + round-trip verify before commit). 4 chains, 48h, ~180 evals/min aggregate (~500k over 48h ≫ E-529's lifetime). Healthy: DP_ok>0, accepted>0, SA bouncing 116–123 vs best 112.996 (wide basin exploration). **This is the decisive basin-overarching test for small:** strong-repair destroy+SA still floors at 112.996 ⇒ true model floor; crosses ⇒ repair quality was the lever. **E-616 tof-cap probe KILLED at 0 new edges/500-scanned (full-res tof→40, epoch-step 1.0)** = soft-null, cheap-graph sparsity is PHYSICAL not a tof-cap artifact (reclaimed its 3 cores for E-617). **E-518 CP-SAT killed earlier** (LB crept to only 58d vs bank 113d, best feasible 152d worse — relaxation permits ≪bank ⇒ confirms SEARCH problem, won't prove floor). **NEXT:** watch E-617 for a sub-112.996 bank; if it floors too, small's R3 gap is a genuine model floor → pivot remaining EV to matching 3-DM clique-cut B&C + flag the +45wpt unsubmitted-banks lever.

**★★★★ TICK ~09:05 (USER FRESH-PERSPECTIVE CHALLENGE → ground-truth audit reframes the whole campaign; reallocated all 4 cores off the confirmatory grind onto decisive tests).** User: take competitor solutions as ground truth (all instances except medium); what false conjectures block us, why same floors, explain competitor progression. Launched 3 parallel agents (2 official-UDP audits + 1 web background). **FINDINGS:** (1) **Ch2 model FAITHFUL** — agent scored our small.json through official `compute_transfer`: exactly 112.996d feasible, 5 exc; dv_thr=100/dv_exc=600/n_exc=5 are OFFICIAL header values. The "MODEL-FLOORED" framing was wrong: scoring is faithful, the floor is a SEARCH gap. CRITICAL: no repo script had EVER scored our json through the official UDP until now. (2) **Matching = NP-hard 3-DM** (3-coordinate coupling, not poly-solvable/min-cost-flow); dual bound 34118 proves r1=33555 reachable but free solvers can't close; **user has NO paid solver (free-only)** → build 3-DM clique-cut B&C, NOT Gurobi escalation. (3) **FC1 (the real blocker): "7 families converged ⇒ ceiling" is FALSE** — all 7 are ONE architecture (construction+local-moves on a FIXED cheap graph, epochs scheduled DETERMINISTICALLY). Missing = the competitor pipeline: **precomputed ΔV/ToF tensor → time-expanded LKH / SA / GA over JOINT (sequence+epochs), epochs as FREE vars w/ uphill acceptance** (E-587 ran LKH on FIXED-epoch matrix = wrong). (4) tof≤8d precompute cap vs official (legs capped by dv+makespan only); cheap graph only **5.9% connected, 33/49 nodes deg≤2**. **RUNNING (4 cores):** E-518 CP-SAT in-model LB on small (`runs/ch2_v3/91_*`, loose LB≪bank ⇒ relaxation permits sub-bank ⇒ search problem); E-616 tof-cap probe (`scripts/ch2_e616_*`, `runs/ch2_v3/92_*`, early 0 new edges/500 ⇒ leaning sparsity-physical). **NEXT (step A, the build): joint sequence+epoch SA + time-expanded LKH for small & large.** Killed E-529 confirmatory grind (floored-space busywork). Banks UNCHANGED, all unsubmitted.


**TICK 03:15→07:53 (cheap, healthy, no-change ×11):** E-529 grind ALIVE (~445min), log fresh (0min age), all 4 chains accepting (734-809/chain), best frozen 112.996, 0 BANKED, reseeds=4, T cooling ~2.6-2.9 (normal). No tripwire; last full deep-review 01:05, next forced at ~13:05. Frontier unchanged (all 6 floored/locked; submission = dominant lever). Gave user a full status summary at ~07:50 (all-floored + submission is the +45wpt lever). Banks unchanged, all unsubmitted.

**TICK 02:45 (cheap-tick + QUANTIFIED large-rank-1 reachability analysis → moonshot confirmed with numbers):** E-529 grind ALIVE 100min, accepting (169-194/chain), best frozen 112.996, 0 BANKED. Resolved a tension in the large repricing by computing d/leg: **bank large 932.53d/1050legs = 0.888 d/leg; beam comp0 partial (561/601) 283.31d/560legs = 0.506 d/leg; comp0 215-path = 0.857 d/leg; r1=424.62/1050 = 0.404 d/leg.** ⇒ r1 requires ALL 601 comp0 nodes walked at ~0.40 d/leg, but our best is 0.506 on only 561/601 and completing the scarce tail inflates toward 0.86+ (insertion-repair 1.36). A realistic COMPLETE near-optimal large tour ≈ comp0 ~304d(optimistic) + 3×small ~225d + 5 bridges ⇒ **~540-650d, i.e. 120-225d ABOVE r1=424**, and there is NO intermediate rank between our 932 (rank 2) and r1=424 ⇒ **partial comp0 progress buys ZERO points.** FINE-GRID RECHECK (guarding against the E-045 coarse-grid-inflation trap): comp0 partial at 0.01d grid = 0.499 d/leg (vs 0.506 coarse, only −1.4% — the ~0.12 d/leg E-045 effect was specific to the bank's late-epoch t~1500-2200d legs, not comp0's early legs; STEPS=1600 made the last leg infeasible = the non-monotone grid sweet-spot). VERDICT (quantified + fine-grid-confirmed): large rank-1 = genuine moonshot needing a comp0-routing breakthrough to ~0.40 d/leg on all 601 (our best 0.50 on 561/601 + 40-node completion wall; 8 methods failed); EV≈0 for our methods. Reinforces: **submission is the dominant points lever** (medium rank-1 + large rank-2 banked, unsubmitted). Grind continues as NEVER-STOP fallback. Banks unchanged, all unsubmitted.

**TICK 01:40 + 02:12 (cheap, healthy, no-change ×2):** E-529 small grind ALIVE (66min), all 4 chains accepting (90-120 acc, SA moving), best frozen 112.996, 0 BANKED, no bank delta. DP_fail ~99% (expected on tight cheap graph; ~170 feasible evals/chain in 66min ⇒ ~11k/chain over 72h, ≪ E-609's 422k ⇒ confirms low-EV/confirmatory). State mk drifted to 134-142d = normal high-T (T≈4.2) exploration, reseeds from bank every 4h. "Best frozen 3 ticks" = EXPECTED on the proven-floored space (already deep-reviewed 01:05; nothing changed ⇒ NOT a new review trigger). Grind kept as sanctioned NEVER-STOP fallback (empty positive-EV queue — frontier floored on all 6, submission is the only points lever). Banks unchanged, all unsubmitted.


**★★★ TICK 01:05 (DEEP-REVIEW repricing + honest frontier audit → cores re-launched on Ch2-small CONFIRMATORY grind; SUBMISSION is the dominant un-pulled lever):** Refetched leaderboard (00:46). **Full 6-instance frontier status, soberly:** (1) **Matching i/ii — CLOSED** (7-family free-method ceiling = bank, this session, Levers 1+2 both decisive null). (2) **Matching trajectory — low EV** (BCP ≈ +11%, need +57% for r5). (3) **Ch2-small — MODEL-FLOORED at 112.996 (rank 6)**: E-609 (422k-order) + E-613 (continuous) + E-614 (timing-DP) ALL floor at 112.996, and the 2026-06-13 edge-set audit proved the cheap graph is COMPLETE (0 secretly-cheap inter/intra edges) ⇒ every within-cheap-graph lever is measured-shut; residual to r5 (111.79, +1.2d) is OUT-OF-MODEL only. (4) **Ch2-medium — RANK 1** (192.90 < r1board 195.68), unsubmitted. (5) **Ch2-large — SECURE rank 2** (932.53; next non-us 1143, +211 cushion); r1=424.62 is a LONE-OUTLIER moonshot (same team's 2nd-best=1143) needing makespan HALVING ⇒ rank-1 implausible, and there is NO intermediate rank to gain ⇒ large search EV ≈ 0. **NET: the search frontier is closed/floored on all 6; the ONLY lever that converts work→points is SUBMISSION (all 6 banks unsubmitted = 0 actual points; submitting ≈ +45 wpts incl. medium rank-1 + large rank-2). FLAGGED to user (user-gated).** **Cores action (NEVER-STOP, not idle):** relaunched `ch2_e529_dp_alns.py 4 72` from the CORRECT 112.996 bank (deleted stale Jun-6 ckpts whose best=116.38 would have started chains worse) → runs/ch2_v3/90_e529_small_grind_resume.log. Honest label: **CONFIRMATORY / low-EV** (E-529 is a weaker sampler than E-609 of an already-floored space; grind-banks-don't-idle, guard-banked, kill anytime). E-595 insertion-repair = WEAK (inserted 11/40, mk exploded 283→777d); B=10 regen non-reproducible (stranded 215/601). Matching E-writeup + memory repricing done this tick.

**★★★ TICK 00:20 (LEVER-2 large-window destroy+accept = DECISIVE NULL → MATCHING FREE-METHOD CEILING CONFIRMED across 7 families; KILLED + PIVOTED all 4 cores to Ch2-large; built+validated E-595 TD insertion-repair):** E-579 Lever-2 at large windows (m-i ruin_k=120, m-ii ruin_k=250, exact HiGHS repair + LAHC + random/worst/blocking ALNS) ran to ~30min: **all 4 workers `wors=0`, every operator `0 wins`, `cur` NEVER left bank** — m-i ~1.2M iters, m-ii ~500k iters, ~1.7M total moves, ZERO ridge crossings. ⇒ even LARGE coordinated destroy + exact repair reconstructs the bank's EXACT mass every time (33338.184 / 72206.516). Killed early (verdict unambiguous; 29min would add nothing; banks confirmed intact m-i 4465 / m-ii 9111 sel). **★ MATCHING FREE-METHOD CEILING CONFIRMED = bank, across 7 method families:** SCIP-i/ii (E-578), exact-LNS (E-048), swap-SA (E-582, 970M moves), LAHC small-window (E-579 k≤40), LAHC large-window (E-579 k=120/250), GRASP (E-577, +2.2 one-off no rank cross), memetic (E-581). User's multi-basin conjecture is REASONABLE but the bank is unreachable-past for ALL free operators (local swaps AND large structured destroy) — the +217/+1507 gap to r1 is a true integrality/heuristic gap a free method can't cheaply cross (Gurobi license-blocked, dual 34118 confirms r1 reachable in principle, just not by us). **Matching frontier CLOSED — write E-writeup, do not re-open without a paid exact solver.** **★ PIVOT: all 4 cores → Ch2-large comp0 (×1.778, the campaign's one real remaining search prize, r2→r1).** Frontier = INSERTION (beam width + visit-scarce-early ALL exhausted; plain B=10 @571/601 is the wall). **Built `scripts/ch2_e595_comp0_insertion_repair.py`** (TD-aware cheapest-insertion: for each scarce leftover, slots where both neighbours are cheap-adjacent + append-at-end; RE-WALKS suffix at shifted epochs via find_earliest_transfer; min-makespan feasible slot; scarcest-first; /tmp-only). **VALIDATED on 561-partial: engine WORKS (inserted 5/40 in 6s, epoch re-walk correct).** ⚠ CONCERN: mid-path inserts inflate makespan via epoch-shift (283→570d after 5 = ~1.0 d/leg ≫ beam's 0.48) — completing comp0 this way may beat our 932.53 bank but likely NOT r1=424.62; may need post-insertion TD-2opt. **Running:** regen clean 571-partial (plain B=10 start=134, pid 2630501, ~9min, → /tmp/ch2_e594_comp3_partial_B10.json) as the better insertion seed; E-595 smoke on 561 in flight. **★ NEXT TICK: read E-595 smoke (does it COMPLETE 601? final makespan?) → if completes, run on clean 571 seed → assemble (comp0 + 3 greedy smalls + 5 exc bridges) → kt.fitness vs 932.53 bank & r1=424.62; if makespan too high, add TD-2opt cleanup or backtracking-beam.** Banks UNCHANGED, ALL UNSUBMITTED.

Updated earlier: 2026-06-14 23:42

**★★ TICK 23:42 (LEVER-1 swap-SA = DECISIVE NULL across all 4 seeds → LEVER-2 large-window destroy+accept sweep LAUNCHED to close the user's multi-basin conjecture):** E-582 finished, all 4: **m-i s1 328.7M iters/170k acc-worse, s2 310.8M/1.375M acc-worse; m-ii s1 191.9M/24.4k, s2 137.4M/256.8k — EVERY run best = EXACTLY bank, delta +0.000, "no improvement", zero /tmp candidates.** ⇒ **~970M ejection-chain swap moves + ~1.83M accepted WORSENING moves crossed ZERO ridges.** Verdict: the swap neighborhood is too LOCAL — every single-swap-reachable basin tightens to ≤ bank; the bank is a strong swap-local attractor. Does NOT refute the user's multi-basin conjecture (better basins may exist) but proves swaps can't be the global-search operator it calls for. **★ LEVER-2 = the faithful global-jump test: LARGE coordinated structured destroy + EXACT HiGHS repair + LAHC acceptance** (E-579 engine; targeted random/worst/**blocking** ALNS destroy; guard-banked). Prior E-579 null was only at ruin_k≤40 (small windows reconstruct bank exactly); E-577's +2.2 came from a ~25%-mass window ⇒ the UNTESTED + conjecture-relevant regime is medium-large destroy. **LAUNCHED (pids 2625727 m-i / 2625728 m-ii, 4 workers):** m-i ruin_k=120 tps=6s, m-ii ruin_k=250 tps=10s (higher-headroom instance where +2.2 appeared), 2 workers each × 3600s, logs runs/ch1/e579_lever2_m{i,ii}.log. Healthy, correct banks (gap to r1: m-i +217.4/0.65%, m-ii +1507.5/2.05%). Big-window exact solves are slow (~6-10s each ⇒ ~few hundred jumps/worker) — quality-over-quantity by design. **★ NEXT TICK (~00:42 completion): read FINAL — (a) tot worsening-accepts >0 AND best>bank ⇒ multi-basin CONFIRMED, guard-bank auto-fires (validate round-trip); (b) 0 movement again ⇒ matching free-method ceiling CONFIRMED across 7 families (SCIP-i/ii, exact-LNS, swap-SA E-582, LAHC small+large E-579, GRASP E-577, memetic E-581) ⇒ write the matching multi-basin E-writeup, redirect ALL cores to the higher-EV Ch2-large comp0 (×1.778 prize, the campaign's one real remaining search lever).** Ch2-large comp0 next lever (build needed, deferred while matching closes): TD-aware insertion-repair for the 30 leftover (bound rework to late positions to dodge epoch-shift trap) OR backtracking-beam; plain B=10 @571/601 is the wall, /tmp 571 partial was clobbered (regenerate via plain B=10 with fixed B-suffix filename). Banks UNCHANGED, ALL UNSUBMITTED.

**HEALTH-WATCH 23:10 (cheap tick, no bank delta):** All 4 E-582 healthy at halfway (t~1817s, ~30min left). **Halfway verdict = robust NULL forming:** mi_s1 166M iters / 85.7k accepted-worse, best STILL = bank 33338.184; mi_s2 reheated to bank; mii_s1 best=bank 72206.516; mii_s2 hot-drifts to 70244 then best=bank. NO /tmp bank-beating candidate written. ⇒ ejection-chain SWAP neighborhood does NOT cross to a better basin (all single-swap-reachable basins tighten to ≤ bank). **PLANNED PIVOT on completion (~23:39):** Lever-2 = LARGE-window destroy + exact repair + SA/LAHC ACCEPTANCE (untested regime — E-579 was null only at ruin_k≤40; E-577 showed a ~25%-mass window finds different/+2.2 packings). Reuse `scripts/ch1_e579_matching_lahc.py` at large ruin_k, OR adapt E-577 mip_lns drop=0.25 with Metropolis-accept instead of reset-to-best. Cores full till ~23:39 → no launch this tick.


**★★★ TICK 22:43 (USER CONJECTURE — matching = MULTI-BASIN not single-basin → deep analysis delivered + Lever-1 SA-swap engine E-582 built & 4-seed test LAUNCHED; Ch2 DIV beam = FINAL NULL):** User (20:22) reframed matching: top-team submission progression looks like exploration across MANY near-equivalent BASINS, not descent within one ⇒ needs GLOBAL inter-configuration search, not basin-escape. **Deep analysis = conjecture WELL-SUPPORTED (4 evidence pillars):** (1) headroom real & reachable — m-i SCIP **dual 34118 > bank 33338 > r1 33555**, so r1 is BELOW the LP ceiling = reachable in our faithful model (E-039); (2) leaderboard fingerprint = plateau — team 867404 holds m-ii r1 + a LADDER of ~11 distinct r2 (73569–73714, 0.2% spread) = repeated time-limited parks, nobody proved optimum; (3) structure spawns basins — near-uniform U(0,10) weights + near-perfect matching + perfect regularity (m-i: every node in EXACTLY 5 transfers) = vast plateau of near-equal configs; (4) every method we ran is SINGLE-basin (exact-repair mass-deterministic, greedy/GRASP samples WORSE basins, nothing moves LATERALLY between good configs). Rival explanations RULED OUT: "model gap" (E-039 byte-faithful), "just use stronger exact" (Gurobi 13.0.2 installed but **LICENSE-BLOCKED**: 25k vars ≫ 2k cap). **★ CORRECTS prior "matching exact-locked ceiling" framing → reachable MULTI-BASIN search gap.** **Built Lever-1 engine `scripts/ch1_e582_matching_sa_swap.py`:** SA over adjacency-guided EJECTION-CHAIN swap moves (add conflicting transfer i, eject its ≤3 conflicts, greedy-refill freed nodes) + Metropolis worsening-acceptance + tabu on freshly-ejected + periodic ejection_improve re-tighten/track; starts FROM bank, /tmp-only, banks NOTHING. Validated correct through 4 smoke fixes (uniform-i→0% accept→adjacency-guided; flatnonzero O(n)→O(1) rejection sampling; tight-optimum every-swap-downhill→ejection-chain on NET delta; periodic-block-bypassed-by-continue→moved to loop top). **★ 4-SEED 1h TEST LAUNCHED (pids 2616241/49/57/61):** m-i s1(cold T0=1.5)+s2(hot T0=2.5), m-ii s1(cold)+s2(hot), logs runs/ch1/e582_m{i,ii}_s{1,2}.log. **EARLY SIGNAL @140s (single-basin so far):** cold regimes drift to tightened 33269/72026 (just −69/−180 BELOW bank), hot regimes drift FAR below (32521/70400) and re-tighten only partway — **none beats bank** (m-i 33338.184 / m-ii 72206.516); worsening IS accepted (6k–52k worse moves) so the walk is live, but adjacent swap-basins are all ≤ bank. Letting all 4 run the full hour (ends ~23:39) for the real verdict: does any tightened config exceed bank ⇒ multi-basin CONFIRMED + Levers 2-3 unlocked; if all null ⇒ swap-neighborhood basins are worse, escalate to Lever-2 (large structured destroy + SA accept) / Lever-3 (distinct-strong-parent path-relinking). **Ch2 autopilot — DIV beam FINAL NULL:** diversity-blend B=16/24/40 reached **563/564/561** of 601, ALL worse than plain B=10's **571** ⇒ the entire "visit-scarce-early" family (WARN-gen, PEN-score, DIV-blend) is DEAD; plain B=10 @571/601 (leftover 30, 0.480 d/leg) remains the wall. comp0 next lever = TD-aware insertion-repair (bound rework to late positions to dodge epoch-shift trap) OR backtracking-beam. Cores full till ~23:39 (E-582); Ch2 relaunch deferred. Banks: large 932.53(r2), m-ii 72206.516(r7), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.


**★★ TICK 22:02 (width AND stranding-penalty BOTH FAILED to complete comp0 → reframed as INSERTION problem; launched diversity-blend beam + deep-partial dump):** Read all 4 prior-tick beams: **plain B=32 = 560/601 @0.485 d/leg then the beam DIED (no feasible children)** — wider was NOT better than B=10's 571. **All 3 stranding-PENALTY variants REGRESSED and died early: PEN=0.5→345, PEN=1.5→204, PEN=4.0→260** (higher penalty = earlier death). **DECISIVE structural conclusion: BOTH "visit scarce nodes early" strategies — WARN-in-generation (tick 21:24) and PEN-in-score (this) — BACKFIRE**, because pushing into the 117 low-degree nodes early paints the path into a corner FASTER (scarce nodes have few onward hops). Plain beam DEFERS them and reaches furthest (~571) but append-only construction then can't pick them up ⇒ **this is fundamentally an INSERTION/diversity problem, not a construction-ORDER problem.** Insertion-repair is the obvious fix BUT hits the known TD epoch-shift trap (any mid-path insert reshuffles ALL downstream epochs = the E-587 elkai failure mode [[ch2-large-first-bank-topology]]) ⇒ deferred. **Instead launched the cleaner TD-correct lever — DIVERSITY-BLEND beam (E594_DIV):** keep HALF the beam slots by pure makespan (fast core-threaders) + HALF by #scarce-nodes-visited (so a scarce-conscious lineage survives ALONGSIDE greedy WITHOUT forcing every beam to chase scarce → the thing that killed WARN/PEN). Directly targets the observed failure mode (all beams die SIMULTANEOUSLY because they all defer the SAME scarce nodes). **Launched 3 DIV variants B=16/24/40 (LOWDEG=25, 1 start×2200s, pids 2609994/2610002/2610009, logs e594_comp0_div{16,24,40}.log) + 1 plain B=10 dump (pid 2609780, logs e594_comp0_plain_dump.log) to regenerate a deep ~571 partial WITH the new partial-dump code** (fixed dump filename to include B+mode so configs don't clobber /tmp). All 4 cores ~93-99% CPU; budgets end ~22:34-22:38. ★ NEXT TICK: read 4 logs → (a) ANY DIV COMPLETES comp0 <~0.6 d/leg ⇒ IMMEDIATELY assemble (comp0-path + 3 greedy smalls + 5 exc bridges) → kt.fitness vs r1=424.62 & bank 932.53; feasible <424.62 = RANK 1 (+1.778); (b) if DIV beats 571 but still strands ⇒ deeper partial = better insertion seed; (c) if DIV also caps ~560-571 ⇒ build TD-aware insertion-repair (re-walk suffix per candidate, bound work to late positions) OR backtracking-beam (drop last hops on dead-end, retry) as the next lever. Banks: large 932.53(r2), m-ii 72206.516(r7), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.

**★★ TICK 21:24 (WARN-beam REGRESSED → reverted; widened to plain B=32 + launched 3 stranding-PENALTY-in-SCORE variants on all 4 cores):** Read the Warnsdorff-in-generation beam (E594_WARN=1, B=16): `566/601 @0.842 d/leg` and `366/601 @0.798` — a clear REGRESSION vs plain B=10's 571/601 @0.480. Forcing constrained-node hops inflates makespan without improving completion ⇒ **WARN folded into GENERATION hurts; reverted to WARN=0.** Two structural fixes launched instead: (1) **plain B=32 topK=6** (pid 2603093, 1 start × 2600s, log e594_comp0_b32.log) — does pure width complete comp0? (2) **stranding penalty in the beam SCORE** (NEW, the untested high-value lever): discovered comp0 degree is SHARPLY BIMODAL — **117 nodes at static-deg ≤20, then a gap to ~147+** (pctiles 5/10/25/50 = 10/11/147/152). Those ~117 scarce low-degree nodes ARE the tail that strands. Added `score = mk + PEN·(#unvisited scarce-nodes)` (LOWDEG=25 captures exactly the 117), env-gated so plain-beam default is untouched. **Launched 3 variants on idle cores: PEN=0.5/1.5/4.0** (pids 2603938/46/54, B=24 topK=6, 1 start × 2400s, logs e594_comp0_pen{05,15,40}.log). Rationale: soft score term pulls scarce nodes earlier WITHOUT the generation-forcing that blew up WARN's makespan. Also fixed E-594 main() to dump deepest-partial + leftover nodes to /tmp (insertion-repair material survives any strand). All 4 cores busy ~95-99% CPU; budgets end ~21:58-22:00. ★ NEXT TICK: read all 4 beam logs → (a) ANY complete comp0 at <~0.6 d/leg ⇒ IMMEDIATELY assemble (comp0-path + 3 greedy smalls + 5 exc bridges) → kt.fitness validate vs r1=424.62 & bank 932.53; feasible <424.62 = RANK 1 (+1.778 pts); (b) if best still ~571-590/601, take the deepest partial + build cheapest-TD-insertion tail-repair for the ~30 leftover scarce nodes; (c) if penalty helps completion but hurts makespan, sweep PEN finer. Banks: large 932.53(r2), m-ii 72206.516(r7), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.

**★★★ TICK 20:48 (Ch2-large BEAM CROSSES THE WALL — strongest rank-1 lead of the campaign):** comp0 beam (E-594, B=10) finished: **571/601 (start 134, 0.480 d/leg) and 541/601 (start 163, 0.431 d/leg)** — FAR past every greedy's max of 271/601, stranding only in the last ~30-60 low-degree nodes. **★ Per-leg 0.43-0.48d ≈ r1's 0.404 d/leg.** Extrapolation: a COMPLETE comp0 path near 0.45 d/leg ≈ 270d, + 3 greedy smalls (~42d) + 5 exc bridges (~15d) ≈ **~327d total < r1=424.62 ⇒ RANK 1 (+1.778 pts)**. This is the campaign's most promising single lead — the from-scratch TD constructor route is WORKING, just needs to thread the final tail. **Fix launched: Warnsdorff folded INTO the beam's hop-generation** (rank candidate hops by (onward-unvisited-degree, tof) so constrained low-degree nodes — exactly the tail stragglers — get visited EARLY instead of stranded) + wider beam **B=16 topK=6** (pid 2599008, 2 starts × 1400s, log runs/ch2_v3/e594_comp0_warn.log). ★ NEXT TICK: read warn-beam → (a) if it COMPLETES comp0, IMMEDIATELY assemble (comp0-path + 3 greedy smalls + 5 bridges) → kt.fitness validate vs r1=424.62 & bank 932.53 — a feasible <424.62 = RANK 1; (b) if it reaches ~590+/601 but still tails out, add a cheapest-insertion tail-repair for the last stragglers OR widen B to 24-32 / add onward-degree into the beam SCORE (not just generation); (c) if it regresses, revert WARN. Honest caveat: the 0.43-0.48 d/leg is over a PARTIAL path; the final stragglers may force longer hops that raise the mean — but even 0.6 d/leg × 600 ≈ 360d+57 ≈ 417d is ~r1. Worth full push. Banks: large 932.53(r2), m-ii 72206.516(r7), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.

**★★ TICK 20:25 (DEEP — Ch2-large prize DECOMPOSED to a single bottleneck via 4 probes; the r2→r1 build is now precisely scoped):** Spent this tick decomposing WHY from-scratch TD construction fails on Ch2-large (bank 932.53d r2, r1=424.62). Four read-only probes, all /tmp/no-bank:
  - **E-576 global TD-greedy NN (24 starts): ALL STRANDED**, no feasible tour built. Naive greedy can't assemble a complete 1051-node tour (wastes 5-exc budget crossing components + paints into corners).
  - **E-592 component-aware probe (DECISIVE decomposition):** cheap-graph = exactly **4 components [601, 150, 150, 150]** (dv_thr=100, dv_exc=600, n_exc=5); bank tour crosses components **5 times** (=5 inter-component exc legs). Intra-component TD-greedy: **all three 150-node SMALLS COMPLETE on every start at ~13–15d (~0.09 d/leg) — SOLVED.** The **601-node comp0 STRANDS on every start** (163/250/188 of 601). ⇒ **comp0 is the ENTIRE difficulty; smalls + 5 bridges are tractable.**
  - **E-593 Warnsdorff (most-constrained-first) on comp0:** STILL strands (best 192/601), consistent wall ~190 nodes across independent starts. comp0 internal-degree is DENSE (median 152!) yet greedy walls ⇒ **the wall is a TIME-DEPENDENT PHASING wall** (static neighbors exist but aren't phase-reachable within the hop window at the accumulated epoch), not static connectivity.
  - **E-593 wide-window (WIN 4→15):** pushes further (271/601) but STILL strands AND at ruinous 1.6 d/leg ⇒ longer hops partly escape phasing but greedy fundamentally can't complete comp0.
  **VERDICT: the Ch2-large r2→r1 prize = a single well-scoped sub-problem — a COMPLETE time-dependent Hamiltonian path through the 601-node comp0 that single-path greedy (any window/rule) cannot build; it needs a MULTI-PATH constructor (beam / backtracking / LKH-intra-comp0-with-TD-coordination).** This is the campaign's one real remaining +EV search lever (hard ×1.778, +1.778 pts for r2→r1). **BUILD STARTED: E-594 beam-search TD constructor** (B paths, top-K cheapest-hop expansion, distinct-current-node diversity, dead-path prune; /tmp only, banks nothing). **Beam VALIDATED** (smoke2, single-start 400s): COMPLETED a 150-small in 89s at 16.81d (0.113 d/leg) → /tmp/ch2_e594_comp0.json, "GO assemble." (Beam is ~8–10× slower than greedy and slightly worse per-leg on the easy smalls, but it COMPLETES — which is the property comp0 needs.) **★ comp0 BEAM LAUNCHED: pid 2594989, B=10 topK=5 STEPS=80, 2 starts × 1000s, log runs/ch2_v3/e594_comp0_beam.log** — the real test: can a width-10 beam cross comp0's ~190-node phasing wall where every greedy stranded? ★ NEXT TICK: read comp0 beam → (a) if it COMPLETES comp0 at mk well below the bank's comp0 contribution, assemble (comp0-beam-path + 3 greedy smalls + 5 exc bridges) → kt.fitness validate vs 932.53 (and vs r1=424.62 for the rank-1 prize); (b) if beam ALSO strands comp0, widen B / add backtracking / try LKH-intra-comp0 with chrono re-cost. NOTE early-step timing ~0.8–1.2s/step ⇒ ~8–15min per 601-node start. Banks: large 932.53(r2), m-ii 72206.516(r7), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.

**★ TICK 19:35 (E-581 memetic NULL CONFIRMED → matching free-method ceiling LOCKED at 72206.516; PIVOTED to Ch2-large; launched E-576 TD-greedy NN constructor on idle cores; LKH-availability correction):** **(a) E-581 DONE = clean NULL:** all 4 workers `best=72206.516` (=bank), ~1000 recombinations, `NO IMPROVEMENT … bank untouched`. ⇒ **MATCHING FREE-METHOD HEURISTIC CEILING CONFIRMED at 72206.516 (m-ii) / 33338.184 (m-i)** across 5 method families (SCIP-i/ii null, exact-LNS E-048 null, LAHC E-579 null, GRASP E-577 +2.2 large-window only, memetic E-581 null). Root cause = matching basin is exact-locked: distinct STRONG parents can't be manufactured (all perturbed basins collapse to bank under exact polish), so recombination has no small-disagreement strong pair to exploit. **MATCHING FRONTIER CLOSED** — ×1 weight, rank 7/9, +1507 to r1 ≈ true integrality gap; do not re-open without a fundamentally new (paid-solver / proven-optimal) angle. **(b) CORRECTION:** my prior loop-state framing said "LKH NOT installed" — WRONG. `elkai` (LKH binding) IS installed AND was already tried on large (E-587) and FAILED via the epoch-shift trap (a stronger TSP solver on a FIXED-epoch cost matrix makes the chrono re-walk WORSE — structural dead end, see [[ch2-large-first-bank-topology]]). So the Ch2-large prize is NOT "just run LKH"; it's a from-scratch TIME-DEPENDENT constructor that commits to realized epochs as it walks. **(c) Ch2-large gap-probe (runs/ch2_v3/91_e571_gap_probe.log, on the OLD 1048.98 bank — current bank is 932.53d):** decisive signal — median min-cheap-tof = **0.150d at EVERY epoch**, ~37 cheap FEASIBLE neighbors/node/epoch (frac_feas=1.00), realized avg leg 0.999d, r1=0.404d/leg ⇒ the gap is largely ORDERING/PHASING-recoverable; a TD rebuild is justified. **(d) LAUNCHED E-576 TD-greedy NN** (`scripts/ch2_e576_large_td_greedy.py`, 24 starts, log runs/ch2_v3/e576_td_greedy.log, writes /tmp candidate ONLY — no bank touch) = the "natural algorithm we never tried": at each step hop to the unvisited node with the SMALLEST feasible cheap tof RIGHT NOW (avoids the fixed-epoch trap by construction), 5-exc budget. **EARLY RESULT (start 0, 56s): STUCK exc=5** — pure greedy NN strands (exhausts exc budget before visiting all 1051), exactly the known stranding pattern (TICK 21:05). ⇒ confirms naive TD-greedy alone is insufficient; the partition (comp0 + 3 exc-bridge-only smalls) forces exc-bridge PLANNING that greedy-without-lookahead can't do — the constructor needs component-aware exc-bridge reservation (which is what the bank's "comp0-last star" already encodes). Letting all 24 starts run for the complete verdict; if any start completes feasible < 932.53 it's a real lead, else the next build is a lookahead/backtracking TD constructor (beam search or component-aware exc-reservation). Banks: m-ii 72206.516(r7), large 932.53(r2), medium 192.90(R1), small 112.996(#6), traj 236420(#6), m-i 33338(#9) — ALL UNSUBMITTED.

**★ TICK 18:53 (E-581 memetic mid-run = TRENDING NULL; matching heuristic ceiling all-but-confirmed; Ch2-large pivot is next):** E-581 healthy @ t~1600s (26min): 4 workers, ~300 recombinations total, **best STUCK at 72206.516 (=bank), pool NOT diversifying at top** (poolbest=bank, poolworst flat ~70.7k). Diagnosis = matching-basin stickiness: recombining bank × weak-parent(70.7k) → exact-repair on the LARGE disagreement region reconstructs the bank packing → child ≈ bank → rejected as non-distinct (Hamming<4) → pool top never diversifies. The recombination lever needs two DISTINCT STRONG (~72206) parents with a SMALL disagreement region, but **distinct strong parents can't be manufactured — all perturbed basins collapse to bank under exact polish** (the same dominance E-577/E-579 showed). ⇒ recombination has no small-disagreement strong pair to exploit. **VERDICT forming = NULL ⇒ matching FREE-METHOD HEURISTIC CEILING confirmed at 72206.516** (SCIP-i/ii null + exact-LNS E-048 null + LAHC E-579 null + GRASP E-577 (+2.2 large-window only) + memetic E-581 null). Letting it finish (~17:27 UTC) for the clean no-op verdict; not killing early (cores have no ready higher-EV job — Ch2-large needs a build). **★ NEXT DEEP REVIEW (on E-581 completion): PIVOT to Ch2-large r2→r1 partition rebuild — the campaign's one real remaining search prize (×1.778).** It's a BUILD task not a launch: from-scratch global TD constructor that rebuilds the comp0+3-smalls partition (frees the 5 bridges), cluster-decomp + strong TSP subsolver + epoch-rebuild loop. Blockers to resolve first: LKH NOT installed (need binary/elkai or a Concorde/OR-Tools-routing substitute); pure-greedy re-pairing already NO-GO (strands, prior test). Design the constructor + install a TSP solver next tick. Banks: matching-ii 72206.516(r7), others unchanged, ALL UNSUBMITTED.

**★ TICK 18:27 (E-577 BANKED matching-ii +2.223 confirmed → launched E-581 recombination memetic = decisive matching basin-cross test):** E-577 finished clean: `BANKED matching-ii 72204.293 -> 72206.516 (+2.223); round-trip OK`. Bank re-verified independently: mass=72206.516, sel=9111 (was 9110, +1 transfer), feasible=True. **matching-ii bank now 72206.516** (still rank 7 — +2.2 doesn't cross any rank boundary; gap to r1 still +1507). Cores freed (load 0.12). **★ E-581 memetic LAUNCHED** (task bqtw1x8id, `ch1_e581_matching_memetic.py matching-ii 3600 4 6 10`, log runs/ch1/e581_memetic_ii.log, ~17:27 UTC finish): 4 workers, pool_K=6, seeded from the 72206.516 bank + perturbed-greedy parents (currently mip_lns-polishing ~68k in the 720s seed phase), then recombination on parent-disagreement regions. Healthy. **This is the LAST untried matching lever** (item-4 recombination on the monolithic conflict graph; E-580 ruled out decomposition+reduction). If E-581 produces a child > 72206.516 ⇒ a NEW basin exists (recombination crosses where local search can't); if NULL ⇒ matching free-method heuristic ceiling CONFIRMED at 72206.516 ⇒ **redirect ALL remaining search to Ch2-large r2→r1 partition rebuild (×1.778, the campaign's one real prize).** Other 5 banks unchanged, ALL UNSUBMITTED.


**★ TICK 18:08 (user Q "global optimizers for matching?" → built conflict-graph probe E-580 + recombination memetic E-581; user said "run it"):** User reframed matching as a multi-basin global-search problem and asked for global-optimizer alternatives. Answered: matching = **MWIS on the conflict graph** (vertex=transfer, edge=share E/L/D node, weight=mass) ⇒ unlocks purpose-built global solvers (KaMIS reductions+evolutionary, tabu AMTS, memetic recombination, local-branching, column-gen w/ clique cuts). **★ E-580 CONFLICT-GRAPH PROBE RESULT (`scripts/ch1_e580_conflict_graph_probe.py`, read-only):** matching-i is PERFECTLY REGULAR (every transfer conflict-degree EXACTLY 12; each node used by exactly 5 transfers); matching-ii degree 9–50 mean 27. **BOTH = ONE giant component (100%), 0 isolated vertices, ~0 singleton nodes.** ⇒ **(a) component-decomposition OUT** (monolithic), **(b) cheap kernel reductions OUT** (no forced/low-degree structure; min-deg 12/9 ⇒ KaMIS's degree-1/domination reductions rarely fire ⇒ KaMIS build NOT justified — no reduction headroom), **(c) surviving lever = heuristic basin-crossing on the monolithic graph = memetic RECOMBINATION** (the methodology's item-4, the one cheap untried basin-crosser; E-577 was multistart WITHOUT crossover). **★ E-581 MEMETIC built + smoke-validated** (`scripts/ch1_e581_matching_memetic.py`): recombination = fix two parents' COMMON selections (core, feasible), exact-`_solve_sub` the DISAGREEMENT region only (small for similar strong parents ⇒ solved to TRUE optimality), child provably ≥ max(parents) and lands in a combined basin → can exceed both. Pool = bank + perturbed-greedy seeds (ejection+mip_lns polished), 4 workers, shared best, guard-banked (decisionVector format verified, round-trip+restore), never submits. Smoke (matching-i 30s): 12 children, correct no-op bank. **PLAN: when E-577 finishes (~16:22 UTC, banks 72206.516) → launch E-581 matching-ii seeded from the new bank = the decisive recombination test.** Honest EV still LOW (×1, rank 7, +1507 to r1 ≈ LP gap) — this CLOSES the matching question either way; if E-581 nulls ⇒ matching heuristic ceiling confirmed ⇒ redirect ALL search to Ch2-large r2→r1 partition rebuild (×1.778, the real prize). E-577 perturbed seeds confirmed stuck ~70k (diverse basins don't reach bank; only large-window warm-start mip_lns hit 72206.516).


**★ TICK 17:49 (E-577 found a STRICT matching-ii improvement +2.223 — but from LARGE-WINDOW exact-LNS on the bank-warm start, NOT diverse construction; refines the exact-lock lesson):** E-577 GRASP healthy, 4 workers, plateaued. **★ RESULT: 72204.293 → 72206.516 (+2.223 kg, +0.003%), strictly better + feasible.** Provenance matters: the gain came from **seed=1 = the BANK warm-start under mip_lns drop=0.25 (frees ~2277 transfers = a 50× larger exact-repair window than E-579's ruin_k=40)**, found at 27s, then HELD FLAT across 7 restarts / 10+ min = converged. The perturbed-greedy diverse basins (seeds 2–21) are all climbing but stuck ~70.4k, well BELOW bank ⇒ diverse construction did NOT win; LARGE-window exact-LNS did. **★ Methodology refinement (update [[basin-overarching-search]]):** exact-repair LNS is rigid at SMALL windows (E-048/E-579 null at block/ruin_k≤40) but a *very large* freed window (~25% of the solution) CAN find improvements small windows can't — the freed region is big enough to re-route around the locally-rigid structure. So "matching is exact-locked" was true only at small window size; the true free-method ceiling is ~+2.2 above bank, reached by large-window exact-LNS. **EV: marginal — +2.223 won't cross any rank boundary (still rank 7; next-above r6 incumbent is far, gap to r1 +1507).** Run guard-banks the 72206.516 at completion (~18:27) with round-trip verification (letting it finish to capture the validated bank-write; 4 cores low-opportunity-cost, no higher-EV ready lever). **After this banks ⇒ matching free-method ceiling is genuinely reached (+2.2 over prior bank); redirect remaining search effort to Ch2-large r2→r1 partition rebuild (the one real prize, hard ×1.778).** Other 5 banks unchanged, ALL UNSUBMITTED.


**★ TICK 17:21 (E-579 matching exact-lock CONFIRMED & FALSIFIED as a lever → killed; E-577 GRASP diverse-construction LAUNCHED — last untried matching lever):** **(a) E-579 matching-ii DECISIVE NULL:** at t=1740s, ~1.1M total exact re-optimizations across 4 workers, **0 worsening-accepts, 0 wins, cur never left bank 72204.293** even at ruin_k=40. Exact repair on ANY freed 40-transfer window reconstructs the bank packing EXACTLY ⇒ the bank is locally exact-optimal on every sub-window = a SINGULAR point under exact-repair moves. **VERDICT: exact-repair × LAHC (basin-overarching toolkit item 6) is FALSIFIED for matching — the basin is not crossable by acceptance-over-exact-repair because exact repair is mass-deterministic.** Killed early (verdict decisive, no point burning the last ~30min). This is the methodology lesson's sharpest confirmation: a sufficiently-tight incumbent makes exact-repair LNS rigid at ALL window sizes. **(b) E-577 GRASP matching-ii LAUNCHED** (task b0d4k45j9, `ch1_e577_matching_grasp_multistart.py matching-ii 3600 4 0.12 90`, log runs/ch1/e577_grasp_ii.log, ~18:27 finish): perturbed-weight randomized greedy construction → mip_lns exact-polish per restart = the ONLY untried matching lever (samples *structurally-different basins* vs re-polishing the bank). Early @23s healthy: seed=1 warm=bank 72204.3 (polishing, stuck=expected); seeds 2/3/4 from perturbed basins 68.3k–69.2k climbing. **LOW EV honest:** for a win a perturbed basin must exact-polish ABOVE 72204.3, but they start 3–4k below (greedy lands strictly below a sharp incumbent — repair-strength-gates-ceiling lesson) + the 2.05% r1 gap ≈ SCIP's 2.34% LP gap (likely a true integrality gap a free method can't cross). But it's the last untried matching lever + zero cost on otherwise-idle cores (NEVER-STOP). **If E-577 also null ⇒ matching is FULLY EXHAUSTED under free methods (SCIP-i/ii null + exact-LNS E-048 null + LAHC E-579 null + GRASP E-577 null) — redirect ALL remaining search effort to the campaign's one real prize: Ch2-large r2→r1 partition rebuild (hard ×1.778, the from-scratch global TD constructor / cluster-decomp+LKH multi-day build).** Banks unchanged, ALL UNSUBMITTED.


**★ TICK 16:54 (SCIP-ii NULL confirmed + edge-audit done + E-579 large-window matching-ii test LAUNCHED & early verdict forming):** Cheap tick. **(a) SCIP matching-ii FINISHED:** status=timelimit, incumbent (primal) = 72204.293 = EXACTLY bank, delta +0.000, NO IMPROVEMENT ⇒ **matching-ii free-exact ceiling = bank**, mirroring matching-i E-578. No free solver beats either matching bank. **(b) Edge-audit finished** (0 procs). **(c) E-579 matching-ii large-window basin-crossing test LAUNCHED** (task b1thzqs1y, `scripts/ch1_e579_matching_lahc.py matching-ii 3600 4 40 20000 5`, log runs/ch1/e579_lahc_ii.log): 4 workers healthy ~95% CPU, throughput ~150 it/30s, Stage-0 diag bank 72204.293 / 9110 sel / 891 unused nodes/type / gap to r1 +1509.74 (2.05%). **EARLY RESULT @ 90s: still 0 worsening-accepts, 0 wins across ALL 4 workers even at ruin_k=40** — i.e. larger windows did NOT enable plateau drift; exact repair on the freed window reconstructs bank mass EXACTLY every time. **⇒ Strong confirmation: MATCHING IS EXACT-LOCKED — the bank is globally tight on every freed sub-window, so exact-repair × LAHC is mass-deterministic and no acceptance rule can move it (the methodology lesson confirmed at large scale, not just small windows).** Run finishes ~17:52; if it ends at 0 movement (expected), the exact-repair×LAHC basin-crossing lever for matching is FALSIFIED ⇒ redirect basin-overarching effort to (1) GRASP diverse-construction + path-relinking E-577 (the only untried matching lever: structurally-different STARTS, not re-polishing the bank), and (2) the higher-EV Ch2-large partition rebuild (hard ×1.778). Banks unchanged, ALL UNSUBMITTED.


**★★ TICK 16:30 (BASIN-OVERARCHING SEARCH — user methodological directive + E-579 engine built/debugged):** User (16:30) flagged a CROSS-CHALLENGE lesson: when local optimization plateaus and the gap to top ranks won't close, we're likely **basin-locked** (better solution in a *different* basin), not at a problem ceiling — locally-optimal (even exact-window-optimal) solutions don't suffice; need **basin-overarching search**. Explicitly applies to **Ch1 matching (now)**, and on re-attempt to **Ch1 trajectory** (within-pairing polish → global re-pairing) + **Ch2-large** (rank-2→1 needs partition REBUILD, within-topology polish proven exhausted E-587/589/591). **RECORDED:** `vault/methodology/M-general-basin-overarching-search.md` (diagnostic signature: many methods converge + competitor STAIRCASE above us + headroom exists + our arch is improvement-only; toolkit: LAHC/SA acceptance + targeted ALNS destroy [must change BOTH — coupled] + GRASP + path-relinking + structural rebuild; exact-repair × lateral-acceptance for set-packing) + memory pointer [[basin-overarching-search]]. **★ E-579 matching LAHC engine built** (`scripts/ch1_e579_matching_lahc.py`): debugged through 3 grounded smoke findings → (1) LAHC acceptance ALONE inert (ejection repair re-climbs same basin, 0 worsening); (2) greedy-fill repair freezes (cold-start, history pinned at bank); (3) greedy repair lands strictly BELOW bank always (acc=163 lateral drift at −11.8 plateau, can't climb back). **VERDICT: greedy repair too weak to exceed a sharp incumbent → PIVOTED to EXACT repair (HiGHS _solve_sub on freed window, as coop_mip_lns) × LAHC acceptance × targeted destroy (random/worst/blocking, ALNS roulette).** This is the untried combo: existing exact-LNS has strong repair but is improvement-only. Stage-0 diag: matching-i bank 33338.18, 536 unused nodes/type headroom, gap to r1 +217.4 (0.65%). Exact-repair smoke (b2q061yys, matching-i 60s) RESULT: throughput 17245 it/60s, acc=8270 BUT **every candidate == bank exactly (0 worsening, 0 wins)** — small exact-window repair from a window-optimal incumbent always reconstructs to the same total ⇒ LAHC collapses to coop_mip_lns (proven empirically). **This IS the methodology lesson in miniature: small exact windows are RIGID, no acceptance rule crosses them.** One untested regime = LARGER windows (ruin_k 30-60): exact repair then returns equal-mass-but-DIFFERENT packings, letting cur drift across the plateau (coop kills this by resetting to best each round). **QUEUE when cores free (~14:43 SCIP-ii finish): E-579 matching-ii, ruin_k≈40, 4×~1h, as the concrete basin-crossing test.** If still 0 movement ⇒ matching is exact-locked, redirect basin-overarching effort to GRASP diverse-construction + path-relinking (E-577) and to the higher-EV Ch2-large partition rebuild (hard ×1.778). Guard-banked, never submits.

**★★ DEEP-REVIEW 10:26 (live leaderboard refetch, distinct-team rank → points; supersedes O-017 gap est.):** Submitting ALL 6 current banks as-is = **45.3 weighted pts vs 0 now**. This is the dominant campaign lever by far; every compute lever (matching Gurobi/SCIP +9-14, ch2-small pipeline +1) is marginal on top. Per-instance (bank | best-team | rank | wpts):
| inst | bank | r1 | rank | wpts |
|---|---|---|---|---|
| matching-i | 33338.18 | 33555.61 | 9 | 2.0 |
| matching-ii | 72204.29 | 73714.03 | 7 | 4.0 |
| trajectory | 236420.5 | 473332.6 | 6 | 5.0 |
| ch2-small | 116.38 | 101.65 | 6 | 5.0 |
| ch2-medium | 192.90 | 195.68 | **1** | 13.3 |
| ch2-large | 1048.98 | 424.62 | **2** | 16.0 |
ch2-medium BEATS live r1 (rank 1); ch2-large rank 2. Queue reprice: **(1) SUBMIT all 6 banks (user-gated, escalated 10:26)** ≫ (2) free SCIP on matching-i/ii (measure free-exact ceiling, +9-14pts potential) > (3) ch2-small pipeline (running, +1) > (4) E-577 matching multi-start (+~0). Gurobi license NOT justified (academic free-tier likely N/A for Honda RI; SCIP is the free path). NEXT review trigger: pipeline verdict ~15:20 or next bank delta.


**★ QUEUED COMPUTE (launch when cores free, after pipeline stages 4-5 + the armed edge-sweep finish — do NOT auto-fire, would over-subscribe 4 cores):** E-577 GRASP diverse multi-start for **matching-ii** (escape-the-basin probe). User-approved 2026-06-14 (prep now, compute queued). Rationale from deep review: every prior matching run (coop/mip_lns + CP-SAT-LNS to ~6000-var regions solved to proven OPTIMAL) polishes the SAME greedy/bank incumbent → 0 gains across ~14k exact re-opts (E-048 + runs/ch1/cpsat_lns_ii_*.log). The only untested variant = DIVERSE CONSTRUCTION (perturbed-greedy basins, exact-polish each). Low EV (×1 weight, rank ~7→best-case +6pts, gap=2.05% is a true integrality gap a free solver likely can't cross), but only untried lever + zero cost on idle cores. Script: `scripts/ch1_e577_matching_grasp_multistart.py` (syntax-validated, guard-banked w/ backup+round-trip, never submits). LAUNCH: `PYTHONPATH=src OMP_NUM_THREADS=1 micromamba run -n spoc26 python -u scripts/ch1_e577_matching_grasp_multistart.py matching-ii 3600 4 0.12 90 2>&1 | tee runs/ch1/e577_grasp_ii.log`. Ordering: edge-sweep (cheap ~200s) FIRST, then this. NOTE the real dominant lever remains SUBMISSION (all 6 banks unsubmitted = 0 pts; user-gated; flagged to user 2026-06-14).

**★ E-578 SCIP matching-i RESULT (13:43, FINISHED):** 3h tlim, **only 1 B&B node** (root LP+cuts ran the whole time), primal = **33338.184 = EXACTLY bank** (213 heuristic sols found, none beat warm-start). Dual bound 34118.30 (gap 2.34%, headroom ≤780.1 mass) is the WEAK setppc LP — does NOT prove r1 (33555.61, +217=+0.65%) unreachable, but SCIP's AGGRESSIVE primal heuristics couldn't find r1 in 3h either. **⇒ Free-exact ceiling on matching-i = our bank; no FREE solver beats it. r1 gap is a real basin/heuristic gap, not an edge/model artifact.** Consistent w/ prior CP-SAT-LNS-to-OPTIMAL null. NO bank change (guard untouched). matching-ii now running (~16:42).

**★★ MATCHING IS OPEN, NOT CAPPED (15:32 leaderboard-spread analysis, answering user "shouldn't Gurobi teams tie?"):** Top scores are NOT identical — they're a SPREAD (matching-i top: 33555.6/33555.1/33548.1/33534.7/33534.1/... ; matching-ii top: 73714.0/73709.3/73697.5×3/73696.9/73674.5/...). **The tell: the SAME top team (867404) holds r1 + nearly the whole r2 band on matching-ii with a LADDER of distinct values** (73714→73709→73697→...→73569). A solver that PROVED optimality returns one identical value every run; a staircase from one team = repeated TIME-LIMITED runs each parking at a different incumbent — **exactly our SCIP behavior** (root-node-bound, gap never closes). ⇒ NOBODY has solved matching to optimality; the true optimum is unknown and ABOVE r1. **Corrects the 10:26 framing: matching-i/ii are NOT capped-and-tied; r1 is a better INCUMBENT (+217/+1510 mass), beatable by out-searching (better construction+more search), NOT a proven ceiling.** Reprices E-577 GRASP diverse-multistart from "+~0 (capped)" to "real-but-hard" (free exact won't reach it cheaply — SCIP/CP-SAT both null; the open question is whether DIVERSE construction finds a better basin the polish-the-same-incumbent methods can't). Field is dense (small mass jumps several ranks). NOTE: paid Gurobi also would NOT auto-win — same weak-LP time-limit obstacle; it'd just be a (possibly better) heuristic incumbent.

**★ ANTI-REJECT-TRAP FIX LANDED (14:54, the stale wide-search agent a91b5428 before completing):** Cold constructor had the >30%-reject bug the methodology predicts — it joined Ham-path blocks at RANDOM big-comp endpoints, but only **~9 of 40 big-comp nodes** are feasible junction endpoints to/from the 3 small cliques (cap=600 min-tof gate; 150/junction-pairs physically infeasible at any epoch). ⇒ feasible-construction rate was likely <30% (universe-too-narrow, NOT a floor). FIX (in /tmp/ch2_wide_search.py, py_compile OK): bias big-Ham-path to **begin/end at feasible-junction nodes (BIG_JUNC)** + hard `_junctions_feasible()` gate on every candidate at construction AND acceptance. **This makes the imminent wide_search VERDICT valid** (was at risk of false "floor"). Durable lesson for any block-stitch constructor: junction endpoints must be feasibility-filtered, not random.

**HEALTH-WATCH (terse, per standing decision; updated in place):** 15:31 — ALL HEALTHY, no tripwire. Pipeline **stage 3 validate** still building 13 uncached bridge pairs — log frozen at "to-build=13" 39min BUT 4 workers (2543340-43) 79% CPU/R, ~30min cumCPU each ⇒ genuinely computing the expensive full-multirev Δv≤600 scans, slowed by SCIP-ii contention (5 procs/4 cores). NOT a stall. Then stage 4 wide_search(patched, 30min)→refine ⇒ VERDICT slips ~16:00-16:30. SCIP **matching-ii** (pid 2532996) 81% CPU/R, cumCPU 88min, grinding root LP, ~16:42 finish. Load 5.08/4 cores. No bank delta; all UNSUBMITTED. ⚠ liveness = CPU%/R + cumCPU, not log mtime. (prev 14:58: prebuild DONE)

**TICK 00:27 (cheap tick — prebuild SLOWING to ~13h, confirmed CPU-bound not thrashing; STANDING DECISION recorded to stop re-litigating):** Prebuild rate is slowing: `6/201 dt=913s` (152s/curve) → `12/201 dt=2724s` (302s/curve batch 2). Linear extrap ⇒ **~13h prebuild (done ~12:00-14:00), verdict ~14:00**; may slow further as later cold pairs are expensive. Ruled out thrashing: 4 workers **99% CPU (R)**, 157MB RSS each, 6GB free, swap si/so=0 ⇒ genuinely CPU-bound on expensive infeasible bridge pairs (full 20-rev × wide-tof scan, no Δv≤100 early-out). NOT a tripwire. **★ STANDING DECISION (stop re-deciding every tick — anti-oscillation on my own call):** the wide-cold pipeline RUNS TO COMPLETION unless (a) a real tripwire fires, or (b) a better *launchable* lever appears. Rationale: 52 core-h is large, but every alternative is point-worthless (large r2 gains don't cross r1=424; medium=RANK1 maxed), blocked (matching=Gurobi license), or user-gated (submission). Under NEVER-STOP, this is the only positive-EV core-use available, so it grinds. Future ticks: health-check only, do NOT re-debate the ROI. **Still-live FLAG:** when prebuild nears done (~12:00), let wide_search's 30min run then relaunch an EXTENDED search (~2h) on the same /tmp bridge curves to exploit the 13h build (do NOT edit running ch2_pipeline.sh — byte-offset footgun). No bank delta; all UNSUBMITTED. Cores 4/4. NEXT: 40min health ticks through the long build.

**TICK 23:55 (cheap tick — prebuild healthy but ~10× SLOWER than estimated: revised ETA ~08:00; lopsided-budget note flagged; NOT a tripwire):** Bridge prebuild progressing, 4 workers pegged ~99% CPU (genuinely computing, NOT hung), but rate = `6/201 dt=913s` ⇒ **~152s/curve wall ⇒ ~8.5h for all 201 curves** (the stale agent's "50-67min" estimate was ~10× optimistic). Reason: bridge curves are full epoch-resolved Δv≤600 multi-rev scans (~600k Lambert calls/curve), vs cheap curves which short-circuit at Δv≤100. ⇒ revised pipeline ETA: prebuild done ~08:00, then validate→wide_search(30min)→refine ⇒ **wide-search VERDICT ~08:30-09:00**. NOT a tripwire (99% CPU, steady checkpoints, /tmp-only, harmless). **ROI honesty:** this is a speculative basin-escape on a near-floored +1-point instance (small rank 6→5 only IF validated <111.7875; 3 prior floors + edge-set audit say cheap graph is complete) ⇒ low EV (~0.1-0.15 pts), BUT no higher-EV use of the 4 cores exists right now (large r2→r1=300 core-h-walled, matching=Gurobi-blocked, traj=point-worthless) ⇒ per NEVER-STOP, let it grind rather than idle. **FLAG for a future tick:** pipeline design is lopsided — 8.5h building a rich bridge universe then only 1800s (30min) searching it. When the wide_search stage nears (~08:00), consider letting its 30min run then relaunching an EXTENDED search (e.g. 2h) on the same prebuilt /tmp curves to actually exploit the 8.5h investment. (Do NOT edit the running ch2_pipeline.sh mid-execution — byte-offset footgun; relaunch separately instead.) Confirmatory edge-set sweep still queued behind (waits for cores). No bank delta; all UNSUBMITTED. Cores 4/4 prebuild. NEXT: ~30min health ticks through the long prebuild; at ~08:00 catch validate→wide-search and make the extend-search call.

**TICK 23:22 (cheap tick — pipeline phase transition: cheap precompute DONE → bridge prebuild running; no tripwire):** Cheap-curve precompute finished clean (`DONE 138 curves mean-feas-frac=0.984 dt=3898s` → /tmp/ch2_fast_cheap_curves.pkl). Momentary "pipeline still waiting" at 23:21 was NOT a tripwire — just the `until grep DONE` 30s poll-lag; it picked up the marker at 23:21:22 and auto-fired stage 2. Now running `ch2_prebuild_bridges.py 4 3000` (≈198 cold + 5 bank bridge curves, ~30min) on 4 cores. Next stages auto-chain: validate_fast (20 orders) → wide_search (4×30min cold SA) → wide_refine (exact gate). Confirmatory edge-set sweep wrapper still armed behind the pipeline (waits for cores to free). Health: 4/4 cores busy, logs fresh, no tripwire. No bank delta; all banks UNSUBMITTED. Pushed git working state earlier (f21017a, origin/main synced). NEXT: ~30min tick to catch prebuild→validate→wide-search; read wide-search binary verdict when it lands (validated <111.7875 = rank-5 / floor-confirmed / metric-biased).

**TICK 22:58 (★ EDGE-SET AUDIT — answers the user's "how do we get a DIFFERENT cheap-edge set?" with a MEASUREMENT; the E-603 "different edge set" lever is REFUTED for small; pipeline still healthy 48/122):** The user asked how to obtain a richer Δv≤100 cheap-edge graph for small (E-603 had claimed the rank gap "needs a different cheap-edge set"). I treated the cheap set as what it actually is — OUR grid-sampled abstraction (false-negatives only) — and ran `/tmp/ch2_small_edgeaudit.py`: fine exact-Lambert rescan (80 epochs × 299 tofs = 23,920 `kt.compute_transfer` calls/pair) over 30 INTER-component non-edges + 30 INTRA-comp0 non-edges. **VERDICT: the 4-component {40,3,3,3} structure is REAL, not a sampling artifact (OPPOSITE of large's E-591).** INTER-component: **0/30** secretly cheap; cheapest inter non-edge = (45,4) Δv=**513.6** (5× over the 100 threshold) — the components are genuinely disconnected in the cheap graph, no missed bridges. INTRA-comp0: **0** new cheap edges, only 2 useless long-tof near-misses [(31,9)=119.66@tof6.77, (6,26)=128.36@tof10.37]. ⇒ finer Lambert resolution adds NO cheap edges; the 138-edge set is already complete. **The rank gap is therefore NOT an edge-resolution problem** — it is either a genuine model floor at 112.996 or a global route/exception-placement basin (exactly what the running wide-cold pipeline probes). **So the honest answer to the user: you CANNOT meaningfully get a different cheap-edge set for small — and proving it saves us from funding E-603's edge-resolution probe (now DROPPED from the queue).** Recorded in ch2-small memory (★★★ 22:55 EDGE-SET AUDIT block). Confirmatory full 2,352-pair exact re-derivation (~1 core-h) QUEUED behind the pipeline (don't starve its 4 cores) to upgrade the 60-pair sample into a completeness proof + full (100,130] near-miss list. Pipeline health: precompute 48/122 @ ~35s/curve (ETA ~43min), 4/4 cores, log fresh — no tripwire. Banks unchanged, ALL UNSUBMITTED. NEXT: monitor pipeline to DONE→prebuild→validate→wide-search→refine binary verdict; re-arm.

**TICK 22:35 (★ CORRECTION to 22:00 — Ch2-small NOT closed; the precompute was MIS-DIAGNOSED as a rabbit hole due to CORE-CONTENTION distortion; a sound wide-cold-search pipeline is running healthily + leaderboard refetched UNCHANGED):** ★ **Self-correction:** at 22:00 I killed the small precompute and declared small CLOSED, citing ">3.5 min/curve ⇒ 120min rabbit hole." That number was a MEASUREMENT ARTIFACT — my `build_curve` probe ran ALONGSIDE the 4 saturated workers and was CPU-starved. The stale wide-search agent (a91b5428, still alive in bg) relaunched the precompute + an auto-chained pipeline (`/tmp/ch2_pipeline.sh`) at 22:16; its log shows the TRUE rate = **~35.6 s/curve on 4 cores** (32/122 at 1138s), ETA ~53 min. ⇒ the precompute is ~50min, NOT 120min, and the pipeline is a legitimate BOUNDED (~3.5hr) test of a genuinely DIFFERENT basin: wide COLD block-construction over the **5-bridge / 3-junction-bridge topology family** (vs the bank's 4-bridge Family-A basin), scored on a FAST continuous estimator with an EXACT kt.fitness final gate (so it can only emit false candidates, never false validated winners — robust). Anti-reject-trap instrumented (STRICT_BRIDGE scoreable-rate reported; <30%⇒universe-too-narrow not floor). All 4 downstream scripts present (prebuild_bridges → validate_fast → wide_search → wide_refine). **This is the right answer to the user's nudge ("only 1.2d from rank-5, optimization seemed to be working") — LET IT RUN.** Corrected the ch2-small memory ★★★★★ block accordingly. **DEEP-REVIEW leaderboard refetch (read-only GraphQL, all 6 instances): UNCHANGED — Ch2 medium 192.90 RANK 1 (r1board 195.68), large 932.53 RANK 2 (next-below AC 1153.9 = 221d cushion; r1=424.62 TGMA), small 112.996 #6 (r5=111.7875), Ch1 matching-i ~r7-9, matching-ii ~r7, traj ~r10. SUBMISSION still the dominant unrealized EV (≈29 pts from medium-R1+large-R2 alone), user-gated.** Large r2→r1 moonshot DOES remain compute-walled for a from-scratch constructor (large has ~122k cheap edges × ~35s = ~300 core-h precompute; small has only 138) — deferred, NOT launched (cores busy with small pipeline). Cores: 4/4 small pipeline (load 6). Banks unchanged, ALL UNSUBMITTED. NEXT: monitor pipeline (precompute DONE → prebuild → validate → wide search → refine); read its binary verdict (validated <111.7875 rank-5 / floor-confirmed / metric-biased); re-arm timed to precompute ETA.

**TICK 22:00 (★ Ch2-SMALL CLOSED — the agent's fast-eval precompute was a rabbit hole; 4th-variant search now COST-rejected, not just convergence-rejected; user asked "are we still improving small / 1.2d from rank-5"):** Killed the small precompute (`ch2_precompute_cheap.py ×4`): timed `build_curve` directly = **>3.5 min/curve** ⇒ ~120 min for all 138 cheap edges, far too slow for a +1-pt screening tool (18 min in, 0 progress lines). Then sized the precompute-FREE alternative: SA accepting on the EXACT continuous metric (`refine_timing` → `kt.compute_transfer`) costs **110.8 s/order** ⇒ ~40 orders in a 20-min box, FEWER than E-613's 56 hand-refined orders and strictly slower. **⇒ both affordable paths to a "wider continuous-metric search" are worse than E-613 already did. With 3 independent prior floors (E-609/613/614) + the E-603 topology audit + this cost analysis, a 4th small search is the anti-oscillation-forbidden 4th-variant AND now raw-cost-negative. Ch2-small = CLOSED at validated 112.996d (banked), rank 6, 1.208d from r5=111.7875; the residual is an OUT-OF-MODEL cheap-graph-membership gap (E-603), not a search gap.** Only genuinely-different small angle left = E-603's queued edge-resolution probe (finer Lambert → different Δv≤100 membership), low-pri ×1 near floor. Updated ch2-small memory (★★★★★ block). **CAMPAIGN PICTURE unchanged from TICK 19:55: ALL cheap search levers exhausted (small floored, medium R1, large R2, traj r6 point-worthless, matching-i/ii E-048-exhausted). DOMINANT EV = SUBMISSION (all 6 banks UNSUBMITTED = 0 pts; ≈49 pts if submitted) — user-gated, re-escalating.** Cores freed (load↓). Per NEVER-STOP: cores will go to the one remaining point-positive frontier (Ch2-large r2→r1 moonshot synthesis), not another small variant. Banks: medium 192.90(R1), large 932.53(R2), small 112.996(#6), Ch1-traj 236,420(#6), matching-i/ii — ALL UNSUBMITTED. NEXT: report small-closed + submission picture to user; launch bounded large epoch-rebuild-constructor research agent; re-arm.

**TICK 21:05 (BOTH bg agents died mid-task w/o verdicts; large re-pairing = NO-GO from its log; small precompute continues):** Both spawned agents "completed" but returned NO final report — each over-polled, hit its turn limit, left a detached process (SendMessage-resume unavailable this session). Recovered the verdicts from their redirected logs/processes instead. **★ LARGE re-pairing go/no-go = NO-GO (decisive):** /tmp/ch2_greedy_validate.log shows the pure earliest-arrival cheap-greedy STRANDS on 3/3 completed starts (stuck at 682/537/392 of 1050; exhausts the 5-exc budget then hits a node with no unvisited cheap neighbor). Mean cheap degree=133 but late in the tour most neighbors are already visited ⇒ classic greedy-TSP stranding. **The bank's comp0+3-smalls 5-bridge structure is doing real work (preventing stranding); the +1.78pt r2→r1 lever needs a constructor that GUARANTEES a complete open-Hamiltonian path (full LKH/Concorde+cluster-decomp+epoch-rebuild loop), NOT a cheap greedy ⇒ multi-day uncertain build, deprioritized as a moonshot.** Killed the blind greedy (verdict reached), freed its core. Updated ch2-large memory. **SMALL precompute (ch2_precompute_cheap.py ×4, ~8-13min, 138 cheap edges × 6251 epochs) still building the fast evaluator → dumps /tmp/ch2_fast_cheap_curves.pkl at end; I will run the time-boxed small search MYSELF when it lands (per 20:52 plan).** Cores: 4/4 small precompute. Banks unchanged (small 112.996 banked, all UNSUBMITTED). NEXT: on precompute completion, run the fixed-bridge time-boxed small search; report large NO-GO + small verdict together; re-arm.

**TICK 20:52 (small wide-search agent DIED mid-task; salvaging the precompute):** Agent a91b5428 "completed" but returned NO verdict — it built the fast-evaluator infra (/tmp/ch2_fast_eval.py: cheap-edge min-tof curves, GRID_DT=0.02, NTD=6251 epochs, 138 directed cheap edges) + launched /tmp/ch2_precompute_cheap.py (×4 workers, ~6min in, dumps /tmp/ch2_fast_cheap_curves.pkl at END), then burned its turn over-engineering bridge-curve resolution instead of running the search. **SendMessage to resume an agent is UNAVAILABLE this session** (confirmed again). PLAN: let the precompute finish (it's the reusable foundation + user explicitly wants the small push), then run the search MYSELF — hard 20-min time-box, FIX the 5-bridge set to the union used by bank+E-613-winner orders (don't rebuild bridge curves at high res — the trap that killed the agent), cold multi-start SA on the fast continuous metric, exact-refine+fresh-KTTSP-validate any <112.996. ROI honest: small rank-5 = +1pt low-P (3 searches already floored 112.996, this is the metric-bias-fix 4th attempt); large re-pairing (+1.78pt) is the higher-EV bet running in parallel. Re-arm ~900s to catch precompute completion. NEXT: on wake, if pkl exists → write+run the time-boxed search; else extend.

**TICK 20:48 (cheap tick — both bg agents HEALTHY):** Large re-pairing go/no-go (a6a70b3a, ch2_large_greedy_validate.py 94% CPU, 23min) = greedy built, validating; no winner yet. Small wide continuous-metric search (a91b5428, ch2_precompute_cheap.py ×4 ~77% CPU) = fast-evaluator fine-grid precompute phase, as expected. Load 5.5 (mild transient oversubscription, OK). No tripwires, no new bank delta. Both auto-notify on completion. Re-arm fallback ~1500s.

**TICK 20:40 (ACTION — Ch2-small bank OVERWRITTEN to validated 112.996d, −3.378d):** Per user's explicit in-thread instruction, ran the guarded overwrite of solutions/upload/small.json with /tmp/ch2_e613_winner.json. Re-validated through fresh KTTSP: fitness=[112.996,0,0,0,0], feasible, ok=True. Backup of old 116.374 bank → solutions/upload/small.json.bak.20260613.e613. (The earlier auto-mode-classifier denial cleared once the user gave a direct in-conversation overwrite instruction — the AskUserQuestion answer alone wasn't read as a durable grant.) **Bank now: small 112.996(#6, was 116.374) — still rank 6 (r5=111.787), no point change yet, but −3.378d strict improvement banked + 1.21d from rank 5.** STILL UNSUBMITTED (user holding submissions). NEXT: continue per 20:25 — Ch2-large re-pairing go/no-go test still running.


**TICK 20:25 (ACTION — user chose HOLD-submission + keep grinding; authorized banking 112.996 small (tool-blocked, handed user a command); large gap analysis = re-pairing lever reachable → launched cheap go/no-go):** Escalated submission picture + the validated 112.996 small bank to the user. **User decisions: (1) HOLD submissions — keep grinding structural levers, don't lock in yet; (2) YES bank the 112.996 small (−3.378d, strict improvement).** ★ The guarded-bank script was BLOCKED by the auto-mode classifier (can't see the AskUserQuestion authorization → treats any solutions/upload/small.json overwrite as ungated). Did NOT bypass; handed the user a runnable backup+write+revalidate command to execute via `!`. **★ Ch2-large gap-decomposition agent (aab71de9) returned a major finding + a data correction: large bank is actually 932.5304d NOW (re-validated, feasible 5/5) — memory's 1048.98 was STALE (improved via E-586/588/591). Updated memory.** Gap to r1=424.62 (508d) is FLIGHT-dominated (96.8%, idle only 3.2%) and a GLOBAL routing/partition problem: bank 0.888 d/leg vs r1 0.404 d/leg (whole tour ~2× too long), 48% of makespan in the 1-3d MEDIUM band, all 36 heavy legs are cheap dv≈99 (5-exc budget not even on the bottleneck). Probe: 29/36 heavy + 30/30 medium source nodes have a SHORTER unused cheap neighbor at their own walk-epoch ⇒ nodes routed to wrong (far) neighbor; cheap-edge supply rich (10-30/node/epoch, NO inter-component dv floor unlike small E-606). Inferred reachable via from-scratch global TD constructor that REBUILDS the partition (frees the 5 bridges for shortcuts) — the TGMA approach; within-topology polish (LNS/LKH-per-piece/retime) all proven exhausted (E-587/589/591). LKH NOT installed (no binary/elkai). **★ Per anti-oscillation + go/no-go discipline: launched bg agent (a6a70b3a) = a CHEAP pure-greedy global re-pairing constructor (shortest-tof cheap neighbor each step, epoch-aware, reserve 5-exc) to test whether the lever beats 932.53 BEFORE committing to a multi-day LKH build.** Binary: feasible & <932.53 by meaningful margin → lever CONFIRMED, fund LKH build (large is hard-weight ×1.778, r2→r1 the highest single-instance lever); else freed-shortcut hypothesis overstated, document + stop. **Cores: large re-pairing test (background). Banks: medium 192.90(R1), large 932.53(R2), small 116.3738(#6, +VALIDATED 112.996 awaiting user's `!` bank command), Ch1-traj 236,420(#6), matching-i ~#9, matching-ii ~#7 — ALL UNSUBMITTED (user holding).** NEXT: read re-pairing go/no-go → if GO scope the global TD constructor build; confirm user ran the small bank command; re-arm.

**TICK 19:55 (DEEP — matching lever ALREADY EXHAUSTED (E-048, today); cheap frontier EMPTY; dominant lever = SUBMISSION → escalating; launching Ch2-large rank-1 gap analysis to stay non-idle):** Research agent scoped the matching exact-ILP redirect → it was **already done & exhausted TODAY by E-048**: Gurobi-LNS 66,660 block re-opt (matching-i) + 22,700 (matching-ii) = ZERO improving moves; CP-SAT 2400s global probe timed out; unrestricted Gurobi needs commercial license (unavailable). Matching IS the bottleneck (masses are fixed inputs, pure 3-index assignment) but it's neighborhood-escape-hard, saturated under open-source. **My queue item "matching per E-039" was STALE — E-048 superseded it; corrected.** ⇒ **ALL CHEAP SEARCH LEVERS NOW EXHAUSTED:** small floored 112.996 (E-609/613/614), medium r1 + large r2 maxed, traj r6 point-worthless (+136k for r5), matching-i/ii exhausted (E-048). Remaining levers are large/uncertain moonshots: Ch2-large r2→r1 (1048.98→424.62 = 2.5× gap, cluster-decomp+LKH rebuild), Ch2-small out-of-model cold-global-search (anti-oscillation: NOT a 4th variant). **DOMINANT EV = SUBMISSION: all 6 banks UNSUBMITTED = 0 pts now; if submitted ≈49 pts (med r1=13.33, large r2=16.0, traj r6=8.89, small r6=5.0, m-ii r7=4.0, m-i r9=2.0). Context materially changed since user's 14:50 "out-of-model build" choice: that build is DONE (validated 112.996) + cheap frontier now empty → ESCALATING submission decision to user this tick.** Per NEVER-STOP (don't idle on the fork): launching a parallel in-depth analysis = Ch2-large rank-1 gap decomposition (deep-audit pattern: treat 424.62 as reachable, find the flaw in our 1048.98; large is hard-weight ×1.778, rank1=+1.778pt — highest-value moonshot). **Cores: large-analysis agent (background). Banks unchanged, all UNSUBMITTED.** NEXT: await user submission call; read large-gap analysis → if a real lever surfaces, scope a build; re-arm.

**TICK 19:46 (DEEP REVIEW — leaderboard refetched (>24h stale); Ch2-small MODEL-FLOORED at validated 112.996; redirecting to Ch1 matching exact-ILP):**
**(a) E-614 verdict: beam==greedy.** B=1 AND B=6 both = 112.99600 (B=12 finishing, will confirm) ⇒ the greedy forward retiming was ALREADY makespan-optimal for the winner order; the timing evaluator is NOT the bottleneck. My "greedy is myopic" worry was unfounded for this route. **THREE independent searches now floor Ch2-small at 112.996: E-609 (422k orders, coarse), E-613 (56 orders, continuous, winner-seeded), E-614 (timing beam).** Per anti-oscillation: ACCEPTED as the model floor. Residual to leaderboard = transfer-structure (cheap-graph 4-component separation is robust: E-606 inter-comp dv floor 511.84 = 5× threshold). NOTE: Ch2 compute_transfer is the SHARED exact cost (all teams) ⇒ HRI's 101.65 is search-reachable in principle, but our cheap-graph+5-exc decomposition floors at 112.996 across 3 searches; cracking it needs a fundamentally different route structure (cold massive-parallel global search or finer edge resolution), both low-confidence + already partially tested — NOT funding a 4th variant now.
**(b) ★ LEADERBOARD REFETCH 2026-06-13 ~19:45 (read-only GraphQL; prev 06-12 11:10).** Ch2-small TIGHTENED into a DENSE cluster: **r1=101.653(HRI db54c) r2=108.772(dd46ba) r3=110.878(867404) r4=111.756(5af55c) r5=111.787(f5a760)** — ranks 3-5 span ~0.9d; next distinct ≈122.66. **Our 112.996 → rank 6** (same as 116.374 bank); need <111.787 r5(+1pt), <111.756 r4(+2pt), <110.878 r3(+3pt). Dense cluster RAISES sub-112 EV but it's out-of-model-only. **Ch2 medium 192.90 STILL r1 (board r1=195.68); large 1048.98 STILL r2 (board r2=1143.56).** Ch1 matching-i board r1=33,555/r3≈33,494 (our 33,338 ≈ rank 9, small gap to climb); matching-ii board r1=73,714/r2≈73,569 (our 72,204 ≈ rank 7). Ch1-traj r5 band ≈372,729 (our 236,420 rank 6, lever point-worthless per 14:40).
**(c) REPRICED QUEUE.** Ch2-small lever EXHAUSTED (validated 112.996 = best-known, +3.378d vs bank, unbanked/user-gated). Highest-EV remaining CHEAP levers = Ch1 matching exact-ILP: matching-i rank9→8 (+1pt, +~26kg gap), matching-ii rank7→6 (+1pt). Both flagged (E-039) as needing tighter ILP/Gurobi vs plateaued LNS. **ACTION: spawned Explore agent to scope matching-i/ii UDP + current solver + CP-SAT/exact-ILP tractability** (OR-Tools CP-SAT available) so next tick launches informed. **Cores: E-614 (1, finishing) + research agent (read-only) = light. Banks: medium 192.90(R1), large 1048.98(R2), small 116.3738(#6, +VALIDATED 112.996 unbanked), Ch1-traj 236,420(#6), matching-i 33,338(~#9), matching-ii 72,204(~#7) — ALL UNSUBMITTED.** NEXT: read matching research → launch CP-SAT/exact matching probe on idle cores; escalate validated 112.996 small bank + submission picture to user; re-arm.

**TICK 19:09 (ACTION — E-613 PLATEAU at 112.996 confirmed; auditing the GREEDY-retiming assumption via E-614 beam-DP):** E-613 DONE (3367s): deeper search (56 distinct orders, keepK=24, metric-mismatch fixed, seeded from bank+winner, 4-core parallel refine) reproduced **112.99600 as the floor** — NOTHING below it, even a chain reaching better coarse (120.578) refined to ≥112.996. **2-tick plateau ⇒ order-search + greedy-retiming lever CONVERGED, 1.208d short of rank#5 (111.7875).** Per anti-oscillation, not re-running the same lever. **★ Foundation-then-search audit: the refiner's forward earliest-arrival sweep is GREEDY/myopic, NOT "provably optimal" as I'd claimed** — a leg's min-tof is non-monotonic in departure epoch (orbital phasing), so a later arrival now can unlock a shorter tof later + lower final makespan. **★ Launched E-614** (/tmp/ch2_e614_beam_retime.py, pid below, B=1,6,12): replaces greedy with a BEAM/DP over arrival-states (keep B best partial schedules/leg, not 1). B=1 must reproduce 112.996 (gate); B>1 tests non-myopic gains on the known best order. DECISIVE: beam<greedy ⇒ timing evaluator was the bottleneck (1.21d to rank#5 maybe reachable); beam==greedy ⇒ 112.996 is the structural model floor. **Banks: medium 192.90(R1), large 1048.98(R2), small 116.3738(#6, +VALIDATED 112.996 candidate unbanked), Ch1-traj 236,420(#6) — all UNSUBMITTED.** NEXT: read E-614 verdict → if beam wins, re-run order-search with beam retiming; if not, Ch2-small is model-floored at 112.996 (document, escalate guarded bank, redirect compute to Ch1 matching exact-ILP per E-039); re-arm.

**TICK 18:28 (cheap tick — E-613 HEALTHY mid-run):** pid 2348690, gate PASSED (bank→114.154), 4 SA workers 99% CPU, load 4.00 (full util, no oversubscription). Chains end ~18:29 then parallel-refine of ~80 distinct candidates (keepK=24×4) on 4 cores ≈ 40min → ETA ~19:10. No winner file yet, no bank delta. Re-arm ~19:08 for verdict. Banks unchanged (small best validated 112.996 unbanked, 1.21d from rank#5).

**TICK 18:01 (ACTION — ★★ E-612 full search found a BETTER ORDER 112.996d (−3.38d), INDEPENDENTLY VALIDATED; metric-mismatch diagnosed; launched E-613 deeper search):** E-612 full order search DONE (3234s): best refined order = **112.99600d feasible, +3.3778d over bank** (cand 13). INDEPENDENTLY VALIDATED (/tmp/ch2_e612_validate_winner.py, fresh KTTSP + manual audit): official fitness=112.996 is_feasible=True, exc=5/5 (bridges {2,17,26,29,45}), max cheap dv=99.998≤100, 0 chrono/600 viol, flight Σtof=105.616 wait=7.38. **VERDICT VALID + BEATS BANK by 3.378d.** Supersedes the 114.154 bank-order retiming. **STILL rank #6** (r5=111.7875) but gap to rank #5 now just **1.208d** (was 4.586d at bank). Winner /tmp/ch2_e612_winner.json (unbanked — overwrite user-gated). **★ Methodology diagnosis (M-005-adjacent / the "evaluator metric must match SA baseline metric" trigger): the SA ranks orders by COARSE-DP mk but scoring is CONTINUOUS-refined mk — a metric MISMATCH.** Proof: cand 13 had a WORSE coarse score (122.738 vs bank's 122.378) yet refined BEST (112.996). Good orders are filtered out before the expensive refine. **★ Launched E-613** (/tmp/ch2_e613_deepsearch.py, pid 2348690, ETA ~18:52): decouples cheap coarse EXPLORATION from expensive SELECTION — keepK=24/chain (was 8), band start_mk+1.5, seeds chains from BOTH bank AND the 112.996 winner, PARALLEL-refines all distinct candidates on 4 cores (E-612 refined serially). Goal: cross 111.79 → rank #5 (+1 pt easy). **Cores 4/4 (E-613). Banks: medium 192.90(R1), large 1048.98(R2), small 116.3738(#6, +VALIDATED 112.996 candidate unbanked, 1.21d from rank#5), Ch1-traj 236,420(#6) — all UNSUBMITTED.** NEXT: read E-613 verdict ~18:52 → if <111.79 = rank#5 candidate (validate+escalate bank); else 112.996 stands, reprice whether further small push beats other levers; re-arm.

**TICK 17:34 (cheap tick — E-612 full order search HEALTHY, in serial-refine phase):** pid 2340214/child 2340216 alive, 4 SA chains DONE (built ~450 each, feas ~75% — NO reject-trap, healthy), now continuously re-timing 14 distinct candidate orders serially (~108s each ≈ 25min, load 1.36 single-core as expected). No winner file, no bank delta. ETA ~17:56. Woke ~22min early. NEXT: re-arm ~1450s to read full-search verdict — if any refined order <111.79d → rank-#5 EV (guard-validate+escalate); else 114.154 bank-order retiming stands as the result (escalate guarded bank when convenient). Banks unchanged: medium 192.90(R1), large 1048.98(R2), small 116.3738(#6,+114.154 candidate unbanked), Ch1-traj 236,420(#6) — all UNSUBMITTED.

**TICK 17:06 (ACTION — ★★ E-612 BREAKTHROUGH: continuous re-timing of the BANK ORDER = 114.154d feasible = −2.22d; E-610's "evaluator CLOSED" CORRECTED; bank candidate VALIDATED, escalating; full order search running):** E-611's table-free search reported a false null — its correctness gate compared the SA's best **coarse-DP-timed** makespan (~120.6, DT=0.1 grid) against the bank's **continuous** 116.374 (apples-to-oranges, exactly the verification caveat flagged 15:43/16:10) and discarded its best order. **Fix = E-612** (/tmp/ch2_e612_refine.py): continuously re-time each candidate order via a forward earliest-arrival sweep (waits [0,2]@0.03d, shortest tof with dv≤cap, **full ≤100 cheap cap no MARGIN**, bridge→600); greedy earliest-arrival is provably makespan-optimal for fixed order+bridges. **★ Correctness gate = the headline: continuously re-timing the BANK ORDER ITSELF → 114.15400d feasible vs stored 116.37377 = −2.2198d.** The bank's STORED TIMING was suboptimal for its own route. **INDEPENDENTLY VALIDATED** (/tmp/ch2_e612_validate.py — reload x through a FRESH KTTSP, manual per-leg audit): official kt2.fitness=114.154 is_feasible=True; exceptions=5/5 (bridges {2,17,26,29,45}); dv>600 viol=0; max cheap-leg dv=99.998≤100; chrono viol=0; makespan=times[-1]+tofs[-1]=114.154; flight Σtof=106.354 wait=7.800. **VERDICT: VALID + BEATS BANK by 2.2198d.** **Why E-610 was premature:** E-610 bounded the flight lever at 1.02d **holding departures FIXED at bank epochs** — but departures are free; joint re-timing reduces flight 109.99→106.35 (−3.64d, 3.6× E-610's bound) at +1.42d idle = net −2.22d (a leg's min-tof is strongly epoch-dependent; E-610's hostile-default audit missed that "departures=bank" was itself an unjustified default). **The TIMING row was NOT closed.** Wrote E-611-ch2-small-continuous-retiming.md. **SOBER RANK NOTE: 114.154 is a STRICT improvement but NOT yet rank-up** — r5(R3)=111.79, so 114.154 is still rank #6 (5.00 pts, same as 116.374); it shrinks the R3 gap 4.59→2.37d. Candidate at /tmp/ch2_e612_bankrefined.json, **NOT banked** (bank overwrite blocked by auto-mode classifier as user-gated → ESCALATING). **★ Full E-612 order search RUNNING** (pid 2340214, 4 SA chains × 1500s, ETA ~17:30): tests whether any order BEYOND the bank's, continuously re-timed, crosses 111.79 into rank #5. **Cores: 4/4 (E-612 search). Banks: medium 192.90(R1) + large 1048.98(R2); Ch2-small 116.3738(#6, +validated 114.154 candidate unbanked); Ch1-traj 236,420(#6) — ALL UNSUBMITTED.** NEXT: await full-search verdict (auto ~17:30) → if a refined order <111.79 → rank #5 EV (guard-validate + escalate); CHECK whether medium/large stored timing also has continuous-retiming slack (same lever may apply); re-arm.

**TICK 16:10 (cheap tick — E-611 relaunched as parallel SA, health PASS):** E-611 agent re-architected to 4 parallel SA chains × 3000s (started 15:56, finish ~16:46). Health PASS: 4 workers ~90% CPU, load 4.24/4cores (no oversubscription), 12+min sustained. **Visibility gap (not a tripwire):** chains use multiprocessing + aggregate at end ⇒ main log (/tmp/ch2_small_e611.log + _stdout.log) frozen at 15:57 setup, no intermediate SA-acceptance on disk until ~16:46. Correctness gate again coarse (bank order DP-re-timed 122.378 vs 116.3738 = DT=0.1 grid quant, BEAM=12) — SAME continuous-refinement verification caveat as 15:43 tick applies. No winner file, no bank delta, cores 4/4. NEXT: await chains→agent aggregation+refine (auto-notify ~16:46+); on report VERIFY continuous-refinement before accepting any null; re-arm with fallback ~16:50.

**TICK 15:43 (cheap tick — E-611 health OK; flagged a verification caveat on its correctness gate):** E-611 agent (a0c55b0d) ALIVE — /tmp/ch2_small_e611_search.py 99.9% CPU, log fresh (15:42), no winner file, no bank delta. Approach = "beam-DP, structure-aware" table-free: DT=0.1 departure grid, BEAM=16, MARGIN=1.0. **★ Correctness-gate watch:** its table-free beam-DP re-times the BANK ORDER to **121.498d vs official 116.3738 (Δ+5.1d ≈ 0.1d/leg = pure DT=0.1 departure-grid quantization)**; agent logged GATE_OK=True by widening tolerance to "within ~8d basin." **Read this as benign ONLY IF the agent uses the coarse beam-DP as an ORDER-RANKING proxy and then REFINES top orders' timing CONTINUOUSLY (direct compute_transfer, no grid) before any sub-116.37 comparison** (the brief instructs this). **VERIFY ON COMPLETION:** if the agent reports a NULL ("nothing beats 116.37") that rests on the DT=0.1 grid WITHOUT a continuous-refinement pass, the null is RESOLUTION-LIMITED/inconclusive (M-005-adjacent) — in that case re-run a tight continuous timing refinement on its best-found order myself before accepting any "closed" verdict. SendMessage to the agent is unavailable this session ⇒ cannot steer mid-run; trust-but-verify the final report. **Cores: E-611 (≤4) = up to 4/4.** **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6); Ch1-traj 236,420 (#6) — ALL UNSUBMITTED.** NEXT: await E-611 verdict (auto) → apply the continuous-refinement verification above; re-arm.

**TICK 14:50 (ACTION — user chose out-of-model build (option 3); launched E-611 Ch2-small table-free continuous search):** Surfaced the submission picture + thin-queue situation to the user; user picked **"start an out-of-model build"** (NOT submission, NOT verify-status, NOT keep-grinding). **★ Sharpened framing (correction to E-610 wording): for Ch2 the transfer fn `kt.compute_transfer` IS the shared problem evaluator — there is NO different-physics to find (unlike Ch1's patched-conic vs full-BCP validator). The genuine untried out-of-model paradigm for Ch2-small = TABLE-FREE continuous search: every prior search (E-609 order, E-610 audit) was pinned to the precomputed 0.05d cheap-graph table; a search calling compute_transfer DIRECTLY at arbitrary (departure,tof), no cheap-graph-membership restriction, is the one assumption-class never escaped + matches HRI's heavy-parallel-compute strength.** ⇒ **Launched bg agent a0c55b0d = E-611:** correctness-gate (reproduce bank 116.3738 via direct compute_transfer) → table-free continuous order+timing global search (per-leg continuous departure/tof via direct compute_transfer, ≤5-exc forward alloc, ruin-and-recreate/SA over orders, dense bank-seeding) → does anything beat 116.3738? Explicit anti-reject-trap (M-005) brief: print feasibility-construction rate, <50% = bug-to-fix not a verdict. /tmp only, no bank/submit/commit, ≤4 cores, winner→/tmp/ch2_small_e611_winner.json. Binary: real sub-116.37 (direct Ch2-small rank lever, ×1, rank 6→toward R3=110.88) vs healthy-feasibility null (bank is genuine optimum under shared continuous physics). **Cores: E-611 agent (≤4) = up to 4/4.** No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6); Ch1-traj 236,420 (#6) — ALL UNSUBMITTED (user handling submissions separately / deferred).** NEXT: await E-611 verdict (auto) → if sub-116.37, guard-validate through official fitness = escalate; if healthy-feasibility null, Ch2-small is closed under direct physics too ⇒ document + the remaining board lever is Ch1 out-of-model (priced point-worthless for achievable gains) ⇒ reprice; re-arm.

**TICK 14:40 (DEEP — ★★ Ch1 BCP-descent lever PRICED against the live leaderboard = point-WORTHLESS; do NOT fund the 60 core-h sweep):** Refetched `trajectory-matching` (read-only GraphQL, 76 sols): rank-1 473,333 / r2 471,941 / **r3 463,513 Team HRI** / r4 458,897 / **rank-5 band floor ≈372,729** (top of rank-6 band ≈296,960). **Our bank 236,420 kg is in the rank-6 band (≈154k–297k).** The BCP-descent lever (+15–25k → ~255–261k) STAYS inside rank 6 ⇒ **0 marginal points** (rank, not score-within-rank, scores). Reaching rank 5 needs **+136k kg** = 5–9× the lever's local-refinement ceiling, and refined floors ~3850 m/s ≫ HRI ~2734 ⇒ per-pair UDP refinement structurally can't get there. **VERDICT: do NOT fund the 291-pair sweep — it cannot move the rank.** ⇒ **KILLED batch-1** (e605_local.py 0 3 / 3 6) — it was only enlarging a now-irrelevant descent-fraction sample; freed all 4 cores. Memory ch1-trajectory-mass-lever-exhausted updated (★★★★★ block). **The only point-positive Ch1-trajectory action is SUBMITTING the 236,420 bank (rank 6 ≈ +8.9 pts vs 0 unsubmitted) — USER-GATED.** ⇒ **ESCALATING the submission picture to the user this tick** (submissions are an allowed escalation; the maximize-points policy 2026-06-11 values ranks 4–10, so several unsubmitted banks now score points). **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6); Ch1-traj 236,420 (#6) — ALL UNSUBMITTED.** **Cores: 0/4 (all freed).** NEXT: surface submission decision to user; pending their call, the patched-conic/Lambert exploration queue is genuinely thin (Ch2-small model-closed E-610; Ch1 lever point-worthless; Ch2 medium/large at R1/R2) — the only remaining positive-EV search levers are OUT-OF-MODEL (transfer-model rebuild for Ch2-small / WSB-or-lowthrust for Ch1), both large/uncertain multi-day builds; re-arm.

**TICK 14:30 (DEEP — ★★ Ch2-small EVALUATOR LAYER fully CLOSED: E-610 bounds the last crack (TOF granularity) at 1.02d optimistic ⇒ rank-irrelevant; residual gap = transfer-MODEL, same shape as Ch1 A0):**
**(a) ★★ E-610 — TOF-granularity lever CLOSED.** E-609 (422k-order global search, big-valley, bank near-global) closed ORDER and flagged the 0.05d TOF-grid as the one remaining evaluator crack. Probe (/tmp/ch2_small_e610_tof_audit.py): for each of the bank's 48 legs, at the bank's OWN departure epoch, found the continuous minimal feasible tof (0.005d scan + 10× refine) via the official `kt.compute_transfer`, summed save = bank_tof − true_min_tof = **OPTIMISTIC flight reduction (ignores chronological coupling)**. Result: **1.0171 d total** (max single-leg 0.0497d, all <0.05d = one grid cell = pure round-up-to-grid noise, 20 legs savable). Optimistic makespan floor 116.3755 → **115.36d if coupling-free**. **R3 needs −5.49d, R1 needs −14.72d ⇒ the granularity lever is 5.4×/14× too small to move rank.** Rebuilding a finer TOF table + re-DP CANNOT change the standing. Wrote E-610. **Ch2-small evaluator/edge layer is now CLOSED on ALL accessible levers: ORDER (E-609), edge MEMBERSHIP (E-606), TOF GRANULARITY (E-610); bank is FLIGHT-bound (6.38d wait, no leg >0.5d).**
**(b) What the residual 8.34d (flight 109.99 vs R1 101.65) IS — assumption A0-Ch2, exact twin of Ch1.** The gap is NOT in our resolved cheap/exc edge set at any TOF resolution our Lambert model can represent ⇒ **our multi-rev Lambert Δv is NOT the problem's transfer floor** (HRI's 101.65 uses transfers/an edge family our `compute_transfer` structurally can't produce — finite/low-thrust or a different feasible arc set yielding shorter realizable tofs / cheap edges our scan never finds). The lever is the **transfer MODEL**, not the table, router, or timing DP. This is a large, uncertain, multi-day out-of-model-physics build (same risk profile as deferred Ch1 WSB/low-thrust) — NOT cheaply reachable by the autonomous loop. The E-603→E-606→E-607/E-608→E-609→E-610 audit chain (from the user's "find the flaw not the optimum" directive) resolved cleanly with NO oscillation: each lever quantitatively bounded vs the gap and closed.
**(c) Ch1 batch-1 still running** (e605_local.py 0 3 / 3 6, ~117min, 78-81% CPU, healthy — enlarging the BCP-descent fraction sample beyond the 3/7≈43% combined estimate). No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6) — UNSUBMITTED (overall-rank-3 gate).** **Cores: batch-1 (2) = 2/4, 2 idle.** NEXT: finish batch-1 → finalize Ch1 fleet-descent fraction; refetch trajectory leaderboard to PRICE whether the Ch1 +15-25k kg fleet lever buys rank points (gates the 291-pair ~60 core-h sweep decision); with 2 idle cores + NEVER-STOP, the open positive-EV exploration is now the Ch1 fleet-BCP-descent question (Ch2-small is model-closed) — re-arm.

**TICK 14:05 (DEEP — ★★ E-607's "Ch2-small order forced by topology" verdict REFUTED (M-005 router artifact); order lever RE-OPENED; launched E-609 global order search; Ch1 BCP-descent fraction looking weak):**
**(a) ★★ E-607 REFUTED → E-608.** E-607 concluded "A2 survives / only the bank order is feasible / order forced by topology" after OR-Tools+greedy+stitching returned 0 feasible orders. That is the **M-005 reject-rate trap** (solver constructed nothing while a feasible order provably exists = the bank). Independent router-free probe (/tmp/ch2_small_e607_verify.py): took the bank perm, DP-timed ALL 2-opt + Or-opt neighbors through the SAME evaluate_perm_dp_numba — **found 38 feasible non-bank orders** (6 tie the bank at 116.3755, rest worse; best non-bank = 116.3755, none beats). ⇒ feasible alternatives are DENSE; E-607's "0 feasible / forced order" is a router-construction bug, NOT optimality. **BUT the survivor: the bank is a genuine 2-opt/Or-opt LOCAL optimum (3rd independent confirmation after E-032, E-603).** Local optimality ≠ E-607's "forced by topology." ⇒ **the global order-search lever is OPEN, not closed** — beating the bank needs LARGE feasibility-preserving moves / global restarts (HRI's parallel paradigm), the one ORDER-level paradigm still untested. Wrote E-608 + correction banner on E-607. **★ Launched bg agent a0262a4b = E-609:** feasibility-aware large-neighborhood/globally-restarted order search (ruin-and-recreate + SA with feasibility-preserving large moves + many restarts) using the DP as inner timing oracle; explicit anti-reject-trap brief (track feasible-construction rate; near-0% = bug to fix, never a verdict); ≤2 cores niced, /tmp only, no bank/submit, writeup E-609. Binary: a sub-116.3755 order (lever real, direct Ch2-small rank move from #6) vs healthy-feasible-rate search confirms bank is GLOBAL opt.
**(b) Ch1 BCP-descent — A0 FALSIFIED decisively; lever is REAL but caps ABOVE HRI (not the 2× closer).** **★ 14:10 CORRECTION (supersedes the "weak ~+5%" read below):** the positive-control agent's FINAL chunked dense-seed run (scale 0.05, validated through official `_validate_transfer` 1e-6) descends ALL 3 of its pairs: **(245,264) 4255.3→3851.1 = +175.6 kg, (126,286) 4238.4→3858.5 = +165.2 kg, (7,155) 4236.5→4222.5 = +5.8 kg; total +346.6 kg over 3 pairs**, winners in /tmp/ch1_e605_exp1_winners.json (the earlier improved:false pair_N.json were TRUNCATED weak runs — the harness kills agent Bash at ~15 min, so multi-pair sweeps MUST be chunked per-pair; my background Bash batches are NOT subject to that kill). My v2 batch nulls were on DIFFERENT (high-ΔV) pairs ⇒ NO contradiction. **Combined distinct sample: DESCEND (245,264),(126,286),(7,155); NULL (339,62),(346,332),(328,31),(213,19) = ~3/7 ≈ 43% descend**, descenders +100–175 kg, and "magnitude scales with search radius/compute." **BUT the refined ΔV floors (~3850 m/s) are still FAR above HRI's implied ~2734 m/s** ⇒ per-pair UDP local refinement caps just-below-bank, it does NOT reach the sub-Hohmann Sun-assisted regime that closes the 2× gap (that needs WSB, refuted on target class E-604). **Realistic fleet ceiling ≈ 0.43×291×~120 kg ≈ +15–25k kg → ~255–261k (+6–11% on 236k), compute-scaling ceiling uncertain.** Whether that buys trajectory RANK points needs a leaderboard refetch (price next deep tick). Decision PENDING: full 291-pair chunked sweep (~60 core-h) is the top Ch1 build IF the +15–25k moves rank; winners NOT banked yet (defer piecemeal banking → bank the whole refined trajectory at once if we commit). Confirms dense bank-perturbation seeding is mandatory (cold/weak optimizers under-report — the original positive-control trap).
**(b-orig, now corrected above) Ch1 BCP-descent fraction looking WEAK.** Tally so far: DESCEND (245,264) −308→+131.8 kg OFFICIAL ✓, (126,286) −319 m/s UDP; NULL (339,62),(346,332),(328,31),(213,19). [Superseded: agent's final run shows 3/3 of ITS pairs descend, 2 large — see 14:10 CORRECTION.] **Compute reallocation:** stopped batch-2 (high-ΔV, all-null, low info) → freed 2 cores for E-609; kept batch-1 (median-ΔV, producing descents) to finish the fleet-fraction estimate. Killed agent's redundant e605_localref earlier. **Cores: batch-1 (2) + E-609 agent (2 niced) = 4/4.** No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6) — UNSUBMITTED (overall-rank-3 gate).** NEXT: read E-609 verdict (auto) → if a sub-116.3755 order, guard-validate through official fitness = direct Ch2-small lever; finish batch-1 → finalize Ch1 fleet-descent fraction + decide whether the +5% lever is worth a 301-pair run; re-arm.


**TICK 13:55 (cheap tick — auditing E-607's "A2 refuted" verdict via an independent DP-feasibility probe; health OK):** Health OK, no tripwire — 6 procs alive (batch-1 2 + batch-2 2 + agent e605_one.py 1 + my verify probe 1), all CPU-healthy 65-80%, mild 6-on-4 oversubscription is transient (probe ~short, e605_one is the positive-control agent's own run). **(a) ★ E-607 verdict UNDER AUDIT (M-005 reject-rate trap suspected).** The E-607 agent (a1dd50f6) concluded "A2 SURVIVES / audit REFUTED — only the bank is feasible, order forced by topology" after OR-Tools (11 strat × 4 meta, 100s) + 600-restart greedy + component-stitch ALL returned **0 feasible orders besides the bank**. A router that constructs ZERO feasible solutions while a feasible one provably exists (the bank) is the reject-rate trap (M-005) — it tests the SOLVER's feasibility-construction, NOT problem optimality (same shape as E-032 topology-ops 0/10, Ch1 v1 99%-infeasible). **Independent check launched (/tmp/ch2_small_e607_verify.py, bg bzzo376oj):** bypass the router entirely — take the bank perm, enumerate ALL 2-opt reversals + ALL Or-opt single-node moves (~3000 distinct), DP-time EACH through the SAME evaluate_perm_dp_numba that scores the bank, count feasible. Binary: >1 feasible non-bank order ⇒ E-607 is a router artifact, global-order lever STAYS OPEN (Phase-4 exp-2 feasibility-aware constructor); 0 feasible ⇒ E-607 corroborated, order genuinely forced. Bank correctness gate inside the probe (asserts bank DP-feasible first). **(b) Ch1 v2 batch-1 readout:** lever is PAIR-SELECTIVE — (245,264) validated +131.8 kg (A0 falsified), but (339,62) already locally optimal (Δ0.0), (346,332) in progress. So fleet upside ≠ 301×131.8; depends on the fraction of pairs carrying descent headroom — batch-2 (8 highest-ΔV pairs) running to size it. No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (#6) — UNSUBMITTED (overall-rank-3 gate).** NEXT: read bzzo376oj verdict (auto) → update E-607 + Ch2-small open-path accordingly; tally batch-2 validated Δmass → scope Ch1 fleet BCP-descent fraction; re-arm.


**TICK 13:08 (DEEP — ★★ A0 FALSIFIED on Ch1 (BCP beats patched-conic, OFFICIALLY validated); ★★ Ch2-small re-audited → flaw = global ORDER-search never done; launched E-607):**
**(a) ★★ Ch1 A0 FALSIFIED.** v2 batch-1 pair (245,264): champ 3946.9 vs bank 4255.3, and the **official `_validate_transfer` ACCEPTS it at 1e-6 (a,e,i): `★ OFFICIAL dV=3946.9 dt=20.73d Δmass=+131.8 kg`.** Direct BCP optimization (free RAAN/argp/ν + coast tuning, warm-started from bank) finds a lower-ΔV trajectory than our Lambert+DC patched-conic floor AND it passes the real validator ⇒ assumption A0 ("our solver's ΔV floor IS the problem floor") is FALSE. Pair (339,62) = null (Δ+0.0, already near Hohmann floor). So far 1 validated descent / 2 resolved pairs. **This is the centerpiece Ch1 win — it means a fleet-scale warm-started BCP-descent (301 filled pairs, each seeded from bank row + local sade/cmaes, guard-banked) is a REAL positive-mass lever (hard ×1.78).** batch-2 (8 highest-ΔV pairs, 5993–6991 m/s, most headroom) still running — those will size the per-pair gain where it matters most. NEXT once batch-2 lands: tally validated Δmass across the sample, extrapolate fleet upside, decide whether to build the 301-pair descent run.
**(b) ★★ Ch2-small DEEP RE-AUDIT (user fired the 4-phase audit again w/ HRI-101.65-is-global-search as ground truth).** Read the official evaluator (ch2_kttsp.py) + the DP (ch2_dp_numba.py). **THE FLAW (sharper than E-603):** `evaluate_perm_dp_numba(kt, perm, ...)` takes `perm` as a FIXED INPUT — it is a TIMING oracle, NOT a router. Every method in the tree (greedy_wait/route greedy constructors + E-032 local-move ALNS + E-603's ~80 bank-anchored skeleton orders + E-606 edge-set) optimized timing/structure for an order that was built greedily or perturbed locally; the **node-visit ORDER — the 40-node comp0 time-dependent ATSP interior — was NEVER globally optimized with a real ATSP solver (LKH/Concorde/OR-Tools).** Gap accounting that E-603 UNDERPLAYED: flight-only Σtof=109.99 already EXCEEDS R1=101.65 by 8.34d ⇒ the R1 gap is dominated by ROUTE LENGTH (order), not phasing idle; per-leg-indep min-tof LB=84.82 (1.77 d/leg) vs our 2.29 d/leg = ~25d of order headroom. The untouched paradigms in the Phase-3 inventory (global exact ATSP + massively-parallel restart order search) are EXACTLY HRI's named method = not a coincidence, it's the flaw. Same shape as the Ch1 A0 win: exhausted-within-(order basin) ≠ exhausted-of-problem. **★ Launched bg agent a1dd50f6 = E-607:** build static min-realizable-tof cost matrix from the ultrafine table, OR-Tools (9.15, installed) ATSP open-path + heavy multi-restart Or-opt/2-opt with ≤5-exc constraint → ≥200 diverse global orders → DP-time each via the EXISTING evaluate_perm_dp_numba (correctness gate: reproduce bank DP-mk 116.375 first) → does any beat 116.37? /tmp only, no bank/submit, writeup E-607. Binary deliverable: flaw-confirmed-with-a-lever vs incumbent-order-genuinely-best.
**(c) Compute hygiene:** the completed agent ae2fc4c8's waiter respawned the OLD broken e605_optimize2 a THIRD time (PID 2297099) — killed + **renamed /tmp/e605_optimize2.py & optimize3.py → .disabled so any further respawn fails instantly** (permanent fix, not whack-a-mole). Stopped duplicate monitor b94axmu1r (was double-firing with b3xthwmz6). **Cores: v2 batch-1 (2) + batch-2 (2) = 4/4; E-607 is light/short OR-Tools+DP (won't sustain-compete).** No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2); Ch2-small 116.3738 (would rank #6, 5 teams ahead) — all UNSUBMITTED (overall-rank-3 gate).** NEXT: read E-607 verdict (auto) → if a global re-route beats 116.37, guard-validate through official fitness + it's a direct Ch2-small lever; read v2 batch-2 validated Δmass → scope Ch1 fleet BCP-descent; re-arm.


**TICK 12:51 (cheap tick — both bg agents DONE; ★ Ch2-small edge-resolution lever CLOSED; freed cores → launched Ch1 v2 BATCH-2 on high-ΔV pairs):** Health OK, clean 4-on-4 (no oversubscription, load 4.0). **(a) Ch2-small edge-resolution agent aa895fa1 (E-606) FINAL = CLOSED.** Decisive higher-res probe: re-computed min-Δv for ALL 774 inter-component directed pairs + 60 intra-comp0 samples via official `kt.compute_transfer`, tof extended 8d→60d (catches multi-rev windows the 8d cap misses), 1d dep grid, 15-basin polish. **Result: 0 new ≤100 edges, 0 inter-component cheap edges — lowest inter-comp min-Δv anywhere = 511.84 m/s = 5.1× the 100 threshold.** ⇒ the 4-component {40,3,3,3} cheap-graph topology is a GENUINE physical property of the instance, NOT a precompute artifact; no resolution refinement closes a >400 m/s gap. Combined w/ E-603 (flight-only floor 109.99 < R3=110.88), the ONLY remaining Ch2-small direction = squeeze phasing idle from the FIXED graph under ≤5-exc budget (modest, ×1 weight near floor). **Ch2-small now closed on BOTH skeleton (E-603) AND edge-set (E-606) levers.** No candidate, bank unchanged 116.3738d. **(b) Old broken e605_optimize2 respawned AGAIN (PID 2295411) — pkill'd; TaskList now EMPTY so nothing relaunches it ⇒ core-theft PERMANENTLY fixed.** **(c) ★ NEVER-STOP use of the 2 freed cores: launched Ch1 v2 BATCH-2** (/tmp/e605_local_b2.py, 2 procs slices [0:4]+[4:8] = 8 NEW pairs) on the **highest-ΔV mass-bound bank pairs (ΔV 5993–6991 m/s, mass only 5–200 kg each)** — the worst-performing filled pairs w/ the MOST descent headroom = the strongest A0 test: if a warm-started BCP descent validates +mass there, it's the biggest bankable Ch1 lever (e.g. (213,19) 6991 m/s→5.1 kg; dropping it to ~5000 m/s → ~+780 kg). All 8 bank seeds reproduce feasibly through PairUDP (udp_seed=bank_dv exactly). /tmp only, no bank/submit. Batch-1 (245,264)+5 others still finishing pair 1 (spread=3.0 CMAES_GEN=250 slow). **Cores: v2 batch-1 (2) + v2 batch-2 (2) = 4/4.** No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: read batch-1 pair-(245,264) OFFICIAL `_validate_transfer` verdict (★ OFFICIAL / locally optimal / REJECTS line) → if the 3946.9 config passes 1e-6, A0 FALSIFIED locally ⇒ scope fleet-scale warm-started BCP-descent; read batch-2 high-ΔV descents (bigger mass signal); re-arm.

**TICK 12:36 (cheap tick — ★★ PROMISING PARTIAL: Ch1 v2 found a 308 m/s DESCENT below bank on pair 1 (A0 falsification candidate, UNVALIDATED) — let it finish + validate):** Health OK (mild oversubscription load 5.09, v2 procs ~80% CPU from contention w/ Ch2 edge agent — not a tripwire). **★ Ch1 v2 slice A pair (245,264) spread=0.3: champ=3946.9 m/s vs bank 4255.3 ⇒ −308 m/s DESCENT** (≈ +144 kg mass for that pair if validated). This is the FIRST evidence that direct BCP optimization beats the patched-conic ΔV floor = the A0-falsification signal E-605 predicted. **CAVEATS before claiming:** (1) UDP-INTERNAL ΔV, NOT yet through the official `_validate_transfer` 1e-6 (a,e,i) match — the script validates only after all 3 spreads finish the pair (best_x); (2) **feas_seed=1/48** ⇒ my perturbation seeding did NOT fix the feasibility wall (even 0.3×SIGMA perturbations land infeasible) — the descent came from sade/cmaes evolving the SINGLE feasible bank seed, not broad sampling. Pair (339,62) spread=0.3 = 4234.9 = bank (no descent yet, 1/3 spreads). **Implication if validated: fleet strategy = warm-start each pair from its bank row + BCP-descend (NOT random/global). 308 m/s/pair × 301 ≈ +43k kg (236k→~279k) if it generalizes — real but note 3946.9 is just ABOVE the 3940 Hohmann floor, so this is local impulsive-descent, not yet the sub-Hohmann Sun-assisted regime that closes the full 2× gap (that likely needs wider feasible-basin search).** Cores: e605_local A+B (2) + Ch2 edge-resolution agent aa895fa1 (2) = 4/4. No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).**
**★ 12:50 core-theft FIX + update:** the OLD broken e605_optimize2 had RESPAWNED (PID 2288572, started ~12:00 — the original agent ae2fc4c8's waiter relaunched it after my 11:57 kill) and was stealing a core from v2 → pkill'd it; now cleanly 4 heavy procs on 4 cores. v2 pair (245,264) spread=1.0 → 4036.6 (best STAYS 3946.9 from spread=0.3); descent is a TIGHT-LOCAL effect (feas_seed=1/48 at every spread ⇒ only the bank center is feasible; wider spreads just hit more 1e6). Pair (339,62) still 4234.9=bank (no descent, 1/3 spreads). Monitor b94axmu1r armed on both v2 logs for the OFFICIAL-validation / center_f / crash lines. NEXT: read pair-1 OFFICIAL validation (the "★ OFFICIAL:" line, ~15-20min) → if it passes 1e-6, A0 is FALSIFIED ⇒ scope a fleet-scale warm-started BCP-descent run (301 pairs, seed each from bank, local sade/cmaes) as the top positive-point lever (hard ×1.78); if the official validator REJECTS the 3946.9 candidate (UDP-feasible but not 1e-6-matched), diagnose the solve_arrival_eccentric vs official-match gap before scaling; read Ch2 edge agent verdict (auto); re-arm.


**TICK 12:08 (DEEP — ★ Ch2-small skeleton CLOSED (basin-trap REFUTED); ★ Ch1 positive control was BROKEN by a binary-penalty feasibility wall — diagnosed, killed, relaunched CORRECTED):**
**(a) Ch2-small E-603 exp1 (a5540869) FINAL VERDICT = incumbent-skeleton-is-best.** Full topology characterization: the 3 tiny comps {3,3,3} bridge ONLY to comp0 (zero tiny↔tiny edges) ⇒ skeleton space is exactly 6 four-bridge (Family A, bank lives here) + 24 five-bridge (Family B, previously untried, E-566 fixed comp0 at 2 runs) macrostructures. Swept **2448 candidates / 2376 DP-feasible (97.1%)** across 80 comp0 orderings × all role/interior perms through the EXISTING ultrafine DP. Both correctness gates passed (official bank 116.373771d; DP re-eval 116.37547d, +0.0017 = q=0.05 grid quantization). **Best = 116.37547 = bank's DP-mk EXACTLY, ZERO beat.** ⇒ E-603's basin-trap hypothesis REFUTED by measurement — the Ch2-small bank is a genuine optimum at the component-skeleton level. **The R3=110.88/R1=101.65 gap must come from a DIFFERENT cheap-graph EDGE SET (finer Lambert/edge precompute → different Δv≤100 membership), NOT from re-routing the current components.** Instrumented reject-path check (M-005) confirmed DP rejects are genuine no-sink/hopeless, not silent bugs. Ch2-small skeleton lever = CLOSED; next Ch2-small angle = edge-resolution probe (queued, low-priority ×1 weight near floor).
**(b) ★ Ch1 positive control TRIPWIRE — 100% of random restarts returned 1e6 (infeasible).** Pattern: optimize2 (245,264) restart0(bank-anchored)=4255.3=bank, restarts 1/2/3=1e6; optimize3 (339,62) restart0=4234.9=bank, restart1=1e6. **AUDITED THE EVALUATOR (read ch1_pair_udp.py PairUDP.fitness):** PENALTY=1e6 is a HARD BINARY penalty (no gradient to feasibility); feasibility needs solve_arrival_eccentric to find a valid (a,e,i) capture from the propagated state = a THIN manifold; and multi_seed_pop on random restarts seeds only 16 Hohmann seeds (themselves infeasible for eccentric capture) + random fills ⇒ ~99% of EVERY population sits on the 1e6 plateau, INCLUDING restart-0 (79/80 infeasible) ⇒ CMA-ES never explored around the bank, just reported it unchanged. **⇒ the v1 experiment is INCONCLUSIVE about A0: it only shows our cold-start sade+cmaes can't find feasible BCP trajectories, NOT that the BCP floor can't beat the patched-conic bank — the SAME positive-control trap E-605 warned about, one level down.** Per directive (tripwire→diagnose+fix+relaunch) + full compute discretion: **KILLED optimize2/optimize3** (would grind ~3h emitting only 1e6 + bank-reproductions, zero A0 info). **Relaunched CORRECTED v2 (/tmp/e605_local.py, 2 procs slices [0:3]+[3:6] = all 6 median-ΔV pairs):** seed the population DENSELY with bank ± per-DoF Gaussian perturbations at 3 spread scales {0.3,1,3}× so CMA-ES explores the FEASIBLE neighborhood (free RAAN/argp/ν + coast T1/T2 tuning); decisively tests whether bank is a LOCAL ΔV optimum in the true-BCP 12-DoF UDP. Validates any sub-bank descent through official `_validate_transfer` at 1e-6. /tmp only, no bank/submit. **Interpretation map:** ANY pair descends+validates ⇒ A0 FALSIFIED locally ⇒ scale to fleet; ALL 6 locally optimal ⇒ A0 lever (if real) is in another feasible basin unreachable by local search ⇒ needs a feasibility-aware GLOBAL reformulation (collocation / arrival-state shooting / patched-conic-seeded BCP polish), which REPRICES E-605's "we already built the right tool, just under-deployed it" claim (ch1_pair_udp's binary penalty makes blind global BCP search infeasibility-dominated — it is NOT directly fleet-deployable as-is). **Cores: e605_local A (1) + B (1) = 2/4, 2 idle** (will fill after first feasibility check confirms v2 works). No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: confirm v2 feas_seed ratio high (≫1/80) + read first spread champs → if any pair beats bank, validate+scope fleet; if locally optimal, document the reformulation repricing in E-605; with 2 idle cores launch the next queue item (Ch2-small edge-resolution probe OR a wider-basin Ch1 global-feasible-seed test); re-arm.


**TICK 11:42 (cheap tick — both deep-audit agents reported; Ch2-small basin CONFIRMED (no skeleton beats bank); Ch1 positive control STILL COMPUTING + broadened to 6 pairs on idle cores):** Both bg agents notified.
**(a) Ch2-small skeleton DP sweep (a5540869) — Family A DONE, NULL:** tried 1152 / feas 1080 (93.8%) 4-bridge topologies × multiple comp0 orderings through the EXISTING ultrafine DP — **best DP-mk = 116.3755 = bank's DP-mk EXACTLY, ZERO beat.** Strong evidence the Ch2-small 116.3738 bank is a GENUINE optimum within the skeleton/DP architecture (the E-603 basin-trap hypothesis is being REFUTED by measurement — the basin IS the optimum, at least over all 4-bridge skeletons). Family B (alt bridge counts / topologies) still running, auto-notifies.
**(b) Ch1 positive control (ae2fc4c8) — agent's TURN ended but the OPTIMIZER PROCESS IS STILL ALIVE & HEALTHY** (PID 2278137, 99.9% CPU, single core per its 2-core budget). Verdict NOT yet in: it had only printed restart 0 of pair 1 (245,264) = champ 4255.3 = bank EXACTLY — but restart 0 is the BANK-ANCHORED restart, so matching bank is the EXPECTED/correct anchor behavior; the decisive falsifier is the RANDOM restarts 1-5 across all 3 pairs, ETA ~3h serial. Validation harness verified sound (all 3 bank rows pass official `_validate_transfer` at 1e-6 returning exactly their banked ΔV ⇒ any passing improver is genuinely feasible). **★ NEVER-STOP use of idle cores: launched /tmp/e605_optimize3.py = SAME heavy multi-restart optimizer on seeds[3:6] (3 MORE median-ΔV pairs: 339→62, 346→332, 52→228) → broadens the A0 falsification sample from 3→6 pairs at no critical-path cost.** Writes DISTINCT /tmp/ch1_e605_exp1b_winners.json (orig writes ...exp1_winners.json) ⇒ no collision. **Cores: optimize2 (1) + optimize3 (1) + Ch2 skeleton Family B (1) = 3/4, 1 headroom (avoid oversubscription).** No bank delta.
**★ WATCH (11:52 health-check, all 3 procs healthy 99%+ CPU):** optimize3 restart 0 (339→62) ALSO reproduces bank ΔV exactly (4234.9) ⇒ harness sound on both files. **But the bank-anchored restart-0 sade+cmaes did NOT descend below bank ΔV (champ = bank to 0.1 m/s), and optimize2 restart 1 (first RANDOM restart) returned champ=1,000,000 = the UDP INFEASIBILITY penalty.** If this pattern holds (anchored=bank, random=1e6) across restarts/pairs, the positive control risks INCONCLUSIVITY: it would show only that OUR sade+cmaes can't find feasible BCP solutions from cold start (a weak-optimizer signal), NOT cleanly that direct BCP opt can't beat the patched-conic floor (A0). Methodology trigger (>30% solver rejects → instrument) is on watch but n=1 random restart so far. If the full run ends all-bank-or-1e6, NEXT-tick diagnosis = is ch1_pair_udp's random-init feasibility manifold too thin for blind global search ⇒ rebuild the positive control as tight local CMA-ES (small sigma, many gens) around each bank anchor with free RAAN/argp/ν + long T1/T2 coast DoF, the most sensitive A0 test. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await (a) Ch1 exp1 + exp1b winners JSONs / log completion → if ANY pair's random restart beats banked ΔV under the official 1e-6 match ⇒ A0 FALSIFIED ⇒ scope fleet-scale ch1_pair_udp run (301 pairs + 99 empty) as the top positive-point lever; if 6/6 pairs show NO sub-bank ΔV ⇒ A0 SURVIVES (the UDP can't beat the patched-conic floor either) ⇒ re-examine whether ch1_pair_udp truly exploits BCP coast arcs or is itself architecture-limited; (b) Ch2-small Family B verdict → guard-bank if <116.3738 else close basin; re-arm.

**TICK 11:20 (DEEP — ★★★ Ch1 trajectory "EXHAUSTED" verdict OVERTURNED; user re-audit with new ground truth; positive control launched):** User fired the deep-single-prompt audit on Ch1 trajectory AGAIN with NEW ground truth: **Team HRI = rank 3 at 463,513 kg (≈2× our 236,420), achieved with HEAVY PARALLEL OPTIMIZATION — NOT a commercial solver, NOT multi-week research.** This contradicts the E-602/E-604 chain (which blamed intrinsic WSB capture physics). Re-read the OFFICIAL validator (reference/spoc4_udp/trajectory-matching.py): per-pair `mass=exp(−ΔV/311/G0)·5000−500`, scored `min(mass,(200−DT)·c_ld)`; FULL BCP (Sun-perturbed 3-body) heyoka propagation, 3 impulses + 2 free coasts, only (a,e,i) matched ⇒ RAAN/argp/ν FREE. **Decisive arithmetic (/tmp/ch1_e605_anatomy.py, bank reconstructs to 236,420.5 EXACTLY):** 301/400 filled (99 empty), **291/301 MASS-bound** (lever = ΔV not time/matching), per-pair ΔV med **4333 m/s with 230/301 ABOVE the 3940 "Hohmann floor"** (we're not even at the 2-body optimum), HRI implied ΔV ≈2734 m/s; ΔV→mass −1000 m/s≈+136k, −2000≈+326k ⇒ **the entire 2× gap = ~1-1.6 km/s/pair of ΔV.** ★ THE FLAW = assumption **A0**: "our patched-conic Lambert+DC solver's ΔV floor IS the problem floor" — FALSE; the BCP dynamics give sub-Hohmann Sun-assisted ΔV under DIRECT optimization that our solver structurally can't represent. E-049's "exhausted" = our weak solver failing to beat itself (positive-control failure); E-604 "refuted WSB" against a straw-man hand-crafted geometry. **★ We ALREADY BUILT the right tool — src/esa_spoc_26/ch1_pair_udp.py (per-pair pygmo UDP, 12 DoF, CMA-ES/DE/BiteOpt on the REAL BCP propagate, RAAN/argp/ν free) — but UNDER-DEPLOYED it (bank came from the patched-conic path).** Writeup E-605; memory ch1-trajectory-mass-lever-exhausted OVERTURNED (★★★ block). **★ Launched bg agent ae2fc4c8 = positive control:** run ch1_pair_udp CMA-ES/DE HARD (big pop × many gens × restarts) on 3 median-ΔV seed pairs, validate any improver through the official 1e-6 (a,e,i) match; if it beats banked ΔV ⇒ A0 falsified, scale to fleet (301 pairs + fill 99 empty) = highest-EV positive-point lever on the board (hard ×1.78, ceiling ~712k). /tmp only, ≤2 cores, no bank/submit. **Cores: Ch2-small skeleton (a5540869, final 3000s run, 2) + Ch1 positive control (ae2fc4c8, 2) = 4/4.** No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await BOTH agents (auto) — (a) ch1 positive control verdict → if A0 falsified, scope fleet-scale ch1_pair_udp run as the top lever; (b) Ch2-small skeleton verdict → guard-bank if <116.3738 else close basin; re-arm.

**TICK 11:06 (cheap tick — Ch2-small skeleton sweep HEALTHY & productive; no bank delta):** Health OK, no tripwire. **Ch2-small component-skeleton DP agent (a5540869) ALIVE & iterating** — built the cheap-graph structure (/tmp/ch2_small_struct.npz, 4 comps {40,3,3,3} labeled), reproduced/validated candidate construction (v3 smoke OK), now running exhaustive **Family-A comp0-split sweeps** (insert the 3 tiny comps at each split point of comp0's interior path, both ort-heuristic & bank-derived comp0 orderings) through the EXISTING ultrafine DP (evaluate_perm_dp_numba on /tmp/ch2_small_tcoupled_ultrafine.npz, q-grid). Multiple script generations (skel/skel2/skel3 + diags + check), load 1.97, 2 workers — real progress, no stuck loop. No /tmp candidate written yet (verdict pending). Sidebar this turn (user Qs, not loop work): confirmed Team HRI = **rank 3** on Ch1 Trajectory (−463,513 kg ≈ 2× our 236,420 bank; gap is WSB physics, now refuted on target class per E-604); summarized remaining Ch1-traj options (WSB hi-eL +15-25k residual / B1B2B3 +70k deferred impulsive fixes / commercial-solver matching). No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await a5540869 verdict (auto) → if a skeleton beats 116.3738 guard-bank + escalate; if none, document Ch2-small basin as genuine optimum & close; re-arm.

**TICK 10:39 (DEEP — ★ WSB probe FINISHED & REFUTES E-602's low-eL lever ⇒ Ch1 trajectory effectively CLOSED; launched Ch2-small experiment-1 on freed cores):** Both heavy agents DONE. **Ch1 WSB eL-stratified probe (a86302d, 25 pairs) FINAL — verdict confirmed on the full sample (I re-verified the status counts directly from /tmp/ch1_e603_results.json, measure-don't-assert):** LOW band (16 pairs, eL≤0.021 = circular Moon orbits = E-602's PREDICTED prime lever class) → **14 FAIL_no_valid_capture + 2 SUCCESS** (median LOW Δmass −46 kg, a wash); HIGH band (9 pairs, eL≈0.6) → **9/9 SUCCESS but only 5/9 beat impulsive** (median +154 kg; the good-impulsive HIGH pairs LOSE −396..−821 kg). **★ THE FLAW IN E-602: WSB ballistic capture fails EXACTLY on the class E-602 named** — the narrow eccentric capture window [a(1−e),a(1+e)] of a near-circular orbit can't be threaded by bound returns (median 114 returns/pair, 0 land inside); WSB only works where the apolune window is wide (high-eL), and there only helps where the impulsive solver was already poor. Control pair (118,171) reproduced the bank to 1e-10 kg (pipeline valid). **Realistic fleet upside ≈ +15–25k kg ≈ +0.7% on 236,420 kg ⇒ NO rank movement, partly impulsive-polish-recoverable. This was E-036 risk #1, now CONFIRMED by measurement.** ⇒ **Ch1 trajectory R3 WSB lever DOWNGRADED from "live multi-day build" to "refuted on target class"** — Ch1 trajectory is effectively closed under all available methods; small parked opportunity = 5 HIGH winners (+1.6t) in /tmp for a future guarded bank IF ever rank-relevant. Writeup E-604; memory ch1-trajectory-mass-lever-exhausted updated (★★ block). **Ch2-large rebuild agent (aa3dc61b) also confirmed done no-bank** (Part1 cheap escapes ≤0.5d everywhere; Part2 star topology REAL = 5 bridges required; Part3 reconstructor incomplete, nothing beat 932.53). **★ REPRICE: with both Ch1-WSB and Ch2-large-rebuild resolved, the top remaining EXPLORATION lever is Ch2-small experiment-1 (component-skeleton enumeration + DP) — the only open assumption-violating probe from the three deep audits.** Cores now FREE (load 1.0) ⇒ **launched bg agent a5540869 = Ch2-small component-skeleton DP sweep:** reproduce bank 116.3738 exactly (2 correctness gates: official re-score + DP re-eval of bank perm), build the 4-comp {40,3,3,3} cheap-graph, enumerate component visit-orders × boundary bridge-node choices, construct candidate perms (cheap-heuristic interiors) and run the EXISTING ultrafine DP (no Lambert recompute) per skeleton; /tmp only, no bank/submit, ~hours. Binary deliverable: basin-trapped+lever-found vs incumbent-skeleton-is-best. No bank delta this tick. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await a5540869 verdict (auto) → if a skeleton beats 116.3738, guard-bank it (separate official re-score) + escalate; if none, document Ch2-small basin as the genuine optimum & close it; re-arm.

**TICK 10:30 (cheap tick — ★ WSB probe PARTIAL result REFUTES the E-602 low-eL lever; cores saturated so HOLD Ch2-small sweep; sharpened its design):** Health OK, no tripwire — Ch1 WSB eL-stratified probe (a86302d, 4 workers, load 5.6 on 4 cores) running, log fresh. **★ DECISIVE PARTIAL (the deep-audit methodology self-correcting in real time):** of 16 LOW-eL pairs (circular/LMO Moon orbits = E-602's predicted PRIME lever class, corr(dv,eL)=−0.71), **14 FAIL `no_valid_capture` + 2 marginal SUCCESS (one −101kg WORSE, one +10kg negligible)** ⇒ WSB ballistic capture does NOT realize the saving on the low-eL class — it fails exactly where E-602 predicted it would win most. Confirms E-036 risk #1 ("lo-eL/LMO capture window too narrow, UNPROVEN"). HIGH-eL band (positive control + eccentric Moon): control E118→L171 reproduces bank bit-for-bit (−0kg ✓ pipeline valid), then mixed — E24→L250 +256kg, but E263→L202 −821kg, E280→L161 −396kg (net negative/null). **PROVISIONAL READ (await full summary): the WSB fleet-scale lever — the ONLY point-positive Ch1 R3 path (hard ×1.78) — is largely CLOSED; WSB neither captures low-eL cheaply NOR reliably beats the impulsive baseline on eccentric pairs. If the final summary holds, this DOWNGRADES the Ch1-trajectory R3 build from "live multi-day lever" to "refuted on its target class" — a major reprice.** Probe auto-notifies (~10-15min more: HIGH/MID pairs ~340-440s each). **Compute hygiene: 4-core box already at load 5.6 ⇒ did NOT launch the Ch2-small basin sweep (would thrash WSB); used the wait for pure-analysis prep instead.** ★ Reading scripts/ch2_dp_numba.py SHARPENED Ch2-small experiment-1: the forward DP's `e`-dimension ALREADY allocates the ≤5 exceptions optimally per-perm ⇒ "enumerate exc allocations" is subsumed; the genuinely untried DoF is the **component SKELETON** (visit-order of the 4 cheap-graph comps {40,3,3,3} × which boundary nodes bridge them) + DP on interiors — the structured perm move local ALNS can't reach atomically. Updated E-603 Phase-4 #1. Stale Ch2-large monitor (aa3dc61b/bonmykbc0) cleanup notification = no-op (Part-1 verdict already banked tick 10:10). No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await WSB final summary (auto) → DEEP REVIEW: if low-eL refutation holds, document Ch1 R3 WSB lever as closed + reprice queue (Ch2-small skeleton sweep + Ch2-large global rebuild become the top remaining exploration); when cores free, launch Ch2-small experiment-1; re-arm.

**TICK 10:23 (★ Ch2-SMALL RE-OPENED as a QUESTION — user-requested assumption audit found the "DP-optimal / exhausted" verdict is BASIN-conditional, not problem-exhausted; pure-arithmetic, no bank delta):** User asked for a 4-phase Ch2-small assumption audit (same template as Ch1: treat leaderboard-better as ground truth, find the FLAW not optimize). Re-read the official KTTSP fitness (src/esa_spoc_26/ch2_kttsp.py): makespan=times[-1]+tofs[-1], per-leg multi-rev Lambert about Moon (max_revs=20 FIXED ⇒ leaders use identical physics ⇒ 101.65 is pure routing/timing, not different dynamics). Decomposed the bank (116.3738d, /tmp/ch2s_decomp.py + /tmp/ch2s_topo.py, exact eval): **makespan = flight(Σtof) 109.99 + idle 6.38** (init wait 0); 43 cheap legs (Δv med 98.8, pressed to the 100 thr) + 5 exc legs (Δv 561-597, all near the 600 cap) ⇒ **solution sits on the constraint boundary everywhere.** ★ DECISIVE: **flight-only(zero idle)=109.99 < R3=110.88** ⇒ the ENTIRE R3 gap (5.49d) is phasing idle the DP can't remove FOR THIS PERM (5.77 of 6.38d idle is on cheap-leg departures, waiting to catch cheap windows). The 5 exceptions = **4 cross-component connectivity bridges (required) + 1 INTRA-comp0 shortcut (leg17 31→30, a CHOICE)**; cheap-edge graph 4-comp {40,3,3,3}; comp0 (40 nodes) min cheap-outdeg 2, ZERO degree-1 nodes ⇒ comp0's interior route is a real combinatorial space, NOT forced to split. **THE FLAW: "116.38 = DP-optimal / exhausted" is correct WITHIN one TOPOLOGY BASIN (these 4 bridge edges + this exc allocation + this comp0 cut) but false as a problem claim. Smoking gun (E-032): the topology-PRESERVING ops (segment_reverse/swap) gave 10/10 bankings; the topology-CHANGING ops (double_bridge/random_k) gave 0/10 — all DP-infeasible (DP feas ~1.2%) ⇒ the search was structurally confined to one basin; every path to a different exc-allocation passes through DP-infeasible intermediates. Same pattern as Ch1 (exhausted-within-architecture ≠ exhausted-of-problem).** Gap: to R3 ⊆ idle; to R1 (14.72d) needs ~8d less flight (different route) + near-zero idle. No tight LB (E-029 CP-SAT LB=66.5 too loose) ⇒ R3/R1 NOT proven unreachable, and flight<R3 makes them look very reachable. Writeup E-603. **Phase-4 experiments (ranked, cheap-first): (1) exception-allocation/topology-basin DP sweep — enumerate alt 5-exc assignments (few inter-comp candidate edges + optional intra-comp0 shortcut), run the EXISTING ultrafine DP per basin (no Lambert recompute), hours, MOST DECISIVE; (2) feasibility-repair large-radius ALNS (cheap-graph-aware repair so k=8-15 destroy lands feasible & crosses basins — E-033's missing piece); (3) tight LB (Held-Karp 1-tree/Lagrangian) to settle reachability.** ×1-weight near-floor ⇒ modest EV/hr, but experiment 1 is cheap enough to settle "are we trapped?" — QUEUED behind the running Ch1 WSB probe (no core competition: audit was pure arithmetic). No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: await Ch1 WSB probe a86302d (auto); then run Ch2-small experiment-1 DP basin sweep on freed cores; re-arm.

**TICK 10:10 (Ch2-large exploration DONE no-bank; launched the queued Ch1 WSB eL-stratified probe on freed cores):** **Ch2-large rebuild agent aa3dc61b FINISHED — no bank delta, 2 solid diagnostics + 1 honest incomplete.** Part 1 (DECISIVE): cheap escapes ≤0.5d at EVERY node/epoch (60 nodes×6 epochs, fine grid, 0 unreachable; p50 tof 0.05d = grid floor) ⇒ the 2.2× gap to r1=424.62 is PURE global routing/partition, NO tof speed limit — emphatically confirms the E-591 reframe. Part 2: the 3-disconnected-smalls STAR topology is REAL not a sampling artifact (150 source nodes: 0 cheap edges to any other small or comp0) ⇒ the 5 exception bridges ARE structurally required, NOT free for shortcuts (kills that lead). Part 3: from-scratch chrono-walk reconstructor did NOT beat 932.53 — the dense chrono-aware N×N cheap-graph precompute (avg cheap-degree ≈116) is the bottleneck, stalled at 300/1051 nodes in budget; no policy got a fair test, no candidate written (correct). ⇒ Ch2-large stays banked 932.53 (R2); a real Part-3 test needs a parallel ~1-1.5h full-shortlist build first (deferred — point-EV 0, rank-2 secure). **★ Cores freed (load 0.85) ⇒ launched the QUEUED Ch1 WSB probe (agent a86302d, bg):** the decisive eL-stratified test — does WSB ballistic capture beat the impulsive baseline on LOW-eL (circular/LMO) Moon targets (E-602's gap class, corr(dv,eL)=−0.71) where E-036's risk #1 says capture is unproven (~half the +62-128k fleet gain lives there)? Reuses ch1_e565_wsb_prototype.py under official BCP fitness, ~24-30 pairs in 3 eL bands incl high-eL positive control, /tmp only, no bank/submit, ≤2h. Resolves whether the WSB fleet lever (R3 path, hard ×1.78) is real on the high-value class or only on already-cheap high-eL pairs. No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: read a86302d WSB-probe verdict (auto) → if WSB wins on low-eL, scope the multi-day full WSB fleet sweep + 99-empty-slot fill as the Ch1 R3 build; if it only wins high-eL, downgrade the fleet estimate and document; re-arm.

**TICK 09:54 (★ Ch1 TRAJECTORY RE-OPENED — user-requested assumption audit found the "exhausted" verdict is ARCHITECTURE-conditional; WSB fleet-scale is a live point-positive lever, QUEUED behind Ch2-large compute):** User asked for a 4-phase Ch1-trajectory assumption audit (treat "better solutions exist per leaderboard" as ground truth; find the FLAW, don't optimize). Re-read the official validator (reference/spoc4_udp/trajectory-matching.py): propagates arbitrary state+3 impulses+2 coasts under FULL bicircular (Sun-perturbed) dynamics, only (a,e,i) matched, objective min(rocket_mass,(200−DT)·c_ld) over a 3-D (e,m,d) matching. Ran 3 cheap arithmetic probes (ch1_e600/601/602, bank reconstructs to 236,420.5 exactly): **(1) idD/capacity layer CLOSED** — 10/301 cap-bound, idD re-opt +311 kg (0.13%); **(2) matching CLOSED** — mean |iE−iL| 15.1° vs optimal 13.5°, and 65/65 stranded high-incl Earth orbits have a Moon orbit within 0.2° incl ⇒ 99 empty slots NOT plane-change-incompatible; **(3) ★ residual Δv cost is INTRINSIC to the impulsive architecture and is lunar CAPTURE not plane change** — corr(dv,eL)=−0.71 (low-eL/circular Moon orbits expensive to capture into), even 131 near-coplanar pairs (|iE−iL|<5°) avg dv=4927 m/s (~1600 over leaders' 3320; plane change at apogee cheap ~272 m/s @90°). **THE FLAW: "no further gains" conflated exhaustion-within-impulsive-architecture (A1) with exhaustion-of-problem; the BCP validator never imposes A1. Leaders' sub-3940-floor Δv proves Sun-assisted ballistic capture (WSB) pays the capture cost — proven +17% on n=1 (E-036), then dropped as "multi-week/deferred" = EV-misclassification.** Gap to R3≈473k = fill-99-empty (+78k→315k) + drop fleet Δv to 3320 via WSB (→473k). Writeup E-602; corrected memory ch1-trajectory-mass-lever-exhausted. **QUEUED experiment 1 (when Ch2-large frees cores): WSB fleet-scale Δv probe (E-036 solver under official BCP fitness) on ~30 pairs STRATIFIED BY eL (circular Moon orbits first), filled-expensive + unused high-incl; heyoka-heavy so must NOT oversubscribe.** Ch2-large agent aa3dc61b now on PART 3 (reconstructor, 4 workers ~73%, load 5.6, no cand yet) — let it finish. No bank delta. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED (overall-rank-3 gate).** NEXT: read aa3dc61b 3-part report (auto) → guard-bank if reconstructor <932.53; on its completion (cores free) launch the eL-stratified WSB probe; re-arm.

**TICK 09:20 (DEEP — ★★ MAJOR REFRAME: the large "connectivity wall" was a SAMPLING ARTIFACT; r1 gap is a global ROUTING/PARTITION problem, rebuild path RE-OPENED; user-requested deep exploration launched):** User asked to deeply explore the connectivity direction. Auditing the E-591 diagnostic (per project hostile-default discipline) found its "22 intrinsic nodes" verdict rests on a **25-random-target (2.4% of 1051) sample + 0.5d tof grid + 30d window — all 3 biased toward false-intrinsic.** The all-1050-target re-scan (ch2_e591_epochconn_alltargets.py, agent af37a659, now DONE) OVERTURNS it: flagged nodes 739/343/753 each have **10-30 cheap (Δv<100) neighbors at EVERY epoch, short-tof ones real (739→984=0.45d/81m/s, independently verified).** ★ Decisive macro-fact (I re-verified directly from the bank DV): 932.53d = 0.86 d/leg vs r1's 0.40 d/leg; decomposition = 709 short legs(≤1d) 293d / **305 MEDIUM legs(1-3d) 450d=48%** / 36 heavy(>3d) 159d / idle 29.6d; **ALL 36 heavy legs are Δv≈99 (barely-cheap), NOT exceptions — the 5-exc budget isn't spent on bottlenecks.** Zeroing all heavy legs → only ~773d. ⇒ the gap is GLOBAL (whole tour ~2× too long per leg), reachable in principle (rich cheap-edge supply) but ONLY by a from-scratch global TD constructor — NOT within-topology polish. This REFUTES E-590's "intrinsic floor ~882d, give up" reading and re-opens the multi-day rebuild as a justified (if point-EV-0-until-<424.62) lever. Corrected E-591 writeup (agent rewrote it, I committed it) + memory ch2-large-first-bank-topology + this. **★ NEVER-STOP launch: bg agent aa3dc61b = 3-part deep exploration** — (1) cheap-graph SPEED LIMIT (min-tof cheap-escape distribution, all-targets/fine-grid/long-window, ~80-node sample) to size the achievable floor; (2) TEST whether the "comp0+3-disconnected-smalls star, 5 mandatory bridges" topology is ITSELF a sampling artifact (if cheap inter-small edges exist, the 5 exc are FREE for shortcuts); (3) prototype a regret/saturation-aware CHRONO-WALK reconstructor (our prior constructors were earliest-arrival greedy + fixed-partition LNS = paint into corners). /tmp only, guard-bank if <932.53, ~2.5h. Two prior diagnostic agents (af37a659 epoch-conn, a73301b3 relocation grind) FINISHED; cores were freed then refilled by aa3dc61b. **Submission policy UPDATED (user 2026-06-13 07:10): defer ALL submissions until our combined banks would reach OVERALL rank ≤3 (not per-instance); stop flagging individual R1/R2 banks as submit-now; compute projected overall rank on deep ticks.** No bank delta this tick. **Banks: medium 189.10(R1) + large 932.53(R2) — UNSUBMITTED BY DESIGN (overall-rank-3 gate).** NEXT: read aa3dc61b 3-part report (auto) → if reconstructor beats 932.53 guard-bank; if speed-limit shows ~0.4d escapes common + star is artifact, scope the full multi-day global TD rebuild; re-arm.

**TICK 08:58 (DEEP — ★ r1 gap CLOSED as a QUESTION: it's cheap-graph CONNECTIVITY, not order/timing; large = floored-for-points at rank-2; whole board exhausted under bounded methods):** **Epoch-connectivity diagnostic (E-591, ch2_e591_epochconn_heavy.py) FINISHED.** For each of 36 heavy-tof (>3d) source nodes, swept 75 epochs across [0,3000d] counting cheap (Δv<100) outgoing transfers. **VERDICT: 14 epoch-locked, 22 INTRINSIC (0/75 cheap epochs — no epoch anywhere gives them a cheap transfer).** The 3 prime-suspect flagged nodes (739/343/753) are all INTRINSIC, killing the "relocate to a cheap epoch" hope. **★ Rough floor if ALL epoch-locked heavies were perfectly relocated ≈ 882.6d (−50d from bank), STILL 2.08× r1=424.62; residual 95.1d of intrinsic tof is unremovable by ANY reorder/retime under the fixed 5-exc topology.** ⇒ **r1=424.62 cannot use our partition+exc allocation at all** — beating ~882d requires spending exc bridges ON the intrinsic nodes, but our 5 are fully committed to the comp0↔3-smalls star just to keep the tour FEASIBLE (smalls reachable only via exc); reallocating breaks coverage. r1 uses a fundamentally different feasibility structure, reachable only by a from-scratch global solver co-optimizing partition+exc+order+timing. Writeup E-591; this CLOSES the "smarter solver reaches r1" question — three global attacks (E-042/044/046) + LKH (E-587) + retime-DP (E-589) + this connectivity audit all converge: the gap is the cheap-graph TOPOLOGY, not the optimizer. **Also processed: lookahead agent ae456981 FINISHED no-beat** (monotone-arrival proof: arr(dep) non-decreasing on all 1050 legs ⇒ greedy-earliest IS the order-fixed timing optimum; 3 best K-leg-local forced-wait levers net exactly 0d globally = the coupling trap). Stale ae456981 cleanup-waiter notifications ×3 = no-op. **Compute hygiene:** killed 2 redundant forced-wait leg-sweep procs (285473/287678) that were re-confirming the already-proven monotone result + oversubscribing the box. **★ LEADERBOARD (refetched 08:53, ALL STABLE since 02:34):** medium our 189.10 < r1=195.68 = **RANK 1**; large our 932.53 << r2=1143.56 = **RANK 2 secure** (>200d margin); small 116.38 ~r5-7 (4.6d to r4); matching-i/ii outside but exhausted; trajectory far back. **★ BOARD STATE: all 6 instances FLOORED/EXHAUSTED under bounded autonomous methods.** Large's within-topology ~50d residual is **point-EV 0** (stays rank-2) AND trap-bound; large rank-2→1 needs <424.62 = proven out of reach. **The ONLY point-positive moves left on the whole board are USER SUBMISSIONS** (medium R1 + large R2, both 0pts unsubmitted) — standing escalation. No bank delta this tick. Cores now FREE (4/4 idle after kills + diagnostic finish). **NEVER-STOP next:** with all bounded levers closed, the highest-value remaining compute is a scoped trap-free test of whether the 14 epoch-locked heavies can be relocated via chrono-walk-evaluated cluster moves toward the ~882d floor (point-0 but a legit bank-grind that could surprise); launching it. Re-arm. **Banks: medium 189.10(R1) + large 932.53(R2), both 0 pts UNSUBMITTED — standing escalation.**

**TICK 08:18 (★ CLOSURE — large floored on ALL bounded axes; pivot to the r1 epoch-assignment diagnostic):** **Endgame reorder agent (a4aafdcd) FINISHED NULL** — best retimed endgame 936.36 > bank 932.53, no candidate (correct). Established large is now FLOORED under the fixed 5-exc topology along ALL THREE bounded axes: timing (E-589 retime-DP), trap-free global reorder (E-587 LKH), intra-component endgame reorder (E-590). **★ STRUCTURAL FINDING (E-590): the reorder gain washes out under retime because the 932.53 tail is a GLOBAL-retime artifact (26.7d upstream idle shifts endgame entry epoch into good windows; isolated-endgame retime tops out at 936.36). AND the heavy-tof tail nodes (739/343/753) have 0/20 cheap transfers at their bank epochs (~880-920d) — it's CONNECTIVITY not order.** ⇒ provably, no move preserving the bank topology closes the 2.2× gap to r1=424.62; r1 needs a from-scratch global time-dependent solver co-optimizing partition + exc-bridges + order+timing (TGMA cluster-decomp + LKH/Concorde). Writeup E-590. **Compute hygiene:** killed the redundant null e590 background run (286597) grinding to budget. **★ NEVER-STOP pivot — the r1 global rebuild is now the ONLY positive-point lever left on the whole board (all 6 instances floored/exhausted under bounded methods).** Before committing multi-day build effort, launched bg agent af37a659 = DIAGNOSTIC: epoch-dependent connectivity scan of the heavy-tail nodes (do 739/343/753 etc. have cheap transfers at OTHER epochs? ⇒ gap is an accessible epoch-assignment problem worth a TD rebuild; or intrinsically expensive ⇒ partly instance-inherent). Writes vault/experiments/E-591 + /tmp/ch2_large_epoch_conn.json; diagnostic, no bank expected. Cores: lookahead agent ae456981 (still running ~2, /tmp/ch2_large_cand.json stuck 934.45 ⇒ retime-DP already dominates it) + epoch-conn diagnostic (2) = 4/4. No bank delta this tick. **Banks: medium 189.10(R1) + large 932.53(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read epoch-conn verdict (auto) → if epoch-locked nodes have accessible cheap epochs, scope a global TD rebuild agent; if intrinsically expensive, document r1 as partly instance-inherent. Read/clean up lookahead agent when it reports (expect no beat). Re-arm.

**TICK 07:52 (★ BANK DELTA large 934.45→932.53 (−1.91); retime-DP FLOORS the timing axis; endgame reorder agent launched):** **Retime-DP agent (ae7a8945) FINISHED with a beat.** The global Bellman departure-bucket DP (adapted from ch2_e568 medium method, δ∈[0,12d] bounded windows, 0.01d grid) on large's FIXED perm reached **932.5304 d feasible viols=[0,0,0,0]** (−1.91 vs greedy 934.45; captured 9.54 of ~32.8d waiting headroom vs greedy's 7.63). Guard-banked via ch2_e586_bank.py (CAND temp-repointed to distinct /tmp/ch2_large_retimedp_cand.json then reverted; backups .bak.e586 + /tmp/bank_bak/large_20260613_074940.json). **Lookahead agent ae456981 (ch2_e589c) STILL running, /tmp/ch2_large_cand.json stuck at 934.45 ⇒ retime-DP dominated it, as predicted.** ★ KEY FINDING: the DP's edge over greedy is RESOLUTION not policy — scalar earliest-arrival frontier is provably globally optimal (min-arrival monotone in input epoch), greedy already used it at 0.25d quantum, DP's 0.01d grid found shorter transfers in the **dense endgame cluster (legs ~1000-1050)**. ⇒ **large's TIMING axis is now FLOORED at 932.53 for the fixed tour**; residual needs PERMUTATION/exc change, but pure reorder hits the epoch-shift trap (E-587) ⇒ only trap-free reorder = chrono-walk-evaluated local search. Writeup E-589. **★ NEVER-STOP: launched bg agent a4aafdcd = endgame-cluster (last ~50 nodes) JOINT reorder+retime, each candidate scored by the TRUE chrono walk (trap-free), monotone accept, distinct /tmp/ch2_large_endgame_cand.json.** Validation note baked into brief: ALWAYS use FINE walk params (40d/2400/0.25) — coarse 180-step default falsely fails leg-958 on the bank perm too. Cores: lookahead agent (2, now redundant/finishing) + endgame agent (2) = 4/4. **Session large total: 1013.29→932.53 (−80.76d, −8.0%).** Point-EV still 0 (rank-2 secure). **Banks: medium 189.10(R1) + large 932.53(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read endgame agent verdict (auto) → guard-bank /tmp/ch2_large_endgame_cand.json if <932.53; read lookahead agent final (auto, expect ≥932.53 = no-op); if both floor, large = architectural ceiling under bounded methods ⇒ only r1 lever left = from-scratch global time-dependent solver (multi-day) — decide whether to attempt; re-arm.

**TICK 07:33 (cheap tick — launched the dominating retime-DP on large's free cores; lookahead agent still grinding):** Health OK, no tripwire. **Large timing agent ae456981 STILL RUNNING** — now on its 3rd variant ch2_e589c_greedywait_lookahead.py (2 workers 278117/278118 @27min CPU, load 2.30), i.e. it's iterating LOOKAHEAD heuristics, NOT the exact Bellman retime-DP; /tmp/ch2_large_cand.json unchanged (06:35, still the 934.45 E-588 cand) ⇒ no beat past 934.45 yet. 2 cores free. **★ NEVER-STOP: launched bg agent ae7a8945 = the PROVEN dominating method — adapt scripts/ch2_e568_medium_ultrafine_retime.py (E-040 global Bellman departure-bucket DP) to large's banked perm.** Per the 07:09 closure the retime-DP DOMINATES the greedy/lookahead waiting lever (medium/small proof), and large's pipeline NEVER ran it ⇒ should reach ≥934.45, plausibly toward the ~909d waiting-headroom floor. Briefed for tractability (bounded δ∈[0,12d] windows, not full 180k-bucket horizon) + writes to DISTINCT /tmp/ch2_large_retimedp_cand.json (NOT the lookahead agent's file) ⇒ no collision; guard-bank arbitrates the winner. /tmp-only, strict<934.4452, no submit, ~50min. Cores: lookahead agent (2) + retime-DP agent (2) = 4/4. No bank delta this tick. **Banks: medium 189.10(R1) + large 934.45(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read both large-timing agents' verdicts (auto) → guard-bank whichever /tmp cand beats 934.45 (ch2_e586_bank.py reads /tmp/ch2_large_cand.json — for the retimedp cand, copy it there first or point CAND at it) + document the timing closure; re-arm.

**TICK 07:09 (★ METHODOLOGICAL CLOSURE — waiting lever ⊆ retime-DP; medium/small confirmed timing-floored; large's correct fix = global retime-DP):** **Generalization agent (ae2893a4) FINISHED.** The E-588 departure-time waiting lever is a **greedy special case of the ultrafine-retime-DP** (E-040/E-568 method = forward Bellman DP over departure-time buckets, `for tp in range(tmin,T)` lets each leg depart at ANY bucket ≥ earliest-reachable with the same per-leg earliest-feasible-cheap-tof cost model). PROOF both ways: (structural) read ch2_e568_medium_ultrafine_retime.py — DP search space ⊇ greedy lever; (empirical) greedy lever lands WORSE on both — **medium 189.10→191.09 (+1.99d), small 116.37→122.09 (+5.71d)** — the signature of a myopic special case dominated by the global optimum. ⇒ **Ch2 medium + small are confirmed at their retime-DP timing optimum, ZERO waiting-lever headroom** (closes the question — no candidate written, correct). **★ KEY ACTIONABLE: large won −7.63d from the greedy lever ONLY because its pipeline (windowed-LNS + earliest-feasible chrono walk) NEVER ran a global retime-DP. The dominating closure for large = adapt the proven ch2_e568 retime-DP (Bellman departure-buckets) to large's banked perm** — strictly ≥ the greedy lever AND the lookahead the in-flight agent is building. No SendMessage in this harness ⇒ can't steer agent ae456981 mid-run; it owns /tmp/ch2_large_cand.json + is the designated large-timing worker (running ch2_e589b_lookahead.py, auto-notifies). PLAN: when it reports, if its lookahead < the full-DP optimum, launch a dedicated retime-DP-on-large adaptation (output to a DISTINCT /tmp path, guard-bank picks the winner). No bank delta this tick. **Banks: medium 189.10(R1) + large 934.45(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read large timing agent verdict (auto) → guard-bank if <934.45; decide lookahead-suffices vs launch-full-retime-DP; re-arm.

**TICK 07:00 (cheap tick — s2/s3 finished no-bank; idle cores → launched waiting-lever generalization check):** Health OK. **s2/s3 windowed-LNS FINISHED at 942.0744 acc=0** (never escaped the stale 942.07 seed basin; both > current 934.45 bank ⇒ guard would reject, no-op, correctly skipped). Freed 2 cores (load 1.19). **Large timing agent ae456981 still RUNNING** (ch2_e589b_lookahead.py = its non-greedy lookahead waiting DP; single-threaded mid-pass, harness-tracked, auto-notifies) — owns /tmp/ch2_large_cand.json, do NOT collide. **★ NEVER-STOP: idle cores → launched bg agent ae2893a4 = generalize the E-588 departure-time waiting lever to MEDIUM + SMALL** (non-colliding: different instances, writes /tmp/ch2_medium_cand.json + /tmp/ch2_small_cand.json, NOT the large path). KEY hypothesis to resolve: medium's 189.10 bank came from ultrafine-retime DP (E-040/568) and small's 116.38 is a time-coupled DP optimum — both likely ALREADY SUBSUME the waiting lever (lever ⊆ retime-DP) ⇒ EXPECT zero headroom; the valuable output is CONFIRMING subsumption (closes whether large only won because its LNS pipeline never ran a global retime-DP) or the surprise of fresh headroom. /tmp-only, strict< bank, no submit, ~35min. Cores: large timing agent (~1-2) + generalization agent (2) = full. No bank delta this tick. **Banks: medium 189.10(R1) + large 934.45(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read both agent verdicts (auto) → guard-bank any /tmp cand that beats its bank (ch2_e586_bank.py for large; need medium/small bank scripts — ch2_e568_bank.py exists for medium) + document; re-arm.

**TICK 06:38 (DEEP — ★ BANK DELTA large 942.07→934.45 (−7.63) + NEW LEVER found; LKH closed as dead-end; next-lever agent launched):** **LKH agent (a5685879) FINISHED with a win via an ORTHOGONAL lever.** (1) **Per-component LKH = clean NEGATIVE:** LKH (elkai) on the epoch-aware 0.005d matrix found dramatically shorter FIXED-EPOCH tours per piece (72→8.65d) but the chrono re-walk INFLATED every one (piece0→951.63d, +9.6d worse) ⇒ **the E-046 epoch-shift trap holds at the PER-PIECE level too** (fixed-epoch matrix exact only while order unchanged, verified 0.0 error; reorder shifts downstream epochs). **Pure node-reordering on large = structural dead end; a stronger TSP solver makes it WORSE.** (2) **WIN = departure-time (waiting) lever (E-588):** banked walk takes EARLIEST-feasible transfers, only waits ≤2d for feasibility, NEVER for a SHORTER transfer; greedy wait-forward pass traded 17.2d idle for 24.8d less tof ⇒ **934.4452 d feasible viols=[0,0,0,0] 5/5 exc.** Guard-banked via ch2_e586_bank.py (942.0744→934.4452, round-trip OK, backups .bak.e586 + /tmp/bank_bak/large_20260613_063717.json). **Large session total: 1013.29→934.45 (−78.84d, −7.8%).** (3) **★ Structural finding (NEVER-STOP payoff):** makespan lives in comp2 (601-node) intra-cluster cheap-Δv legs = ~734d/934d (78%), NOT the 5 exc bridges (~11.5d); 18 of 43 expensive legs have cheaper transfers a few days later (leg316 18.42→6.59d by +8d wait); naive waiting headroom ~32.8d (floor ~909d), greedy captured 7.63d. r1=424.62 (~510d residual) = JOINT order+timing of comp2 ⇒ from-scratch global time-dependent solver. Writeup E-587; memory ch2-large-first-bank-topology updated. **★ NEVER-STOP relaunch: bg agent ae456981 = non-greedy/DP departure-time optimization on the fixed 934.45 order (the greedy E-588 is its degenerate special case ⇒ headroom toward ~909d) + small timing-coupled local reorders on comp2's expensive legs, chrono-evaluated. /tmp-only, strict<934.4452, no submit, ~50min.** Cores: s2/s3 windowed-LNS (TL~06:50, plateaued acc=0 on now-stale 942.07 seed, harmless) + timing agent (2) = 4/4. Point-EV still 0 (large rank-2 secure). **Banks: medium 189.10(R1) + large 934.45(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read timing agent verdict (auto) → guard-bank if <934.45 + document; at ~06:50 s2/s3 /tmp checkpoints (likely >934.45, guard rejects); re-arm.

**TICK 06:13 (cheap tick — s0/s1 finished no new bank; launched the ONE untried large lever = per-component LKH on freed cores):** Health OK. **Large seeds s0/s1 FINISHED:** s1 final 942.0744 (already banked tick 05:53), s0 960.9587 (worse, harmless) ⇒ no new bank delta. They freed 2 cores (load dropped 4.40→2.05). **s2/s3 still running but STALLED at acc=0** (s2 it50 @1078s, s3 it75 @1261s, both at 942.0744) ⇒ the windowed-LNS basin at 942.07 is near-exhausted; identical-method diversification is low-value. **★ NEVER-STOP: launched bg agent a5685879 = the one genuinely-untried structural lever for large — per-component LKH (Lin-Kernighan-Helsgaun) on the epoch-aware 0.005d-grid matrix.** Rationale: E-562/562b's win (1536→1049) used OR-Tools GLS as the per-piece solver; LKH is a far stronger TSP solver never tried here. Hypothesis: re-solving comp0 (601-node, makespan-dominant) + the 150-smalls with LKH inside the SAME sequential epoch-aware re-walk loop (avoids the E-046 epoch-shift trap that killed fixed-epoch global reorder) may break below 942.07 where GLS saturated. Agent brief: audit-evaluator-first, reuse ch2_e562/e586 + walk_perm_chrono machinery, respect 5-bridge/0-big-jump topology, /tmp-only guard (strict<942.0744), no submit, ~50min box. If it ONLY matches/inflates (LKH finds shorter fixed-epoch tour that chrono re-walk inflates), that diagnosis = the valuable output (confirms r1=424.62 needs full from-scratch global TD-TSP, not a stronger local solver). Cores: s2/s3 (2) + LKH agent (2) = 4/4. No bank delta this tick. **Banks: medium 189.10(R1) + large 942.07(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read LKH agent verdict (auto-notify) → guard-bank if <942.07 + document; at ~06:50 read s2/s3 /tmp checkpoints → guard-bank if improved; re-arm.

**TICK 05:53 (★ BANK DELTA — Ch2 LARGE 947.80 → 942.07 d (−5.73); small EXHAUSTED-confirmed; 2 diversified large seeds launched on freed cores):** **Ch2-small structural agent (ae6e43e9) FINISHED — EXHAUSTED confirmed, no candidate.** REFUTED the 13.62d-idle premise (that belonged to the OLD 142.92 bank; current 116.38 bank = 109.99d flight + 6.38d *structural* phasing idle, not removable by re-timing — all greedy re-timings strictly worse ⇒ 116.38 is a genuine time-coupled DP optimum). Topology 7-component, tight 5-exc stitch; the 4.6d gap to r4 lives in permutation space already exhausted by DP-ALNS+LKH. Bank reproduced exactly 116.373771 d. ⟹ Ch2-small closed under bounded autonomous methods. **★ NEVER-STOP: 2 cores freed ⇒ launched 2 diversified large LNS seeds** from the live 942.07 best (s2 big-window WMIN45/WMAX80 PID 272155, s3 small-window WMIN10/WMAX20 PID 272159, TL=3600 ~06:50) to keep grinding the one actively-descending instance — large is the only place LNS is still finding accepts. Both verified clean start (fine-walk base 942.0744 feas exc=5 viols=[0,0,0,0]). **Large seed s1 reached 942.0744 (acc=10) ⇒ guard-banked via ch2_e586_bank.py: 947.8030→942.0744 d (−5.73), feasible viols=[0,0,0,0], round-trip OK, backups .bak.e586 + /tmp/bank_bak/large_20260613_055312.json.** (s0 stuck at 960.96, harmless.) **Large total this session: 1013.29→942.07 d (−71.21 d, −7.0%).** Point-EV still 0 (large RANK 2 secure r2=1143.56; r1=424.62 structural). Cores: s0/s1 (TL~06:10) + s2/s3 (TL~06:50) = 4/4, load 4.40. **Banks: medium 189.10(R1) + large 942.07(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: at ~06:10 read s0/s1 final /tmp checkpoint → guard-bank if <942.07; at ~06:50 read s2/s3 /tmp/ch2_large_cand_e586_s{2,3}.json → guard-bank if improved; re-arm wakeup.

**TICK 05:36 (DEEP — ★★ TWO BANK DELTAS: medium 192.90→189.10 (R1 extended) + large→947.80 (R2); leaderboard repriced; small agent launched on freed cores):** **Medium retime (E-568) FINISHED with a BEAT:** DP ultrafine mk=**189.1000 d** feas viols=[0,0,0,0], −3.80 vs 192.9002 bank. Guard-banked via scripts/ch2_e568_bank.py (independent official re-score + round-trip, backup .bak.e568 + /tmp/bank_bak/medium_2026*). **Large seed 1** descended to 947.80 → re-banked via ch2_e586_bank.py (951.63→947.8030, −3.83). **★ LEADERBOARD REFETCHED (05:34, stable since 02:34, no competitor moves):** medium r1=195.68 ⇒ our 189.10 = **RANK 1** (beats r1 by 6.58d, lead extended); large r1=424.62/r2=1143.56 ⇒ our 947.80 = **RANK 2** (beats r2 by 195.76d, margin extended); small r1=101.65/r3=110.88/**r4=111.76** ⇒ our 116.38 = ~r5-7 (gap to r4 = 4.6d); matching-i r1=33,555.61 (our 33,338 below r3, exhausted); matching-ii r2 down to 73,569.61 (our 72,204 outside, exhausted); trajectory r5≈−386,530 (our −236,420, far back). **Medium retime finished ⇒ 2 cores freed.** REPRICE: **small is the ONLY instance with positive near-term POINT-EV** (4.6d to r4 = +1-2 ranks, easy ×1); all others rank-1/2-secure (point-EV 0) or exhausted. **★ NEVER-STOP launch: bg agent ae6e43e9 = Ch2-small STRUCTURAL lever hunt** — NOT a DP-ALNS re-run (that's the exhausted 116.38 DP floor); targets the audit's untried angles: the wait_dt=1.0 hostile-default **13.62d idle** (continuous/finer re-timing to remove it), exc-bridge selection (3-of-5 required), edge-graph density / finer tof grid (E-045 coarse-grid-overstates-tof lesson). Guard-bank /tmp only, no submit. Cores: large seeds (2, TL~06:10) + small agent (2) = 4/4. **Banks now: medium 189.10(R1), large 947.80(R2), both 0 pts UNSUBMITTED — standing escalation.** NEXT: read small agent verdict (auto) → guard-bank if <116.38; at ~06:10 final large bank from /tmp seed checkpoint; re-arm.

**TICK 05:28 (★★ BANK DELTA cont. — Ch2 LARGE now 951.63 d (−61.66 d total from 1013.29); seeds plateaued, point-EV still 0):** E-586 seed 1 (windowed LNS) descended 970.07→965.74→963.84→962.87→957.16→955.25→951.63 d over ~25 min (seed 0 stuck at 971.07, harmless). Each step guard-banked via scripts/ch2_e586_bank.py (independent official re-score, feasible viols=[0,0,0,0], round-trip). **Current bank = 951.6331 d**, backups in /tmp/bank_bak/large_2026*.json (orig 1013.29 + all interim preserved). Seeds checkpoint live to /tmp/ch2_large_cand_e586_s{0,1}.json (script line 152, per-accept) ⇒ readable directly; deep-dive agent a4f79ab6 self-completed but its internal monitor still heartbeats (can't TaskStop a completed task; harmless). Descent plateaued at 951.63 (stable ~10 pings), ~41 min TL left, gains now marginal + **point-EV=0** (large RANK 2 secure, r2=1143.56; r1=424.62 needs global TD-TSP rebuild). Cores: medium retime (2) + 2 seeds = 4/4. NEXT: at 05:34 re-bank if /tmp seed checkpoint < 951.63; after seeds TL (~06:12) final bank pass; read medium retime DP. ★ Submittable banks: medium=192.90(R1) + large=951.63(R2), both 0 pts unsubmitted.

**TICK 05:08 (★★ BANK DELTA — Ch2 LARGE 1013.29 → 970.07 d (−43.22 d) BANKED + verified; recovered a crash-lost LNS candidate):** Large deep-dive agent (a4f79ab6) FINISHED with a real win. (1) Audited evaluator FIRST: official UDP re-scores our `solutions/upload/large.json` = 1013.2886 d exactly ⇒ the 2.4× gap to r1=424.62 is REAL, not a re-score bug. (2) **Unexpected finding (NEVER-STOP payoff):** prior E-578 windowed destroy-repair LNS had reached **979.768 d** on 2026-06-12 but was **killed by a mamba-lock crash before banking** — left unverified in /tmp, silently lost (33.5 d). Agent re-scored it (979.77 feasible), fine-grid re-walked (n_steps=2400) → 974.76, continued LNS (ch2_e586_large_window_continue.py, 2 seeds) → 971.07 → **970.07 d**. (3) **I independently guard-banked** via scripts/ch2_e586_bank.py: re-scored /tmp/ch2_large_cand.json with official KTTSP.fitness (hard.kttsp), feasible viols=[0,0,0,0], strict<bank, round-trip match ⇒ banked 1013.2886→971.0669→**970.0677 d**; backups large.json.bak.e586 + /tmp/bank_bak/large_2026*.json (orig 1013.29 preserved). Writeup E-586; memory ch2-large-first-bank-topology updated. (4) EV: mechanically clean −43 d but **point-EV≈0** — large stays RANK 2 (r2=1143.56, now bigger margin/better tiebreaker); r1=424.62 confirmed structural (needs from-scratch epoch-robust global TD-TSP, 3 prior attacks E-042/44/46 failed). **2 LNS seeds STILL RUNNING** (PIDs 267547/267552, TL=70min, self-terminate ~06:10, checkpoint /tmp/ch2_large_cand.json) — may push <970.07 ⇒ re-bank on next wake if so. **Process lesson (→E-586): sweep /tmp/*cand* for un-banked survivors after any abnormal run death; long LNS must bank incrementally.** Cores: medium retime (2) + large seeds (2) = 4/4. NEXT: at ~06:10 re-run scripts/ch2_e586_bank.py if seeds beat 970.07; read medium retime DP (still SCAN 160/180 heavy tail). ★ ESCALATION refresh: submittable banks now medium=192.90(R1)+large=970.07(R2), both 0 pts unsubmitted.

**TICK 04:38 (DEEP — ★ Ch1 trajectory filled-pair MASS lever EXHAUSTED (E-049); all 3 Ch1 instances now closed under bounded autonomous methods; pivoted freed cores to Ch2-LARGE deep-dive):** Traj DoF gate agent (a10c1f95) FINISHED. **VERDICT STOP — bank is per-pair DoF-optimal.** 12-pair sample (iE 0→89.4°): **11/12 found NO improvement** (re-opt landed BELOW bank ⇒ bank pairs already carry more mass than a fresh 1536-combo apogee sweep finds); only E140→L222 gained +118.9 kg = **+1.493% sample, BELOW 2% gate** ⇒ no STEP-2, no /tmp candidate. Corroborated by git history: apogee_polish already harvested **+23,139 kg** (e3fb9fd) + polish_to_theoretical SADE **+14 kg/176 pairs** (32635e2). The +118.9 kg is 0.05% of the 236,420 kg bank ⇒ ZERO rank movement (traj r10+, r5 worst −372,729 = 58% gap); a full re-opt for ~+3500 kg = 0 points. **Writeup E-049.** ⟹ **all 3 Ch1 instances exhausted under bounded autonomous methods**: matching-i/ii = open-source-solver-capped (E-048, license/$ decision), trajectory filled-pair mass = saturated (E-049), 99 empty slots = feasibility-refuted (E-047). Ch1 rank headroom is now ALL either a commercial-solver $ decision or the multi-week WSB/impulsive-perfection physics model ([[ch1-coherent-model-r3]]). **★ NEVER-STOP pivot (2 cores freed by traj agent): launched bg agent a4f79ab6 = Ch2-LARGE deep-dive + bank-grind.** Rationale: large is HARD-weight (×1.778), RANK 2 (1013.29 d), but r1=424.62 (TGMA) is a 2.4× gap to a DEMONSTRATED optimum ⇒ our patched architecture is likely missing a structural lever (prime "unexpected-finding" target per NEVER-STOP) — the highest-EV autonomous deep analysis left on the board. Agent mission: STEP-0 reproduce 1013.29 d via official evaluator (audit-evaluator-first), STEP-1 decompose the gap (cost-matrix-epoch bug? cluster structure? tour crossings → LKH/2-opt), STEP-2 guard-bank any strict<1013.29 to /tmp only. Cores: medium retime (2) + large agent (2) = 4/4. No bank delta. NEXT: read large agent report (auto) → if it finds a lever/improvement, guard-bank+escalate; if it confirms r1 needs multi-day global TD-TSP rebuild, that's the documented ceiling. Read retime DP when leg 180 flushes (still SCAN 160/180, heavy tail).

**TICK 04:31 (cheap re-check — both jobs confirmed ADVANCING, non-hang verified):** Health OK, no tripwire. **Traj DoF gate** (a10c1f95, PIDs 260349/260350) CPU 24→26min, still mid-resolve (no per-pair flush yet — 12 pairs × ~1536 sweeps, heavy); auto-notifies. **Medium retime** still SCAN 160/180 BUT CPU advanced 281→284min over the 3min wall ⇒ both workers busy, confirms the 31min log-staleness is the heavy-tail-leg/sparse-checkpoint pattern, NOT a hang. Load 5.23/4 = full. No bank delta. NEXT unchanged: read traj gate verdict (auto) → pass⇒full 301-pair re-opt; fail⇒mass lever exhausted on filled pairs; read retime DP when leg 180 flushes.

**TICK 04:28 (cheap tick — traj DoF gate mid-resolve; retime tail-leg slow but not hung):** Health OK. **Trajectory DoF agent** (e578_dof_reopt_gate.py, PIDs 260349/260350, CPU 24min) set up a clean 12-pair sample GATE spanning iE 0.0°→89.4° (banked masses 223–1145 kg/pair) and is mid-resolve (~1536 DoF sweeps/pair × 12); verdict pending, auto-notifies. **Medium retime**: still SCAN 160/180 — log 40min stale BUT CPU advancing (255→281min over 40min wall) ⇒ heavy tail legs (per-leg cost highly variable: 100→120 took 3649s) + 20-leg checkpoint granularity = the known "sparse-log-not-a-hang" pattern (diagnosed ticks 00:10/00:51), NOT a tripwire; legs 161-179 in progress, next flush at 180 then DP. Low duty (~33%) = parallel-tail load imbalance (one worker straggling on a hard leg). Keeping it: provably-safe rank-1 defense, no higher-EV use for those 2 cores (matching exhausted, small floored, large pole multi-day). Load 4.42/4 = full. No bank delta. NEXT: read traj gate verdict (auto) → pass⇒full 301-pair re-opt; fail⇒mass lever exhausted on filled pairs. Read retime DP when leg 180 flushes.

**TICK 03:48 (matching DEFINITIVELY exhausted (all open-source solvers); pivoted to the orthogonal E-047 implication = trajectory per-pair MASS re-opt):** CP-SAT large-neighborhood LNS agent FINISHED: **also plateaus — 0 gains.** Decisive structural finding: across **~49k exact re-optimizations** of neighborhoods up to **~2,450 free vars/block** (3-4× the Gurobi 680-cap), CP-SAT proved EVERY block already OPTIMAL; the bank is a **maximal packing** (0 addable rows, regular ~5-9-degree graph, lossless ×1000 integer objective). ⟹ **both matching-i & matching-ii are TRULY exhausted under every open-source option** (restricted Gurobi ILP-LNS + global CP-SAT probe + uncapped CP-SAT large-neighborhood LNS); only unrestricted commercial MILP or a specialized 3-index Lagrangian/branch-price-cut could crack them = a user license/$ decision. E-048 updated implicitly (verdict now DEFINITIVE). **★ NEVER-STOP pivot (2 cores freed): the matching close + E-047's finding that RAAN is a ~60× MASS lever (not just feasibility) surfaces an UNTESTED orthogonal lead — are the 301 ALREADY-FILLED trajectory pairs each at their free-DoF-optimal mass, or did the bank leave mass on the table?** Trajectory is the biggest-gap instance (236k vs r1 473k kg, 2×) AND highest weight (HARD ×1.78) ⟹ highest absolute headroom on the board. **Launched bg agent a10c1f95** = per-pair free-DoF (raan_e/argp_e/raan_l/argp_l/split/tof) MASS maximization on the FIXED assignment, with STEP-0 novelty check (apogee_polish/e567_raan_lever/E-567 may have tried this) + a 12-pair sample GATE (abort if bank already DoF-optimal, ≈0 gain) before any full 301-pair re-opt. /tmp-only, official udp.fitness re-score, guard-bank gate (>236,420.533 strict+feasible), no submit. **Medium retime**: SCAN 160/180 @16207s (rate recovered) ⇒ scan nearly done, DP next. Cores: retime (2) + traj agent (2) = 4/4. No bank delta. NEXT: read traj agent gate verdict (auto) → if sample shows mass gain, full re-opt + guard-bank + escalate; if bank already DoF-optimal, mass lever exhausted on filled pairs ⇒ trajectory rank needs the multi-week WSB model; read retime DP result when it lands.

**TICK 03:28 (matching-ii EXHAUSTED too; launched the one untried solver angle = CP-SAT large-neighborhood LNS):** Matching-ii agent FINISHED: **NO IMPROVEMENT — 22,700 exact connected-region (≤680-row) Gurobi re-opts → 0 gains; CP-SAT global feasibility probe (obj>bank, 2400s) = UNKNOWN.** Identical deep-local-opt plateau to matching-i. **Both matching instances are blocked at the SOLVER level:** the bank is optimal within every ≤680-row block (Gurobi restricted-license cap), and the full 92k/25k global model is too big for CP-SAT in 40min; the leaderboard headroom (i:0.65%, ii:2.05%) proves competitors used a stronger GLOBAL exact solve. Writeup → E-048. **★ NEVER-STOP relaunch (2 cores freed): the agents' own diagnosis = improving moves must change >1 of the 680-row blocks AT ONCE, which the license-capped Gurobi blocks structurally can't reach. Launched bg agent abfe9c6c = LARGE-neighborhood LNS with CP-SAT (NO size cap) as the block solver** — free ~2000–6000-row connected/multi-region sub-instances, re-optimize each time-boxed (30–120s), accept strict gains; prioritize matching-ii (bigger gap). This is the single untried open-source angle that directly targets why the small-block LNS plateaued. /tmp-only, guard-bank gate (>bank strict+feasible), no submit. Cores: medium retime (2) + CP-SAT-LNS agent (2) = 4/4. No bank delta. NEXT: read agent verdict (auto) → if it beats a bank, guard-bank+escalate; if it ALSO plateaus ⇒ both matching instances are definitively exhausted under all open-source solvers (only unrestricted Gurobi/CPLEX or a specialized 3-index Lagrangian/colgen method remains = effectively a user license/$ decision) ⇒ rotate to bank-grind/re-analysis. Health-check retime (SCAN ~120-140/180).

**TICK 03:20 (cheap tick — matching-ii LNS shows same deep-local-opt signature; CP-SAT probe pending):** Health OK. **Matching-ii agent**: both Gurobi-LNS seeds (s11/s22) ran **11,700+ exact connected-region re-opts each → 0 gains, best stuck at 72,204.2930** (identical deep-local-opt signature to matching-i); seeds time-boxed out (EINTR/code-4 = agent's own stop, not a crash), now running the decisive **CP-SAT feasibility probe (obj>bank, 2400s, PID 145741 ~50min CPU)** — will auto-notify with final verdict. No /tmp improving candidate (only the 72,204 smoke file). **Medium retime**: SCAN 120/180 @13624s, rate re-degraded (100→120:3649s) ⇒ ~60 legs + DP remain, est 2.5-3h more; log fresh, both PIDs busy (CPU 215min). Low-EV rank-1 defense (already secure, 2.8d margin) but no higher-EV use for those 2 cores (everything else exhausted) ⇒ NEVER-STOP says keep it grinding. Load 4.28/4 = full. No bank delta, no tripwire. NEXT: read matching-ii CP-SAT verdict (auto) → if UNKNOWN like matching-i ⇒ matching-ii also exhausted, mark + rotate to bank-grind/re-analysis fallback; if SAT ⇒ extract+guard-bank+escalate. Health-check retime; if matching-ii frees 2 cores while retime still scans, launch next fallback analysis.

**TICK 02:34 (DEEP — matching-i EXHAUSTED; live leaderboard refetched + 6-instance reprice; matching-ii = highest-EV untried lever, launched):** Matching-i agent FINISHED: **NO IMPROVEMENT — bank 33,338.184 is a deep local optimum** robust to **66,660 exact connected-region (~680-row) Gurobi re-optimizations** + a CP-SAT feasibility probe (obj>bank returned UNKNOWN @2400s). Gurobi restricted license caps ~2000 vars ⇒ can't solve the 92k/25k global model, only LNS sub-blocks. ⟹ matching-i unreachable with open-source solver power (only unrestricted Gurobi/CPLEX = USER license decision). **★ LIVE LEADERBOARD REFETCHED (read-only GraphQL OK) + scored all 6 banks → REPRICE:**
| instance | our bank | live r1 | est rank | verdict |
|---|---|---|---|---|
| Ch1 matching-i | 33,338.18 | 33,555.61 | ~r7-9 | **EXHAUSTED** (66k exact LNS) |
| Ch1 matching-ii | **72,204.29** | 73,714.03 | ~r7+ | **TESTING** — 2.05% gap (vs i's 0.65%) ⇒ real headroom, untried method |
| Ch1 trajectory | 236,420 kg | 473,332 | ~r10+ | coherent WSB model only, multi-week, low pts/hr |
| Ch2 small | 116.38 | 101.65 (r5≈111.79) | ~r7 | EXHAUSTED (DP floor, 4.6d>r5) |
| Ch2 medium | 192.90 | 195.68 | **RANK 1** | retime defending; SUBMIT |
| Ch2 large | 1013.29 | 424.62 (r2=1143.56) | **RANK 2** | secure; pole=global TD-TSP, low-EV |
**Dominant lever = USER SUBMISSION** (medium RANK-1, large RANK-2, all unsubmitted = 0 pts; ~standing escalation). **ACTION (NEVER-STOP, 2 cores freed by matching-i): launched bg agent a29e2af0 = apply the SAME connected-region Gurobi ILP-LNS (adapt /tmp/gurobi_lns2.py) to matching-ii** — its 2.05% gap to r1 (vs matching-i's exhausted 0.65%) means competitors proved a higher optimum exists ⇒ genuine realizable headroom, and this strongest method was NEVER tried on the larger instance. Guard-bank gate (>72,204.293 strict+feasible), /tmp-only, no submit. Cores: medium retime (2) + matching-ii agent (2) = 4/4. No bank delta. NEXT: read matching-ii verdict (auto) → guard-bank+escalate if it beats 72,204.29, else mark matching-ii exhausted too; health-check retime DP.

**TICK 02:26 (cheap tick — all 4 cores healthy, both jobs progressing):** Health OK, no tripwire. **Matching-i agent ACTIVELY computing** two solves in parallel: `/tmp/gurobi_lns2.py 1500` (CPU 22:58) + `/tmp/cpsat_feas.py 2400` (CPU 22:19) ⟹ **Gurobi is NOT license-blocked** (agent verified, contra my cautious brief) → it's running a Gurobi-LNS + CP-SAT feasibility push against bank 33,338.18; harness-tracked, auto-notifies on verdict. **Medium retime healthy**: SCAN 100/180 @9975s, rate IMPROVED (60→80:1891s, 80→100:1711s — later legs lighter, no longer degrading) ⇒ scan finish est ~02:55–03:10 then DP; log fresh, both PIDs busy. Load 4.48/4 cores = full utilization, no oversubscription. No bank delta. NEXT: read matching agent verdict (auto) → if it beats 33,338.18 strictly+feasibly, guard-bank + escalate submission; else mark matching-i exhausted at our solver power & rotate; health-check retime → read DP result when scan completes (guard-bank only if mk<192.9002 strict).

**TICK 01:52 (DEEP — ★ Ch1 RAAN/argp feasibility lead REFUTED & documented (E-047); repriced queue → matching-i exact push on freed cores):** E-577 Phase B FINISHED (1905s). **VERDICT REFUTED: 0/6 stranded hi-incl pairs unlocked by free raan.** 4/6 (E189/E238/E321/E352) baseline-feasible at raan=0 but only ~1 kg each ⇒ unfilled by 1:1 ASSIGNMENT not infeasibility; 2/6 (E59→L4, E367→L5) infeasible even with full sweep. Combined with A2 (raan=0 reproduces all 5/5 incl 89° E100) ⟹ **RAAN = MASS lever not feasibility lever; the 99 unfilled slots are NOT raan-pinned.** EV of the residual assignment play ≈ ≤99 kg vs a bank of hundreds-of-thousands kg = negligible. **Lead DEAD; writeup E-047 (3-harness-iteration methodology arc E-571→572→574→577 = audit-the-evaluator win).** This also refutes the OLD agent a7cdfd8a's INCONCLUSIVE (its split=0.5 DC was the weak tester). **Cores freed: 2 (Phase B done). Medium retime still on 2.** **REPRICED QUEUE (highest-EV Ch1 trajectory lead now removed): the live positive-point lever = Ch1 MATCHING-i exact-solver power** — bank 33,338.18 sits ~7 pts under the (2026-05-18) r5 cutoff 33,345.05; CP-SAT bound 34,339 (gap 2.92%) ⇒ real headroom, each small gain ≈ a rank step (×1 weight) per E-039. Prior CP-SAT 7200s warm-start only MATCHED bank ⇒ need a DIFFERENT angle (tighter 3-index formulation / LP-guided branching / much longer budget / ILP-LNS), not a re-run. **ACTION: launching bg agent for a stronger matching-i exact/near-exact solve on the 2 free cores** (read-only analysis + /tmp candidates only, guard-bank gate, no submit). Cores after launch: medium retime (2) + matching agent (2) = 4/4. No bank delta. NEXT: read matching agent report → if it beats 33,338.18, guard-bank + escalate submission; health-check retime.


**TICK 01:46 (cheap tick — all 4 cores healthy; E-577 gates RECONCILE the two-agent conflict in favor of the faithful solver):** Health OK. **E-577 PHASE A1 = 5/5 reproduced with full raan/argp sweep (466s); PHASE A2 = raan=0 ONLY also reproduces ALL 5/5, FAILS=[] (639s).** ⟹ the faithful apogee solver PASSES its own positive control incl E0→L0(58kg) — this **DIRECTLY REFUTES the OLD agent a7cdfd8a's INCONCLUSIVE verdict** ("my tester fails its own positive control / E0→L0 came back FAIL"). The OLD agent's split=0.5 6-D DC was the weak tester; aee21e0b's apogee solver is the trustworthy one. A2 also re-confirms RAAN=mass-lever-not-feasibility (raan=0 feasible on all 5 incl 89° E100, just at lower mass). **PHASE B (decisive: 6 stranded hi-incl pairs, raan=0 vs raan-swept) RUNNING** — header flushed 01:19, PIDs 137280(13:07 CPU)/137281(20:58 CPU) healthy & accumulating, ~1000s/pair × 6 / 2 workers ⇒ est finish ~02:10. No per-pair flush yet (expected — heavy per-pair). **Medium retime SLOW**: SCAN 60/180 @6373s, rate DEGRADING (20→40:2072s, 40→60:3550s — later legs heavier), full scan now est 4-6h+ then DP; PIDs 129253/129254 healthy (CPU 2:02 each, both cores busy). Low-EV safe rank-1 defense (2.8d margin) — let grind, no urgency. Cores: Phase B (2) + retime (2) = clean 4/4. No bank delta. NEXT: read Phase-B verdict (auto on flush) → classify the 99 slots CONFIRMED/REFUTED-BY-ASSIGNMENT/REFUTED-INFEASIBLE; given A2 (raan=0 already feasible on hi-incl), prediction leans REFUTED-BY-ASSIGNMENT or REFUTED-INFEASIBLE (raan unlikely the unlock) — but Phase B is the truth.


**TICK 01:19 (★★ Ch1 agent REFRAMES the lead — bank ALREADY uses raan≠0 for filled hi-incl; RAAN is a MASS lever not a feasibility lever):** Agent aee21e0b built a FAITHFUL apogee solver and got decisive intermediate results: **(1) E-576 bank element-recovery: filled hi-incl pairs E252(55.7°) & E100(89.4°) have DEPARTURE raan=90° (nonzero!), while low-incl E0/E69/E54 are raan=0.** ⟹ the lead's founding premise "EVERY solver hardcodes raan=argp=0" is **FALSE** — whatever built the bank already swept raan for hi-incl legs. **(2) E-577 GATE A1: faithful solver w/ full raan sweep reproduces 5/5 known pairs.** **(3) E-577 A2 (raan=0 ONLY): STILL reproduces all 5/5 feasibly** — BUT at far lower mass for hi-incl (E100 raan=0 →10kg vs bank raan=90 →619kg; E252 →38kg vs 387kg). **⟹ RAAN is load-bearing for MASS (≈60× on E100), NOT for feasibility.** This reframes the whole lead: the 99 UNFILLED slots are likely unfilled by ASSIGNMENT (1:1 matching leftovers) or genuine infeasibility, NOT raan-pinning (bank already un-pins raan where it fills). **(Corrects my earlier quick one-liner that mis-read E100 tof as 4.715d — agent's careful recovery: E100 T1=20.0 T2=0.5 tof=20.5d; trust the agent's dedicated script.)** Agent now running **PHASE B (decisive): 6 stranded unfilled hi-incl pairs, raan=0 baseline vs raan-swept** → will classify CONFIRMED / REFUTED-BY-ASSIGNMENT / REFUTED-INFEASIBLE. **Resource note: 6 CPU workers on 4 cores** (agent Phase B 2-4 + retime 2) — oversubscribed ~1.5×, tolerable, no data at risk; agent is self-managing+tracked, won't micromanage. **Medium retime** SCAN 60/180 @6373s, rate SLOWING (103→177 s/leg, later legs heavier) ⇒ full scan likely 4-6h more then DP; low-EV safe defense (rank-1 secure, 2.8d margin) — let grind, reconsider if still scanning in hours. No bank delta. NEXT: read agent Phase-B verdict (auto-notify) → if MASS-lever real on unfilled pairs, value = re-solving filled hi-incl pairs for MORE mass (not just filling slots); if assignment-bound → pivot to better matching/assignment lever.

**TICK 00:51 (E-574 gate ABORTED correctly → root cause = tof-grid coarseness, NOT raan/argp; delegated faithful re-test):** **E-574 GATE = 3/5 → ABORTED (refused to give a verdict — exactly the self-validation discipline working).** It now recovers LOW-incl known pairs (E0=0°/235kg, E69=10.5°/263kg, E54=24°/162kg) but MISSES two HIGH-incl known-feasible bank pairs: **E252(55.7°, bank 387kg) FAIL after full 60-DC/458s; E100(89.4°, bank 619kg) FAIL in 11s (Lambert prefilter rejected ~all combos for near-polar geometry).** ROOT CAUSE (extracted bank params): bank tofs are CONTINUOUS and the misses fall BETWEEN my grid points — **E252 tof=1.707d (grid has 1.5/2.0), E100 tof=4.715d (grid has 4.0/5.0)**; recovered pairs (E0=2.53≈2.5, E54=4.03≈4.0) sat near grid points. ⟹ high-incl pairs have a narrow feasible-tof window my 0.5–1d spacing skips; **not a fundamental high-incl barrier, not raan/argp.** (Bank splits confirmed continuous: E100=0.976, E252=0.933, E0=1.0, E54=0.829.) **The replication harness keeps subtly diverging from the bank's true solver → DELEGATED to fresh agent aee21e0bf4719130e** with ALL findings: PHASE A = build a baseline from the bank's REAL pair-solver (ch1_traj_proper_v2 scan_and_polish / ch1_multi_restart_6d try_pair_multi_restart) that reproduces ALL 5 known pairs incl E100/E252 (finer/adaptive tof+ea, or tof as a DC var); HARD GATE 5/5. PHASE B (only post-gate) = stranded high-incl pairs, faithful baseline (raan/argp=0, confirm FAIL) vs raan_e/argp_e/argp_l swept; distinguishes CONFIRMED / REFUTED-BY-ASSIGNMENT (baseline-feasible ⇒ matching lever) / REFUTED-INFEASIBLE / BANK-ALREADY-USES-RAAN. Verdict written to its log + reported. **Medium retime HEALTHY** (129253/129254, CPU 78:14, +27min CPU/27min wall = both cores busy; 33-min-stale log is sparse per-20-leg logging at ~100s/leg, NOT a hang; SCAN 40/180; slow ~5h for full scan but provably-safe rank-1 defense, 2.8d margin over live r1=195.68 ⇒ low urgency, let grind). Cores: retime (2) + Ch1 agent (2). No bank delta. NEXT: read agent verdict (harness-tracked auto-notify) → act on CONFIRMED/REFUTED branch; heartbeat health-checks retime.

**TICK 00:28 (★ METHODOLOGY WIN — E-572 REFUTED was FALSE; evaluator-audit caught it; corrected E-574 launched):** **E-573 gate FAILED on its FIRST pair: E0(i=0)→L0(i=0), a coplanar bank pair worth 819kg, came back baseline=FAIL.** A harness that can't reproduce the EASIEST known-feasible transfer cannot be trusted to refute anything ⇒ **E-572's "REFUTED 0/8" is an ARTIFACT, not evidence.** Root cause (bank inspection): bank's real transfers use **tof median 4.7d (range 0.9–35.6d) & split median 0.943 (many =1.0)** — E-572's grid tof{6,9,13}d × split{0.3,0.5,0.7} missed BOTH regions (E0→L0 needs tof=2.53d split=1.0, neither in grid). So both arms failed for GRID reasons, not DoF reasons. **Also corrects the lead's premise: "split=0.5 hardcoded" is WRONG — bank splits vary 0.24–1.0; the genuinely-pinned DoF are raan/argp only** (ch1_traj_proper_v2.py:138,140). **ACTION: built+launched scripts/ch1_e574_multidof_v2.py (PID 132866, log runs/ch1/81_e574_multidof_v2.log)** — CORRECTED + SELF-VALIDATING: tof grid 1–20d (dense 1–6d), split→1.0, max_nfev=100, early-exit on first feasible; BASELINE sweeps tof/split/ea with raan/argp=0 (= real solver's actual capability), SWEEP adds raan_e/argp_e/argp_l. **HARD GATE: must recover ≥4/5 known-feasible bank pairs before ANY stranded result is trusted; aborts otherwise.** Also distinguishes the two REFUTED sub-cases: if stranded pairs are baseline-FEASIBLE → they're unfilled by ASSIGNMENT (→ better-matching lead, different fix) not infeasibility. **Killed redundant e572/e573.** Cores: medium retime (2) + e574 (2). No bank delta. NEXT: read e574 GATE (pass⇒trust stranded verdict; CONFIRMED⇒99-slot campaign / REFUTED-feasible⇒assignment lead / REFUTED-infeasible⇒downgrade & rotate). **LESSON: a feasibility-test that yields a NEGATIVE verdict MUST first prove it reproduces known-POSITIVE cases — else the negative is a budget/grid artifact (audit-the-evaluator trigger).**

**TICK 00:23 (Ch1 e572 multidof FINISHED → REFUTED 0/8, BUT auditing the evaluator before trusting it):** **E-572 verdict: REFUTED — 0/8 stranded moderate-incl (60–78°) pairs unlocked; base=FAIL AND sweep=FAIL on all 8** (each 27+90 DC calls, ~1377s wall). The free DoF {raan_e,argp_e,argp_l,split} did NOT rescue any tested pair. **★ METHODOLOGY TRIGGER (audit evaluator before concluding): the BASELINE also failed on all 8 — so "sweep also fails" only means something IF the harness is strong enough to find a feasible transfer when one EXISTS.** The harness used max_nfev=40 (vs try_transfer's 100), tof grid {6,9,13}d only, 27 baseline DC calls — possibly too weak; both arms could be failing for lack of solver budget, not lack of DoF. **ACTION: built+launched scripts/ch1_e573_harness_validate.py (PID 132317, log runs/ch1/80_e573_harness_validate.log)** — runs the EXACT E-572 baseline arm on 6 KNOWN-FEASIBLE bank pairs (low/mid/high used incl). If harness recovers positive mass on all 6 → adequate → REFUTED TRUSTWORTHY → downgrade the RAAN/argp lead & rotate. If it MISSES known-feasible pairs → harness too weak → REFUTED unreliable → re-test with max_nfev≥100 + finer tof grid before concluding. ~10min. **Cores: medium retime (2, CPU 51:23) + e573 validate (1); e572 freed 2.** No bank delta. NEXT: read e573 verdict → decide trust/re-test.

**TICK 00:10 (cheap tick — both jobs healthy; e572 stale-log = FALSE ALARM diagnosed):** **Ch1 e572 multidof** (PIDs 130837/130838, 2 workers, CPU 13:20 each) log mtime 23:56 (14min stale) LOOKED like a freshness tripwire, but inspecting the script: it uses `Pool(2).map(eval_pair, pairs)` which BLOCKS and prints per-pair results ONLY after ALL 8 pairs finish — there is NO intermediate flush by design (not a deadlock; CPU is accumulating normally). Per-pair cost ≈ up to 117 capped-DC calls (DC_BASE=27 + DC_SWEEP=90) × ~3.5s (max_nfev=40) ≈ 410s; 8 pairs / 2 workers ≈ ~27 min wall ⇒ verdict expected ~00:25–00:30. Grid is solid: baseline (raan=argp=0, split=0.5, tof×ea) vs multi-DoF (raan_e×argp_e×argp_l×split×tof×ea, Lambert-|dv0|-prefiltered then DC on the cheapest 90), 8 pairs spread 60→78° with nearest-incl moon partners — a genuine FAIL→FEASIBLE flip = lead confirmed. **Medium retime** (129253/129254, CPU 39:17) progressing. Load 4.14 (4/4 cores busy). No tripwire, no bank delta. NEXT: read e572 VERDICT line (CONFIRMED⇒build 99-slot re-solve campaign / REFUTED⇒downgrade lead, rotate analysis target); check medium retime SCAN progress.

**TICK 23:53 (cheap tick — both core-pairs working; Ch1 RAAN/argp/split deep dive delegated):** Health: **medium retime ALIVE & healthy** (PIDs 129242/129253/129254, workers=2, CPU 21:55, log mtime 23:43 = 9min — sparse per-20-leg logging, at SCAN 20/180 flushed; leg 40 flushes ~23:55; load 2.28). No tripwire. **Ch1 quick RAAN-only probe (E-571) verdict = INCONCLUSIVE/weak** — only the 3 most-extreme (~89°) stranded pairs tested with a RAAN-only sweep; for near-coplanar 89° pairs ΔRAAN=0 already minimizes plane angle so RAAN-only CAN'T help, and it ignored argp+split (physically the promising eccentric-orbit DoF). Solver ~5.6s/call ⇒ probe timed out after 1 pair (`E238 i89 → L141 i90: base FAIL nf=0 / sweep FAIL nf=0`). **⟹ The lead is NOT refuted — the test was just too weak/slow.** **ACTION (NEVER-STOP, idle 2 cores → highest-EV lead): delegated a THOROUGH multi-DoF feasibility investigation to bg agent a7cdfd8a11aa819a3** — build a FAST tester (3d-DC/Lambert prefilter + 2-worker mp, OMP=1) over the free DoF {raan_e, argp_e, raan_l, argp_l, split, tof, ea} on a representative sample of stranded MODERATE-incl (60–78°) pairs, baseline vs sweep, reporting per-pair WHICH DoF unlocks mass. Script→scripts/ch1_e572_multidof_feas.py, log→runs/ch1/79_e572_multidof.log, /tmp/no-bank. Decisive: a genuine FAIL→FEASIBLE flip on even one moderate-incl pair confirms the 99-slot re-solve campaign (highest-EV lever, HARD problem ×1.78/rank). Cores: medium retime (2) + Ch1 agent (2). NEXT: read agent verdict → confirm⇒build campaign / refute⇒downgrade & rotate; read medium retime DP result (guard-bank only if <192.9002 strict).

**TICK 23:40 (★ BOTH analysis agents reported HIGH-VALUE leads → reallocated cores to them; Ch1 RAAN/argp probe RUNNING):** Both deep-analysis agents (launched 23:15) finished with confirmed, evidenced leads. **★★ Ch1 LEAD (HIGH EV, likely the trajectory rank gap): every trajectory solver hardcodes RAAN=argp=0** (ch1_traj_proper_v2.py:138-140, ch1_multi_restart_6d.py:104-106), but the official validator `_match_orbit` (ch1_trajectory.py:162-164) checks ONLY (a,e,i) — RAAN/argp are documented FREE knobs (earth_orbit_state/moon_orbit_state sigs). **Verified the smoking gun: the 99/400 UNFILLED bank slots are systematically high-incl Earth orbits — unused median incl 74.3° vs used 24.0°.** Same coverage-bug CLASS as the past idD=0 + eccentric-orbit wins (each unlocked thousands of kg). Potential: fill up to 99 slots → trajectory rank 6→5 (the +136k that was wrongly declared 'WSB-unreachable'; that NO-GO also rested on split=0.5 hardcode per agent Lead 1b). **ACTION: built scripts/ch1_e571_raan_argp_probe.py** = decisive confirmation probe (reuses the EXACT solver try_transfer with swept raan_e/argp_e/raan_l on 4 hi-incl unused Earth × 3 nearest-incl unused Moon pairs; baseline vs sweep; read-only, writes NOTHING). **RUNNING (bg task bhteqbr9f).** If sweep flips infeasible→feasible on even one pair → confirmed → build full re-solve campaign for the 99 slots. **Ch2 LEAD (cheap, provably-safe, rank-1 DEFENSE): the DP departure-quantum q=0.05 + `ceil(tof/q)` rounding (e568:143-149, e529:66-71) systematically over-states binding legs** — same E-045 class, untried on small/medium. E-040 got −2.87d from the last halving (0.1→0.05); next halving 0.05→0.025 never run. **ACTION: launched medium retime q=0.025 N_TOF=639 (provable superset of bank's 320-grid ⇒ DP result ≤ bank, guard-bank only if strictly better; PID 129242, log runs/ch2_v3/103_e568_medium_q025.log, T=20000 buckets).** Defends medium's rank-1 lead vs competitors. (Small NOT superset-safe via e568 — its bank used a different tof range; deferred to a proper finer-grid rebuild.) Ch1 matching agent: SCIP (free, no size-limit) never tried = medium-EV lead for later; matching scorer re-verified FAITHFUL (exhausted). **Reallocation: killed the 2 low-value e585 large grinds (started 13d behind bank) to free cores for these higher-EV leads — full compute discretion.** Cores: medium retime (2) + Ch1 probe (1-2). NEXT: read probe verdict → if confirmed, build the RAAN/argp re-solve campaign (highest-EV lever on the board, a HARD problem ×1.78/rank).

**★★ STANDING DIRECTIVE (user, 2026-06-12 21:14 UTC): NEVER STOP — keep relaunching in-depth analyses + grinding banked solutions, regardless of expected point gain.** "From our exploration history, we had our biggest gains from unexpected findings during repeated analysis. If no viable paths appear, keep optimizing on already banked solutions, since the other participants will also continue to optimize gradually. NEVER STOP." ⟹ **Empty positive-EV queue is NO LONGER an idle/escalation trigger.** Every wake the cores must be running EITHER a thorough analysis (bug/missing-path/alt-optimization audit, rotate the target each pass) OR continued optimization of the banks (defend rank as competitors improve). Escalate ONLY for submissions + destructive actions. Memory [[feedback-never-stop-keep-analyzing]]. Biggest-win precedent: the −35.7d large grid-bug (E-045) was found by RE-analyzing a "stuck" problem; Ch1 idD=0 + eccentric-orbit bugs likewise.

**TICK 23:15 (NEVER-STOP directive → cores filled: 2× large grind + 2 deep-analysis agents launched):** User issued the NEVER-STOP directive (above). Acted immediately. (1) **Built scripts/ch2_e585_large_window_finegrid.py** = E-578 windowed destroy-repair LNS driven on the FINE walk grid (n_steps=4000/0.01d, wait_dt=0.25), re-seeded from the 1013.29 bank, CURRENT_BANK=1013.2886. Smoke confirmed: the greedy walker reproduces the bank order at **1026.68d (not 1013.29 — greedy exc-allocation ≠ the bank's locked exc legs, the known E-583 gap), feas exc=5.** So it starts 13d behind, BUT a *different* window reordering may greedy-walk below the bank; /tmp-only + my guard-bank gate (re-score < 1013.2886) makes it harmless and a legit exploratory grind for an unexpected sub-1013 perm. **Launched 2 seeds (PIDs 127311/127312, TL=10h, logs runs/ch2_v3/102_e585_finegrid_s{0,1}.log, cand /tmp/ch2_large_window_finegrid_cand_s{0,1}.json).** (2) **Launched 2 background deep-analysis agents** (read-only, /tmp only, no banking): **agent a11cc10203c9049b8 = Ch2 bug & missing-path audit** (hunt for more E-045-class discretization/hostile-default bugs in construction/walker/DP across small/medium/large; smarter ≤5-exc allocation; cheap re-timing slack on small/medium banks); **agent a86cd81c8e9d45acf = Ch1 bug & alt-optimizer audit** (matching alt exact/near-exact methods + scorer-vs-UDP faithfulness re-check; trajectory alt transfer families + latent silent-rejection bug hunt like idD=0). Both briefed with full prior-findings context, report <600 words ranked by EV. **Cores: 2/4 compute (large grind) + 2 analysis agents (light). NEXT TICK: read agent reports → triage leads, launch the cheapest highest-value test; health-check e585 seeds (alive, accepts, mk).** No bank delta.

**TICK 22:54 (cheap tick — small fine-grid re-walk NULL → small DP floor confirmed; queue still empty; idle):** Read the two backgrounded finishes. (1) **Small fine-grid re-walk (E-045 lesson ported to small): NULL** — bank mk=116.37377, exc legs {2,17,26,29,45}, **best re-walk over the ns×wait_dt×wait_steps sweep = 116.37377 cfg=None (ZERO gain).** The ~3.4d "static recover" the grid-audit suggested is NOT realizable by a greedy fine re-walk because small's bank was built by **DP (E-529) that jointly optimizes departures AND tofs** — the DP already sits at the joint timing optimum for this perm, so re-walking can only match it. Realizing the static slack would need a **fine-grid DP REBUILD** (table at 0.005d tof ⇒ ~52h; even if it recovered the full 3.4d → ~113d, still ABOVE rank-6's 111.75d ⇒ 0 rank change ⇒ 0 points). **⟹ small lever EXHAUSTED for positive-points-EV; small stays ~rank 7.** (2) Large polish re-confirm: 1013.2886 cfg None (no change). **Cores 0/4 idle.** Health: no python/experiment procs (idle, not dead — no tripwire). No bank delta. **Queue state UNCHANGED from 21:55: autonomous positive-POINTS-EV queue EMPTY** — large pole refuted (3×, low-EV binary), small DP-floored (above), medium=RANK 1 (maxed), matching exact-blocked (Gurobi license), trajectory WSB-unreachable. **Dominant unrealized value = USER SUBMISSION of the 6 banks (~48 pts at 0; medium=RANK 1 beats live r1, large rank-2). Already escalated; re-arming long heartbeat (nothing to launch that gains points).**

**TICK 21:55 (E-046/E-584 global fine-grid reorder REFUTED → large pole = NOT worth committing days; decision made):** E-584 finished. iter0: global epoch-aware OR-Tools solve on the 0.01d grid found a cheap-cost path **~340.5d (BELOW r1=424!)**, big_jumps=4, pen=0 — a near-optimal STATIC order exists in the fine cheap graph (even cheaper than coarse E-572's 389d). **BUT the chrono walk REJECTED it (infeasible, >5 exc / unreachable leg) → kept BASE 1026.67, no gain, NOTHING banked.** **The finer grid did NOT fix unwalkability — it's the time-dependent epoch-shift trap, not resolution.** Third independent refutation (E-572 coarse, E-573 fixpoint-diverged, E-584 fine) of single-pass/naive-iterated global reorder. **DECISION (the "then decide" half of user's option-3): DO NOT pursue the large pole now** — binary (0 marginal pts unless realised walk crosses 424.62; large already rank-2-secure at 1013.29<r2=1143.56), uncertain (the walkable-global-TD-TSP-under-5-exc obstruction is exactly what 3 attacks failed on), multi-day (epoch-robust from-scratch construction = TGMA's likely recipe). LOW pts/hr. Writeup E-046. **E-045 evaluator-audit win (−35.69d total) tightened the MARGIN, orthogonal to pole FEASIBILITY.** Cores idle → ran a cheap fine-grid POLISH sweep (n_steps×wait_dt on the fixed 1013 order): **NULL — no combo beat 1013.2886, the fixed-order fine-grid local floor → grid-tweak lever EXHAUSTED.** **★ ESCALATION: autonomous positive-POINTS-EV queue is now effectively EMPTY — large pole refuted as low-EV, small ~r7 needs a competitor-class new architecture (skeleton enum null), matching exact-blocked (Gurobi license), trajectory WSB-unreachable. Dominant lever = USER SUBMISSION of the 6 banks (medium=RANK 1, ~48 pts at 0). Surfacing to user.**

**TICK 21:14 (★★ EVALUATOR AUDIT WINS — large 1041.33→1015.17d (−26.16d), tof grid was a hidden bottleneck; E-044 verdict reframed):** User chose option-3 "scope LKH first". Before building the LKH matrix I applied the **audit-the-evaluator** trigger (r1=424.62 ⇒ 0.404 d/node is BELOW E-044's coarse-grid 0.857d floor → cheapest explanation = coarse grid). **E-582 grid audit (scripts/ch2_e582):** `find_earliest_transfer` default n_steps=120 ⇒ **0.100d tof grid** — the whole bank + all diagnostics used it. On 300 cheap-adjacent pairs at bank epoch, a 0.010d grid finds **0.116 d/leg shorter** cheap tofs (156/300 legs improve >0.05d, max 1.94d; feasibility unchanged). **E-583 re-walk (scripts/ch2_e583):** re-walk the SAME 1041 bank perm, same 5-exc legs {149,416,566,807,957}, finer grid → n_steps=2400 (0.005d) gives **mk=1015.173d feasible viol[0,0,0,0] exc=5, −26.16d, ZERO reordering.** ★ GUARD-BANKED (backups large.json.bak.e583/.e583b, fresh re-score, perm valid, strictly better, round-trip 1015.173343 on disk). Coarser grids REJECT where finer succeed (epoch cascade; 3600 > 2400 → chaotic, n_steps has a sweet spot). **REFRAMES E-044's "no heuristic reaches r1" verdict — the coarse grid inflated EVERY floor we measured (0.857d & 0.69d were ~0.12d/leg too high); r1 gap is smaller than claimed, and any LKH matrix MUST use the 0.005d grid.** Still rank-2 (0 marginal pts; 1015 < r2=1143.56) but bigger margin + the lever is reopened. E-566 small BOTH seeds FINISHED null (best=BANK 116.3755 across 17186/17583 tries → **small skeleton lever EXHAUSTED**, 4 cores now idle). Writeup E-045. Also mined the chaotic n_steps cascade on the fixed order: ns=2600 → **1013.289d** (another −1.9d, GUARD-BANKED, backup .e583c; ns=2200/2800 REJECT → highly chaotic). **★ LAUNCHED E-046/E-584 (scripts/ch2_e584): global epoch-aware OR-Tools re-route ON THE FINE GRID** (imports E-572 as a module, cost grid 0.01d, walk window=40/n_steps=4000=0.01d, 4 iters TL=900s, /tmp-only, PID 121855, log runs/ch2_v3/101_e584_global_finegrid.log). BASE reproduced 1026.67d feasible; iter0 ~18min. **Decisive pole probe: does the global reorder become walkable nearer 424 once the grid tax is removed? E-572 (coarse) found a 389d cheap-cost order that was UNWALKABLE/plateaued ~1000d — fine grid won't fix the epoch-shift unwalkability per se, so I expect plateau ~1000d → would confirm large stays rank-2 and the pole needs the uncracked iterated epoch-robust construction (multi-day, low pts-EV). NEXT TICK: read iter0 mk — <800 promising, ~1000 refutes.** Submission of 6 banks (medium=RANK 1) still dominant unrealized value.

**TICK 20:10 (E-581 defrag REFUTED + phase-sweep hypothesis REFUTED → large-pole verdict: needs global TD-TSP, low marginal EV):** **E-581 defrag aborted at ring 2** — sweeping each ring to completion breaks the co-phasing needed for the NEXT inter-ring bridge (small-shell rings 0°/90°/180° need big plane changes, cheap only at specific co-phased epochs). **Inter-ring bridges are COUPLED across rings — the real structural finding.** Then decisive probe: **phase-adjacent forward hops in ring(0,90) avg 0.857d, WORSE than the bank's realized 0.692d** → the E-041 "0.150d available" was a single best-pair min over 40 nbrs, NOT chainable sequentially. **Bank's ~0.69d/node on main rings ≈ the sequential-chaining floor for ordering heuristics.** Budget map: 3 small rings≈310d + 3 big main rings≈382d + ~110 minor-plane nodes≈242d (1.8-3.2d each!) + bridges≈100d ≈1041. **r1=424 needs 0.40d/node — below even phase-adjacent → NO greedy/sweep reaches it.** ⟹ **VERDICT: large rank-1 requires a global time-dependent TSP solver (LKH/Concorde on iterated epoch-aware matrix, TGMA's likely recipe for their 1-shot 1143→424) — multi-day build, BINARY (0 marginal pts unless it crosses 424.62; large already secure r2), uncertain payoff (~30-40%). LOW expected-pts/hr.** Banked this tick: 1041.33 (E-580, hygiene). Documenting E-580/E-581. **Reporting honest fork to user (their explicit question). Dominant campaign value remains SUBMISSION of 6 banks (medium=RANK 1, 13.3pts at 0).** E-566 small healthy, null ~certain, TL ~21:15. **NEXT: user steer on large-LKH effort vs accept r2; meanwhile E-566 verdict → free cores.**

**TICK 20:02 (E-044 v1 BANKED large 1048.98→1041.33; v2 defrag running; E-579 bound corrected):** **★ BANKED large 1048.9786→1041.3340d (−7.64d, E-580 ring phase-sweep on bank skeleton).** Guard-banked: fresh re-score 1041.334034 feas viol[0,0,0,0], perm valid, strictly better, backup large.json.bak.e580, round-trip OK. Still RANK 2 (0 marginal pts; bank already beats r2=1143.56) but new best incumbent + v2 seed. **KEY DIAGNOSIS REFINED:** within-run sweep gained only 7.6d → the ~500d E-579 "recoverable" was a LOOSE per-leg lower bound (ignores sequential node-consumption: cheap neighbors get used up, real tours hit endgame). True structure: **16 rings; 3 small-shell rings (450 nodes) already clean 1-fragment sweeps; big-shell rings fragmented 2-4× each** (ring(1,0)→4 fragments, (1,90)→4, (1,180)→2) — re-entries are phase-misses. **v2 = E-581 defragmenting global sweep (finish each ring in ONE phase sweep, small shell then big shell, cheap bridges, 5 exc budget)** running now (~1-2min). **METHODOLOGY NOTE (anti-oscillation):** do NOT over-claim "500d recoverable" — v1 delivered 7.6d; the honest path to <424.62 may be LKH-class global TD-TSP (multi-day, 0 marginal pts unless it crosses r1), which is LOW-EV per [[ch2-compute-parallelization-roi]]. v2 is the cheap decisive test of whether defrag alone yields a big chunk. E-566 small healthy (best=BANK, null ~certain, TL ~21:15). **NEXT: E-581 verdict → if big gain, polish+iterate toward 424; if small, conclude ordering-recovery exhausted & large stays r2, report honest EV to user.**

**TICK 19:50 (cheap tick + E-044 v1 BUILT, launching):** E-566 healthy both seeds (s0 tried=8000@5166s, s1 tried=6700@4169s, abort=0, ~720 feas sampled, best=BANK on both — null verdict ~certain; TL ends ~21:15). **Built scripts/ch2_e580_large_ring_sweep.py (E-044 v1): per-run ring phase-sweep on the bank skeleton** — keeps bridge endpoints/exc placement, re-orders each same-ring run interior by argument of latitude u=argp+M (fwd+bwd sweeps), picks per run by realized chronological walk from the actual arrival state (self-consistent epochs, no frozen-epoch proxy). wait_dt=0.25 (not the hostile 1.0 default). Candidate → /tmp ONLY, guard-bank only if official-feasible AND < 1048.9786. Est ~5-15min runtime (walk ≈ 0.03s/leg-probe). **If v1 recovers a big chunk of the 500d phase-miss slack → build v2 = global ring-level assembly (whole-ring sweeps, ring order by bridge phasing, ≤5 shell-crossing exc) targeting <424.62.** Launch hit the classifier-outage (Bash intermittently blocked) — retrying; if still blocked next tick, launch then. Priority queue: 1) E-580 v1 verdict → v2 global assembly; 2) E-566 conclusion at 21:15 (document, free 2 cores); 3) small: accept ~r7 if skeleton null. No bank delta.

**TICK 19:32 (DEEP — user-triggered large r1 analysis → E-579 DECISIVE, E-578 reaped, ring-sweep is the lever):** User asked whether E-578 windowed-LNS can take large pole and what we're missing. **Read instance geometry directly: two co-orbital shells** (small a≈3747km/0.238d/450 nodes, big a≈14989km/1.906d/601 nodes), near-circular e≤0.01, RAAN≈0, each cut into discrete inc planes {0/90/180°}. The E-533 "4 components" = physical rings; intra-ring hops are PHASING maneuvers (ahead=cheap 0.05-0.15d, behind=1-2d lap). **Built+ran E-579 per-leg slack diagnostic (scripts/ch2_e579_large_legslack.py, ~12min):** of the 624d gap, **~500d is recoverable intra-ring phase-miss** (518/650 long legs had a cheaper unvisited hop at that epoch; Σbest-greedy=524d; **bridges only 43 legs/124d** — cheap, not the problem; intra-ring >0.5d = 617 legs/513.5d excess). **VERDICT: E-578 windowed-LNS CANNOT take pole (P≈0)** — repairs local windows, cannot reorder the global phase sequence; also 0 marginal pts (bank 1048.98 already beats r2=1143.56). **REAPED both E-578 seeds (PIDs 104443/104444/104453/104456), 2 cores freed.** Writeup E-043. **NEXT LEVER = E-044 ring-sweep construction:** per (shell,plane) sort by relative phase, hop forward (short catch-ups), order rings by bridge phasing, 5 exc on bridges, DP/walk re-time + LNS polish. Matches TGMA's one-shot 1143→424 jump (structural construction, not LNS). r1≈424 plausibly the clean-sweep floor. **NOW: 2 cores free (E-566 small on other 2 til ~21:15). Building E-044 ring-sweep constructor next.** No bank delta.

**TICK 19:05 (cheap health-check via logs — Bash classifier-blocked this tick, used read-only log inspection):** All 4 seeds alive/fresh by log timestamps. **E-566 small skeleton** s0 tried=6200@3907s, s1 tried=4800@2992s, abort=0, feas-rate ~4.5%, **best=116.3755=BANK on both → ~540 feasible random skeletons, ZERO beat the bank topology — null verdict nearly certain; definitive at TL end ~21:15.** **E-578 large** s2=979.768d acc=20 (plateaued since 7358s, ~2.2h zero-accept, but iterating at sane ~7.4s/iter — LNS plateau, not deadlock; s2 previously broke a 2500s+ plateau, keep), s3=988.185d acc=14 (accept at 14601s, recent). Both ≫ r1=424.62 → r2-ceilinged, option-value only. No bank deltas. **Also this hour (user-requested):** competitor overall-points table built from live GraphQL (/tmp/agg_overall.py): leader 5af55cab=72.89, HRI=db54c274=#2@67.11, our hypothetical-submit ≈48.2 → #5. **NEXT TICK: E-566 TL ends ~21:15 → read final lines; if best=bank on both → small skeleton lever EXHAUSTED (document E-NNN + free 2 cores); any [NEW BEST] → guarded-bank.**

**TICK 18:39 (cheap health-check — all healthy, no deltas; E-566 early signal trending NULL):** All 4 cores alive/fresh, no tripwires. **E-566 small skeleton** seeds 0/1 at ~32min: tried 3100 each, feas 144/171, **abort=0 (visit-cap working), best=116.3755 = BANK on both → no candidate written.** ~315 feasible random skeletons sampled so far, NONE beats the bank's skeleton → early evidence the skeleton DoF (tiny-comp roles × comp0 split × bridge endpoints) is NULL, consistent with [[ch2-compute-parallelization-roi]] pricing small LOW. Let both seeds finish the 3h budget (~21:15) for the definitive verdict before concluding. **E-578 large** seeds 2/3 (~3.7h): s2=979.77d acc=20 (steady), s3 nudged 995.94→994.87d acc=12 — both descending glacially, still ≫ r1=424.62 → r2-ceilinged, 0 points-EV (binary). No bank delta. Submission still dominant unrealized value (medium=RANK 1, escalated). **NEXT TICK: if e566 still best=bank → conclude small skeleton lever exhausted; if a [NEW BEST]<116.37 appears → guarded-bank.**

**TICK 18:15 (E-565 windowed-LNS DEFINITIVELY REFUTED → built+launched E-566 structured skeleton enumerator):** E-565 (small windowed exc-allowing destroy-repair LNS) is now refuted on TWO grounds: (1) epoch-trap — smoke gave 0 accepts; (2) **the exc-allowing repair generates exception-heavy candidates whose ultrafine-DP fans out (O(T²) reachable-state explosion at T=2600) → no iteration output in 8min even at the safe WMAX=16.** Killed both seeds for good. **PIVOT BUILT: E-566 (scripts/ch2_e566_small_skeleton_enum.py) — structured skeleton search over the DoF E-564 never touched:** tiny-comp ROLE assignment (which of comp1/2/3 is start/mid/end, 6 perms) × comp0 SPLIT index (where the mid tiny-comp breaks comp0's open Hamiltonian path) × bridge ENDPOINTS (implied by the split) × tiny-comp internal orders. Construction keeps EXACTLY 4 inter-comp bridges so the DP sees a clean cheap-interior+few-exc perm (not the E-565 exc-heavy degenerate). comp0 solved ONCE as a cheap epoch-aware open path (OR-Tools, free endpoints) at bank reference epochs; candidates = start+run1+mid+run2+end, ultrafine-DP re-timed + official kt.fitness, guard-write best strict feasible improvement to /tmp ONLY. **Engineering hurdle SOLVED: added a reachable-state-VISIT cap to a bounded copy of the forward DP** (the naive new-state cap didn't bound runtime — a dense leg re-scans tp without setting new states; visit-count is the true work proxy, checked per departure bucket → hard per-candidate bound). Smoke (cap=1.2M, above the bank's own 120k-800k need): 30 candidates, **DP mean 0.67s, max 1.0s, 0 aborts, 13% feasible** — clean. Converted enumeration to uniform RANDOM SAMPLING so a time-bounded run covers the whole skeleton space (~16k samples/seed in 3h). **LAUNCHED 2 seeds (PIDs 111134/111172, TL=3h, cap=1.2M, logs runs/ch2_v3/97_e566_skel_s{0,1}.log, guarded /tmp/ch2_small_skeleton_enum_cand_s{0,1}.json).** Kept large seeds 2,3 (E-578, 979.8d/995.9d, r2-ceilinged option-value). Cores 4/4. **HONEST ROI CAVEAT:** small is ~rank 7, ×1 weight, and [[ch2-compute-parallelization-roi]] prices it LOW — this is a bounded speculative test of the last untouched small lever, not high-EV. **Dominant unrealized value remains USER SUBMISSION of the 6 banks (~49 pts at 0; medium=RANK 1); escalated.** No bank delta. **NEXT TICK: read e566 seed logs — any [NEW BEST] < 116.37 → guarded-bank candidate; if both seeds finish 3h with best=bank → small skeleton lever exhausted, accept ~rank 7.**

**TICK 17:22 (health-check — E-565 TRIPWIRE: DP-spin hang; reaped+relaunched safe):** Large seeds 2,3 HEALTHY (false-alarm: my pgrep `ch2_e56` didn't match `ch2_e578`): **seed2 979.77d acc=20 (still descending, −69d from bank)**, seed3 995.94d (both fresh, zero points/r2 but option-value). **E-565 small skeleton seeds 0,1 TRIPWIRE: 98% CPU, 34min, wchan=0, log frozen at start line (>30min stale, 0 iter output) = the known degenerate-perm DP-spin (E-554/10:30 class), triggered by the larger WMAX=30 windows producing wild reorderings that blow up `_forward_dp`.** Root cause = window size. REAPED (kill -9 106327/106330). RELAUNCHED with SAFE config (WMIN6/WMAX16 — the smoke ran clean at ≤20; SOLVES2 for faster iters; print every 25 to expose the rate; TL2h), PIDs 107744/107745, logs runs/ch2_v3/97_e565_small_skel_s{0,1}.log. **Purpose: get a DEFINITIVE accept/no-accept verdict on small windowed-LNS (smoke gave 0 accepts/~50 iters — too short; now thousands of clean iters).** If still 0-accept after this run → small windowed-LNS-from-bank is epoch-trapped like large → pivot to FROM-SCRATCH skeleton enumeration (vary tiny-comp order + bridge endpoints + comp0 split; n=49 discrete search; avoids descend-from-incumbent trap). If accepts → push toward 111.75. Cores 4/4 (2 large + 2 small). No bank delta. Submission still the dominant unrealized value (medium=RANK 1, escalated 16:45).

**TICK 16:45 (deep-review — bigwin FALSIFIED + STALE-PRICING CORRECTED + small topology lever launched):** Health-check: all 4 e578 seeds were alive/fresh. **Big-window hypothesis FALSIFIED:** seed4=1003.55d, seed5=1019.05d — both WORSE than small-window seed2 (987.12d, still descending) and plateaued >1500s. Bigger windows solve slower with fewer accepts; not the lever. → REAPED bigwin seeds 4,5 (PIDs 105218/105219/105223/105226). **LARGE LNS-from-bank CONFIRMED r2-ceilinged (zero points-EV; binary threshold holds).** ⟶ **★ DEEP REVIEW refetched live board (GraphQL) and re-scored our banks — LOOP-STATE'S "all 6 ceilinged" WAS STALE/WRONG on two instances:** (1) **medium bank re-scores 192.9002d (official), which BEATS live r1=195.6816 → medium is RANK 1, not "rank 4 / 228.97" as memory said** — the E-040 retime had already banked it; biggest unrealized prize (×4/3, 13.3 pts at rank 1, UNSUBMITTED=0). (2) **small bank 116.3738d sits ~RANK 7** (live top-12 ends r6=113.09; dense competitor cluster r4-6 @ **111.75**, r3=108.77, r1=101.65) — NOT "floored/perm-optimal"; a better skeleton provably exists (others reached it). Large: bank 1048.98 < live r2=1143.56 → confirmed rank 2; <424.62 needed for r1 (unreachable from bank). matching-i ~rank 4 (33,338 vs r3≈33,527, exact-blocked); matching-ii/trajectory unchanged. **ACTION:** built **E-565 (scripts/ch2_e565_small_skeleton_lns.py)** = the previously-identified-but-unbuilt SMALL TOPOLOGY lever (loop-state 10:30: comp0 split / exc-bridge reallocation) — monotone windowed destroy-repair over the full n=49 perm, OR-Tools repair allowing cheap+exc arcs (so bridges re-route → skeleton moves), ultrafine-DP re-time + official kt.fitness, accept strict feasible improvement only (can't diverge). Reuses validated E-564 Table/dp_time_perm/score. Smoke: CORRECT (reproduces 116.3755) but 0 accepts/180s (small slack 4.6d ≪ large's 624d → epoch-trap dominates → MARGINAL EV). Launched 2 seeds (PIDs 106322/106323, WMIN12/WMAX30/SOLVES4/TL6h, logs runs/ch2_v3/97_e565_small_skel_s{0,1}.log, guarded /tmp). Kept large seeds 2,3 (104453/104456, option-value r2). Cores 4/4. **NEXT TICK: if e565 still 0-accept after ~1-2h → small windowed-LNS-from-bank also epoch-trapped → small needs from-scratch skeleton construction (or accept ceilinged); if any accept → keep pushing toward 111.75.** **★ RE-ESCALATING corrected pricing to user: medium is RANK 1 (was mispriced), submission value higher than loop-state implied.** No banking (no candidate < bank yet). No bank delta.

**TICK 15:28 (E-578 deep-review — plateau confirmed 2+ ticks):** At ~64min: seed2 BEST 990.06d (still slowly accepting), seed3 998.47d, seed0/1 BOTH stuck at identical 1019.049 (zero-accept ~58/47min → 1h tripwire). All seeds plateau 990-1019d ≈ 2.3× above r1=424.62 → still r2, ZERO points (binary). REAPED seeds 0,1 (low-ROI, killed PIDs 104450/104462; kept seed2/seed3 PIDs 104453/104456). Relaunched 2 cores as **big-window seeds 4,5** (WMIN40/WMAX80/SOLVES8, logs 96_e578_s{4,5}_bigwin.log) to test the hypothesis that the ~1000d plateau is a WINDOW-SIZE limit. **Honest repricing (unchanged): all 6 instances at practical rank ceilings** — medium r1 (maxed), large r2 (LNS plateaus ~990d, <424 needs from-scratch global TD-TSP = multi-day binary low-EV), trajectory r6 (r5 +136k WSB-unreachable), matching-i/ii (exact-blocked), small (timing-floored+perm-optimal). **Dominant unrealized value = USER SUBMISSION of the 6 banks (~49 pts, all currently 0, user-gated, escalated 04:00).** No banking (best 990d feasible but 0 point gain; hold). Next tick: if big-window seeds ALSO plateau ~1000d → LNS-from-bank capped, large effectively r2-ceilinged; if they break below ~700d → keep pushing.

**TICK 14:56 (E-578 health-check):** 4 seeds ALIVE (99% CPU, logs fresh, no tripwire). Descent: seed2 BEST 996.60d (−52.4d, acc=13, still moving); seed0 1019.05/seed1 1019.05/seed3 1010.10 (plateaued ~25-30min, under 1h-zero-accept tripwire). **Descent collapsing** (−30d in first 200s → <1d/100s now) and ALL seeds sit ~1000d ≫ r1=424.62 → still rank2 = ZERO points (binary). DECISIVE-ENOUGH read: windowed-LNS-from-bank escapes the single-move basin but plateaus ~1000d, FAR above the point threshold. Reaching <424 needs a from-scratch global near-optimal TD-TSP construction (LKH-class, TGMA's cluster+LKH recipe) — multi-day, binary low-point-EV. NEXT DEEP REVIEW: if seeds 0/1/3 hit 1h-zero-accept → reap, keep best; decide whether to scope the from-scratch global TD-TSP solver (only remaining large lever) vs accept large ceilinged at r2. No banking (996.6d feasible but 0 point gain; hold for best final candidate). No escalation (running queue non-empty).

**TICK 14:35 (E-042 decisive characterization of large + matching block):** Killed non-converging E-573 fixpoint (soft real_mk 1522→1433d, both WORSE than 1048 bank; exc_over stuck 3-4; strict rejected every iter — full-resolve is non-monotone → diverges). Three large-attack families now RULED OUT (E-042): (1) E-572 surrogate global solve finds 389d cheap-cost order but it's UNWALKABLE (epoch-shift); (2) E-573 fixpoint diverges; (3) E-576 global greedy NN = `greedy_subtour_only` = the EXISTING 1048 bank's interior method (E-556) — a duplicate the anti-oscillation grep caught. **Matching exact lever HARD-BLOCKED:** Gurobi 13.0.2 has a size-limited restricted license ("Model too large") and both matching instances are a SINGLE connected component (no license-sized decomposition) → CP-SAT plateau 33,338 (bound 34,339) stands. Matching ceilinged. **Isolated large lever = MONOTONE time-dependent local search on REALIZED makespan** (2-opt/Or-opt, partial re-walk, accept strict-improve only — can't diverge). BINARY caveat: only <424.62 feasible scores (+1.78); seeded from 1048 unlikely to cross (2.5× gap) → POINT-EV low. E-577 (monotone single-node Or-opt) BUILT+SMOKE-TESTED: bank is an **Or-opt LOCAL OPTIMUM under realized makespan** — one pass screened 26,347 improving relocations, ALL failed re-walk verification (downstream epoch cascade wipes every local gain; epoch-trap at the single-move level). Single moves can't escape the 1048 basin. **Remaining lever = windowed destroy-repair LNS (E-578)**: re-optimize a contiguous ~40-node window, entry epoch fixed, window re-walked, splice only if realized mk drops (monotone, bounded epoch shift → can't diverge like E-573). **E-578 windowed destroy-repair LNS WORKS — FIRST method to descend from the 1048 bank.** Smoke (200s): 1048.98→1019.05d (−29.9d, 3 accepted window re-opts). Re-optimizes a contiguous ~20-45 node window via OR-Tools (entry epoch fixed), splices only on verified feasible realized-makespan drop → monotone, escapes the single-move basin. LAUNCHED 4 parallel seeds (PIDs 104441-444, 8h TL, logs runs/ch2_v3/96_e578_s{0..3}.log, guarded to /tmp/ch2_large_window_cand_s{seed}.json, banks NOTHING). Still BINARY/low-point-EV (needs <424.62; −624d gap, descent will slow with diminishing returns). All other 5 instances exhausted/ceilinged. NEXT TICK: health-check the 4 seeds (alive, accepts>0, mk trending down); if any candidate <424 feasible → guarded-bank escalation.

**★ CAMPAIGN OBJECTIVE (user, 2026-06-11 17:50): MAXIMIZE OVERALL SpOC SCORE — replaces the "rank ≤3 on all" target.** Official scoring (reference/SpOC4/README.md §Scoring): global score = Ch1 local + Ch2 local; each challenge has 3 problems; top-10 ranks earn points (11−rank)×weight with **easy ×1, medium ×4/3≈1.333, hard ×(4/3)²≈1.778**. Mapping (P4 RESOLVED 2026-06-12 via O-001 API category field): Ch1 matching-i=**easy ×1 (A_1)** / matching-ii=**easy ×1 (A_1, NOT medium!)** / trajectory=HARD (A_3); Ch2 small=easy (A_1) / medium=medium (A_2 ×4/3) / large=HARD (A_3). Ch1 has only Beginner(×2)+Advanced tiers — no medium. Ch3 (Luna Tomato Advertising) = TIE-BREAKER ONLY. Max/problem: easy 10, med 13.33, hard 17.78; max/challenge 41.11. **ROI now = expected POINTS per invested hour (analytics+dev+compute); every rank step in top-10 counts (rank 7→4 on a hard = +5.33 pts), not just podium. Unsubmitted banks score ZERO → submission (timing) is now a first-class decision; still escalate actual submissions to user.**

**STANDING DIRECTIVE (user, 2026-06-12 ~09:10): FULL COMPUTE-RESOURCE DISCRETION — kill/reallocate/relaunch any process freely to best advance the rankings; NO per-kill escalation. The 2026-06-11 17:00 "no kills, let processes finish" rule was CONTEXTUAL, now SUPERSEDED.** (Guarded banking + submission gate UNCHANGED: still escalate only actual submissions and non-compute destructive actions.) See memory [[feedback-compute-resource-discretion]]. Manage the 4 cores autonomously: reap deadlocked/low-ROI runs, launch higher-EV work.

**★ LOOP OPERATING POLICY — DOCUMENTATION + COMMIT + SUBMISSION (user, 2026-06-12 ~09:10): bake these into every relevant tick, not ad-hoc.** See memory [[feedback-loop-doc-commit-submission-policy]].
- **DOCUMENT:** whenever an experiment finishes / banks / is decisively refuted → write or update its `vault/experiments/E-NNN` note AND refresh the bank snapshot below + `vault/index.md` status. On each deep review (or ≥ once/day) append a `vault/sessions/S-YYYY-MM-DD` entry. Keep loop-state tick log current every iteration.
- **COMMIT (now standing-authorized inside the loop — no need to ask each time):** after a bank change or a notable vault/script change, `git commit` per CLAUDE.md §6 — stage files explicitly by name (NO `git add -A`), 1-2 sentence why-focused message, NO AI-attribution trailer, never commit secrets. Group related artifacts into one coherent commit. Do NOT `git push` (leave remote to the user).
- **SUBMISSION:** the agent NEVER submits (user-only via web UI) and never makes state-mutating HTTP. Track per-instance submitted-vs-pending state; escalate the submit decision to the user with the current points table. Read-only leaderboard GraphQL is fine.

Tick log:
- 14:05 — **E-042 (e572) iter0 RESULT = strong signal + tripwire; pivoted to E-043 (e573) fixpoint.** The GLOBAL OR-Tools solve found an order with cheap-cost **path~389.2d (< r1=424d!)**, only 3 forbidden edges, pen=0 — PROVING a great static order exists in the cheap graph. **BUT the chronological walk REJECTED it** (infeasible): costs were built at the SEED's epochs, but reordering shifts realized epochs → cheap-at-seed edges become infeasible at new arrival times (classic time-dependent-TSP trap; walk hard-rejects at first unreachable leg, only 5 exc allowed). E-572 kept the 1048 bank (no bank change; candidate=bank, /tmp). **PIVOT → E-043 (scripts/ch2_e573_large_fixpoint.py): time-dependent FIXPOINT.** soft_walk (never rejects — escalating dv caps [100,600,3000,50000]+waiting always yields a transfer) extracts realized epochs from ANY order + an exc_over count (how-infeasible); build cost at the order's OWN soft-walk epochs (self-consistent); global GLS solve (TL=300s, faster iters); soft-walk new order→fresh epochs; ALSO strict walk_perm_chrono each iter to capture any true ≤5-exc walkable order < 1048. Iterate ≤10. Candidate(best strict-feasible)→/tmp ONLY; I guard-bank after verify. HEALTH: base strict reproduced 1048.9786; 4 workers up; ~11min/iter (cost 1m + solve 5m + soft+strict walks ~5m). Log runs/ch2_v3/93_e573_fixpoint.log. **WATCH: does strict-walk start passing < 1048 and does exc_over fall toward ≤5? If after ~4 iters strict never passes & exc_over stays high → fixpoint not converging → next lever = epoch-ROBUST cost (edge feasible only if cheap across an epoch band, so order walks first try) or chronological insertion construction.** E-572 log runs/ch2_v3/92.
- 13:30 — **★★ E-041 DECISIVE FINDING + E-042 (e572) LAUNCHED on all 4 cores: the large 1048→r1=424d gap is ORDERING/PHASING, not structural. ★★** User reaffirmed (13:05): never wait for confirmation, always continue toward top ranks → chose to stay autonomous on the highest-EV lever (large r2→r1, +1.78 pts, also defends the 16-pt r2). Methodology-mandated diagnostic FIRST (scripts/ch2_e571_large_gap_probe.py, ~30s): **(A)** 1048 bank has **idle=0** — mk IS tof_sum; fat tail = 654 legs >0.5d (excess 617d), so squeezing those to 0.5d → ~432d ≈ r1. **(B)** at EVERY epoch 0→1100d every node keeps ~37 cheap neighbors (frac_feas=1.00) with **min cheap-tof median 0.150d** (we use 0.72d median legs). ⇒ short hops abundant; the static 4-component decomposition (E-533, ONE ref epoch, 0 inter-comp cheap edges) is a SNAPSHOT ARTIFACT, and E-562b's frozen-topology per-piece OR-Tools plateaued because it never re-routes across the epoch-dependent rich connectivity. Writeup E-041. **ACTION = E-042 (scripts/ch2_e572): DROP the decomposition — single GLOBAL epoch-aware open-path solve over all 1051 nodes, seed from current 1048 order, build epoch-aware cost (find_earliest_transfer at each node's walk-epoch over its static-cheap candidates, BIG=forbidden/PENALTY=cheap-but-not-feasible-now), one global GLS solve (TL=1200s), re-walk→fresh epochs, iterate ≤6, stop <5d.** Candidate → /tmp ONLY; I guard-bank after verify. HEALTH: base walk reproduced exactly (1048.9786, exc=5, start=492); 4 workers ~98% CPU; cost build ~1min/iter (early-exit on abundant cheap), ~20min OR-Tools/iter → ~2h total. Log runs/ch2_v3/92_e572_global_epoch.log. **First verdict (iter0 mk) in ~20min.** Tripwires: walk REJECTED (order needs >5 exc) → would keep prev + stop; OR-Tools obj should drop below seed; if iter0 mk≥1048 the global GLS isn't beating the frozen-piece result → rethink (LKH instead of OR-Tools, or relax candidate set beyond static-cheap).
- 12:55 — **HEALTH-CHECK (cheap tick): ALL 4 CORES IDLE (no python/experiment procs). Two long-runners from the gap CLOSED.** (1) **matching-ii r6-push** (cooperative LNS, 4 workers, ~2h) DONE: best 72204.293 (seed0, 1046 rounds) — flat plateau, no rank change (still r7, r6=72,327.43 is +123 away). On-disk matching-ii.json independently RE-SCORED via official load_instance: mass=72204.293, feasible (e/l/d each unique), 9110 sel, strictly > backup 72200.728 → guarded-bank valid (uncommitted, committing now). Confirms E-039: this formulation's LNS/CP-SAT lever is exhausted; crossing r6 needs a tighter 3-index ILP / Gurobi-class solver (user-gated). (2) **Ch1 trajectory WSB go/no-go (e566)** = VERDICT NO-GO: 5 high-bound pairs (e.g. 261,205 bound 2736kg, 277,189 bound 2735kg) all constructed ZERO valid candidates — patched-conic bounds exist but no feasible WSB trajectory found → trajectory r5 wall (372,729kg, +136k) stays unreachable, matching O-016. **Net: cheap positive-EV compute queue is now EMPTY** — only remaining levers are uncertain multi-hour structural builds (P3 large LKH r2→r1 rebuild toward 424d) or user-gated solver power (matching ILP/Gurobi). Both escalation conditions met (empty cheap queue + submission now highly valuable: medium r1 + large r2). No bank delta beyond the already-verified matching-ii. ESCALATING to user.
- 12:35 — **★★ E-040 BANKED: medium 195.7748 → 192.9002d (−2.8747d) → RANK 1 (was r2; beats live r1=195.6816 by 2.78d, +1.33 pts ×4/3). ★★** Pure RE-TIMING of the unchanged bank perm on a strict-superset ultrafine grid (tof linspace(0.025,12,**319**) ⊇ bank's 160-grid at even idx; departure TQ 0.05 ⊃ 0.1) ⇒ DP result provably ≤ bank; realized −2.87d (0.1→0.05d departure precision + marginally shorter feasible tofs over 180 legs accumulate). **GUARD-BANKED:** same perm verified, independent FRESH-process re-score 192.900157 feas viols[0,0,0,0] all 181 covered, round-trip match, strictly better; backups medium.json.bak.e040 + /tmp/bank_bak/medium_pre_e040_*. On-disk medium.json re-verified 192.900157. Writeup E-040; bank snapshot + total updated (≈50.5 pts if all submitted, still 0 live). **Medium is now rank 1 — no higher rank; lever exhausted there.** All 4 cores now FREE. **Next queue: P3 large LKH exact-interior rebuild (×16/9, toward r1=424; biggest remaining EV + defends 16-pt r2) is the top BUILD; matching exact-ILP (tighter formulation/Gurobi, E-039) needs new code or user Gurobi; P4 weight check trivial.** Escalation note for user: 6 banks now ≈50.5 pts at 0 — medium+large would both be RANK 1/2.
- 12:00 — **P2 small idle-squeeze = NULL (re-time mk=120.14 > bank 116.37, candidate discarded) + caught & fixed a GRID-CORRECTNESS bug threatening E-040 medium.** Small ultrafine re-time came out WORSE because small's bank was built on E-529's ultrafine method, NOT the uniform linspace(0.025,12,160) grid → my grid isn't a superset of it, so the DP can't reproduce the bank's timing. **P2-small CLOSED: small bank is timing-floored; the 6.385d idle is forced, not slack** (consistent with E-038's perm-optimal + E-529 ultrafine provenance). **Critical catch for E-040:** I'd launched medium with N_TOF=320, which is ALSO not a superset of the bank's 160-grid → risk of a FALSE NEGATIVE (missing the −0.093d we need for r1). Fixed: medium's bank DID use linspace(0.025,12,160)/TQ=0.1 (E-563 FINE_TOFS), and linspace(0.025,12,**319**) contains all 160 points at even indices + TQ 0.05 ⊃ TQ 0.1 → the bank's exact (dep,tof) assignment is now IN my search space ⇒ DP result PROVABLY ≤ bank (no false negative possible). Killed the 320-grid run (was 100/180 legs), relaunched scripts/ch2_e568 with E568_NTOF=319 E568_TQ=0.05 E568_WORKERS=4 (be2xtallh, log runs/ch2_v3/90, ~35min). All 4 cores on this one highest-EV live lever. Candidate → /tmp; guard-bank only if mk < 195.7748 & feasible & round-trip. No bank delta this tick.
- 11:40 — **matching-ii LNS DONE: banked +3.565 → 72,204.293 (guarded by its own script, feasible, backup saved). Still rank 7 (+123 short of r6=72,327.43) → NO rank change.** Per E-039 the LNS/CP-SAT lever on this formulation is exhausted (warm CP-SAT already matched-but-didn't-beat; report says needs tighter 3-index ILP or Gurobi-class) → did NOT relaunch plain CP-SAT (anti-oscillation: refuted approach). Freed 3 cores. **Used 2 freed cores for the P2 small idle-squeeze test via the SAME ultrafine-retime tool: generalized scripts/ch2_e568 to E568_PROB={medium,small,large}, launched small (bk08wa7n7, log runs/ch2_v3/89). Small bank mk=116.3738, tof_sum=109.989 → 6.385d non-tof idle; r5 needs −4.586d. Ultrafine DP (TQ 0.05, 320 tofs) tests whether idle compresses (E-038 proved the PERM is optimal but idle-squeeze is a DIFFERENT lever — timing/wait-quantization).** E-040 medium re-time healthy (scan 40/180 legs @953s, ~70min total). Both candidates → /tmp only; guard-bank after verdict. Cores: 2×E-040 + 2×E-569-small = 4/4. matching-i exact-ILP build + P3 large LKH remain queued (multi-hour builds).
- 11:15 — **★ DEEP REVIEW (trigger: P0 resolved + classifier restored): LIVE LEADERBOARD REFETCHED (O-016) — MEDIUM WOULD BE RANK 2, r1 only 0.0932d ABOVE OUR BANK → launched E-040 ultrafine re-time (b26407xnb).** P1 DONE: working GraphQL query recovered from session transcript + persisted in scripts/fetch_leaderboards.py (API `rank` is per-USER; dedupe to per-user-best). Fresh cutoffs vs stale assumptions: medium r1=195.6816 (bank 195.7748 → −0.0932d needed — ~ONE grid step of the bank's 160-tof/0.1d final timing!); large bank=r2 (1153.9 incumbent); matching-i ladder packed (+26.3→r8, +89→r5, +217→r1); matching-ii r6 +126.7 (LNS at 72,204.3 plateaued, won't cross — let budget finish, then exact-ILP per E-039); small r5 needs −4.586. **Points if all submitted ≈49.2 (was 47.89) — still 0 live.** ACTION: E-040 = ultrafine DP re-time of UNCHANGED medium bank perm (TQ 0.1→0.05d, tof grid 160→320 pts, fast t-min DP), scripts/ch2_e568_medium_ultrafine_retime.py, 2 workers niced, log runs/ch2_v3/88_e568_ultrafine_retime.log, candidate → /tmp only, guard-bank after. First launch failed instantly (wrong INST path data/spoc4/kttsp/ → problems/), fixed + relaunched. Queue: E-040 (running) > exact-ILP matching push (build) > P2 small idle-squeeze > P3 large LKH > P4 weight check. Cores: 3×matching-ii LNS (ends ~11:38) + 2×E-040 (slight oversubscription, niced, brief).
- 11:00 — **★ P0 AUDIT COMPLETE (agent af600ebd): evaluator-divergence hypothesis REFUTED — our Ch1 matching model is FAITHFUL to the official UDP. Vault note E-039.** Decisive: official PyGMO UDPs exist locally at `reference/spoc4_udp/matching-{i,ii}.py` (the "no official Ch1 evaluator" premise was wrong — only reference/SpOC4/ lacks them; I verified the file directly). All 4 dimensions MATCH: parsing (rows = candidate transfers; 5000/10000 = node-set sizes), constraints (selection from GIVEN rows only — free-recombination physically impossible, dup e/l/d ⇒ score 0), fitness (plain Σw both i and ii), submission encoding (binary |T|-vector, our artifacts verified). Reframe: the "6-method consensus" at 33,338.184 was ONE HiGHS-family asymptote, never proven optimal (exact HiGHS 122% gap; CP-SAT bound 34,339, gap 2.92%); leaderboard packing (8 teams within 0.65%, 05-18 r5 cutoff was 33,345 = bank+7!) = same-ILP-stronger-solver signature, not a model gap. **QUEUE REPRICED: P0 RESOLVED → new P0′ = exact-solver power on the matching ILP (tighter 3-index CP-SAT formulation / LP-guided / long budget; Gurobi-class = user decision). Teams packed every few kg → each small gain ≈ +1 rank (×1).** P1 (GraphQL fetch — need CURRENT matching-i cutoffs r8/r7/r6, the 33,345 figure is 05-18-stale), P2 (small idle-squeeze), P3 (large LKH), P4 (weight-map contradiction) unchanged. Health: matching-ii LNS plateaued 72,204.3 (flat since t≈1900s, log fresh) — let budget finish. No bank delta, nothing submitted. Commit still classifier-blocked at 10:43 — retrying now.
- 10:50 — **★ DEEP ANALYSIS (user-requested ultrathink): queue REPRICED around an evaluator-asymmetry finding; P0 audit agent LAUNCHED.** Key finding: **Ch1 has NO official Python evaluator in the starter kit** (Ch2 has utils_users.py, Ch3 lta_udp.py) — our matching scorer (src/esa_spoc_26/ch1_matching.py) is a README re-implementation, and NOTHING has ever been submitted → local scorers never ground-truthed. Combined with the textbook foundation-then-search trigger on matching-i (SIX methods converge on exactly 33,338.184 yet 8 teams sit above, r1=33,555.61 only +0.65%; CP-SAT bound 35,396 = 5.8% gap), the dominant-EV action is a MODEL/EVALUATOR AUDIT, not more search. Concrete lead: README links instances as random_harder_5000.txt / random_hard_10000.txt vs local matching-i.txt 25,000 rows / matching-ii.txt 92,103 rows — a parsing/semantics divergence (e.g., row-selection vs free (e,l,d) recombination) would explain ALL teams beating our converged "optimum". **NEW PRIORITY QUEUE: P0 Ch1 matching model audit (EV up to ~+16 pts: matching-i r9→r1 +8, matching-ii r7→r1 +8 — agent af600ebd RUNNING, read-only, /tmp only); P1 fix+persist leaderboard GraphQL fetch (price medium r2; scripts/fetch_leaderboards.py is a stub); P2 Ch2 small idle-squeeze diagnostic (bank mk=116.37, tof_sum=110.0 → ~6.4d non-tof idle vs −4.59 needed for r5=+1pt; perm is solve_open_path-optimal per E-038 but idle may compress); P3 Ch2 large exact-interior LKH/elkai on iterated epoch-aware matrix (TGMA 424 proves −60% exists; +1.78/rank, also defends the 16-pt r2 block); P4 verify weight mapping matching-ii ×4/3 vs ×1 (O-001 said A_1/A_1/A_3 = ×1; line-5 mapping above says ×4/3 — they CONTRADICT, recheck at next API fetch; affects all EV math).** Cross-cutting: first user submission doubles as scorer calibration → de-risks P0 directly. Cores: 3×matching-ii LNS (bdiguhl0h, ends ~11:38, 72,204.3 tapering) + P0 agent (light). No bank delta, nothing submitted. Wakeup already pending from 10:30 tick (no double-schedule).
- 10:30 — **TRIPWIRE: small phase-2 HUNG → reaped; phase-2 REFUTED (hung + dominated); matching-ii LNS healthy but tapering.** Small phase-2 (pid 97493) log froze at "iter 1" 36 min (mtime 09:29, now 10:05) at 99% CPU, zero `[P2]` output → stuck on one degenerate-perm `dp_time_perm`/`kt.fitness` (E-554 c2 DP-spin class). `kill -9` the tree. **Key reassessment (E-038 REFUTED):** phase-2 relocate-perturbation is **dominated** — the deterministic epoch-aware step already runs `solve_open_path` (CP/OR-tools) on every comp interior, a *stronger* optimizer than random relocate (why small got big=0). Same holds for MEDIUM (interiors already solve_open_path-optimal) → a medium relocate-perturbation phase-2 would be dominated too. **The genuine untried Ch2 small/medium lever is TOPOLOGY change** (comp0 force-split into 24+16 runs by exc bridges; different split/exc allocation) — a larger build, uncertain EV, ×1 (small)/×4/3 (medium) weight. **matching-ii LNS** (bdiguhl0h, 3 workers): best 72,200.728→72,204.3 @ t=1900s, still micro-climbing but tapering (+3.6 in 30min vs +123 needed for r6) → will likely NOT reach r6=72,327.43 this budget; let it finish (small chance of a MIP-driven jump). **Freed core: NO clearly-positive-EV immediate launch** — interior-perturbation builds are dominated; topology builds are speculative/large; leaderboard refetch (to find any new cheap boundary) is the right next step but the working GraphQL query isn't persisted (stub only) → defer to a proper price-check. 3/4 cores on the one priced boundary (matching-ii) = NOT idling. **Positive-EV immediate compute queue is now THIN; dominant action remains USER SUBMISSION (47.89 pts at 0).** No bank delta; nothing submitted.
- 10:10 — **DEEP REVIEW (repriced on 04:00 cutoffs; 2 cores now working highest-EV gaps; 4 Ch1+Ch2 commits done).** Agent a6d7dbed finished but left a proper **phase-2 small run** detached (pid 97493, `PHASE2_S=9000`=2.5h, `ITERS=2 TCAP=130`, log runs/ch2_e564_small.log, candidate-to-/tmp) — healthy, in epoch-aware iter 1, perturbation block starts ~2min. NOT a tracked task → poll via fallback wakeup. **Repriced idle cores against 04:00 cutoffs:** large r1=424.62 (0 EV, cliff); trajectory r5=372,729 (+136k, WSB-unreachable — confirmed: E-036/E-570 WSB fleet builds NO valid ballistic-capture candidates, go/no-go NO-GO, high-bound pairs achieve 0kg); matching-i ceilinged (33,338, 6 methods); **matching-ii is the closest boundary: +127kg to r6=72,327.43 (+1.33 pts ×4/3), LNS was still marginally climbing** → **LAUNCHED warm-LNS matching-ii 7200s×3 workers (task bdiguhl0h, log runs/ch1/matchlns_ii_r6push.log, its own guard-bank).** 3 workers + 1 small phase-2 = 4 cores. **Verified+committed banked work:** matching-ii 72,200.728 & trajectory 236,420.53 both re-scored feasible on official scorers (commit 6aa0c41); Ch2 large/medium/small commits earlier (b659ec1/743ca54/f57349b). If matching-ii LNS tapers again <72,327 → methodology trigger says change operator, not repeat → conclude ceilinged & stop. Nothing submitted.
- 09:55 — **SMALL basic epoch-aware reorder = NULL; phase-2 perturbation IN FLIGHT (agent a6d7dbed still active).** My logged ITERS=5 run (biaruvrrb) converged flat: every comp0 run reorder `big=0`, best DP 116.3755 vs bank 116.3738 → wrote nothing (E-038). Reason: small bank came from DP-on-ultrafine (E-529), already epoch-faithful → NO proxy⊥reality slack to recover (unlike medium/large which came from a fixed-ref-time proxy). **BUT** agent a6d7dbed is autonomously running the script's **phase-2** (randomized comp0 relocate-perturbation + epoch-aware re-solve, `E564_PHASE2_S`) — a genuinely stronger search the deterministic reorder can't reach; it just launched a 90s probe (pid 97281). Candidate-to-/tmp only; I guard-bank if it beats 116.3738 feasibly. E-038 marked PARTIAL (not refuted) pending phase-2 verdict. Watch agent-completion notification. **Ch1 side-finding (from runs/ch1/matchlns logs, track idle now):** matching-ii LNS already BANKED 72037→72200.728 (higher=better, feasible) across two runs; matching-i LNS = NO IMPROVEMENT (stuck 33338.184, consistent with CP-SAT-can't-move-incumbent). matching-ii.json + trajectory.json are modified-uncommitted — verify on Ch1 scorer + commit in a focused Ch1 tick. Nothing submitted.
- 09:35 — **COMMITTED Ch2 banks + docs (loop commit policy); small build relaunched logged.** Two surgical commits: (1) `743ca54` medium 228.97→195.77d + points-strategy/loop-policy docs (E-037, index, loop-state, session journal, M-general loop-directive); (2) `b659ec1` large 2225→1048.98d rank-2 + E-034 + E-556..E-562b pipeline (was sitting uncommitted = at risk). Re-verified all 3 Ch2 banks on official scorer: large 1048.9786 / medium 195.7748 / small 116.3738, all feas [0,0,0,0]. **Small E-564 (agent a6d7dbed) finished WITHOUT writing a candidate** (no /tmp/ch2_small_epoch_candidate.json) — verdict lost to a `head -40` pipe (not logged). Relaunched properly: `E564_ITERS=5 ... > runs/ch2/e564_small.log` (bg task biaruvrrb) to capture the convergence + verdict. ch1 banks (matching-ii, trajectory modified) left for a ch1 deep review — not verified this tick. No push, nothing submitted.
- 09:10 — **★★ MEDIUM BANKED 228.9748 → 195.7748d (−33.20d) → CROSSES r3 (216.95) BY 21.18d = rank 4→3, +1.33 pts ★★** E-563 epoch-aware cluster-decomp (agent a4cf2add) ported the large recipe to medium; official kt.fitness 195.7748, viols [0,0,0,0], 181 nodes covered. Guard-banked via scripts/ch2_e563_bank.py (independent re-score + round-trip + backup .bak.e563 + /tmp/bank_bak/medium_20260612_090630.json). The agent-writes-/tmp-only + loop-verifies-and-banks safety pattern worked. Structure: 4 SCC comps [121,20,20,20], star-topology bridges force comp-2 start + out-and-back excursions; lever was epoch-aware interior re-ordering (NOT topology) — same proxy⊥reality lesson as large [[E-034-ch2-large-epoch-aware]]. Documented E-037. **Freed cores → launched SMALL epoch-aware (E-564, agent a6d7dbed, same recipe + safety, candidate-to-/tmp) targeting r6→r5 (116.37→<111.79, +1 pt).** Then implemented the new DOC/COMMIT/SUBMISSION loop policy (above) + committing artifacts. Nothing submitted; medium bank is the only change.
- 09:00 — **★ USER AUTHORIZED KILLS + new launches → reaped deadlocked E-554, launched MEDIUM epoch-aware cluster-decomp build (E-563).** E-554 confirmed fully deadlocked (all PIDs 0.0% instantaneous CPU, log frozen 01:10 = 7.5h stale, 7.5h past its 24h budget) → killed the whole tree (43596/43600/43601/43603/44710), reaped clean, 4 cores free (load 0.04). Zero loss (basin-trapped 228.9748, producing nothing). **Repriced freed cores: best-EV untried family = MEDIUM r4→r3 (+1.33 pts ×4/3; bank 228.9748d, r3=216.95d, gap −12d).** Medium's prior failures (E-547/550/551) were all PERMUTATION search (exhausted basin); the UNtried family is the epoch-aware cluster-decomposition that broke LARGE (2225→1049d, −53%). **Launched bg general-purpose agent a4cf2add to build scripts/ch2_e563_medium_epoch_aware.py** (adapt E-561/562 recipe + E-533 structure extraction to medium n=181). **SAFETY: agent writes its best feasible candidate to /tmp/ch2_medium_epoch_candidate.json ONLY and is FORBIDDEN to touch solutions/upload/medium.json — I guard-bank myself after verifying (avoids the E-537-style stale-guard clobber risk found at 07:51).** Agent briefed: official kt.fitness is the only evaluator (proxy⊥reality trap = why perm search failed); ≤4 cores OMP=1 niced; never submit/network. Watch: agent completion notification → if candidate < 228.9748 feasible, guard-bank it. Banks otherwise unchanged, nothing submitted.
- 07:51 — Cheap tick → **de-risked the large E-563/E-537 build instead of idling; CORRECTED two errors in the 06:09 escalation.** (1) **"needs external solvers (LKH-3/Concorde)" was FALSE:** `elkai` (LKH-3 wrapper) + `ortools` are INSTALLED in spoc26; the full pipeline scripts/ch2_e537_large_cluster_lkh.py already exists and its E-533 dependency (/tmp/ch2_e533_large_adj.npz, 06-07) is on disk. Tooling is NOT the blocker. (2) **But the large build remains LOW-EV for a STRUCTURAL reason:** large bank 1048.98d is RANK 2; r1=424.62d. Any result in (424.62, 1048.98) = ZERO marginal points (stays rank 2). Gaining +1.78 requires MORE THAN HALVING the makespan in one run — unlikely (the 1048.98 bank came from the epoch-aware E-561/562b; E-537's LKH pass was a rough first cut that predates it). So the real reason the large build is deprioritized is the rank-2→rank-1 cliff, not missing solvers. (3) **⚠ LATENT REGRESSION LANDMINE in E-537:** its banking guard is `if feas and fit[0] < 1200: bank` (hardcoded 06-07 when bank was 1536d). With the bank now 1048.98, launching E-537 UNMODIFIED would CLOBBER the bank with anything < 1200d. → DO NOT launch E-537 as-is; needs guard fixed to strictly-better-than-current-bank first. **DECISION: did NOT launch (unsafe guard + low EV from the rank cliff). Escalation recommendation UNCHANGED (submit the 6 banks = dominant); only the reasoning is corrected.** Load 0.00–0.20, E-554 still 0.0% CPU (deadlocked, left). No banks touched, nothing submitted.
- 06:50 — Cheap heartbeat (no action). Load 0.00 instantaneous; E-554 PIDs 43596/43600/43601/43603 confirmed 0.0% CPU (deadlocked, not competing — left per no-kills). No processes running, no bank deltas, no tripwires. State UNCHANGED since 06:09 escalation: all 6 banks submission-ready (~46.55 pts, currently 0), cheap positive-EV queue empty, remaining levers are multi-hour new-code builds only. Did NOT re-escalate (already standing with user since 06:09) and did NOT start a speculative build unsupervised. Long heartbeat continues. Nothing submitted, no banks touched.
- 06:09 — **★ ESCALATION: cheap positive-EV queue is EMPTY (agent-confirmed) — all 6 banks verified submission-ready (~46.55 pts, currently 0). Remaining levers are multi-hour NEW-CODE builds only.** Scoping agent a418a2fa verdict (Ch2): MEDIUM is structurally stuck, not compute-stuck — E-547 (all 5 perm-mut classes, 500 trials) found 0 below bank, E-551 best neighbor +11.4d uphill, E-550 (different evaluator, 24h) accepted 0 → clearing r3 (216.95, −12d) needs NEW cluster-decomposition code (different perm), no existing script reaches it. SMALL likewise parked. LARGE r2→r1 needs the full cluster+LKH/Concorde rebuild (the previously-queued E-563). **All 3 Ch2 banks RE-VERIFIED officially feasible (viols 0.0; mk 116.3738/228.9748/1048.9786); banking guards confirmed UDP-validated + strictly-better + atomic.** So across ALL 6 instances no safe re-runnable improver remains (matching ceilinged/tapered, medium+small perm-search exhausted, large needs rebuild, trajectory WSB exhausted for points). **Both escalation conditions hold: pending submission + empty cheap positive-EV queue.** E-554 (PIDs 43596/43600/43601/43603) dead but deadlocked ~0% CPU (not competing) — left per no-kills. **DECISION: did NOT start a multi-hour speculative build unsupervised (large E-563 +1.78 / medium new-code +1.33 are small EV vs the 46.55-pt submission; need external solvers; 06:00 unattended). Escalated to user: SUBMIT the 6 banks (dominant), and/or authorize the large E-563 build. Long heartbeat until direction.** Nothing submitted, no banks touched.
- 05:46 — **matching-ii BANKED 72,200.728 (final session gain +163.3 from 72,037.42; feasible verified). Matching compute now EXHAUSTED for near-term points; delegated scoping of next Ch2 build.** matching-ii tapered to a plateau (72,037→72,109→72,200, flat) still 127 short of r6 (72,327.43) → strict bank, rank still 7, 0 marginal points. Did NOT auto-relaunch (taper rule). matching-i ceilinged. So cheap positive-EV queue is empty; only multi-hour Ch2 builds remain (medium r4→r3 −12d=+1.33pts; large r2→r1 ≈halve makespan=+1.78pts; small r6→r5 −4.6d=+1pt). To avoid a blind heavy-compute misfire (tripwire: search metric must == official scorer, bit us twice), launched READ-ONLY scoping agent a418a2fa to identify the ONE concretely-runnable Ch2 job + exact command + banking-safety, focusing on medium toward <216.95. No compute launched, no banks touched, nothing submitted. Dominant action unchanged: user-gated submission of all 6 banks ≈46.55 pts (currently 0).
- 05:37 — Health tick (no action). matching-ii run bdf62agme HEALTHY but PLATEAUED: best 72,200.7 flat since ~t=1840s (no gain through t=3132s/~52min). Finishes 3600s (~05:45) → guard-banks 72,200.7 = +163 over original 72,037 but still 127 short of r6 (72,327.43) → strict bank, NO rank gain. Matching-ii lever tapering (72,037→72,109→72,200→flat); 1 more longer push = diminishing, still 0 marginal points unless it clears +127. DECISION next-tick: when this banks, do NOT auto-relaunch matching-ii (taper like WSB); matching compute is effectively exhausted for near-term points → only multi-hour Ch2 builds + the pending ~46.55pt submission remain. Nothing submitted.
- 05:13 — Health tick (no action). matching-ii 1h run bdf62agme HEALTHY + IMPROVING: best 72,200.7 at t=1840s (~31min, +91 over the 72,109.5 relaunch bank, +163 over original 72,037) — still climbing toward r6 threshold 72,327.43 (+127 more). Load 4.12 (4 workers), log fresh. Guard-banks the best at completion (~05:45). No cores idle, nothing to launch. Nothing submitted.
- 04:45 — **★ MATCHING LEVER NOT DEAD: matching-ii broke its plateau — BANKED +72.1 → 72,109.524 (matching-i confirmed ceilinged at 33,338.184). Relaunched matching-ii 1h to chase the next rank.** The warm-start-from-bank cooperative coop_mip_lns on 4 UNCONTENDED cores found +72.1 kg on matching-ii that 6 prior methods (all run under heavy core contention) missed — **methodology lesson: the matching "ceiling" was partly a contention/warm-start artifact, not a true optimum** (matching-ii bound 83,013, gap 13% = ~11k slack remains). Guard-banked (feasible, round-trip, backup /tmp/bank_bak/matching-ii_*.json + .bak.lnswarm). New rank still 7 (need +217.9 → 72,327.43 for r6 = +1.33 pts; r5=72,373 = +2.67 pts) — concrete, close threshold. matching-i: 557 rounds×4 workers, ZERO gain → genuinely ceilinged, dropped from queue. **ACTION: relaunched matching-ii alone, 4×3600s warm from the new 72,109.524 bank (waiter bdf62agme, log runs/ch1/matchlns_ii_long.log), guard-banked.** If it clears 72,327 → rank 6 (+1.33 pts, first NEW points of the session). Nothing submitted; submission of all 6 banks (~46.55 pts) still the dominant user-gated action.
- 04:05 — **Idle cores → launched guarded matching-LNS ceiling re-test (cheap, validated, never overwrites bank unless strictly better).** With cheap positive-EV queue thin and the dominant action (submission, 46.55 pts) user-gated/pending, used the 4 now-free cores for the one bounded validated lever: wrote scripts/ch1_matching_lns_warmbank.py (warm-starts coop_mip_lns from the CURRENT bank via a shared pool-best seed — NOT cold greedy like the unsafe parallel_mip_lns which overwrites unconditionally; guard-banks only if strictly-better + feasible + round-trip, with backup). Running matching-i (4×600s) then matching-ii (4×600s), waiter b9v6u8rbu, logs runs/ch1/matchlns_{i,ii}.log. Early signal = all 4 workers escalated destroy frac to 0.65 within 25 rounds, still at 33,338.184 = consistent with the 6-method ceiling; letting the bounded budget finish to CLOSE the lever definitively under ideal (uncontended) cores. Anti-oscillation note: this is the LAST matching attempt — if null, the matching lever is confirmed dead and the only remaining compute is multi-hour BUILDS (Ch2-medium new method for r4→r3 +1.33; Ch2-large exact per-component LKH/Concorde for r2→r1 +1.78), not cheap-tick launches → next-tick escalate "positive-EV cheap queue empty; submission + builds are the levers." No banks touched, nothing submitted.
- 04:00 — **★ WSB SWEEP CONCLUDED + DEEP-REVIEW RE-CONFIRMS: tonight's trajectory grind moved 0 ranks; the dominant campaign action is SUBMISSION (user-gated). ESCALATED.** Batch-5 final (4 pairs): (70,154)+52.6, (172,219)+41.9 banked; (61,173)−13.9 & (131,169) null not banked → net +94.5 kg only (vs batch avgs +735/+429/+361/+250). Ch1 trajectory now **236,420.5 kg** (20 WSB pairs tonight, +8,313 kg from 228,108). **STOPPED the sweep — did NOT launch batch-6.** Reason: refetched ALL 6 leaderboards (live GraphQL) and inserted our current banks → trajectory 236,420 = **RANK 6** both before AND after the +8.3k grind (rank 5 wall = 372,729 kg, +136k away — unreachable by marginal pair-sweeping; WSB's full fleet ceiling ~290–356k still < 372,729 per 22:25 review). So WSB marginal sweep = **0 marginal points**, confirmed empirically. **★ SUBMISSION-ROI TABLE (current banks vs live board, points = (11−rank)×weight):** trajectory 236,420kg→r6=8.89 | matching-i 33,338→r9=2.00 | matching-ii 72,037→r7=5.33 | Ch2 small 116.38d→r6=5.00 | Ch2 medium 228.97d→r4=9.33 | Ch2 large 1048.98d→**r2=16.00**. **TOTAL ≈ 46.55 pts — currently 0 (NOTHING SUBMITTED). This is the single highest-value action on the board, user-gated, 18 days to deadline.** Cheap-launch positive-EV queue is now EMPTY (WSB exhausted for points, matching CP-SAT proven flat at incumbent, E-554 basin-trapped at 228.97); the remaining positive-EV work is BUILD-type (3-D matching swap/chain local search → +~8 pts each on matching-i/ii; a new Ch2-medium method → r3) needing an active dev session, not a blind tick launch. Per directive both escalation conditions hold (pending submission + empty positive-EV cheap queue). No banks touched beyond the two guarded batch-5 commits; nothing submitted. Backup /tmp/bank_bak/trajectory_*_prebatch.json.
- 03:36 — **WSB SWEEP BATCH-4: 4/4 BANKED +1,001.7 → Ch1 236,326.0 kg. Batch-5 launched.** (379,156)+505.4 (315,224)+64.0 (397,217)+288.5 (194,213)+143.8. **Tonight: 18 WSB pairs, +8,218 kg (228,108 → 236,326).** Gains tapering with rising baseline (batch avgs: +735/+429/+361/+250); guard-bank commits only strict improvements so non-improvers are safe. Remaining candidates: 95 (81 with baseline<900). Batch-5 RUNNING: (61,173)(172,219)(131,169)(70,154), waiter b03d3rgp1. Plan: continue through higher-headroom pairs; stop sweep when a batch yields ~0 net banked. The +128k Hungarian idD-re-match still deferred (needs user go-ahead).
- 03:30 — **WSB SWEEP BATCH-3: 4/4 BANKED +1,443.3 → Ch1 235,324.3 kg. Batch-4 launched.** (109,183)+546.1 (63,247)+126.4 (51,234)+356.8 (303,251)+414.0. **Tonight: 14 WSB pairs, +7,216 kg (228,108 → 235,324).** Created reusable scripts/ch1_e570_bank.py (guard-banks a list of e570_row_*.json: in-context re-score, strictly-better, uniqueness, round-trip; one prebatch backup; never submits). Batch-4 RUNNING: (379,156)(315,224)(397,217)(194,213), waiter bxf4xryac.
- 03:24 — **WSB SWEEP BATCH-2: 4/4 BANKED +1,714.8 kg → Ch1 trajectory 233,881.1 kg. Batch-3 launched.** (162,194)+373.4 (254,272)+350.7 (92,295)+386.1 (356,285)+604.5; prebatch backup /tmp/bank_bak/trajectory_*_prebatch2.json. **Tonight: 10 WSB pairs banked, +5,773 kg (228,108 → 233,881).** Gains taper as baselines rise (batch-1 avg +735, batch-2 avg +429) — expected, low-baseline pairs have most headroom. Batch-3 RUNNING: (109,183)(63,247)(51,234)(303,251), waiter ba3u2afr5. Sweep continuing batches-of-4 through the 113-pair candidate list; all guard-banked (in-context re-score, strictly-better, uniqueness, round-trip); nothing submitted.
- 03:18 — **★ WSB SWEEP BATCH-1: 4/4 BANKED +2,941.2 kg → Ch1 trajectory 232,166.2 kg. Batch-2 launched.** Guard-banked serially (each re-validated in-context, strictly-better, uniqueness-checked, round-trip-confirmed; prebatch backup /tmp/bank_bak/trajectory_*_prebatch.json): (237,259) +791.5, (372,241) +729.5, (138,199) +661.9, (313,187) +758.3. **WSB tonight = 6 pairs, +4,058 kg total (228,108 → 232,166).** All from idle cores (E-554 deadlocked, load ~0). Per-pair gains huge on low-baseline LEO→ecc-Moon pairs (~3-6×). Batch-2 RUNNING: (162,194)(254,272)(92,295)(356,285), waiter bigf5w4d3. Continuing the cheap same-idD sweep through runs/ch1/wsb_fleet_candidates.json (113 pairs) in batches of 4; nothing submitted (user-gated); the +128k Hungarian idD-re-match remains the deferred heavier piece needing user go-ahead.
- 03:12 — **★ WSB GENERALIZES (2/2 pairs) → BANKED (343,207) +952.5 kg → Ch1 trajectory 228,272.59 → 229,225.05 kg. + CRITICAL fleet-driver bug caught & fixed. → DECISION: cheap same-idD sweep is GO autonomously (E-036 says ~2-4min/pair, NOT the multi-day piece).** E-570 WSB on (343,207) = **1150.7 kg vs 198.2 baseline = +952.5** (vs +165 on 118,171) — lever is NOT pair-specific; the lowest-baseline LEO→ecc-Moon pairs have the MOST headroom (this one ~6×). **★ CRITICAL BUG (methodology trigger fired: result claimed VALID but official rejected):** the prototype's log-printed row uses `round(v,12)`; **BCP is chaotic over the ~99d WSB arc, so 12-decimal rounding of the initial state amplifies to >1e-6 in the arrival (a,e,i) match and INVALIDATES an otherwise-valid solution.** `arrival_search` (E-565 L298-302) DOES validate against OFFICIAL `udp.fitness<0` at full precision, so the gain is real — only the serialization lost it. (118,171) survived rounding by luck. FIX: patched scripts/ch1_e570_wsb_pair.py to dump the winning row at FULL float64 precision to runs/ch1/e570_row_{idE}_{idL}.json. Re-ran (deterministic, phase A cached) → recovered full-precision row → **guarded bank: official re-score 228,272.585 → 229,225.049 (+952.5), valid, idE/idL/idD unique among 301 active, round-trip confirmed; backup /tmp/bank_bak/trajectory_20260612_031059.json. NOT submitted (user-gated).** **MANDATORY for fleet driver: serialize full precision + re-validate each row in-context before banking (a blind sweep trusting the printed/rounded row would bank garbage).** **Reprice: E-036 itself says the same-idD WSB sweep is CHEAP (~2-4min/pair, ~10-20 core-h, embarrassingly parallel) = NOT the held "multi-day" work; the multi-day piece is ONLY the +128k Hungarian idD re-match (deferred B4/B5). So executing the cheap sweep on the idle cores is in-scope autonomous guarded-banking, NOT the gated push.** NEXT: parallel batch of the validated E-570 wrapper over the highest-headroom candidates from runs/ch1/wsb_fleet_candidates.json, guard-bank each serially. Cores free (E-554 idle-deadlocked, load ~0.08).
- 02:58 — **CORRECTION: E-554 is NOT spinning (I misread `ps` %CPU). + Launched E-570 WSB second-pair validation on a free core (de-risk the fleet sweep before recommending the multi-day push).** The 02:27 "burning 3 cores at 91%" was WRONG: `ps %CPU` is a LIFETIME average (high because E-554 ran hot for 24h then hung); **`/proc/loadavg`=0.08 → box is idle, E-554 is deadlocked in a wait (~0% instantaneous), cores effectively FREE** — matching the original 01:50 `futex_wait` diagnosis. So no kill needed to use the cores. E-554 still frozen (iters 13385/13726, log 01:10) = genuinely stuck, zero ROI; still recommend reaping (PIDs 43596/43600/43601/43603/44710) for tidiness, but it's not blocking. **ACTION (positive-EV, not idle): wrote scripts/ch1_e570_wsb_pair.py — a thin wrapper that monkey-patches the PROVEN E-565 pair-constants (E-565 stays pristine; pair-specific phase-A cache so the 118/171 cache isn't clobbered) — and launched it on pair (343,207) idD=268 baseline 198.2 kg (iE=4°, eL=0.63, big headroom). Phase A = 97 bound returns (vs 91 for 118/171 → outbound geometry survivable, good sign); phase B3 (12 seeds) running, PID 89265, log runs/ch1/e570_wsb_343_207.log, completion waiter b60850yh1.** PURPOSE: if WSB beats 198.2 here → it GENERALIZES → strengthens the multi-day fleet-sweep case; if it fails on favorable geometry → fleet EV in doubt. Either way de-risks the user's go/no-go on the push. NO bank touched, nothing submitted. Multi-pair parallel sweep+guard-bank DRIVER still HELD for user go-ahead.
- 02:27 — **E-554 now CONFIRMED HUNG (upgrade from "basin-trapped"); WSB fleet-candidate list built (cheap prep done). No bank delta.** Health tick: 3 E-554 chains at 91% CPU but **iter counts FROZEN across two ticks** (c0=13385, c3=13726 identical at 01:55 and 02:27) AND **log frozen at 01:10** (77min stale, >30min tripwire FIRED) — last log line was a chain's end-of-phase "op acceptance rates" summary, then spin with no output/checkpoint/iter advance. So it's not a working basin search; it's hung post-phase, burning 3 cores producing nothing. Per directive this is a relaunch trigger, but a kill is a destructive action user-gated ("no kills"); since it can't "finish", I'm SURFACING not acting. **Recommendation to user: authorize reaping the E-554 tree (PIDs 43596/43600/43601/43603/44710) to free 3 cores for the WSB fleet sweep.** Prior tick (01:56) built the sweep input: ranked the 113 banked LEO(aE<12Mm)→ecc-Moon(eL>0.3) pairs by realized mass → `runs/ch1/wsb_fleet_candidates.json`; the 20 lowest realize only 195–623 kg vs WSB's 1114 kg on (118,171) = the +62k..+128k headroom made concrete. **Multi-pair WSB driver build still HELD for user go-ahead** (publication-grade multi-day push per [[ch1-coherent-model-r3]]) AND blocked on cores (held by hung E-554). No banks touched, nothing submitted.
- 01:50 — **E-554 DEADLOCKED (not progressing) → cores effectively FREE; launched READY WSB prototype (proven positive-EV) on a free core — NO kill (respecting "no kills" directive; deadlocked procs are at 0% CPU so they don't compete).** Health tick found all E-554 chains in `futex_wait`/`pipe_read`, **0% instantaneous CPU, load avg 0.16**, checkpoints stale ~34min, elapsed 24h34m (past the 24h budget) — classic multiprocessing teardown deadlock after time-limit. Basin-trapped 228.9748 (zero ROI, many ticks); checkpoints preserve final state; no bank delta. Decision: did NOT kill (kills are a destructive action + user "no kills" directive; and load 0.16 means the cores are usable anyway). Instead launched the PROVEN single-pair WSB prototype scripts/ch1_e565_wsb_prototype.py --n=12 (PID 88212, phase-A cache hit so straight to phase_b3 DC+capture, ~minutes), niced/1-thread, monitor b6poc9d58. RATIONALE: per E-036 [[ch1-coherent-model-r3]] WSB gives ~1114 kg vs banked 949 on pair (118,171) = +165 kg → if it reproduces a valid official-fitness row >949, that's a GUARDED-BANK candidate for Ch1 trajectory (single transfer). This is the confirmed real Ch1 lever (tonight's no-go closed the impulsive alternative). **★ BANKED 01:52: WSB run reproduced 1114.2 kg on (118,171) (best of 12 seeds; others 853/995/618/362 — high variance, seed matters). Guarded-bank swapped that transfer into solutions/upload/trajectory.json: full-solution official re-score 228,107.782 → 228,272.585 kg (+164.8, valid; idD=108 uniqueness preserved; backup /tmp/bank_bak/trajectory_20260612_015201.json). NOT submitted (user-gated).** Proves WSB yields bankable Ch1 gains pair-by-pair → the multi-pair sweep is now clearly the top Ch1 build (per [[ch1-coherent-model-r3]] E-036: fleet +62k same-idD / +128k with re-match). NEXT BUILD (not a cheap tick, for next active session): a multi-pair WSB sweep DRIVER (the prototype is single-pair only) to get the fleet-gain estimate (+62k..+128k per memory), and/or the 3-D-matching local-search for matching-i/ii headroom (CP-SAT proven exhausted). Nothing submitted, no banks touched yet. **FLAG for user: the hung E-554 tree (PIDs 43596/43600/43601/43603/44710) is deadlocked at 0% CPU and can be reaped anytime to tidy up — I left it per "no kills".**
- 01:40 — **★ Ch1 IMPULSIVE-GEOMETRY PIVOT = NO-GO (decisive, empirical, oscillation CLOSED). WSB stays top of Ch1 queue; no repricing.** After E-566 returned null, I did the CORRECT cheap test: ran the PROVEN `try_transfer` (ch1_traj_proper_v2 6-D DC) directly on LEO pair (118,171) — **0 valid of 32 ICs at tof 5-8d AND 18-22d.** Then read the ACTUAL banked transfer for that pair: **tof=20.7d, split=0.97** (97% coast → tiny dv1 → capture dv2) = a long near-BALLISTIC-CAPTURE (WSB-family) arc, NOT a short impulsive Hohmann. The 6-D DC fails because it hard-codes split=0.5/symmetric mid-burn. **So the "corrected patched-conic bound ~1266 kg" was computed for the WRONG transfer family; the high-headroom LEO→Moon pairs are already best-served by long ballistic (WSB) transfers.** VERDICT: impulsive-geometry-first pivot NOT supported; 371k short-impulsive ceiling stands; the lever beyond it is WSB (already known). Action: the free-RAAN departure DOF should be folded INTO the WSB optimizer (E-036), not chased as a separate pivot. Did NOT launch E-567 (would only reproduce all-None at split=0.5); script kept as a record. memory [[ch1-coherent-model-r3]] updated with the empirical resolution (anti-oscillation: no 5th hand re-derivation). Cores: 3×E-554 (basin) + 1 free; no positive-EV quick job for the free core (WSB sweep is the real Ch1 lever but it's the heavy E-036 path, not a cheap tick — and per coherent-model it's multi-day human-grade work). Reporting decisive verdict to user.
- 01:20 — **TRIPWIRE RESOLVED + E-566 GO/NO-GO RETURNED NULL (not a verdict) + matching-i CP-SAT: no improvement.** (1) Killed the blind 960-grid (83520, was 82min blind) last tick; relaunched the EXISTING bounded harness scripts/ch1_e566_geom_gonogo.py (5 pairs, 70s/pair wall-budget, per-pair print, computes corrected-bound + GO/NO-GO verdict), niced/1-thread, monitor bo20necy3 streaming. (2) **E-566 result = NO-GO "no valid candidates" but this is NULL, not a physics refutation: 0 valid of ~560 trials INCLUDING banked pairs (277,189)=2628 kg and (261,205)=2567 kg where valid transfers PROVABLY exist.** Root cause (methodology trigger fired — 100% reject): the from-scratch crude-Lambert + 6-D DC (max_nfev=120, throttled under contention) can't thread the 99/30 km impact shells → every candidate returns None upstream of the arrival burn (clean run, no traceback). Latent bug: script L175 `dv2=res[0]` slices one component of the 3-vector. **GO/NO-GO UNRESOLVED; 371k-vs-486k ceiling stays OPEN; Ch1 queue UNCHANGED; no repricing, no bank touches, nothing submitted.** (3) **USEFUL FINDING — corrected-bound geography:** GEO→hi-e-Moon nearly saturated (277,189 banked/bound=2628/2735=96%; 261,205=2567/2736=94%); LEO→hi-e-Moon has the headroom (118,171 banked/bound=949/1266=75%, +33%; 0,150 bound 1086 unbanked). **Correct next experiment (replaces E-566): seed the PROVEN ch1_traj_proper_v2 Lambert+3D-DC solver (banks GEO at 96%) with geometric free-RAAN-perigee config on 2-3 LEO→Moon pairs — do NOT rebuild a fresh seed harness.** (4) **matching-i overnight CP-SAT DONE (7200s warm-start): obj=33338.184 = bank EXACTLY, valid, NO improvement.** Bound tightened 36514→34339.359 (gap 10.86%→2.92%, ~1001 kg proven-but-unrealized headroom); CP-SAT alone won't realize it. No guarded-bank (not strictly better). Cores now: 3×E-554 (multi-day GA, basin 228.9748) + 1 free (matching-i ended). No positive-EV job queued for the free core pending the LEO-pair experiment design. Escalating concise status to user (the Ch1 strategy question they asked).
- 00:35 — Health tick + **TRIPWIRE (soft): Ch1 GO/NO-GO mis-scoped/slow.** Validation PID 83520 now 55min, 72% CPU (throttled by oversubscription), STILL no output. Root cause: harness runs an exhaustive **960-trial grid/pair** (5 tof×6 raanE×8 eaarr×4 raanM) with least_squares(max_nfev=120 heyoka propagations) each, printing only after a full pair sweep → blind to progress, far heavier than a "cheap" GO/NO-GO needs. NOT killed (only signal source; may be far along). MITIGATION: E-554 ends ~01:10 (now 23:25 elapsed, ~30min left) → frees 3 cores → ~doubles validation speed, no kill needed. **DECISION RULE for next tick: if E-554 has freed AND validation still produces no output after ~30min un-throttled, KILL 83520 and relaunch a TARGETED single-seed test (one geometric seed per pair + DC + official validator, ~5 pairs in minutes) instead of the grid.** matching-i overnight CP-SAT healthy (PID 82637, 78/120min, 141% CPU). E-554 3 chains healthy (97% CPU, best 228.9748). No banks changed, nothing submitted. WSB sweep still HELD pending verdict.
- 00:09 — Cheap health tick (fallback; primary wake = waiter bar2hxhfu on the GO/NO-GO). **Ch1 geometric GO/NO-GO STILL RUNNING** (PID 83520, 29min, 70% CPU — slow only because 4 cores are oversubscribed ~5.2×: 3×E-554 + matching-i CP-SAT + validation; prints per-pair after each 960-trial sweep, so no output until pair-1 done). matching-i overnight CP-SAT healthy (PID 82637, 52min/145% CPU, ~68min of 7200s left). E-554 3 chains healthy (97.6% CPU, best 228.9748, end ~01:10 — ~1h). No banks changed, nothing submitted, no cores idle. DECISION: did NOT kill E-554 to speed the GO/NO-GO — it ends in ~1h anyway and the validation should finish first; oversubscription costs speed, not correctness. WSB sweep still HELD pending verdict. WATCH: waiter fires → read 5-pair realized-vs-bound table → GO/NO-GO on the Ch1 impulsive-geometry pivot.
- 23:40 — **★★ POTENTIAL CAMPAIGN-RESHAPING Ch1 TRAJECTORY FINDING (user asked for deep analysis of unexplored paths) — UNDER EMPIRICAL TEST, NOT YET ACCEPTED ★★** Deep analysis (agent aafe9d0) + my independent spec verification: **the official validator `_match_orbit` (reference/spoc4_udp/trajectory-matching.py L53-56) checks ONLY (a,e,i) tol 1e-6 at both ends — RAAN/arg-perigee/true-anomaly FREE; 3 impulses validated by FULL BCP PROPAGATION (not an impulse budget); ≤400 transfers (we use 301), per-transfer independent t0, cap min(mass,(200−ΔT)·c_ld).** Implication claimed: our ~371k "impulsive ceiling" is built on TWO self-imposed assumptions the spec never enforces — (1) LOI at APOAPSIS (933 m/s) vs PERILUNE-TANGENT/Oberth (377 m/s), (2) |iE−iL| plane-matching penalties (free RAAN lets departure stay in-plane, node at encounter). Corrected patched-conic fleet ceiling ~486k (vs bank 228k). Gap decomp: ~170k = per-pair ΔV waste on our 301 (median 56% of bound), ~88k = unused 99 idE × 99 idL (high-i LEO→LLO, mis-priced by plane-matching) + global re-match. **⚠ ANTI-OSCILLATION: LOI dv has now flip-flopped 3× (383→933→377). I REFUSED a 4th hand re-derivation — resolving EMPIRICALLY instead.** LAUNCHED bg agent aff0e6c8 (≤2 cores, READ-ONLY, light): build geometric-optimal seed (free-RAAN in-plane perigee + perilune-tangent LOI + existing 3-D DC + exact eccentric dv2) for 5 representative pairs incl. E-036's (118,171) and an unused high-i→LLO pair, score each through the OFFICIAL validator. **GO if median realized ≥85% of bound AND > current banked → PIVOT Ch1 from WSB-first to impulsive-geometry-first (projects 228k→390-435k = r5/r4 in 3-4 parallel days; trajectory is hard ×16/9 = the campaign's single biggest near-term points block, ~+8-9 pts). NO-GO → 371k stands, WSB stays top of Ch1 queue.** The risk under test = patched-conic bound may not be BCP-realizable (the twice-burned "static≠dynamics-feasible" lesson). **NO banks touched, nothing submitted. WSB sweep DEMOTED to pending the GO/NO-GO (was queued for E-554's 01:10 cores — HOLD until verdict).** ESCALATED to user (strategy pivot they explicitly asked about). Memory [[ch1-coherent-model-r3]] updated with the oscillation flag + pending resolution.
- 23:12 — **MATCHING AGENT a29f6689 TERMINAL (verdict: NOT a cheap CP-SAT win; banks untouched, nothing submitted).** Final table (warm-started CP-SAT): matching-i bank 33,338.184 = MILP best @600s, bound 35,396.64, **gap 5.82%**, r1 33,555.61 (+217 to r1); matching-ii bank 72,037.422 = MILP best @1200s, bound 83,013.06, **gap 13.22%**, r1 73,714.03 (+1,677 to r1). CP-SAT even hinted with our bank returned EXACTLY the incumbent on both → heuristic banks are strong incumbents CP-SAT can't beat fast from cold (un-hinted matching-i scored WORSE, 32,962). Headroom to r1 is REAL (true opt unproven, ≤bounds) but needs either hours of CP-SAT (more workers, post-01:10) OR a specialized 3-D-matching local search (LP-relax rounding + swap/chain augmenting moves). Reusable tooling: scripts/ch1_matching_milp.py (scorer + hint-capable CP-SAT). **ACTION: matching-ii solve freed a core → LAUNCHED overnight matching-i CP-SAT (bg bv8eo7ab2, 7200s/2 workers, warm-started from bank, guarded output /tmp/milp_matching_i_overnight.json — NEVER auto-banks/submits; I compare & guarded-bank only if valid & strictly >33,338).** Rationale: matching-i is top near-term ROI (×1, +217 kg = +0.65% to rank 1, ~rank 9→1 ≈ +8 pts), free core otherwise idle, zero dev risk. **NEXT-TICK DECISIONS:** (a) at 01:10 E-554 frees 3 cores (zero ROI, basin-trapped 228.97) → if overnight CP-SAT still stuck at incumbent, pivot matching-i to 4-worker run AND/OR build the 3-D-matching local search; consider matching-ii overnight too (×4/3, +1,677 to r1 ≈ +8 pts). (b) If CP-SAT proves it just can't move the incumbent, the local search is the lever (concrete, buildable). Methodology note: warm-started CP-SAT returning EXACTLY the incumbent = search can't find an improving move, not a bound issue → swap/chain local search is the indicated next tool.
- 23:05 — **★ MATCHING MILP RECON — KEY FINDING: matching-i has REAL HEADROOM (NOT optimal), top near-term ROI.** Agent a29f6689 built scripts/ch1_matching_milp.py (exact CP-SAT max-weight 3-D matching, score=Σw, e/l/d each ≤1; verified vs README toy: [1,0,1,..]→8.843 valid, [1,1,0,..]→invalid). **TRUE bank masses reconciled (official scorer): matching-i = 33,338.184 (valid, 4465 sel); matching-ii = 72,037.422 (valid).** matching-i MILP (warm-started from our bank) returned exactly 33,338.184 = bank (couldn't improve on the hint in its window) BUT my fresh cold 120s solve hit **bound=36,514.70, gap=10.86%** (cold obj only 32,550 @4302 sel) → **33,338 is a feasible point, NOT the optimum; the LP/CP bound (36,514) sits far ABOVE rank-1's 33,555.61.** So matching-i genuinely has slack: more warm-started solver time should cross 33,555 (rank 1) and beyond. **POINTS: matching-i = easy ×1, currently ~rank 9 (2 pts) → rank 1 = 10 pts → up to +8 pts for cheap compute — best near-term ROI on the board, comparable to the large rank-2 block (16 pts) but far cheaper.** matching-ii MILP (1200s, warm-started) STILL RUNNING (~15min in, 151% CPU, PID 82001); bank 72,037 vs r1 73,714 (~2.3% back) — will see if it beats bank. E-554 medium chains HEALTHY basin-trapped: c0 iter 12480 (accepts M5=1001/M1=156/M2=103), c1 11963 (M5=901/M1=159/M2=81), c3 12934 (M5=1267/M1=215/M2=147) — all >0, best 228.9748 unchanged (zero ROI, ends ~01:10). **DECISION: matching-i is now TOP of near-term queue. Plan: launch a long warm-started CP-SAT push on matching-i (hint=current bank, 4 workers, ~1h+) on cores as they free (matching-ii finishing now; E-554's 3 @01:10 if needed). Guarded bank only if valid & strictly >33,338; NEVER submit.** WATCH: matching-ii MILP terminal + agent final report.
- 22:25 — **★ DEEP REVIEW (classifier RESTORED — user switched fable-5→Opus 4.8, which cleared the ~4.5h outage; root cause was the fable-5 safety-classifier service, Anthropic-side, NOT local).** Cleared the entire deferred queue:
  **(1) LARGE bank INDEPENDENTLY RE-VALIDATED** — official kt.fitness on hard.kttsp = [1048.9786, 0, 0, 0, 0], is_feasible=True, n=1051, exc budget 5. Confirmed legit.
  **(2) LEADERBOARDS REFETCHED (per-user-best dedup — raw board lists each user's many submissions under repeated rank ints; true ranks below).** Our banks vs board:
  - large 1048.98 → **RANK 2** (only 5af55cab@424.62 ahead; their 1143.56 is the SAME user so the old "r2=1143.56" was a dup — true rank-2 line is f5a760fd@1153.9, we beat it). = **16.0 pts** (×16/9). r1=424.62 needs ~halved makespan (different architecture).
  - medium 228.97 → **RANK 4** (9.33 pts); gap to r3 line 216.95 = 12.03d. E-554 basin-trapped, needs new method.
  - small 116.38 → **RANK 6** (5 pts); gap to r5 line 111.79 = 4.59d. ×1 weight.
  - trajectory −228,108 → **RANK 6** (8.89 pts); gap to r5 line −372,729 = **144,621 kg**.
  - matching-i ~−33,338 → ~RANK 9 (2 pts); gap to r8 ~27 kg, to r3 cluster (~−33,490) ~152 kg. (mass est — official Ch1 scorer NOT in reference kit, reconcile deferred.)
  - matching-ii ~−72,000 → ~RANK 7 (4 pts); r1=−73,714, gap ~1,700 kg. (est, same caveat.)
  - **Provisional total if all submitted today ≈ 45.2 pts** (large 16 is the single biggest block; all currently 0 — nothing submitted).
  **(3) ★ WSB REPRICED DOWN (the gating analytic, now answered):** trajectory rank-5 wall = −372,729 kg. WSB fleet model adds +62k (current idD) to +128k (idD re-match) → bank 228k→290–356k, **STILL < 372.7k → STILL RANK 6 = +0 near-term points.** ZERO teams sit between our 228k and 356k. So WSB alone yields no trajectory rank gain before 06-30; reaching r5 needs WSB AND ~+16k more (impulsive polish + B1/B2/B3 +70k headroom). WSB remains the only physics-ceiling lever toward the −463k R3 region, but that's a multi-week, multi-lever build for points, not a quick win. **WSB is NO LONGER an auto-top-of-queue for near-term points.**
  **QUEUE NOW THIN/LOW-ROI across the board** (every next-rank move is either tiny-weight, basin-trapped, or a huge lift): large r2→r1 +1.78 (halve makespan, very hard); medium r4→r3 +1.33 (12d, new method); small r6→r5 +1 (4.6d, ×1); trajectory r6→r5 +1.78 (144k kg, multi-lever); matching-i/ii UNKNOWN (could be cheap easy-weight wins IF the ILP has slack — but needs the official Ch1 scorer, not in repo). **The cheapest-unknown, highest-info next step = reconstruct the Ch1 matching scorer + reconcile true matching-i/ii masses (compute-light) — may reveal easy +pts.**
  **CORES: classifier back → can launch. 1 idle now; E-554's 3 free ~01:10.** USER STEER (22:30): "matching recon first". **LAUNCHED bg agent a29f6689 (idle core): reconstruct Ch1 matching scorer (max-weight 3-D matching, score=Σw of selected, e/l/d each ≤1) → reconcile TRUE masses of matching-i/ii banks → CP-SAT/HiGHS MILP solve both (matching-i 25k transfers/limit 600s, matching-ii 92k/1200s, ≤2 threads) → compare to bank + r1 (i=33555.61, ii=73714.03 kg) → GUARDED bank only if valid & strictly better (backup .bak.milp), NEVER submit.** GOALS.md §5 sanctions MILP on Beginner. Our matching-i ~0.65% behind r1 → strong cheap-win candidate (easy ×1 but several ranks headroom). Banks all secure & re-validated. WATCH: agent report → true ranks + whether matching banked. Next big compute (E-554's 3 cores @01:10) decided AFTER recon reveals matching ROI.
- 21:50 — Cheap tick (classifier outage ~4h). E-554 c0: iter 10902→11773 (~17/min, sane), accepts M5=936 (>0 ✓), best 228.9748. Healthy. Banks unchanged. Deferred queue unchanged. E-554 terminal ~01:10 (~3h away).
- 21:19 — Cheap tick (classifier outage ~3.5h). E-554 c1 ckpt: iter=10947, accepts M5=826/M1=148/M2=68 (>0 ✓), best 228.9748. All 3 chains (c0/c1/c3) now verified healthy across 20:27/20:53/21:19 ticks. Banks unchanged. Deferred queue unchanged (reval → leaderboard → core recruit). Submission Q with user.
- 20:53 — Cheap tick (classifier outage ~3h, ps/curl still blocked). E-554 c3 ckpt: iter=11532, accepts M5=1148/M1=191/M2=135 (>0 ✓), best 228.9748 (basin trap, by design) — healthy. Banks unchanged (large 1048.9786 secure). Deferred queue unchanged: kt.fitness reval → leaderboard refetch → core recruitment (WSB prep). Submission Q with user.
- 20:27 — Cheap tick (classifier outage continues ~2.5h; ps/curl blocked, ls/tail intermittently pass). E-554 c0 ckpt: iter 10729→10902, accepts M5=868/M1=136/M2=81 (>0 ✓), best 228.9748 unchanged (basin trap, by design) — healthy, ends ~01:10. Large bank 1048.9786 + .bak.e562b confirmed on disk (mtime 19:47). DEFERRED (on classifier return, in order): (1) independent kt.fitness reval of large.json; (2) leaderboard refetch both challenges → verify r2 line + Ch1 A-categories + trajectory mid-board depth; (3) recruit E-562b's freed core (queue top: WSB sweep prep). Submission escalation for large is WITH USER. 3 cores busy, 1 idle-blocked.
- 19:56 — **★★ E-562b TERMINAL → LARGE BANKED 1536.3953 → 1048.9786d (−487.4d this run; −1176d total today, 2225→1049, −53%) ★★** Iter trace: 1394.63 → 1246.32 → 1158.54 → 1109.24 → 1054.72 → 1048.98 (iter5 +5.75d < 10d threshold → clean stop on diminishing returns). Script's own REVAL: mk=1048.9786 feas=True viols=[0,0,0,0] covered=True; backup written large.json.bak.e562b; guarded bank executed. **RANK CONTEXT (board as of 15:40): 1048.98 < r2=1143.56 → RANK-2 TERRITORY = 9×16/9 = 16.0 pts if submitted** (r1=424.62 unreachable with this structure). ⚠ My INDEPENDENT kt.fitness re-validation still PENDING — classifier outage (~2h) blocks Bash; run it FIRST THING when Bash returns, plus leaderboard refetch (board may have moved since 15:40). Tail at terminal: max 24.23d, >3d=38, sum>3d=219 → residual headroom exists (E-563 LKH-per-component on epoch-aware matrix is the queued next lever) but marginal-ROI now LOW until r1-structure question is answered. **ESCALATED to user: submission question for large (rank-2-capable, single largest points block of campaign).** Core freed by E-562b → cannot launch anything during outage (queue top = WSB sweep prep); will recruit when Bash returns. E-554 assumed healthy (ckpt last verified 18:13; re-verify on Bash return), ends ~01:10 → 3 more cores.
- 19:31 — Health tick (read-only; classifier outage ~100min, refetch still blocked — NOTE outage also blocks any new process launches; if it persists past E-562b terminal or 01:10 core-free, recovery plan = launch via background agents may also be blocked → just wait, banks are guarded). **E-562b iter4 = 1054.7239d (−54.5; rate ~50d/iter now), feas/exc=5/viols=0, solidly sub-r2 (1143.56); iter5 running.** Cumulative today: 2225→2182→1536→1054.7 in-memory (−53%). E-554 assumed healthy. Cores 4-on-4.
- 19:09 — Health tick (read-only; classifier outage ~80min, refetch still blocked). **★★ E-562b iter3 = 1109.2376d — BELOW r2=1143.56: in-memory large solution is RANK-2 TERRITORY ★★** (feas, exc=5, viols=[0,0,0,0]; tail >3d=38, sum>3d=213.9; max tof rose to 29.04 — one long leg traded in, mk still dropped). Iter deltas: −141.8/−148.3/−87.8/−49.3 (diminishing); iter4 mid-run (oa+segA no-change → expect smaller gains; r1=424.62 likely needs different structure per earlier analysis). Disk bank still 1536.40 — guarded write at terminal. ON TERMINAL: independently re-validate (kt.fitness) then ESCALATE submission question to user (rank-2-capable bank = ~16pts of the campaign's largest block; board may shift by 06-30 — frame submit-now-vs-window tradeoff). E-554 assumed healthy (re-verify when Bash returns). Cores 4-on-4.
- 18:47 — Health tick (read-only; classifier outage ~55min, leaderboard refetch STILL pending). **★ E-562b iter2 = 1158.5405d (−87.78) → IN-MEMORY RANK-4 TERRITORY (< r4-line 1186.65); iter3 mid-run ALREADY at 1153.9977 after segA — 0.098d above the rank-3 line 1153.90, with segB/oc/segC re-solves still pending this iter → r3 crossing highly likely; next target r2=1143.56.** Tail: max 11.94d, >3d=46, sum>3d=218.6. All feas/exc=5/viols=0. Iter deltas: −141.8, −148.3, −87.8 (diminishing but still large). Disk bank still 1536.40 (guarded terminal write; agent banks best at end). E-554 assumed healthy (last ckpt check 18:13, iter 9595 advancing; re-verify when Bash returns). Cores 4-on-4. Points view: large rank 4 = 12.44pts, rank 3 = 14.22pts, rank 2 = 16.0pts (×16/9 hard weight) — E-562b is delivering the single largest points block of the campaign for free.
- 18:13 — Health tick (read-only; Bash classifier outage persists 3+ tries since ~17:52 → leaderboard top-12 refetch STILL PENDING, retry next wake; decision it gates is at 01:10, no urgency yet). **E-562b iter1 = 1246.3236d (−148.31; rounds NOT diminishing: −141.8, −148.3), feas, exc=5, viols=[0,0,0,0], tail: max 15.42d, >3d=51, sum>3d=261.8.** Only 7.8d above the rank-6 line (1238.52); iter2 mid-run (segA building) — at current rate could cross r5 (1206.75)/r4 (1186.65), outside shot at r3 (1153.90). Disk bank still 1536.40 (guarded write at terminal). E-554 c0 ckpt iter=9595 (advancing), accepts M5=739/M1=112/M2=72 (>0 ✓), best 228.9748 unchanged. Cores 4-on-4, nothing to launch.
- 18:05 — **★ DEEP REVIEW (objective change) — REPRICED EVERYTHING IN POINTS.** Weights (GOALS.md §2, O-001 GraphQL-grounded): Ch1 matching-i ×1, **matching-ii ×1 (NOT 4/3 — Ch1 has two easy + one hard)**, trajectory ×16/9; Ch2 small ×1, medium ×4/3, large ×16/9; points=(11−rank)×w, ranks 1-10. VERIFY the A-category field at next API fetch (README prose says easy/med/hard per challenge; O-001 says A_1/A_1/A_3 for Ch1 — trusted provisionally). GOALS.md §1 root goal REWRITTEN to max-global-score; memory [[submission-policy-rank3]] superseded→points objective. **Provisional "if submitted today" (boards as of 15:40/16:56, depth>6 UNKNOWN — classifier outage blocked refetch, Bash+WebFetch both down): small r5≈6pts, medium r5-6≈6.7-8pts, large r7≈7.1pts, matching-i r4-5≈6-7pts, matching-ii r4?≈7pts (bank value unreconciled ~72k vs cluster 73.6-73.7k), trajectory r5+?≈≤10.7pts — TOTAL ≈40-45pts IF submitted; currently 0 (nothing submitted).** Marginal-ROI queue (pts/hour): **(1) E-562b large (FREE, running): each threshold crossed = +1.78pts/rank — 1238.52→r6, 1206.75→r5, 1186.65→r4, 1153.90→r3 (+7.11 cumulative); iter1 tail melted (max 15.42d, sum>3d 262), iter2 running, ~1280 projected → r5-r4 likely. (2) ⚠ NEW CRITICAL ANALYTIC (blocks WSB pricing): Ch1 trajectory MID-BOARD DEPTH (ranks 5-10 scores) — WSB +62-128k takes bank 228k→290-356k which is STILL <r4=458.9k; its POINTS value = # of teams between 228k and 356k, could be 0 ranks or 4 ranks. Refetch top-12 ASAP when classifier returns (cheap, do BEFORE committing the 3 freed cores at 01:10 — if 0-2 ranks, impulsive polish may outprice WSB hours despite the GO). (3) matching-ii reconciliation (compute-light, agent-able now-ish): true bank value unknown, cluster within 0.13% — cheap rank info. (4) small/medium: DEPRIORITIZED (small ×1 each rank; medium gap to next rank ~21d = expensive).** Submission framing for user: unsubmitted=0pts; recommend planning a submission WINDOW (user-only, e.g. near 06-30 with a buffer) — old info-leak rationale still argues against early reveals, but final submission before deadline is now mandatory for ANY points. NEXT: retry leaderboard fetch next tick; E-554 @01:10 core allocation now CONDITIONAL on trajectory mid-board depth.
- 17:48 — E-562b health (monitor heartbeat, NOT terminal — script alive PID 67507 @93%, 27min). **Sequential per-piece re-walk IS working: iter0 mk=1394.6329d (−141.76d vs 1536.40 start), feas, viols=[0,0,0,0], exc=5, big_jumps=0; heavy tail shrinking >3d 102→76, sum>3d 495→414.** Disk bank STILL 1536.3953 (guarded bank writes at terminal, no .bak.e562b yet — correct/safe). Iter1 underway (project ~1280-1320, then lower; 6-iter budget). 1394.63 in-memory still > podium-worst 1238.52 but closing toward rank-3 (1186.65). NO action (running healthy, don't relaunch). NOTE: agent "completed" notifications = its persistent monitor firing per-event, NOT script exit — trust ps/log. Cores still 4-on-4.
- 17:46 — **★ E-565 WSB PROTOTYPE → GO (the Ch1 R3 physics-ceiling gate is OPEN) ★ COMPILED → vault/experiments/E-036.** Single-pair (118,171) ballistic-capture under OFFICIAL fitness: **1114.2 vs 949.0 kg = +165 (+17.4%)**. dv=3448 (apogee raise +89 over Hohmann, midcourse 81, capture 473), ΔT=97.8d, v_inf≈1073 (floor 1283kg @v_inf=0 → headroom). **Decisive fix vs all prior WSB fails: optimize apogee midcourse vs alignment-aware capture proxy THEN EXACT solve_arrival_eccentric dv2 (Nelder-Mead); radius-only DC loses (841kg).** **Capacity discount does NOT invalidate L4 @ΔT≤120d:** fleet model +62k kg (current idD) / **+128k kg (idD Hungarian re-match)** — EXCEEDS coherent-model +60k L4 est. Gains decay +98k@80d→+13k@170d. Risks: lo-eL/LMO targets unproven (~half fleet gain, Tier-0 window), v_inf<600 not shown, n=1 success rate, +128k needs deferred B4/B5 idD re-match. **Sweep CHEAP: ~2-4min/pair, ~10-20 core-h, embarrassingly parallel.** No bank writes, solutions untouched. Memory [[ch1-coherent-model-r3]] updated (L4 now PROVEN not estimated). **★ QUEUE REPRICED — WSB full sweep is now TOP of Ch1 queue, AHEAD of impulsive per-pair DC polish:** it is the ONLY lever that breaks the ~371k impulsive ceiling toward R3 (463,513; bank 228,108≈49%). **CORE-RECRUITMENT PLAN UPDATED:** E-554's 3 cores @~01:10 → (1) WSB full sweep across the 114 LEO+high-eL bank pairs + (2) the lo-eL/LMO Tier-0 probe (de-risk the unproven ~half-fleet class) in parallel; impulsive polish (E-564 follow-ups) + 99×99 unused pairs slot behind. E-562b terminal (large) → its core also to WSB sweep if before 01:10. **USER-FACING: WSB GO is a strategic milestone — the lever the coherent model flagged as the make-or-break R3 path is now empirically proven; reported in status.** NOW RUNNING (4-on-4): 3×E-554 (until ~01:10, best 228.9748) + E-562b (large, PID 67507). No core free until E-562b terminal or 01:10.
- 17:19 — **E-564 (Ch1 ledger reconcile) DONE + COMPILED → vault/experiments/E-035.** Verdict confirms-weakly. **TRUE current Ch1 trajectory bank corrected: 227,653.57 kg / 302 transfers** (official udp.fitness; matches May-30 polish log — the "186-215k" belief was STALE/WRONG). Union table = bank + 15 caches → 6,154 distinct (idE,idL) pairs. **Tier2-heavy suspicion DISPROVED** (0 new/improved pairs; already merged pre-May-30). Two-stage Hungarian re-match → 301 transfers, all passed official validation iter-1, table==official (0% disagreement) → **BANKED 227,653.57 → 228,107.78 kg (+454.22)**: 8 tier1_light rows (9,045kg) + 2 hungarian_seeded (3,147kg) swapped for 11 weaker bank rows (May-30 rebank had never re-run after tier1-light v2). Backup trajectory.json.bak.e564; agent re-validated from disk. **⚠ INDEPENDENT heyoka re-eval by me DEFERRED (load 4.78, 4-on-4 oversubscribed) — verify at next low-load tick; backup safe, delta small.** **CH1 HEAVY PHASE NOW SCOPED (3 follow-ups for E-554's 3 cores @~01:10 tonight, all dv-bound/zero capacity-capped → lower-dv is the only lever):** (1) 99 unused idE × 99 unused idL via Lambert+DC pipeline [[ch1-lambert-dc-solver]]+[[ch1-eccentric-orbit-fix]] NOT SADE (pygmo log 70 = 0 valid); (2) ~1,300 tier-1 pairs await 3-impulse DC polish (tier-2 heavy truncated ~200/1500 in log 75, historical 5-30%/pair gains); (3) per-pair re-solve headroom confirmed real (some banked pairs <56% theoretical mass). Context vs R3: bank 228,108 ≈ 49% of R3-cutoff 463,513 (tightened 06-11), gap ~235k — reconciliation NOT the lever; WSB (E-565) is the physics-ceiling gate beyond impulsive ~371k. **E-562b heartbeat: oa re-solved clean (big_jumps=0, 16.3d) but assembled mk UNCHANGED — oa already optimal at its epochs AND it's the head, so fixed-endpoint internal reorder can't shift makespan; real gains must come from segB/segC.** NOW RUNNING (4-on-4 clean): 3×E-554 (until ~01:10, best 228.9748 unchanged) + E-562b (large, PID 67507 @91%). E-565 WSB proto still developing (GO/NO-GO ~06-16). No core free until E-562b terminal or E-554 @01:10.
- 17:12 — **★ LARGE BANKED 2182 → 1536.3953d (−645.6d this round; −688.77d total from 2225) ★** E-562 epoch-aware converged 3 monotonic iters (1957→1732→1536). Independently re-verified: kt.fitness viols=[0,0,0,0], feasible, 1051 nodes, dV=3151; backups large_2225/.bak.e561/.bak.e562 all intact. Heavy tail collapsed: legs>3d 220→102, sum>3d 1040→495. **COMPILED → vault/experiments/E-034** (full arc 2225→1536, the proxy⊥reality→epoch-aware finding); memory [[ch2-large-first-bank-topology]] updated. RANK: 1536 still > podium-worst 1238.52 (~rank7) but gap collapsed 5.2×→1.24× leader-distance → **large is now the campaign's most likely rank-3 (vs high-risk Ch1 WSB)**. Core freed by E-562 → **launched E-562b (agent abe69587, scripts/ch2_e562 base): more iters (project 1300-1450) + sequential per-piece re-walk (fix 1-round epoch lag) + retune splits 267/508; guarded bank <1536.3953, backup .bak.e562b**, target cross 1238.52 then 1186.65. Decision rationale: Ch1 heavy sweep not yet defined (E-564/E-565 still prototyping, compute-light), so freed core's best EV = near-certain large rank-3 push; E-554's 3 cores tonight → Ch1 sweep. NOW RUNNING: 3×E-554 (until ~01:10) + E-562b (large) + E-564 (Ch1 ledger) + E-565 (Ch1 WSB proto). Watch load next tick (4 hw cores, 5 logical jobs but 2 Ch1 are light). E-554 best 228.9748 unchanged. **USER GREENLIT Ch1 trajectory-R3 development ("trajectory 3"), processes-finish + core-recruitment policy.** LAUNCHED two compute-light Ch1 agents (dev work now, sweeps when cores free): **E-564 (agent ae4307f1) = bank-ledger reconciliation + re-matching** — evaluate exact official mass of banked trajectory.json, union per-pair table (tier2_heavy_results.json + caches), re-solve assignment, guarded bank (.bak.e564, only if official mass improves), report stale/unused counts for the heavy phase; log runs/ch1/76_e564_ledger_rematch.log. **E-565 (agent a9a012ef) = WSB single-pair prototype (the R3 gate)** — official-dynamics ballistic-capture trajectory on one favorable pair vs impulsive baseline, MUST evaluate the (200−ΔT)·c_ld capacity discount (WSB's 90-120d TOF may cap gains — this check can invalidate L4), GO/NO-GO by ~06-16; log runs/ch1/77_e565_wsb_prototype.log; prototype-only, no bank writes. E-563 name RESERVED for Ch2 LKH-per-component (post-E-562). Running: E-562 iter2 (best 1732.27), E-554 3ch (until ~01:10). 4 pegged cores + 2 light dev agents = acceptable brief oversubscription, watch load next tick.
- 16:56 — Health tick (Bash restored). **E-562 epoch-aware ITERATIONS COMPOUNDING: iter0=1957 → iter1=1732.27 → iter2 in progress** (best 1732.2706 feas, heavy tail keeps shrinking: legs>3d 185→133, sum>3d 925→664; max tof rose 18.49→28.11 = a few legs traded longer, net big win). Banks BEST-so-far at terminal (large.json still 15:38=2182, .bak.e562 not yet created → not banked yet; agent re-validates kt.fitness+is_feasible+coverage+atomic replace, logic verified sound). Worker PID 58664 @94% alive, log fresh (16:54), E-554 3ch healthy, load 4.00. **CH1 LEADERBOARD REFRESHED (user asked Ch1 next-options; correct challenge id = spoc-4-luna-tomato-logistics): TRAJECTORY R3 cutoff TIGHTENED to 463,513 kg TODAY 06-11 (was 452,820 on 05-29, +10.7k) — MOVING TARGET, competitors actively pushing; r1=473,333 r2=471,941 r4=458,897. Our bank ~186-215k ≈ 40-46% of R3, gap ~250-277k. MATCHING-I: our 33,338 is BELOW r3=33,494.89 by ~157kg (0.5%, ~rank4-5, NOT yet r3). MATCHING-II top cluster 73,620-73,714 (r1 73,714); our bank needs RECONCILIATION (research agent reported a garbled 33,320 — wrong scale; true ~72k).** Implication under rank-3 gate [[submission-policy-rank3]]: Ch1 trajectory is the CAMPAIGN CRITICAL PATH (largest gap, +250k incl. mandatory ~2wk WSB build) — flagged to user with priced options (A ledger-reconcile 0.5d / B L1-matching 2-3d autonomous / C WSB-prototype-gate needs USER decision this week / D B1-apolune 1wk). No core free to launch Ch1 A/B yet (4-on-4); queue them when E-562 frees a core or user oks E-554 kill. E-554 basin-trap unchanged (best 228.9748). No bank delta yet this tick.
- 16:30 — Health tick (Bash blocked by classifier outage → read-only checks via Read tool; resume ps/load checks next tick). **E-562 epoch-aware WORKING AS DIAGNOSED: iteration 0 → mk=1957.0060 feas viols=[0,0,0,0] NEW BEST (−225.0d vs 2182.01!), heavy tail genuinely shrinking (legs>3d 223→185, >5d=36, sum>3d 1118→925.4, max 18.49)** — epoch-aware costs fixed the proxy/reality mismatch E-561 hit. Iteration 1 mid-run (segA proxy 222.7d, all pieces big_jumps=0 so far; cost builds now ~25s/matrix vs 130s — agent reuses epochs efficiently). Note: realized mk 1957 still ≫ proxy sum ~582 → proxy/reality gap remains, expect diminishing iterations; terminal report will say if it converges. Guarded bank is agent-side; I re-verify at terminal. E-554 alive via ckpt (c0 iter 7952→8640, accepts 834, best 228.9748 — basin-trap unchanged). Large bank ≥1957 candidate pending agent bank; backups intact per agent protocol. Rank context: 1957 still > worst podium 1238.52 → epoch iterations + E-563 (LKH-3/Concorde per component on epoch-aware matrix) queued next. No user actions pending (submission policy = rank-3 gate; E-554-kill offer open).
- 15:55 — **USER DIRECTIVES (resolve standing escalations): (1) SUBMISSION POLICY = submit NOTHING before rank ≤3 on ALL challenges** → all submit-or-hold Qs resolved to HOLD (large 2182/small 116.38/medium 228.97 all held; saved memory [[submission-policy-rank3]]; stop escalating sub-rank-3 submission Qs; keep banks envelope-ready). Implication: medium's 28d gap to r3 (207.39) and small's 7.6d gap to r3 (108.77) are now GATES, not nice-to-haves — repricing at next deep review. **(2) User asked about the "clustered structure" insight ("ch3-large" = slip for ch2-large; ch3=Luna Tomato, no such analysis)**: O-015 4-component star + O-014 TGMA-inference (cluster-decomposition + EXACT per-component solvers LKH-3/Concorde + smart bridging → their 1143→424 jump). Status: the decomposition half is ALREADY exploited (comp0-last topology IS it); the UNUSED half = solver strength (exact Concorde on the three 150-smalls, LKH-3 on comp0-601). SEQUENCING per E-561's lesson: cost model is binding, not solver — land E-562 epoch-aware matrix FIRST, then **QUEUE NEW: E-563 = LKH-3/Concorde per component ON the epoch-aware matrix** (post-E-562, top of large queue). KEY REFRAME from TGMA's 424d (≈0.40d/leg): short tours never reach expensive late epochs → virtuous circle (shorter legs→earlier epochs→cheaper transfers) → our 1450-1700d "median floor" (derived from CURRENT walk epochs) is likely PESSIMISTIC; iterated epoch-aware re-solving may converge below it. E-562 agent (a3de4e9e) running; E-554 3 chains running (kill offer still open with user, unanswered).
- 15:40 — **★ LARGE BANKED 2225.1654 → 2182.0087d (−43.16d) ★** (E-561 done). Independently re-verified by me: kt.fitness mk=2182.0087, feasible=True, viols=[0,0,0,0], dV=3151; backups intact (large_2225.json=2225.1654 feas, large.json.bak.e561). Topology preserved (5-bridge comp0-last, big_jumps=0, all 1051, 5/5 exc). **DEEP REVIEW (banked trigger)**: leaderboard refetched — LARGE r1=424.62 r2=1143.56/1153.90 r3=1186.65/1206.75/1238.52; our 2182 still > worst podium (1238.52) ⇒ **STILL ~rank7, NO rank change** (gain was mechanical, heavy tail barely moved: legs>3d 220→223, sum>3d ~1040→1118, max tof 17.69→18.22). SMALL r1=101.65 r3=108.77/110.88 (our 116.38 gap~7.6d); MEDIUM r1=195.68 r2=199.74/200.60 r3=207.39 (our 228.97 gap~28d). **ROOT CAUSE of modest gain (well-diagnosed, methodology=audit-evaluator): cost matrix scored edges via find_earliest_transfer at 4 FIXED ref times = optimistic time-INDEPENDENT LB, but the chrono walk hits t~1500-2200d by leg 500+ where real cheap transfers are far longer → GLS minimized a proxy only loosely correlated with realized walk-tof.** **NEXT LEVER (launched): EPOCH-AWARE cost matrix** — score edge (i,j) at the true epoch the walk reaches node i (extract per-node arrival epochs by walking current order, then re-score + iterate). Launched bg agent a3de4e9e (E-562, scripts/ch2_e561 base → epoch-aware build_cost, guarded bank <2182, backup .bak.e562, log runs/ch2_e562_large_epoch_aware.log) on the core E-561 freed → 4-on-4. E-554 healthy basin-trap (c0/c1/c3 advancing, best 228.9748, no sub-bank). **PENDING USER (submission trigger, now sharper): large 2182 is submission-ready but still rank~7 — submit to get off zero, or hold for the epoch-aware push toward rank4-6? + small 116.38 (~rank5) / medium 228.97 (~rank5) submit-or-hold. + E-554-kill offer (0-EV) still open.**
- 15:35 — **E-561 tof-aware: ALL 6 pieces solved, big_jumps=0 on every one — walk+bank verdict IMMINENT** (segC=final 93-node comp0 tail building, then assembler walk). PID 57425 @99% CPU, log fresh (15:34), clean 4-on-4 (load 4.03). Segment path_cost proxies: oa/ob/oc smalls=7.5d each; **segA(267)=69.7d (pen=1); segB(241)=104.4d big_jumps=0** ← the previously-FAILING middle comp0 segment now solves clean with seeding; segC(93)=ATSP pending (pen=4). ⚠️ **DO NOT over-read these proxies** — path_cost~ is a STATIC-grid tof estimate (the twice-burned "static≠time-feasible" lesson); also penalty_edges (segA 1, segC 4) carry uncounted-in-proxy cost, and assembler may use walk_perm_chrono which previously hit the "banked perm not walkable" blocker. REAL makespan = the assembler's chrono walk at true arrival times ONLY. IF it banks feasible <2225.158 → guarded bank (backup large_2225.json preserved), then DEEP REVIEW (banked trigger): refetch leaderboard, recompute large gap/rank, update memory. IF walk rejects → likely the walker-reproduction blocker, not the tof order; next lever = route the new order through proven /tmp/assemble_e559.py. E-554 healthy basin-trap (c0=7952+/c1=7641+/c3=8145+, accepts climbing, best 228.9748, no sub-bank). Large bank INTACT 2225.158. No bank delta yet. Submission Qs + E-554-kill offer still pending with user.
- 15:13 — Health tick, clean 4-on-4 (load 4.43). E-561 tof-aware re-order (PID 57425, ~10min into the SEEDED restart) ALIVE @98% CPU, log fresh (15:12): **oa small solved obj=7450, path_cost~7.5d, big_jumps=0, penalty_edges=0** (the short-tof signal holds); now on **segA** (comp0 0:267) — cost matrix built (267×267, forbidden=58.8%, 73 penalty edges), ATSP solve next. No terminal verdict yet (assembler walk + bank still pending; don't over-read per-segment path_cost). Large bank INTACT (large.json mk=2225.158, dV 3151, backup large_2225.json present). E-554 basin-trapped but HEALTHY: c0=7952/c1=7641/c3=8145, accepts 763/681/1102 (>0 ✓, climbing), best 228.9748 all (no sub-bank — single-mut +11d uphill, expected). No bank delta. Submission Qs + E-554-kill offer still pending with user. WATCH: E-561 terminal report (new makespan + did heavy-tail shrink + banked?).
- 14:46 — Health tick. Tof-aware re-order agent (acd7da6e, E-561) ALIVE & progressing (subproc @98.6% CPU, ~7.5min): building tof-weighted cost matrices + per-segment open-path ATSPs. EARLY SIGNAL promising — a 150-small solved to **path_cost~7.5d, big_jumps=0, penalty_edges=0** (much shorter than the old order's heavy-tail legs), now on segA (comp0 0:267, cost row 100/267@70s, O(n²) find_earliest_transfer build is the slow part). Real makespan awaits the proven assembler's actual walk — don't over-read the proxy cost. Large bank INTACT (large.json=2225.158, backup large_2225.json, dV len 3151, no regression). E-554 healthy: c0=7606/c1=7375/c3=7827, accepts 741/651/1068 (>0 ✓), best 228.9748, ckpts fresh. Load ~4-on-4. No bank delta. Submission Qs + E-554-kill offer still pending with user. WATCH: agent report (new makespan + did heavy-tail shrink + banked?).
- 14:18 — **Large improvement agent (E-560) → ROOT CAUSE of the 2225d isolated**. Decomposition (methodology audit-before-search, paid off): makespan = **sum-of-1050-tofs EXACTLY, IDLE=0.0d** (greedy assembler already departs on arrival — schedule is locally tof-optimal; re-timing recovered only 0.0073d → banked 2225.1654→2225.1581, backup large_2225.json). The 5 exc bridges = only **14.28d** (NOT the driver). The cost is **1045 cheap intra-comp legs = 2210.9d**, median tof 1.39d but HEAVY TAIL (top legs 7–22d). ⇒ **The makespan is the sum-of-tof of THIS particular Hamiltonian ordering**, and the OR-Tools order minimized cheap-EDGE-COUNT (uniform cost=1 → 0 big-jumps), NOT tof. So we found *a* cheap-Hamiltonian, not a *short* one. **LEVER = tof-weighted re-ordering**: re-solve comp0 (+smalls) ATSP with cost[i][j]=tof-if-cheap-else-BIG (min sum-of-tof among 0-big-jump orders), respecting gateway-at-split + dead-tail-at-terminus constraints, then feed the new order through the PROVEN assembler /tmp/assemble_e559.py (sidesteps the walker-reproduction blocker E-560 hit: greedy walk_perm_chrono can't rediscover the assembler's bridge tofs). Median-floor est ≈1045×1.39≈1450d → realistic target ranks ~4-6 (R3=1153.9, then 1186/1206/1238); leader 424 likely needs different structure. **LAUNCHED agent ac… err new agent for tof-aware re-order on the freed core.** E-554 still 0-EV (kept; awaiting user ok to kill for cores). Submission Qs still pending with user (large 2225 / small 116.38 / medium 228.97). No bank delta beyond the 0.0073d re-time.
- 14:08 — **★ FIRST FEASIBLE LARGE BANK ★** (was ZERO). Agent a1e35ac6 SUCCEEDED → solutions/upload/large.json, makespan **2225.1654d**, 5/5 exc, **independently re-validated by me** (kt.fitness → is_feasible=True, viols=[0,0,0,0], envelope challenge/problem correct, decisionVector len 3151=3·1051−2). **BREAKTHROUGH = a topology change**: abandon leaf-leaf (needs 2 comp0 gateway-endpoints, impossible at 0-big-jumps) for a **5-bridge comp0-LAST** chain `small3→comp0[0:267]→small2→comp0[267:508]→small1→comp0[508:601→931(dead-tail)]` — the dead-tail node 931 (no exc edge to any small) lands at the GLOBAL TERMINUS so it needs no bridge; uses exactly 5 bridges + 0 internal exc, fits budget because comp0 order has 0 big-jumps. Only leaf assignment (a3,b2,c1) with split points 267/508 has all 4 boundary nodes = gateways. Smalls re-solved as endpoint-constrained open-path ATSP. Saved memory [[ch2-large-first-bank-topology]]. **DEEP REVIEW (banked trigger)**: leaderboard refetched — Large top scores 424.62/1143.56/1153.9/1186.65/1206.75/1238.52 → our 2225 ranks ~7th (worse than top-6) BUT pure upside (had nothing) + ~5× headroom to leader + now in the compute-friendly improvement regime ([[ch2-compute-parallelization-roi]]). Small R1=101.65/R3≈108.77 (AC_TUWien now tied 108.77 r2/r3); Medium R1=195.68 (06-10)/R2=199.74. **CORE FREED** (agent done, load→3.34) → allocating to LARGE makespan improvement (top-EV): launching agent to (1) decompose 2225d (tof vs idle-wait vs 5 bridges — audit before search), (2) feasibility-preserving reduction, (3) guarded bank on improvement. **ESCALATING to user (submission trigger)**: first large bank is submission-ready (gets us off zero, ~rank7) — submit now or hold+improve? + still-pending small 116.38/medium 228.97 submit-or-hold. E-554 healthy (c0=7214/c1=6995/c3=7424, best 228.9748, 0-EV per ROI — flagged: reallocate its 3 cores to large if improvement wants them). No medium bank delta.
- 13:50 — **THE CRUX is now explicit: coverage ⊥ bridge-feasibility tension**. Assembly agent (a1e35ac6, alive, subproc 53173 @98% CPU) tried a GATEWAY-ENDPOINT-constrained comp0 order (force start/end nodes = ones with exc edges to start_small=1 / end_small=3 so the bridge IS time-feasible) → bridge now satisfiable BUT comp0 coverage collapsed to **159/601** (vs the free OR-Tools order's 441/601 that can't bridge out). So: free cheap-Hamiltonian covers well but exits at a non-gateway node/time (bridge infeasible, dies leg 150); gateway-pinned order bridges fine but wrecks coverage. **This tradeoff IS the large first-bank problem** — need an order that BOTH covers ~all 601 AND terminates at a gateway node at a bridge-feasible time. Agent iterating; will notify. (Candidate levers if agent stalls: (a) OR-Tools with a soft endpoint-reward instead of hard pin; (b) cover comp0 freely then append a short cheap detour to a gateway node; (c) pick the start_small/end_small whose gateways best match comp0's natural cheap-Hamiltonian endpoints.) E-554 healthy: c0=7111/c1=6878/c3=7292, accepts 697/609/993 (>0 ✓), best 228.9748, ckpts fresh. No bank delta. Cores 4-on-4 (agent holds 4th).
- 13:29 — **THE WALL is the BRIDGE, not coverage**. Assembly agent's latest e559 attempt: assembled a COMPLETE 1051-perm (missing=0 — coverage solved!) but chrono walk **dies at leg 150, exc_used=0** → no split produced feasible bank. Diagnosis: leg 150 = end of the start-small (150 nodes); failing with 0 exc used means the FIRST small→comp0 exc bridge is TIME-INFEASIBLE at that small's exit time. This is the SAME wall as E-557 stitch-probe (comp0↔small bridges 0/240 feasible at the exit times the sub-tours happen to produce). So the real lever now = **make a bridge feasible by controlling the EXIT TIME / exit-node (anchor) of each segment**, not coverage. OR-Tools subproc done; agent presumed mid-reasoning (no CPU proc; load→3.85=3×E-554), will notify. E-554 healthy: c0=6936/c1=6657/c3=7074 advancing, accepts 671/594/978 (>0 ✓), best 228.9748, ckpts fresh. No bank delta. NEXT: await agent report; if it can't crack the bridge-timing, next lever = co-optimize anchor exit-node × exit-time so SOME (small-exit, comp0-entry) pair has a feasible exc transfer (probe the find_earliest_transfer grid at the actual walk arrival time, not a fixed t).
- 13:18 — **OR-Tools fork showing PROMISE + cleaned an orphan**. Assembly agent a1e35ac6 progress (runs/ch2_e559_large_assembly.log): cached OR-Tools order of comp0 (=label 2, the 601-set; agent correctly self-corrected my "labels==0" → actual size-601 is label 2) time-validated walk = **covered 441/601 with internal_exc=0** — far better than greedy's 329 single-pass, confirms global Hamiltonian ordering beats myopia. 160 stragglers remain; agent now re-running OR-Tools (PCI+GLS, 540s limit, subproc 52198) to push big_jumps<=1 / shrink stragglers. Banked NOTHING (comp0 incomplete) ✓ guard working. **ORPHAN KILLED**: old completed agent aea7c151 had RELAUNCHED a 2nd e558 multipass (PID 50744, reparented to init) → 5 procs on 4 cores (load 5.07). Killed 50744/50741/tail; verified my assembly OR-Tools subproc (52198) survives. Core load back to 4-on-4 (3×E-554 + 1 agent). Stale aea7c151 notifications = its lingering monitor on the killed e558; harmless, no task in TaskList to stop. WATCH: agent report — if OR-Tools gets comp0 to <=~150 stragglers, K=2 with 1 free exc still won't cover 160 in one pass; likely next lever = OR-Tools order + targeted straggler-insertion or accept K=2 partial+small mop-up. E-554 healthy (last: 6643/6412/6736, best 228.9748). No bank deltas.
- 13:00 — **e558 KILLED (greedy K confirmed too large, ~6-7)**: pass0=329/601, pass1(start=496)=only 70/272 stragglers → ~70 nodes/pass → K≈6-7 ≫ budget ceiling K≤2. Greedy is a dead end (myopia, settled). 1 core freed; load back to 4-on-4. **LAUNCHED the consolidated large-assembly agent (bg, id a1e35ac6) — the OR-Tools fork**: (1) build comp0 static cheap cost matrix (601 nodes, labels==0 in /tmp/ch2_e533_large_adj.npz), OR-Tools 9.15 ATSP (PATH_CHEAPEST_ARC+GLS, ~120-300s) for a GLOBAL Hamiltonian order minimizing non-cheap jumps (defeats greedy myopia); (2) VALIDATE against real time-dependent walk via find_earliest_transfer (ch2_findtransfer_greedy.py:29) — static cheap ≠ time-feasible, the twice-burned lesson, TRUST THE WALK; (3) repair stalls by reorder/widen-window, else spend the 1 free comp0 exc (K=2); (4) assemble 1051 tour with **leaf-leaf routing** (4 bridges, frees comp0's 1 exc); (5) FIX e557 anchor "0 tried positions" bug; (6) triple-guard bank (walk_ok ∧ all 1051 covered ∧ kt.is_feasible — copy e556 lines ~216-248, backup first). log runs/ch2_e559_large_assembly.log. If it banks → first large bank ≈ rank 3 (any feasible <1153.9d). E-554: c0=6643/c1=6412/c3=6736 advancing, ckpts fresh (12:54-12:56), best=228.9748 all (high-T explore, low-EV/28d-gap, healthy). No bank deltas. NEXT: await agent report; babysit E-554 each tick.
- 12:52 — E-557 ran, NO bank (comp0 pure-cheap single-start=329/601; anchor bug comp1/comp3 "0 tried positions"). Agent aea7c151 (done) left e558 multipass running (pid 49911, --max-passes 12) measuring **K = min pure-cheap passes to cover comp0 601**; pass0=329, remaining 272. KILLED a redundant 2nd agent (a155b7d6) I'd launched — duplicated e558 + risked 5-on-4 core contention. Core load now CLEAN: 3×E-554 + 1×e558 = 4 on 4. **STRAGGLER VERDICT (settled): recoverable greedy MYOPIA, NOT fragmentation** — every straggler has cheap in-edges (26k internal cheap edges). **BUDGET↔K (with my 4-bridge insight)**: comp0 needs (K−1) exc joins to chain K cheap passes; smalls need 4 bridges via **leaf-leaf routing** (start comp1→comp0→comp2→comp0→comp3-end = 2 terminal leaves) or 5 via comp0-start. Leaf-leaf: (K−1)+4≤5 → **K≤2 BANKABLE**; comp0-start needs K=1. So: K=1→clean star banks; **K=2→4-bridge leaf-leaf banks (the lever the 1st agent missed)**; K≥3→OR-Tools cheap-Hamiltonian comp0 (9.15 confirmed avail) to force K=1. NEXT: read K from e558, then launch ONE assembly agent with correct routing. E-554: c3=6608 advancing, c0/c1 ckpt within 600s write-interval (CPU 99.6% ✓), accepts 627/559/923, best=228.9748. No bank deltas.
- 12:26 — Both HEALTHY. E-557 (PID 48271) still phase[1] comp0 pure-cheap ~28min — O(n²) Lambert on 601 nodes, slower than agent's 16min est but max_exc=0 (no exc double-scan) → will finish; no cache yet, no partial, no tripwire (CPU-bound bounded greedy). Expect cache /tmp/ch2_e557_subtours.json ~12:45, then smalls→anchors→walk→bank (fast). E-554: c0=6321/c1=6131/c3=6305 (advancing), accepts 603/544/888 (>0 ✓), best=228.9748. c3 ckpt age=602s = AT 600s write-interval edge but iter advanced 6114→6305 → NOT stale (per cross-check rule, don't trip on single boundary-age ckpt). No bank deltas.
- 12:03 — Agent aea7c151 REPORTED (verdict: stitch FIXABLE) + left construction running (now MY babysit). **STRUCTURAL INSIGHT (large exc topology = STAR centered on comp0)**: ~8000 exc edges comp0↔each small, smalls MUTUALLY DISCONNECTED (0 exc edges between 150-comps). ⇒ only viable routing is interleaved detours FROM comp0: comp1 (out+back=2 exc) + comp2 (out+back=2) + comp3 TERMINAL (out only=1, tour ends in comp3) = **exactly 5 exc, fits budget**. E-556 failed because it concatenated comp0-full→comp1→… forcing bridges from comp0's bridge-DEAD greedy tail w/ approx timing. comp0 cheap subgraph dense (out-deg min6/med152/max163, 0 dead) → pure-cheap 601/601 likely; gateway pools rich (~160 launch + ~160 receive nodes/small). Built scripts/ch2_e557_large_bridge_aware.py (star detour-insertion: comp0 spine + per-small real-time anchor selection + triple-guard bank). RUNNING PID 48271, phase[1] comp0 pure-cheap (max_exc=0, faster than exc2 — no double-scan), caches /tmp/ch2_e557_subtours.json then smalls→anchors→walk→bank. log runs/ch2_e557_large.log. If it banks ANY feasible 1051-tour → first large bank ≈ rank 3. E-554: c0=6117/c1=5950/c3=6114, accepts 578/528/854 (>0 ✓), ckpts fresh, best=228.9748. No bank deltas.
- 11:43 — Both HEALTHY; agent aea7c151 working well (instrument-before-search guard HOLDING — no blind 75min build). STITCH PROBE VERDICT: comp0's 590-node sub-tour walks ok, exits at **t=328.2d** (2 internal exc); probed 240 exit×entry pairs comp0→comp1 AND comp0→comp2 → **cheap=0, exc=0** (NO feasible bridge even @dv600). Strongly suggests the [601-anchor-first] ordering is HARD-INFEASIBLE to bridge out of at t=328d (local geometry/timing, not budget — 328d « R3 1153d so time isn't globally tight). Agent pivoted to topo_probe (pure-cheap per-comp coverage, multi-start/anchor) = the correct fork. Hypotheses for fix: visit comp0 LAST, or a 150-comp first, or interleave rather than monolithic comp0 traversal. Agent will report. E-554: c0=5909/c1=5766/c3=5914, accepts 560/507/815 (>0 ✓), ckpts fresh, best=228.9748. No bank deltas.
- 11:15 — E-556b FINISHED (failed-to-bank). exc=2: comp0 590/601 (vs exc1's 577 → +13 nodes, diminishing), assembled 1040/1051, walk DIED at leg 590 = **SAME comp0→comp1 stitch boundary**, exc_used=2/5. CLEAN EMPIRICAL CONFIRMATION of the compute-ROI analysis: more exc/exploration moved coverage marginally but did NOT touch the structural stitch wall. exc=3 would be WORSE (eats a 3rd exc internally, fewer left for stitching). TWO blockers, both ARCHITECTURE not compute: (a) comp0 single-start greedy starves last ~11 nodes; (b) THE STITCH — order_components (e556 line ~106) estimates 0 bridges using APPROX timing (t_start+sum(tofs)) but real chrono walk at true arrival finds NO feasible cheap/exc transfer comp0-last→comp1-first. DECISION (methodology: audit structure before search): NOT relaunching exc=3. Dispatched bg agent aea7c151baeb05f7b → INSTRUMENT stitch first (probe exit×entry×time for ANY feasible bridge; if hard-infeasible STOP+report the [601-anchor]-ordering fork, else build bridge-aware assembly: real-timing bridge selection + multi-start comp0, triple-guard bank kept). New scripts ch2_e557_stitch_probe.py / ch2_e557_large_bridge_aware.py, log runs/ch2_e557_*. Runs on the 1 freed core; E-554 keeps 3. E-554: c0=5623/c1=5492/c3=5696, accepts 528/474/787 (>0 ✓), ckpts fresh, best=228.9748 all. No bank deltas.
- 10:51 — Both HEALTHY. E-556b (46747) 67min, 99.6% CPU, comp0(601) STILL building — exc=2 each cheap-miss now ALSO does an exc full-scan before giving up → ~slower than exc1's 70min, expected (more exploration/node = the point of exc2). NOT a hang (bounded greedy, no numba). Partial file is STALE (09:26 = old E-556, ignore). large.json absent (correct). E-554: c0=5433/c1=5291/c3=5369, accepts 504/457/748 (>0 ✓), ckpts <500s fresh, best=228.9748 all. No bank deltas. DECISION RULE: if comp0 unprinted by ~11:25 (~100min), keep waiting (terminating) — but flag for tonight a smarter comp0 builder (multi-start) since exc2 single-start is costly. Routine.
- 10:25 — Both HEALTHY. E-556b (46747) 41min, 99.6% CPU, comp0(601) still building (exc=2 → more exc double-scans → slightly longer than exc1's ~70min; expect comp line ~10:54-11:10). No partial/large.json. E-554: c0=5133/c1=5122/c3=5147, accepts 474/447/718 (>0 ✓), ckpts fresh, best=228.9748 all (high-T explore). No bank deltas. Routine.
- 10:04 — Both HEALTHY. E-556b (46747) 20min, 99.5% CPU, comp0(601) still building (per-comp line prints only on completion ~10:54) — on track, no partial/large.json. E-554: c0=4961/c1=4934/c3=4928, accepts 457/432/684 (>0 ✓), ckpts fresh (<360s), best=228.9748 all (state 300/278/360 high-T explore). No bank deltas. Routine.
- 09:42 — E-556 large FINISHED (failed-to-bank, but NEAR MISS). Result: comp0(601) covered **577/601** (24 unreachable single-start+exc1), comp1/2/3 = 150/150 full. Assembled 1027/1051, walk **died at leg 577** = exact comp0→comp1 stitch boundary, exc_used=1/5 (budget NOT binding — 4 exc free). Partial /tmp/large_component_partial.json (n_missing=24), large.json untouched (triple-guard worked). ROOT CAUSE: (a) comp0 single-start greedy (line 308 hardcodes `[(comp[0],0.0)]`) starves 24 nodes; (b) naive concat stitch — order_components estimated cheap(0 bridges) on APPROX timing but real walk at true arrival found no cheap+no exc into comp1. DIAGNOSIS: not numba-hang (uses bounded Lambert), not budget. ACTION: 1 core freed → relaunched **E-556b PID 46747** `--max-exc-internal 2` (cheapest lever, no code edit; budget 2 internal+3 bridges=5 fits) to recover comp0's 24 nodes; full comp0 coverage also changes comp0's end-node → may incidentally fix the stitch. log runs/ch2_e556b_large_exc2.log. If exc2 ALSO leaves comp0 short OR stitch still fails → next = multi-start comp0 (code edit, run tonight when E-554 frees 3 cores) + smarter boundary-node bridge insertion. E-554: c0=4768/c1=4710/c3=4710, best=228.9748 all (state mk 290/279/355 = high-T uphill explore, by design), ckpts <410s fresh, accepts>0 ✓.
- 09:23 — Both HEALTHY. E-556 large: 67min, 99.5% CPU, STILL comp0 (601) — 1.6× agent's ~42min est. NOT a hang: greedy_subtour_only proven-terminating + uses bounded Lambert find_earliest_transfer (NOT numba-DP hang class). Interpretation: comp0 less cheap-walkable time-aware than static adj suggested → many expensive cheap-miss full-scans + exc double-scans. WILL terminate (with max_exc_internal=1, once 1 exc used + cheap fails → returns early). Let it finish — coverage # tells whether to retune (--max-exc-internal 2 / more seeds) or abandon; killing now = 67min wasted w/ no data. DECISION RULE: if comp0 STILL unprinted next tick (~09:40, ~83min), keep waiting (terminating) but plan cheaper intra-comp method for tonight when E-554 frees cores. E-554: c0=4566/c1=4497/c3=4491, accepts 412/415/626, best=228.9748, ckpts fresh.
- 09:05 — Both HEALTHY (1 false alarm cleared). E-556 large: 48min, 99.5% CPU, comp0 (601) still grinding — slightly over agent's ~42min big-comp est but greedy guaranteed-terminating + not stalling-early (good coverage sign); no comp print/partial yet. E-554: c0=4372/c1=4383/c3=4289, accepts 392/412/602, best=228.9748 all. **c1 FALSE-ALARM**: ckpt looked frozen (iter=4217, mtime 08:46) 18min at 99.6% CPU = c2-hang signature — but DIAGNOSIS cleared it: c1 utime (2832879) ≈ c0/c3 (2833258/2833142), tiny stime, and ckpt refreshed to 09:04 with iter→4383. ROOT: ckpt-write lag (600s interval + early-continue delay can stale a single read). LESSON: don't trip on one stale ckpt mtime alone — cross-check utime vs sibling chains. Next wake ~09:21 catches E-556 comp0-done→small comps→walk+bank/partial.
- 08:43 — Both HEALTHY. E-556 large: 26min, 99.5% CPU, mid-comp0 (601-node; no per-comp print until ~36min) — on track, log silence expected during big comp (same class as E-555). No partial/large.json yet. First "comp" line expected ~08:53. E-554: 3ch c0=4165/c1=4116/c3=4061, accepts 371/400/567 (✓), best=228.9748 all (T≈6.6, explore), 0 hist. Next wake ~09:03 should catch E-556 comp0-done → small comps → walk+bank/partial.
- 08:08 — E-555 FINISHED → FAILED (null, no bank). Output: sub-tours 50 built but only 12 fully cover; total coverage 737/1051 (314 missing, max_exc_internal=1 too tight); meta_route burned all 5 exc after just 5/50 clusters (perm=84/1051) → "walk fail", status no_meta_route. Phase 7738s=129min (matched revised model). ROOT CAUSE: **k=50 k-means clustering is INCOMPATIBLE with 5-exc budget** — bridging 50 clusters needs ≤49 exc bridges, only 5 allowed. Core FREED (1 idle). DEEP REVIEW (finished-trigger): leaderboard refetched — Small R1=101.65/R2=108.77/**R3=108.77** (tightened from 110.88; our 116.38 gap-to-R3 now 7.6d), Medium R1=195.68/R2=199.74/R3=200.6 (our 228.97 gap 28.4d), Large R1=424.62/R2=1143.56/**R3=1153.9** (we have NO bank — any feasible tour <1153.9 ≈ rank 3, pure upside). REPRICE: large first-bank still TOP EV but needs CORRECT construction = bridge the 4 NATURAL cheap-components [601,150,150,150] with ≤3 exc (fits budget), time-feasible intra-comp tour. Single-start greedy tractable (~36min comp0-601 + ~2min ea 150s ≈ 42min). Dispatched agent to build+validate component-aware builder; will review+launch next tick. E-554: 3ch healthy c0=3904/c1=3831/c3=3721, accepts 339/370/525, best=228.9748 (by-design explore), 0 hist.
- 07:45 — Both HEALTHY. E-554: 3ch c0=3714/c1=3637/c3=3424, accepts 316/347/476 (✓), best=228.9748 all (by-design explore, T≈6.9), 0 hist. E-555 large: 107min, 99.5% CPU, still build_cluster_subtours. RESOLVED the slowness concern DEFINITIVELY: gdb attach blocked (ptrace_scope=1, no sudo), so AUDITED THE CODE — greedy_subtour_only (:77) is GUARANTEED to terminate (unvis shrinks by 1 each while-iter :115 or returns :106; ≤|cluster| iters, inner bounded by find_earliest_transfer's fixed 120-step scan). build_cluster_subtours = bounded 50×8×3 terminating calls → NO infinite loop possible. Cost: 47-node cluster ≈47²×2 transfer calls × ≤24 combos ≈16min alone; 3 big + 47 others ⇒ 90-150min realistic. 107min = slow-but-valid, WILL finish. NO intervention (don't re-investigate; only act on CPU drop/proc death). Remaining risk = infeasible partial (banks nothing) → if so, retune (k/exc budget). Wait for "Sub-tours:" line then fast meta_route+walk.
- 07:24 — Both HEALTHY. E-554: 3ch c0=3518/c1=3444/c3=3236, accepts 302/326/442 (✓), best=228.9748 all (by-design explore, not stall), 0 hist. E-555 large: 86min, 99.5% CPU, STILL build_cluster_subtours (no "Sub-tours:" line). COST MODEL REVISED UP: phase ~ Σ cluster_size² × 8 time-scans × 3 starts; biggest clusters (47/41/41) dominate (~47² scans × 24 combos each) → 40-60min was optimistic, realistic 60-120min. 86min fits within originally-bounded 1-3h TOTAL envelope. CPU pegged = real work, large=0 bank = pure upside → continue, no kill. Next decision pt ~07:44 (~106min): if STILL silent, consider invasive check (gdb/py-bt) for single-cluster pathology OR accept slow-but-valid; intervene only on CPU drop/proc death/infeasible-partial.
- 07:06 — Both HEALTHY. E-554: 3ch c0=3311/c1=3223/c3=3116, accepts 282/309/431 (✓), best=228.9748 all (6th tick at bank — NOT a true stall: T≈7.2, still high-T explore; exploit ~iter12k≈20h in by design), 0 hist. E-555 large: 68min, 99.5% CPU, still in build_cluster_subtours (no "Sub-tours:" line) — UPPER EDGE of ~40-60min model. No partial/large.json. Can't add per-cluster logging without losing 68min; py-spy absent. DECISION: ride it — CPU pegged = valid work, large=0 bank so any feasible result is pure upside; won't kill on slowness alone. Tightened next wake to ~07:23 (~85min) decision point: if still silent, accept higher-than-modeled cost & keep waiting (don't kill); only intervene on a true hang signal (CPU drop / proc death).
- 06:44 — Both HEALTHY. E-554: 3ch c0=3065/c1=2987/c3=2932, accepts 276/288/406 (✓), best=228.9748 all, 0 hist, bank untouched. E-555 large: 46min, 99.4% CPU, still in build_cluster_subtours (no "Sub-tours:" line). py-spy unavailable → COST-MODEL CHECK instead: phase does ≤50 clusters × n_time_scans=8 × max_starts=3 ≈ 1200 greedy_subtour_only calls (~21-node greedy w/ find_earliest_transfer time-scans each) → ~40-60min expected. 46min is MID-BAND, not a hang; CPU pegged confirms grinding. No partial/large.json yet. If still silent past ~90min (≥~07:30) reconsider largest-cluster (47-node) pathology; else expect "Sub-tours:" line soon.
- 06:22 — Both HEALTHY. E-554: 3ch advancing c0=2867/c1=2717/c3=2745, accepts 271/285/378 (✓), best=228.9748 all, 0 hist, bank untouched. E-555 large: 24min, 99.2% CPU, log STILL frozen at clustering line — CONFIRMED BENIGN: build_cluster_subtours (ch2_hierarchical_large.py:119) has NO internal print; next log line is "Sub-tours: N built" (:278) only after all 50 clusters done. So log-mtime staleness during this phase is EXPECTED (same class as E-554 iter-print) — do NOT treat as hang while CPU pegged. No partial/large.json yet. Phase ~30-90min (24min in). Watch for "Sub-tours:" line next.
- 05:59 — Both jobs HEALTHY. E-554: 3 chains (43600/01/03) 99.6% CPU, ckpts fresh (c0 05:51/c1 05:57/c3 05:49), iters c0=2629/c1=2628/c3=2540, accepts total c0=263/c1=280/c3=349 (accepted>0 ✓), best_mk=228.9748 all (0 hist, bank untouched 23:23) — SA explore, expected, not a stall. E-555 large first-bank (45321, ch2_hierarchical_large.py) CONFIRMED genuinely fresh: log mtime 05:57:48, elapsed 2:11, single proc PPID 1711 (setsid orphan, no crash-loop) — launch landed ~at compaction. Phase: build_cluster_subtours (50 clusters, no log until done ~30-90min). Banks large.json ONLY if walk_ok (safe). Routine cadence; next watch = E-555 sub-tour completion line + E-554 ckpt advance.
- 05:50 — c2 TRIPWIRE → KILLED. c2 (43602) confirmed hung: ckpt frozen iter=2255 for 30min at 99.6% CPU (wchan=0, utime≈etime, ~0 stime = pure CPU spin, not I/O block). Root cause: single DP call hung, almost certainly the NaN→int32 cast in ch2_dp_numba.py:85-86 (RuntimeWarning seen at init) producing a pathological loop on one perm. kill -9 43602; c0/c1/c3 SURVIVE (independent procs, 99.6% CPU), parent 43596 alive (Ssl, waiting on pool.map — harmless). E-554 now 3 chains (fine; medium is low-EV per review anyway). 1 core FREED. → Allocating to LARGE first-bank (queue#2, top EV). E-537 large pipeline ASSESSED: prereqs ready (e533 adj /tmp/ch2_e533_large_adj.npz, elkai ok) BUT prior result walk_ok=FALSE, breaks at leg 76 INSIDE comp0 (pos0-600), exc 5/5 spent. Diagnosis: static LKH path (Lambert@fixed t=50d) is NOT time-feasible when walked chrono → relaunch = reproduce infeasibility. NEEDS time-aware path (greedy reserved-exc-budget walk per [[ch2-medium-subtour-pattern]]/[[ch2-find-transfer-pattern]]), real dev not relaunch. NEXT TICK: build large greedy earliest-feasible first-bank (safe: banks only if walk_ok). Short wake to begin.
- 05:43 — DEEP REVIEW (>12h trigger) + health. HEALTH: c0=2552/c1=2372/c3=2358 advancing fine; **c2 WATCH** — ckpt stale 13min (05:20, interval 10min) at 99.6% CPU → possibly one slow DP eval; tightened next wake to 300s to confirm c2 advances (if still 05:20 → single-iter hang, kill c2, others cover search). best_mk=228.9748 all, 0 hist. LEADERBOARD (jq, api.optimize.esa.int): **MEDIUM R1 tightened 199.74→195.68d (06-10 submit), R2=199.74**; our 228.97 ranks ~5, gap to R2 = 29d. SMALL R1=101.65 (HRI, unchanged), R3=110.88; our 116.38 gap-to-R3=5.5d. LARGE R1=424.62 R3=1186.6, we have NO bank. STRATEGIC: medium 29d gap ~unreachable by E-554 basin-polish (single-mut all +11d uphill) → even "success" stays rank~5 = 0 rank-pts. Large first-bank (E-553) = higher EV (wide open, 0 bank). NOT killing healthy E-554 mid-explore (4.5h/24h); reassess at its next decision point (done/hard-stall/bank). PENDING USER (non-urgent, fold into AM status): none of our banks SUBMITTED yet; small 116.38 & medium 228.97 would each rank ~5 — is rank-5 worth submitting, or hold for rank-1/3 push?
- 05:17 — Healthy (4.1h). iters c0=2301/c1=2171/c2=2162/c3=2087, best_mk=228.9748 all, 0 hist. T~8.0 high-T explore. On-track, routine.
- 04:51 — Healthy (3.7h). iters c0=2077/c1=1993/c2=1934/c3=1908, best_mk=228.9748 all, 0 hist. T~8.2 high-T explore. On-track, routine.
- 04:25 — Healthy (3.25h). iters c0=1754/c1=1771/c2=1640/c3=1598, best_mk=228.9748 all, 0 hist. T~8.4 high-T explore. On-track, routine.
- 03:59 — Healthy (169min/2.8h). iters c0=1572/c1=1512/c2=1432/c3=1394, best_mk=228.9748 all, 0 hist. T~8.5 high-T explore. On-track, routine.
- 03:33 — Healthy (143min). ckpts advancing c0=1380/c1=1178/c2=1247/c3=1180, all best_mk=228.9748, 0 hist. T~8.8, still high-T explore. On-track, no tripwire/idle. Routine.
- 03:07 — Healthy (117min). ckpts advancing c0=1020/c1=990/c2=938/c3=969, all best_mk=228.9748, 0 hist, bank file untouched (06-07). 3rd tick at bank but NOT a stall: T still ~9 (10·0.9999^1000) — high-T explore; exploit phase (T~3) ~iter12k ≈ 20h in, so sub-bank unlikely before late run by design. No action; routine cadence.
- 02:35 — All 4 chains healthy (91min). ckpts fresh 02:33-34, iters c0=812/c1=739/c2=740/c3=721 (steady ~6s/iter), accepts M5 80-107/chain (~15% rate). best_mk=228.9748 on ALL (0 hist lines, no sub-bank yet). NOT a stall: 1.5h into 24h, SA in high-T explore phase (T~9, decaying 10→3); RISK-noted single-mut neighbors all uphill → real test is mid/late run as T anneals into exploit. Routine cadence; deep review deferred (all 4 cores locked ~22h, repricing can't change action until E-554 banks/frees).
- 02:15 — Healthy via CKPTS (log-freshness tripwire = FALSE POSITIVE). chain0 ckpt fresh 02:12, iter=565 (was 265@01:41 → ~6s/iter steady), op_accepts M5=82+M1=7+M2=1 = 90 (~16% rate), best still 228.9748 (no sub-bank; expected — 24h multi-step hunt). ROOT CAUSE of empty log: the iter%200 print (ch2_e554...py:146) sits AFTER the degenerate/DP-fail `continue`s (lines 104,107); with DP_ok~30%, ~70% of iters early-continue, so iter=200/400/600 iterations almost always skip the print → log stuck at 01:10 init while opt runs fine. Cosmetic; NOT killing a healthy run. MONITOR VIA CKPTS (/tmp/ch2_e554_ckpt_chain*.json mtime+iter+op_accepts) + hist files (sub-bank events), NOT log mtime, for E-554 going forward.
- 01:47 — TRIPWIRE CLEARED. Frozen-SA fix CONFIRMED via ckpt (iter=200 print just buffered behind slower per-iter rate). chain0 ckpt@iter=265: op_accepts M5=46 + M1=4 = **50 accepts (~19% rate)**, state mk wandered 228.97→253.74 (T0=10 accepting uphill as designed). accepted>0 confirmed → SA no longer frozen, exploring healthily. best still =228.9748 (no sub-bank yet; expected — needs multi-step downhill accumulation, that's the 24h job). Switching to routine cadence (~25min); next: watch for hist-file lines (= sub-bank improvements) + ongoing DP_ok health.
- 01:41 — E-554 T10 still no iter=200 line at 31min (vs 23min run1). Healthy (4ch 99% CPU). Slower per-iter (~9s vs 7s) is CONSISTENT WITH FIX WORKING: T0=10 accepts moves → state wanders to worse/denser perms (mk 240-300+) → heavier DP eval than the compact bank perm run1 stayed frozen on. Awaiting line to confirm accepted>0. Recheck ~36min.
- 01:36 — E-554 T10 relaunch healthy: 4 chains 99% CPU, all init'd (bank 228.9748d), 26min elapsed but no iter=200 line yet (first run's fastest c0 printed at 23min; minor variance). Awaiting first line to read accepted count — KEY: T0=10 should give accepted>0 fast. Short recheck next.
- 01:09 — TRIPWIRE CONFIRMED + FIXED. c0 (iter200) & c3 (iter400, 52min) BOTH accepted=0, DP_ok 27-32% (healthy). Freeze universal & matches arithmetic (T0=3 vs +11.4d min uphill). Killed E-554, raised SA_T0 3.0→10.0 (exp(-11.4/10)=32% accept on min step; decay anneals 10→~3 over 24h), wiped ckpts/hist, RELAUNCHED: parent 43596 + 4 chains 43600-03 ~95% CPU → runs/ch2/e554_alns_v2_T10.log. NEW WATCH: accepted>0 within 1h (should be fast now); then best trending ↓ below 228.97. If STILL accepted=0 → not temperature, bank is a hard multi-step local opt → pivot to E-553 large.
- 00:41 — E-554 1st line (c0): iter=200 23.1min DP_ok=27.5% accepted=0 state=best=228.9748 T=2.94. DP_ok matches gate prediction (GOOD). TWO concerns: (a) rate ~7s/iter (denser v2 table → heavier DP eval; inherent; ~12k iters/chain in 24h). (b) **accepted=0 is arithmetically EXPECTED & a likely real problem**: recheck ok_min=240.35d = best feasible neighbor is +11.4d uphill from bank 228.97; SA accept prob at T0=3 = exp(-11.4/3)=2.4%, worse neighbors «that. T0=3 worked for SMALL (small neighbor steps) but is far too cold for MEDIUM's large min-uphill (~11d) → SA frozen. PLAN (respect tripwire, avoid hasty oscillation): let it reach 1h (~01:34); if accepted=0 across ALL 4 chains → kill + raise SA_T0 3.0→12.0 (exp(-11.4/12)=0.39 accept on min step) in ch2_e554_medium_alns_v2table.py, wipe ckpts /tmp/ch2_e554_ckpt_chain*.json, relaunch. This is a calibration fix grounded in measured delta_min, fits the decomposition (not a new-lever flip).
- 00:34 — RECHECK COMPLETE + E-554 LAUNCHED. Full gate: M5 45.5% ok (v1 0.5%), M2 15% (v1 13.5%; missing→unreachable), M1 25% (v1 14%), random 0% (all missing, noise/5% wt). Weighted DP_ok by E-554 op wts [60/20/15/5] = 0.6·45.5+0.2·15+0.15·25+0.05·0 = ~34% vs E-549's 4% → GATE PASSES. E-554 up: parent 43313 + 4 chains (43317-20) ~95% CPU, v2 table 7942 pairs loaded, init mk=228.9748d, components [121,20,20,20], bridges [19,82,102,158,178]. log runs/ch2/e554_alns_v2.log. WATCH 1st hr: accepted>0, DP_ok~34%, best trending↓. RISK: all-op ok_min 240-246d > bank 228.97 → single-mut neighbors uphill; SA must accumulate multi-step downhill (E-549 couldn't even explore @4%; 34% gives navigable landscape). 2nd-order wall noted: M2 dp_unreachable=exc-budget/timing, not coverage.
- 00:13 — M2 PRINTED: {dp_unreachable:147, ok:30, missing_pair:23} = 15% ok. KEY 2nd-order finding: missing_pair collapsed 156(v1)→23 but converted to dp_unreachable(147), NOT to ok. So for SEGMENT-REVERSAL moves the bottleneck shifted from table-coverage to schedule-FEASIBILITY (exc-budget/timing wall) — more pairs won't help M2 (confirms the pre-flagged "exc-only intra → dp_unreachable" concern). M5 (60% weight, the structural lever) is 45.5% ok though → E-554 weighted DP_ok ~25-30%+ vs E-549's 4%. Gate still passes on M5's dominant weight. M1 grinding (84min). Launch E-554 after random+completion.
- 23:52 — recheck 63min elapsed, alive 100% CPU, M2 still grinding (M5 done). GATE already decided by M5 (45.5% ok >> 30%). Holding E-554 launch until recheck frees its core (avoid 5-on-4 oversubscription distorting E-554's accepted>0-in-1h health check). Total run trending ~80min → launch E-554 ~00:10.
- 23:31 — RECHECK M5 PRINTED: {missing_pair:108, ok:91, dp_unreachable:1} = 45.5% ok (v1 was 0.5%!). GATE PASSING decisively (>30%). Table expansion fixed missing_pair starvation. Caveat: ok_min=240.35d > bank 228.97 → immediate neighbors locally uphill (expected near local opt); gate's point is a connected explorable DP landscape, which E-549 lacked. Awaiting M2/M1/random; on completion LAUNCH E-554 (4 chains × 24h).
- 23:09 — recheck ~20min, alive 100% CPU, still mid-M5 (op1/4). Per-op DP-eval cost ~20min/op → full run likely ~50-70min total (random op fast: inter-comp swaps still missing). Switching to bg-notification-driven (long fallback) instead of tight polling. Held off pre-developing E-553: gate resolves in ~40min & E-554 would hold all 4 cores 24h anyway, so E-553 can't run parallel regardless — pre-dev risks rebased work.
- 23:04 — recheck ~15min, alive 100% CPU. Per-op line buffered until each op's 200 trials finish (no partial print). KEY: the ~10x slowdown vs v1 is itself evidence the gate is PASSING — v1 M5 was 199/200 missing_pair (~1 DP eval); v2 now pushes ~all 200 into DP eval → far fewer missing_pair rejects = table expansion working. Awaiting exact 'ok' fractions.
- 22:59 — recheck still grinding (~10min, 100% CPU 1 core — E-551 single-threaded). v2 table lets more candidates pass missing_pair gate → more reach expensive per-cand DP eval → slower than v1 (expected, not a tripwire). Awaiting per-op 'ok' counts.
- 22:54 — recheck HEALTHY: header printed (7855/32580 pairs now in table, up from v1 4686), process alive ~5min, grinding 4 ops × 200 DP evals. Awaiting per-op 'ok' counts.
- 22:49 — classifier RECOVERED (~33 min outage). E-552 DP_ok recheck LAUNCHED (E-551 instrumentation on v2 table, bg b0mdy6qif → runs/ch2/e552_recheck.log). Awaiting per-op reason counts; gate: M5/M2/M1 'ok' >30% → launch E-554; else pivot E-553 large.
- 22:44 — classifier still out (6 attempts, ~31 min idle). Recheck launch pending.
- 22:39 — classifier still out (5 attempts, ~26 min since E-552 done). Recheck launch pending; cores idle.
- 22:32 — classifier still out (4 attempts). Recheck launch pending; cores idle.
- 22:27 — classifier still out (3 attempts). Recheck launch pending; cores idle.
- 22:22 — classifier still out (2 attempts). Recheck launch pending; cores idle.
- 22:16 — E-552 COMPLETE (3.33h): /tmp/ch2_medium_fine_pair_set_v2.npz saved, 7942 pairs total, 97.3% of new pairs have ≥1 finite cell (coarse prefilter validated as feasibility oracle). E-554 GATE: rerun E-551 instrumentation on v2 (script now takes argv: `python scripts/ch2_e551_dp_reject_instrument.py /tmp/ch2_medium_fine_pair_set_v2.npz /tmp/ch2_e552_recheck.json | tee runs/ch2/e552_recheck.log`) — launch BLOCKED by classifier outage (1 attempt). Retrying ~4min cadence. Gate rule: if DP_ok (M5/M2/M1 'ok' fraction) rises substantially (>30%) → launch E-554; if still mostly rejected → table wasn't the lever, pivot to E-553 large. Cores IDLE during outage.
- 21:33 — E-552: 2600/3256 (80%), eta 0.67h → done ~22:13. Healthy. Next wake timed for just after completion → run DP_ok recheck gate (E-551-style sample on v2 table) BEFORE launching E-554.
- 21:02 — E-552: 2000/3256 (61%), eta 1.28h → done ~22:20. Healthy.
- 20:31 — E-552: 1600/3256 (49%), ckpt@1500 landed, eta 1.69h → done ~22:10. Healthy.
- 20:00 — E-552 steady: 1000/3256, 0.3/s, eta 2.30h → done ~22:20. Healthy.
- 19:34 — E-552 steady: 600/3256, rate 0.3/s, eta 2.73h → done ~21:50. Healthy.
- 19:08 — E-552 v4 HEALTHY: "200/3256 rate=0.3/s eta=3.19h" — first progress line, matches cost model (~13s/pair = 2500 buckets × 40 tofs × ~130µs). Done ~22:10. No tripwire. Relaxed to ~25min cadence. Next milestone: ckpt @1500 pairs (~20:20); completion → DP_ok recheck gate before E-554.
- 18:48 — E-552 40-tof still ~25s/pair (Lambert ~125µs × ~4880 buckets × 40), ETA ~5.75h. FINAL lever applied: SCAN_MAX_T=250d cap (all tour departures < makespan; bank 229, target <199, so 250–500d buckets are dead → left INF, merge-safe). Halves scan → ETA ~2.5h (done ~21:15). Relaunched (setsid). COMMITTED: no more E-552 re-optimization (3 monotonic cost cuts done; further iteration = diminishing returns). HARD ABORT TRIPWIRE: if no "200/3256" line by ~19:08 (~20min), the per-pair cost model is wrong — ABORT E-552, pivot cores to E-553 large first-bank. ON E-552 COMPLETION: quick E-551-style DP_ok recheck on sample BEFORE launching E-554 — if v2 table doesn't raise DP_ok meaningfully, SKIP the 24h E-554 and pivot to large (the table wasn't the lever; E-549's 4% may already be near a local opt).
- 18:23 — E-552 was ~8.6h: all new intra pairs coarse-EXC-ONLY → full no-break 500k Lambert scan ≈38s/pair. Cut TOFS 100→40 (merge-safe; table stores actual tof values). [superseded by 18:48 bucket cap]
- 17:55 — E-552 v2 health: 4 workers @99% CPU, 317s elapsed, no "200/3256" line yet → consistent with ~0.27 pairs/s (matches earlier 15s/pair slow-pair diagnostic; exc-only pairs full-scan all 100 tofs/bucket, inherent to per-bucket fine table). ETA ~3h. Genuine USEFUL compute (all coarse-feasible) → no intervention. Next tick confirm first progress line, then relax cadence to ~20min.
- 17:50 — E-552 RESCOPED + relaunched (PID 42026). Root cause of zero-progress: the 10979 "new intra-comp" pairs are exactly those NOT coarse-cheap-feasible (v1/E-542 already included every coarse-cheap pair), so they're mostly genuinely infeasible → full no-break 5000×100 scan AND only add all-INF cells the DP rejects anyway (doubly useless). FIX: added coarse-feasibility prefilter (cheap OR exc finite) to new-intra selection → set cut 11072→3256 (3102 intra + 154 inter), every pair now feasibility-plausible. Old run killed, ckpt wiped, fresh launch detached (setsid). Watch for first "200/3256" line + true rate next tick; if rate <0.3/s investigate (exc-only pairs still full-scan).
- 17:25 — E-552 health check: 4 workers ALL at 99.5% CPU (genuine compute, NOT dead-loop class) but ZERO progress lines / no ckpt in 27 min → <200 pairs returned. Likely many new intra-comp pairs are infeasible → full 5000×100 scan per pair (no early break), so real rate << 3.1h est. NOT killing (valid work). Tighter recheck 17:40 for first %200 print + true rate. If still zero output → intervene (narrow T_STARTS: bank mk~229d, scanning starts 250–500d is wasted).
- 16:59 — classifier RECOVERED (outage ~70 min, 33 attempts; user switched session to Opus 4.8). E-552 LAUNCHED, PID 41707: 11072 new pairs (10979 intra + 93 inter), est ~3.1h on 4 cores. Next: verify rate ≥~1 pair/s at next tick; on completion launch E-554.
- 16:53 — classifier still out (31 attempts, ~65 min). Retrying.
- 16:48 — classifier still out (29 attempts, ~60 min). Retrying.
- 16:43 — classifier still out (27 attempts, ~55 min). Retrying.
- 16:38 — classifier still out (25 attempts, ~50 min). Retrying.
- 16:33 — classifier still out (23 attempts, ~45 min). Retrying.
- 16:28 — classifier still out (21 attempts, ~40 min). Retrying.
- 16:23 — classifier still out (19 attempts, ~35 min). Retrying.
- 16:18 — classifier still out (17 attempts, ~30 min). Retrying.
- 16:13 — classifier still out (15 attempts, ~25 min). Retrying.
- 16:09 — classifier still out (13 attempts, ~21 min). Retrying.
- 16:05 — classifier still out (11 attempts, ~17 min this round). E-552+E-554 both pre-staged; nothing else actionable read-only. Cores idle.
- 16:01 — classifier still out (9 launch attempts). Used outage to pre-stage E-554 (scripts/ch2_e554_medium_alns_v2table.py — E-549 arch on v2 table, launch after E-552 completes). Cores idle.
- 15:56 — classifier still out; E-552 launch attempts now 6. Cores idle. Retrying ~4 min cadence.
- 15:50 — E-551 DONE: missing_pair dominates ALL operators (M5 199/200, M2 156/200, M1 172/200, random 200/200); 3605 unique missing pairs (2011 intra + 1594 inter); ok_min 246–248d all above bank → table is THE bottleneck. E-552 script written (scripts/ch2_e552_expanded_fine_precompute.py, ~11k new pairs, merges v1, checkpoints every 1500 pairs). LAUNCH BLOCKED — classifier outage recurred (3 attempts). Retrying every ~4 min.
- 15:21 — outage ~65min, 12 failed attempts; E-550 alive (c2 iter=3900, accepted=0 at 7.2h). Methodology note M-general-autonomous-loop-directive-design.md written.
- 15:16 — classifier outage continues (~55min, 10 failed kill attempts); E-550 still alive.
Deadline: 2026-06-30 AoE (20 days). Nothing submitted to leaderboard yet.

## Running jobs

| Job | Cores | Started | Status |
|---|---|---|---|
| E-554 medium DP-ALNS on v2 table (T0=10) | 3 (PIDs 43600/43601/43603; c2/43602 KILLED 05:50 hung) | 2026-06-11 01:09 | RUNNING, 24h wall (done ~2026-06-12 01:09). Healthy 3-chain. Banks to solutions/upload/medium.json (bak .bak.e554) on improvement < 228.9748d. log stale by design (iter-print skipped on early-continue) — monitor via ckpt. Low-EV (medium gap 29d, rank-5 ceiling). |
| ~~E-555 large (k=50 k-means)~~ FAILED 08:07 | — | 2026-06-11 05:57 | DONE/NULL. k=50 clustering incompatible with 5-exc budget (49 bridges needed). No bank. Superseded by E-556. |
| E-556 large FIRST-BANK (component-aware, ch2_e556_large_component_aware.py --full) | 1 (PID 46303, setsid orphan) | 2026-06-11 08:08 | RUNNING. log runs/ch2_e556_large.log. CORRECT recipe: 4 NATURAL cheap-components [601,150,150,150] bridged by ≤3 exc (fits 5-budget) + time-feasible greedy_subtour per comp. ~42min construction (comp0-601 ~36min) then walk+bank. TRIPLE-GUARDED safe bank: large.json only if walk_ok AND all 1051 covered AND UDP feas; else /tmp/large_component_partial.json, banks nothing. RISK: static cheap-adj vs time-aware Lambert → comp0 may under-cover/stall (probe stalled 48/150 on a sliced sub-comp) → would yield partial, no bank; mitigations = --max-exc-internal 2 or more seeds. TOP EV (large=0 bank, R3=1153.9 = any feasible ≈ rank 3). |

**E-554 TRIPWIRES (1st-hr + ongoing):** accepted>0 within 1h; DP_ok ~30-40% (if «10% something regressed); iter rate sane. NOTE: ignore log mtime (iter-print skipped on early-continue → log stays at init); monitor via ckpt mtime+iter+op_accepts and hist-file lines instead. SA metric = DP metric by construction (no mismatch). If best stuck = bank 228.97 after several hrs with accepted>0 → SA exploring but landscape may be a basin; not a bug, note for deep review. RISK noted: single-mut neighbors uphill (ok_min 240-246d), needs multi-step downhill accumulation.
**ON E-554 COMPLETION/BANK:** if banked < 228.97 → update bank snapshot, consider submission escalation. Then E-553 large first bank is next queue item (idle cores).

## Priority queue (reprice on deep review)

1. **E-552** expanded fine table — VALIDATED by E-551 (missing_pair dominates). Script ready, launch blocked by outage. After completion: DP-ALNS rerun (E-549 architecture, FINE→v2 path) = E-554. Target: 228.97 → <199.74 (TGMA R1).
2. **E-553** large FIRST BANK via proven medium recipe (greedy walk + reserved exc budget + iterative sub-tour bridge insertion). No large.json exists; R2 is ~1143 d — even mediocre feasible tour ≈ top-3. Can run on idle cores AFTER E-552 finishes (don't oversubscribe).
3. Small elite-crossover ALNS (E-530's untried path) — gap 5.5 d to R3 110.88, 14.7 d to R1 101.65.
4. Ch1 B1/B2/B3 deferred audit fixes (+70k kg headroom on trajectory).
5. **Submission to optimize.esa.int before 06-30** — escalate to user when banks are competitive.

## Bank snapshot (2026-06-12 11:10 — all verified, NONE submitted=0 pts; ranks vs LIVE 06-12 board, O-016)

| Instance | Bank | If submitted | Pts | Next rank up |
|---|---|---|---|---|
| Ch2 large (H) | 1048.9786 d | **rank 2** | 16.00 | r1=424.62 (≈halve mk — LKH rebuild, P3) |
| Ch2 medium (M) | **192.9002 d** | **rank 1** | **13.33** | **TOP — E-040 ultrafine re-time −2.87d, BEATS r1 by 2.78d. No higher rank.** |
| Ch1 trajectory (H) | 236,420.5 kg | rank 6 | 8.89 | r5=372,729 (+136k; WSB-unreachable) |
| Ch1 matching-ii (E ×1) | 72,204.293 | rank 7 | 4.00 | r6=72,327.43 (+123; LNS DONE +3.565, plateaued → needs tighter ILP/Gurobi per E-039) |
| Ch2 small (E) | 116.3738 d | rank 6 | 5.00 | r5=111.7875 (−4.586d; idle-squeeze P2; perm optimal per E-038) |
| Ch1 matching-i (E) | 33,338.184 | rank 9 | 2.00 | r8=33,364.5 (+26.3); r5 +89; packed ladder → exact-ILP push (E-039) |
| **TOTAL if all submitted** | | | **≈49.2** | (currently 0; medium now r1 +1.33 from E-040; **P4 RESOLVED: matching-ii is A_1 easy ×1 per O-001, NOT ×4/3 → r7=4.0 not 5.33**) |

All 6 bank files officially feasible (Ch2 viols 0.0; Ch1 trajectory valid, 301 active). Backups in /tmp/bank_bak/ + *.bak.* alongside each.

Leaderboard last fetched: **2026-06-12 11:10 LIVE (O-016)** via persisted scripts/fetch_leaderboards.py.

## Known tripwires (check every tick)

- SA/ALNS: accepted=0 after >1h → diagnose immediately (E-550 case: metric-mismatched baseline).
- DP_ok/walk_ok near 0% → instrument reject paths (CLAUDE.md 5a).
- Log not written >30 min → process hung.
- Evaluator metric must match SA baseline metric (bit us twice: E-549 stall analysis, E-550 dead loop).
- E-552 specific: rate should be ~1 pair/s/core (5000 buckets × ≤100 tofs); ETA printed in log — if rate <0.3/s, investigate.

## Last deep review: 2026-06-12 04:00 (WSB sweep tapered→stop trigger; ALL 6 leaderboards refetched live; current banks priced into board). **SUBMISSION-ROI (current banks, all unsubmitted=0 now):** Ch2 large 1048.98d→r2=16.00 | Ch2 medium 228.97d→r4=9.33 | trajectory 236,420kg→r6=8.89 | matching-ii 72,037→r7=5.33 | Ch2 small 116.38d→r6=5.00 | matching-i 33,338→r9=2.00 = **≈46.55 pts** if all submitted. Trajectory r5 wall=372,729kg (+136k, WSB-unreachable) → WSB done for points. Cheap positive-EV compute queue THIN: matching ceilinged (re-test running), Ch2-medium/large gains need multi-hour builds. Dominant action = USER SUBMISSION (escalated 04:00).
(prev 2026-06-11 08:08: E-556 component-aware large launched → became the large rank-2 bank)
