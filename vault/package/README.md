# Package docs — index + status board

Living, named reference docs about our software stack — environment
setup, submission pipeline, per-module notes. Not ID'd, not
append-only; grow on demand. See `META.md §11`.

`src/` carries code; these docs carry how-it-fits-together. They
reference each other by path — never duplicate content (`META.md §12`).

## Index

*(none yet — fresh-start scaffold, 2026-05-18)*

| doc | covers | status |
|---|---|---|

## Planned (post-bootstrap)

- `environment.md` — the `spoc26` micromamba env, validation status
  (draft `environment.yml` at repo root; first frontier item is its
  validation per `user.md` working constraints).
- `submission-pipeline.md` — JSON artefact format, `solutions/upload/`
  convention, Optimise tie-breaking open question (`GOALS.md §5`).
